# 智能调度产品需求文档

**文档版本**：V2.4 实现与验收契约

**更新时间**：2026-08-10

**文档状态**：V2.4 产品契约已落地；按第 17 节持续回归验收

**适用范围**：渠道监控中的文本模型智能调度、样本补充、稳定性保护、人工控制、执行记录和开发验收。

**口径说明**：本文是 V2 的唯一目标产品口径。旧版“主渠道 / 第一备用 / 第二备用”三层设计、独立低优先级轮转和旧探索状态不再适用，不读取、不迁移旧智能调度配置或状态。

**评审建议**：优先审核第 1.4 节的决策对照、第 2.3–2.5 节的严格流量准入、第 5.4–5.6 节的统一采样和自适应备援、第 10 节的临时真实流量、第 11 节的稳定性保护与恢复时序，以及第 16 节的具体例子。第 17 节是可以直接转成测试用例的验收口径，第 19.2 节说明 V2.4 的实现边界。

> 本文描述 V2.4 代码、接口和页面必须共同遵守的行为；生产环境是否已经生效仍取决于实际部署版本。

---

## 1. 背景与目标

### 1.1 背景

同一个模型通常会配置多个上游渠道。渠道在成本倍率、首字、TPS 和稳定性方面不同，而且上游短时波动是常态。仅依靠管理员长期手工设置优先级和权重，会出现以下问题：

- 高优先级渠道长期吃满流量，低优先级渠道没有新鲜的真实请求样本。
- 评分最高渠道频繁切换时，上游缓存命中率容易下降。
- 同一渠道模型在不同分组产生的数据重复学习或互相割裂。
- 渠道短时不稳定时，系统容易在降级、恢复和固定主渠道之间产生冲突。
- 管理员只能看到结果，无法确认本轮用了什么数据、怎么算分、为什么调整。

### 1.2 产品目标

1. 以 `(分组, 模型)` 作为独立调度池，独立决定路由顺序。
2. 以 `(渠道, 模型)` 作为观测键，在所有关联分组之间共享真实请求、手动测试和定时探测样本。
3. 使用成本倍率、首字、TPS 和稳定性生成可解释的评分。
4. 让评分最高且满足切换条件的渠道获得最多流量，同时控制无意义的主渠道切换。
5. 在优先级分层模式下，为每条正常路由保留独立优先级，并用统一采样队列按配置顺序补充健康备用渠道的欠缺样本。
6. 对不稳定渠道自动降级、冷却、试放和恢复，并保留最终兜底能力。
7. 支持管理员按分钟固定主渠道，并自行决定固定期间是否允许稳定性降级；默认允许。
8. 任务失败或配置变化时保留上一轮完整结果，并提供可追溯的执行记录。
9. 继续以官方 `Ability.Priority` 和 `Ability.Weight` 表达参与路由之间的顺序和比例；智能调度开启时，对命中分组策略及模型范围的调度池以参与状态作为强制流量准入，再按当前分组路由顺序循环重试，不维护允许未参与路由绕过准入的第二套逻辑。
10. 把按需完整调度与请求级实时处理分开：配置、人工操作触发一次完整调度；采样对象切换、自适应备援、抖动更新、稳定性降级和探测恢复按各自事件或计时器及时执行。

### 1.3 非目标

- 不把缓存命中率加入评分或自动切换条件。
- 不增加分组级暂停；异常时使用全局开关、渠道参与开关或单路由参与开关。
- 不增加模拟运行模式。
- 不针对单个用户、提示词或请求内容实时重算全池完整评分和基础排名；请求事件只刷新受影响池的统一采样、自适应备援、滚动稳定性得分、保护与恢复覆盖层。
- 不为图片、音频、视频等非文本能力强制生成文本探测请求。
- 不保证临时采样百分比在低请求量下精确命中，只保证官方权重对应的目标比例。
- 不把探索请求上限作为硬性 Token 校验、计费上限或请求拒绝条件；它只用于选路前的软过滤。

### 1.4 本轮确认项对照

| # | 最终产品决策 | 主要落点 |
| ---: | --- | --- |
| 1 | `sample_mode=traffic` 时，统一采样对象临时提升到实际最高优先级层，并按探索比例取得少量真实流量 | 10.1、10.2 |
| 2 | 稳定性降级统一进入最后兜底层；所有高层不可用或重试到最后时仍可被官方选路使用 | 11.3、16.4 |
| 3 | `priority_weight` 下，每条正常参与路由都有唯一基础优先级；降级路由统一进入 `P0/W0` | 9.2、16.1 |
| 4 | 全局智能调度开启后，命中分组策略及模型范围的调度池只允许明确参与的路由接收流量，不回退默认 P/W；未配置分组和策略外模型池继续使用官方选路 | 2.3、2.5、3.3、9.2.3、16.6 |
| 5 | 探索流量与低优先级轮转合并为统一采样队列；候选按成本倍率或当前分组基础 P/W 排序，不保留独立轮转字段 | 5.4、5.5、10.3、19.1 |
| 6 | 页面区分“评分第一、实际主渠道、官方最高层”，避免把评分结果误认为最终请求入口 | 8.1、8.3、16.6 |
| 7 | 缓存命中率只展示，不参与评分或自动切换；异常由管理员取消参与、禁用或人工调整 | 1.3、7.5 |
| 8 | 真实业务、手动测试和定时探测等价计入共享样本；流式手动测试展示首字和总耗时 | 6.1–6.3 |
| 9 | 性能窗口和稳定性评分窗口支持分钟级整数配置；稳定性硬保护和请求比例统计都使用独立的“最近分钟数 + 最近请求数”窗口 | 4、5.3、5.6、6.5、11.4 |
| 10 | 固定主渠道允许降级时，降级只改变当前路由，不清除固定意图；固定期内恢复后重新置顶 | 12.2、16.5 |
| 11 | 固定到期仍不稳定时只清除固定意图，继续保持降级或试放，由正常调度接管 | 12.2、12.4 |
| 12 | 调度任务失败时整池保留上一轮完整结果，同时保存可见失败记录 | 13.3、13.5 |
| 13 | 配置中途变化使用控制版本、路由修订号和整池 guard 阻止旧任务覆盖新配置 | 13.2–13.4、16.7 |
| 14 | 不提供模拟运行；使用隔离开发数据和真实调度任务验收 | 1.3、18 |
| 15 | 不提供分组级暂停；使用全局开关、渠道参与开关或单路由参与开关 | 1.3、3.1 |
| 16 | 智能调度开启后，普通重试按当前 `(分组, 模型)` 的有效路由顺序循环；本轮全部候选尝试完且仍有重试次数时回到队首 | 2.5、16.8、17.6 |
| 17 | 样本不足探索与稳定性释放分别配置最大预估 Prompt Token，`0` 表示无限制 | 5.4、5.7、10.5、17.4 |
| 18 | 两类上限只做候选软过滤：已选中或已发往上游的请求不切换；没有其他候选时仍允许特殊路由兜底 | 10.5、16.9、17.4 |
| 19 | 稳定性恢复时推进共享观测边界，后续调度和监控主表从空数据重新积累，但不删除任何原始样本、日志或分钟聚合 | 6.5、11.3、14.6、16.10 |
| 20 | 智能调度实际启用时，渠道监控主表的统计范围跟随性能窗口；关闭时才允许手动配置分钟数 | 4、14.6、17.7 |
| 21 | 不再周期执行完整调度；配置、人工操作触发一次完整调度，请求事件按受影响调度池更新评分、采样、保护、恢复和抖动 | 4、11.4、13.1 |
| 22 | 未硬降级且有样本欠账的健康或未知备用必须进入业务采样候选；样本充足的健康备用停止采样但保留成熟接管资格 | 5.5、6.6、10.1 |
| 23 | 自适应备援与稳定性硬保护独立判定；自适应期间暂停常规探索，结束后在同一次事件刷新中恢复探索 | 5.6、10.1、11.4 |
| 24 | 当前候选达到最少样本后，在下一次按池合并的 1 秒事件刷新中立即切换到下一个欠账渠道，不等待调度间隔 | 4、5.5、10.3、13.1 |
| 25 | 降级探测连续成功数按 `(渠道, 模型)` 持久化并跨实例共享；达标立即恢复，失败立即清零并续满冷却 | 6.3、11.3、15.3 |
| 26 | 429 是独立的 `(渠道, 模型)` 冷却；普通选路优先避让但无其他可用候选时仍可兜底，亲和、手动测试、全部探测和全部采样继续阻断 | 2.5、6.4、11.4 |
| 27 | 慢成功仍是成功；超过容忍数量的部分按稳定性总样本数逐个扣分，1 秒事件刷新滚动稳定性得分 | 11.1、11.2、16.14 |
| 28 | 自适应备援由错误率超过错误告警阈值，或首字告警成功请求占比达到独立阈值触发；两个信号按 OR 判定，不再使用合并风险请求比例作为统一进入门槛 | 5.6、11.4、17.1 |

第 6 项的例子：A 得分 92、B 得分 90，但切换门槛是 3 分，所以评分第一是 A，实际主渠道仍是 B；若参与路由 C 正在接受探索流量并被临时提升到同一最高层，实际最高层会同时包含 B 和 C。三个字段描述的是计算、智能决策和官方选路三个不同阶段，不是三个主渠道。

第 13 项的例子：任务开始时读取到 A/B/C 和配置版本 `R1`，随后管理员取消 B 参与并产生 `R2`。旧任务即使已经算完，也会在写入前发现版本或路由快照不一致，整个池零写入，A/B/C 继续使用上一轮结果；执行记录标记“配置冲突”，下一轮再按 `R2` 计算。

---

## 2. 核心模型

### 2.1 三个维度

| 维度 | 唯一键 | 用途 | 是否跨分组共享 |
| --- | --- | --- | --- |
| 调度池 | `(分组, 模型)` | 排名、主渠道、优先级、权重、固定意图和任务事务 | 否 |
| 观测键 | `(渠道, 模型)` | 业务请求、手动测试、定时探测、首字、TPS、稳定性样本和恢复后的共享观测边界 | 是 |
| 路由状态 | `(渠道, 分组, 模型)` | 参与状态、基础路由值、临时流量、稳定性状态和最近执行结果 | 否 |

### 2.2 共享样本原则

同一个渠道访问同一个上游模型时，不论请求来自哪个用户分组，上游性能都属于同一个观测对象。因此：

- `vip + model-a` 的真实请求会更新 `(渠道 1, model-a)`。
- `default + model-a` 读取同一份 `(渠道 1, model-a)` 数据。
- `vip + model-b` 不共享 `model-a` 的样本。
- 原始业务日志仍保留实际请求分组，便于审计；智能调度读取时再按渠道和模型聚合。
- 稳定性恢复推进的是 `(渠道, 模型)` 共享观测边界，不是单条 `(渠道, 分组, 模型)` 路由的私有边界；读取该渠道模型的所有分组从同一时刻重新积累当前指标。

### 2.3 官方请求路径

```text
智能调度任务
  -> 读取策略和观测数据
  -> 按 (分组, 模型) 生成完整计划
  -> 整池更新 Ability.Priority / Ability.Weight
  -> 刷新渠道缓存
  -> 官方选路处理真实请求
```

官方首请求在智能调度关闭时保持原有选路；智能调度开启时，对命中策略范围的调度池增加不可绕过的流量准入：

1. 全局智能调度关闭时，按原有分组、模型、请求路径、渠道状态、Ability 状态和排除渠道规则过滤。
2. 全局智能调度开启时，若当前分组存在已保存的分组策略且当前 Ability 模型命中策略模型范围，路由必须满足 `ParticipationSet=true`、`Excluded=false`；未配置策略的分组以及策略模型范围之外的模型池继续按原有规则过滤。
3. 在准入后的候选中继续过滤请求路径、渠道状态、Ability 状态、暂停和请求级排除渠道。
4. 从最高 `Priority` 层选择，同一优先级层内按 `Weight` 分流。
5. 未开启智能调度或当前池未命中策略范围时继续使用原有重试规则；命中策略范围时按第 2.5 节生成并循环当前分组的重试队列。

智能调度开启后，对于命中分组策略及模型范围的调度池，参与状态是流量资格，不是调度任务是否修改 P/W 的提示。从未设置参与状态或已取消参与的路由，即使拥有最高 P/W、渠道亲和、旧调度结果或人工残留值，也不能接收首请求、普通重试或同渠道快速重试；候选为空时直接返回无可用渠道，不读取默认 P/W 兜底。未配置分组和策略模型范围之外的模型池继续按官方规则接收流量。

优先级决定请求先进入哪一层；权重只决定同层渠道之间的比例。

### 2.4 精确模型与匹配模型

带参数模型遵循官方选路的“精确优先、匹配回退”规则。例如请求 `gemini-2.5-pro-thinking-2048` 时：

1. 先查找精确配置 `gemini-2.5-pro-thinking-2048` 的可用渠道。
2. 只有精确池没有任何可用渠道时，才回退到 `gemini-2.5-pro-thinking-*`。
3. 内存缓存开启和关闭时必须使用同一规则；不能因为关闭缓存而提前混入通配能力。

观测数据仍归一到匹配模型键，因此精确路由和它实际命中的上游模型读取同一份渠道模型性能数据；管理页面返回路由指标时再展开为具体的 `(分组, 模型, 渠道)` 行，确保所有关联分组看到一致数值。

### 2.5 智能调度重试队列

全局智能调度开启后，本节只对命中有效分组策略及模型范围的请求生效。纳管的 `(分组, 模型)` 只有存在可用参与路由时才会形成候选队列；没有参与路由时直接返回无可用渠道，不回退渠道菜单默认 P/W。没有有效策略或模型不在策略范围时继续使用官方原有候选集合和重试规则。

命中智能调度策略范围后，普通重试以本次请求已经确定的 `(分组, 模型)` 为范围，按照该池当前实际生效的优先级和权重生成候选队列：

1. 先应用与首请求一致的分组、精确模型、匹配模型、请求路径、渠道状态和 Ability 状态；429 冷却作为优先避让过滤，保留无其他候选时的最终兜底。
2. 按当前实际 `Priority` 从高到低进入队列；同一优先级层内继续按 `Weight` 选择，但本轮已经尝试过的渠道不再重复参与，实现同层按权重无放回。
3. 首请求失败并进入普通重试后，把失败渠道加入“本轮已尝试”集合，继续选择当前层剩余渠道；当前层耗尽后进入下一优先级层。
4. 当前轮所有可参与渠道都尝试过，而普通 `RetryTimes` 仍有余额时，清空本轮已尝试集合，重新读取当前有效路由并回到新队列的队首。
5. 新一轮必须读取最新生效的 P/W 和可用状态。渠道在上一轮失败后被稳定性降级、禁用或进入 429 冷却时，新一轮按变化后的队列排序；冷却渠道在存在其他候选时跳过，无其他候选时仍可回到队列兜底。
6. 如果当前没有任何真正可用的渠道，直接返回“无可用渠道”，不能只因为队列可循环而空转。

“当前轮全部候选已尝试”与“当前没有可用候选”是两个不同状态：前者清空排除集合并回到队首，后者直接结束本次请求。

队列循环只决定“下一次普通重试选谁”，不增加请求允许的总尝试次数。一次请求最多仍是“首次尝试 + `RetryTimes` 次普通重试”。快速失败同渠道重试使用独立额度：它先在当前渠道执行，不消耗普通 `RetryTimes`，也不推进或排除重试队列；该额度用尽后才进入下一条普通重试候选。

错误是否允许进入普通重试仍由现有错误分类、`skip_retry`、指定渠道限制和剩余 `RetryTimes` 决定；智能调度队列不能把不可重试错误强行变成重试。

例如唯一优先级为 `A(P3) -> B(P2) -> C(P1)`，且每次都失败：

```text
A -> B -> C -> A -> B -> C ...
```

只有 A 一条可用渠道时：

```text
A -> A -> A -> A ...
```

以上序列都在普通 `RetryTimes` 用尽时停止。稳定性降级的 `P0/W0` 和稳定性释放的 `P0/W10` 仍属于最后兜底候选，不因进入智能调度重试队列而被永久排除；禁用、模型或路径不匹配的渠道不属于当前可用候选，429 冷却渠道只有在没有其他可用候选时进入最终兜底。

当请求使用 `TokenGroup=auto` 时，先沿用现有自动分组选择和跨组回退规则确定当前分组；每次进入一个新分组都重新按该 `(分组, 模型)` 池构建队列，不能把不同分组的 P/W 混成一条队列。本文的循环规则约束的是每个已确定分组内的候选顺序。

---

## 3. 权限与控制边界

### 3.1 管理员能力

管理员可以：

- 开关全局智能调度。
- 配置性能窗口、稳定性评分窗口和保护失败窗口。
- 新增、修改和删除分组策略。
- 设置整个渠道或单条路由是否参与调度。
- 手动执行或强制重置调度。
- 固定、续期或解除主渠道。
- 选择固定期间是否允许稳定性降级，默认勾选允许。
- 手动解除稳定性保护。
- 查看评分明细、路由详情和完整执行记录。

### 3.2 普通用户

普通用户不配置、不查看智能调度。普通请求只读取已生效的官方路由值。

### 3.3 路由值所有权

| 路由状态 | 优先级和权重所有者 | 调度任务行为 |
| --- | --- | --- |
| 参与调度 | 智能调度 | 可以按策略修改 |
| 未参与调度 | 官方路由或无流量资格 | 调度任务不修改；命中已启用策略范围时首请求、重试和亲和均排除，未命中时按官方规则选路 |
| 渠道或 Ability 禁用 | 官方状态 | 禁用路由本身不接收流量；若它承载有效固定意图，立即撤回池内统一采样提权，但保留未到期的固定意图 |
| 管理员固定 | 管理员固定意图 | 固定优先于正常评分计划 |
| 稳定性保护 | 稳定性状态机 | 按降级、试放或恢复规则修改 |

取消参与调度时清除该路由的分组优先级和权重。当前调度池命中已启用策略范围时，该路由立即失去流量资格；重新参与后由当前策略重新写入分组优先级和权重。调度池未命中策略范围或全局智能调度关闭时，官方选路忽略参与状态并按当前 Ability 或渠道默认值工作。

---

## 4. 全局配置

| 页面字段 | API 字段 | 默认值 | 范围 | 说明 |
| --- | --- | ---: | ---: | --- |
| 智能调度 | `smart_schedule_enabled` | 关闭 | 布尔值 | 开启后只接管已配置策略及模型范围；未配置分组继续使用官方选路；保存时仍要求至少存在一项完整策略 |
| 性能窗口 | `smart_schedule_performance_window_minutes` | 60 分钟 | 1–43200 | 成本之外的首字和 TPS 评分窗口 |
| 稳定性评分窗口 | `smart_schedule_stability_window_minutes` | 5 分钟 | 1–43200 | 成功、失败、失败耗时和抖动的软评分窗口，不直接触发硬降级 |
| 429 冷却时间 | `smart_schedule_rate_limit_cooldown_seconds` | 30 秒 | 0–300 | 上游返回 429 后，按“渠道 + 模型”优先避让；无其他可用候选时仍可兜底，0 表示关闭 |
| 分组策略 | `smart_schedule_group_policies` | 空 | 最多 100 组 | 每个分组只能有一份完整策略 |
| 强制重置 | `smart_schedule_force_reset` | 一次性命令 | 布尔值 | 忽略切换分差，重算当前完整计划 |

配置值使用任意整数分钟，不再限制为 15、60、360、1440 等固定档位。智能调度实际启用（全局开关开启且存在有效分组策略）时，渠道监控主表的统计范围固定使用 `smart_schedule_performance_window_minutes`，手动分钟参数不参与最终计算，查询范围允许完整的 `1–43200` 分钟。智能调度关闭时，主表恢复手动统计范围并限制为 `1–1440` 分钟。后端必须返回实际采用的分钟数和来源，不能由前端自行推断。

智能调度不再依赖分钟周期：

- 配置变更、管理员手动执行、强制重置和需要重建基础路由的管理操作会创建一次性完整调度任务。
- 统一探索采样、采样对象切换、自适应备援健康与预算、成熟候选接管判断、首字慢成功和滚动稳定性得分，在有效请求或样本完成后按 `(分组, 模型)` 合并 1 秒刷新。
- 稳定性硬保护在有效非 429 失败完成后立即更新秒级失败窗口并执行降级；429 在收到响应时立即建立独立门禁，二者都不等待事件合并或定时任务。
- 常规和降级探测按 `probe_interval_minutes` 独立运行；探测成功达标、探测失败续期、冷却到期试放和 429 冷却到期由结果事件或独立计时器立即处理。
- 完整调度正在计算时暂存软事件，待新的基础排名和基础 P/W 提交后基于新快照重放；刷新期间到达的新事件必须至少再触发一次刷新。硬保护和 429 不等待完整调度。
- 分钟聚合只服务于完整评分、看板和历史统计，不是统一采样、稳定性保护、自适应备援、降级探测或恢复生效的前置条件。

关闭全局调度时：

- 不创建新的调度和定时探测任务。
- 保留已生效的基础路由结果。
- 清除统一探索和自适应备援产生的临时提权，恢复基础路由值；已有硬保护状态不因关闭全局调度自动解除。
- 停止使用分组策略和参与状态作为请求流量门禁，恢复官方 Ability 候选集合。
- 不删除样本和历史执行记录。

---

## 5. 分组策略

### 5.1 基础字段

| 字段 | 默认值 | 范围或枚举 | 说明 |
| --- | ---: | --- | --- |
| `group` | 无 | 1–64 字符 | 分组唯一键 |
| `models` | 空 | 最多 100 个 | 空表示分组内全部模型 |
| `model_order` | 空 | 最多 100 个 | 只影响看板顺序 |
| `strategy` | `smart` | `smart/ratio/first_token/tps` | 评分策略 |
| `apply_mode` | `priority_weight` | `priority_weight/weight` | 路由应用方式 |
| `min_samples` | 5 | 1–100000 | 必需指标的最少样本数 |

### 5.2 评分字段

| 字段 | 默认值 | 范围 | 说明 |
| --- | ---: | ---: | --- |
| `scoring.stability_percent` | 50% | 0–100% | 稳定性在最终得分中的占比 |
| `scoring.primary_traffic_percent` | 90% | 51–99% | 仅权重模式中的主渠道目标流量 |
| `scoring.primary_switch_threshold_percent` | 3% | 0–100% | 挑战渠道至少领先多少分才切主渠道 |
| `scoring.smart.cost_ratio_percent` | 40% | 0–100% | 综合评分成本占比 |
| `scoring.smart.first_token_percent` | 40% | 0–100% | 综合评分首字占比 |
| `scoring.smart.tps_percent` | 20% | 0–100% | 综合评分 TPS 占比 |
| `scoring.ratio.cost_ratio_percent` | 70% | 0–100% | 成本策略成本占比，必须大于 0 |
| `scoring.ratio.first_token_percent` | 20% | 0–100% | 成本策略可选首字占比 |
| `scoring.ratio.tps_percent` | 10% | 0–100% | 成本策略可选 TPS 占比 |

`smart` 和 `ratio` 内部的三个指标占比都必须合计 100%。

### 5.3 稳定性与抖动字段

| 字段 | 默认值 | 范围 | 说明 |
| --- | ---: | ---: | --- |
| `stability_enabled` | 开启 | 布尔值 | 稳定性评分和保护总开关 |
| `recovery_stability_score` | 95% | 0–100% | 试放阶段的恢复评分参考；不用于正常路由进入硬保护 |
| `fast_failure_penalty_percent` | 40% | 0–100% | 快速重试失败的单次惩罚 |
| `fast_failure_seconds` | 1 秒 | 大于 0 且小于 60 | 快速失败界限 |
| `fast_failure_same_channel_retry_count` | 0 次 | 0–10 | 快速失败时当前渠道的独立额外重试次数，0 表示关闭 |
| `fast_failure_same_channel_retry_delay_ms` | 1000 毫秒 | 0–60000 | 每次同渠道快速重试前的固定等待，0 表示不等待 |
| `slow_failure_seconds` | 10 秒 | 大于快速失败且不超过 60 | 完整失败惩罚界限 |
| `burst_failure_window_minutes` | 1 分钟 | 1–60 | 正常路由进入硬保护时统计的最长时间范围 |
| `burst_failure_window_requests` | 100 次 | 1–1000 | 时间范围内只统计最近的请求数；成功请求也进入失败率分母 |
| `consecutive_failure_threshold` | 2 次 | 1–100 | 连续上游失败达到该次数时立即进入硬保护；成功会清零连续计数 |
| `burst_failure_threshold_percent` | 3% | 大于 0–100% | 最近请求窗口内失败请求占比达到该值时立即进入硬保护；429 不计入 |
| `recovery_success_threshold` | 2 次 | 1–100 | 稳定性试放或降级探测连续成功达到该次数时允许恢复 |
| `cooldown_minutes` | 30 分钟 | 1–525600 | 降级冷却时间 |
| `degraded_probe_enabled` | 关闭 | 布尔值 | 是否在稳定性降级期间按探测间隔主动探测并允许达标后提前恢复 |
| `jitter_enabled` | 开启 | 布尔值 | 首字慢成功保护 |
| `jitter_tolerance_percent` | 5% | 0–50% | 窗口内允许的慢成功比例 |
| `jitter_slow_threshold_seconds` | 10 秒 | 0–60 | 固定首字慢成功阈值 |

当前策略只接受 `jitter_slow_threshold_seconds`；已删除的旧抖动字段不再参与配置或运行时计算。

稳定性评分和稳定性硬保护是两个独立概念：分钟级稳定性评分用于比较渠道、分配流量和展示趋势；硬保护只响应最近 `burst_failure_window_minutes` 分钟内最多 `burst_failure_window_requests` 次请求。窗口按请求完成时间取最新请求，成功进入分母，失败率达到 `burst_failure_threshold_percent` 才触发；连续失败阈值仍可独立触发。历史低分可以让渠道在正常排名中获得更少流量，但不能单独把正常路由降到 `P0/W0`。

### 5.4 样本补充与探索流量字段

| 字段 | 默认值 | 范围或枚举 | 说明 |
| --- | ---: | --- | --- |
| `sample_mode` | `off` | `off/traffic/probe` | 关闭、探索流量、定时探测 |
| `exploration_traffic_percent` | 3% | 大于 0 且不超过 20% | 样本不足探索的目标流量 |
| `exploration_max_prompt_tokens` | 50K（50000 Token） | `0K` 或 `1K–1000K`，按 `1K` 递增 | 样本不足探索流量的最大预估 Prompt Token；`0K` 表示无限制 |
| `sampling_order` | `priority_weight` | `priority_weight/ratio` | 探索和自适应备援共用的欠账渠道排序 |
| `probe_interval_minutes` | 10 分钟 | 1–525600 | 常规样本探测和降级期间探测共用的间隔 |

页面先展示“样本补充方式”。选择“探索流量”后，在同一个探索流量配置组内依次展示“目标探索流量”“探索请求上限”“统一采样顺序”；选择“定时探测”时展示“探测间隔”。“统一采样顺序”不作为样本补充组外的独立常驻字段展示；切换到其他补充方式后仍保留已保存值，供自适应备援复用。探索请求上限在页面中以 K Token 输入和回显，`1K = 1000 Token`；接口、持久化和运行时字段仍保存实际 Token 数，因此页面提交时乘以 `1000`、回显时除以 `1000`。

探索流量和自适应备援只允许用于 `priority_weight` 应用方式。`sampling_order=priority_weight` 时，先按当前分组模型的基础优先级降序，再按基础权重降序；`sampling_order=ratio` 时，按换算后的成本倍率升序。临时提权后的当前 P/W 不参与排序，避免采样结果反过来改变自己的顺序。

### 5.5 统一采样候选与切换

探索流量和低优先级轮转是同一种“给非主渠道补充真实样本”的行为，合并为一条统一采样队列。候选按以下三层处理：

1. **未硬降级且有样本欠账**：可以接受探索或自适应业务采样。健康备用有欠账时必须保留在候选中；健康证据未知或正在逐渐变差但尚未硬降级时仍可受控采样，但不能仅凭缺少比较对象的相对满分接管。
2. **已硬降级或稳定性试放**：只接受降级探测、试放或管理员手动测试，不得被业务采样临时提权。
3. **429 冷却、禁用、未参与或模型不匹配**：禁用、未参与或模型不匹配会完全阻断；429 冷却不进入探测和采样候选，普通请求优先避让但无其他可用候选时允许最终兜底。

样本充足的健康备用不再取得采样流量，但仍是成熟接管候选。采样候选和接管候选必须分开计算，不能因为“无需补样本”把健康备用从正常选主候选中删除。

每个池同时只给一条欠账渠道业务采样流量。选择时先应用候选层，再按 `sampling_order` 排序；同序时优先继续当前仍有欠账的对象，再按最久没有新增有效样本、最久没有被采样、渠道 ID 升序决定。每个有效样本完成后触发池级事件，事件合并 1 秒后重新计算欠账；当前渠道达到要求、进入硬降级或 429 冷却时立即撤回其采样覆盖并选择下一条欠账渠道，不等待完整调度时间。

系统不再配置探索租约或轮转间隔，也不按固定时间强制更换仍有欠账的候选。以下字段直接删除，不读取、不回显、不迁移旧值：

- `priority_sampling_enabled`
- `priority_sampling_interval_minutes`
- `priority_sampling_base_percent`
- `priority_sampling_decay_percent`
- `priority_sampling_min_percent`

### 5.6 自适应备援采样字段

自适应备援采样属于稳定性保护中的软降级。正常情况下保持主渠道的基础流量；主渠道逐渐不可用时，从主渠道流量中切出配置预算，按第 5.5 节的统一队列验证有样本欠账的备用渠道。硬保护、自适应备援和常规探索分别判断：满足硬保护就立即降级；仅满足软压力条件时执行自适应备援；自适应期间暂停常规探索，结束后在同一次事件刷新中恢复探索。

| 字段 | 默认值 | 范围 | 说明 |
| --- | ---: | --- | --- |
| `adaptive_sampling_enabled` | 开启 | 布尔值 | 是否启用软降级期间的自适应备用采样 |
| `adaptive_sampling_base_percent` | 3% | 0–10% | 主渠道有软压力时的起始备用预算 |
| `adaptive_sampling_max_percent` | 30% | 1–49% | 备用预算上限；正常单主渠道场景下，主渠道最低保留比例自动为 `100% - 本值` |
| `adaptive_sampling_error_warning_percent` | 5% | 0–100% | 非 429 错误率超过该值后开始增加错误压力，并独立触发压力状态 |
| `adaptive_sampling_error_critical_percent` | 15% | 0–100% | 错误压力达到最高的非 429 错误率阈值 |
| `adaptive_sampling_first_token_warning_seconds` | 5 秒 | 0–60 秒 | 首字延迟开始增加压力的阈值 |
| `adaptive_sampling_first_token_critical_seconds` | 10 秒 | 0–60 秒 | 首字延迟达到最高压力的阈值 |
| `adaptive_sampling_first_token_warning_request_percent` | 10% | 大于 0–100% | 窗口内首字达到告警秒数的成功请求占比达到该值后，独立触发压力状态 |
| `adaptive_sampling_recover_request_percent` | 95% | 大于 0–100% | 窗口内健康请求比例达到该值后恢复健康；与首字告警请求占比阈值之和必须大于 100% |
| `adaptive_sampling_window_minutes` | 10 分钟 | 1–60 | 请求比例统计的最长时间范围 |
| `adaptive_sampling_window_requests` | 100 次 | 1–1000 | 时间范围内只统计最近的业务、手动测试和定时探测请求 |
| `adaptive_sampling_switch_confirm_request_percent` | 95% | 大于 0–100% | 评分胜出的备用渠道窗口内健康请求比例达到该值后才允许接管 |
| `adaptive_sampling_min_comparable_channels` | 2 | 2–10 | 相对指标参与比较所需的最少可比渠道数 |

页面在基础和最大两项备援预算之后，依次展示错误告警/高风险阈值、首字告警/高风险秒数、首字告警请求占比、恢复健康请求占比、请求比例窗口、切换确认请求占比和最少可比渠道数。

```text
非 429 错误率 = 非 429 错误请求数 / 窗口有效请求数 × 100%
首字告警成功请求占比 = 首字达到告警秒数的成功请求数 / 窗口有效请求数 × 100%
进入压力状态 = 非 429 错误率 > 错误告警率
              OR 首字告警成功请求占比 >= 首字告警请求占比阈值
```

错误率、首字告警成功请求占比、风险请求占比和健康请求占比都从自适应窗口内等价计数的真实业务、手动测试和定时探测样本计算；带探测或手动测试标记的生产日志先排除，再从共享样本缓冲区各计一次，避免重复。429 和最终重试汇总不计入这些比例。风险请求仍是非 429 错误与首字耗时达到告警阈值的成功流式请求的并集，用于看板和执行明细展示；健康请求是成功且首字未达到告警阈值的请求，没有首字数据的成功请求按健康处理。

进入压力改为两个独立信号的 OR：非 429 错误率**超过** `adaptive_sampling_error_warning_percent`，或首字耗时达到告警秒数的成功请求占比**达到** `adaptive_sampling_first_token_warning_request_percent`。合并后的风险请求占比不再作为统一进入门槛。进入后只有两个进入信号都已解除，且健康请求占比达到 `adaptive_sampling_recover_request_percent` 时才退出；备用渠道接管仍使用自己的健康请求占比和 `adaptive_sampling_switch_confirm_request_percent`，不再使用调度轮数或独立租约时间。

备用预算由样本欠账和主渠道健康压力共同决定：

```text
样本欠账 = max(最少样本 - 当前有效样本, 0)
样本需求 = clamp(最大样本欠账比例, 0, 1)
健康压力 = max(错误压力, 首字延迟压力)
备用预算 = 样本需求 × (基础预算 + 健康压力 × (最大预算 - 基础预算))
```

错误压力在错误告警和高风险错误率之间线性归一；每个成功首字样本在首字告警和高风险秒数之间线性归一，再按窗口有效请求聚合为首字延迟压力。任一进入信号成立后，使用 `max(错误压力, 首字延迟压力)` 计算预算；两个信号是进入状态的 OR，不先把错误和慢首字合并后再通过同一个比例门槛。进入后，只有两个进入信号都解除且健康请求比例达到 `adaptive_sampling_recover_request_percent` 才退出，仍命中的进入信号优先于恢复判断。实际预算不得超过 `adaptive_sampling_max_percent`；正常单主渠道场景下，主渠道实际流量等于 `100% - 实际备用预算`，因此最大预算同时决定主渠道最低保留比例。保存时必须校验首字告警请求占比阈值与恢复健康占比之和大于 `100%`，保证首字进入和恢复之间存在滞回。

自适应采样与常规探索共用 `sampling_order`、样本欠账、Prompt Token 上限和候选层。自适应条件满足时，暂停 `exploration_traffic_percent` 产生的覆盖，只应用自适应预算；主渠道恢复后撤回自适应覆盖，并在同一次 1 秒事件刷新中对仍有欠账的候选恢复常规探索。候选达到样本要求后立即进入成熟候选，并把采样流量切给下一条欠账渠道；成熟候选只有同时满足最少可比渠道数、自身健康比例、切换分差和主渠道状态条件时才可以即时接管。

| 配置组合 | 主渠道健康 | 主渠道进入软压力 |
| --- | --- | --- |
| `sample_mode=off`，自适应关闭 | 不主动补量 | 仅保留硬保护和普通重试 |
| `sample_mode=off`，自适应开启 | 不主动补量 | 按自适应预算验证欠账备用 |
| `sample_mode=traffic`，自适应关闭 | 按探索比例执行统一采样 | 仍保持固定探索比例，不自动放大 |
| `sample_mode=traffic`，自适应开启 | 按探索比例执行统一采样 | 暂停探索，由自适应预算接管；结束后立即恢复探索 |
| `sample_mode=probe`，自适应开启 | 按探测间隔补量 | 允许自适应业务采样验证欠账备用；恢复后继续原探测方式 |

“主渠道半可用时不会获得自适应流量”不是指主渠道被排除或拿不到请求。自适应流量是**从主渠道现有流量中切给备用渠道的验证预算**，不是给主渠道新增的流量；半可用主渠道仍保留不低于配置下限的大部分请求。真正不再取得采样流量的是“已经样本充足的健康备用”，但它仍保留成熟接管资格。

例如窗口为 600 秒，主渠道 A 最近 100 个有效请求中有 6 个非 429 错误，错误率 `6%` 已超过默认错误告警率 `5%`，即使合并风险请求占比只有 `6%` 也会开始备援采样。另一种情况是 A 没有错误，但 100 个有效请求中有 12 个成功请求的首字达到告警秒数，首字告警请求占比 `12%` 达到默认阈值 `10%`，同样会独立触发。两种信号按 OR 判定；恢复仍需要至少 `95%` 的请求健康。备用渠道 B 即使评分领先，也要在同一窗口内达到 `95%` 健康请求比例并满足最少样本、最少可比渠道数和切换分差，才可以接管 A。

### 5.7 稳定性释放请求上限

| 字段 | 默认值 | 范围 | 说明 |
| --- | ---: | ---: | --- |
| `stability_release_max_prompt_tokens` | 0K（0 Token） | `0K` 或 `1K–1000K`，按 `1K` 递增 | 稳定性释放（`P0/W10`）流量的最大预估 Prompt Token；`0K` 表示无限制 |

探索请求上限已按前端展示顺序放在第 5.4 节的“探索流量”配置组内；稳定性释放请求上限位于稳定性配置组。两个值都在页面中以 K Token 输入和回显，按路由类型分别保存，不是请求次数、QPS、计费上限或硬性请求大小限制。探索字段默认 `50K`；稳定性释放字段默认 `0K`，避免升级后突然改变既有稳定性释放行为。接口、持久化和运行时仍使用实际 Token 数，只接受 `0` 或 `1000` 的整数倍，最大 `1000000 Token`。两类特殊路由可以使用不同阈值，互不继承、互不覆盖。

请求上限只在每次选路前参与候选软过滤，具体规则见第 10.5 节。它不改变基础 P/W、稳定性评分或样本计数；阈值配置变化只影响后续请求和重试的候选排序，已经发往上游的请求不回收、不切换。

---

## 6. 样本与统计口径

### 6.1 样本来源

| 来源 | 是否等价计数 | 可提供稳定性 | 可提供首字 | 可提供 TPS |
| --- | --- | --- | --- | --- |
| 真实业务请求 | 是 | 是 | 流式请求可提供 | 有 usage 时可提供 |
| 手动渠道测试 | 是 | 是 | 选择流式时提供 | 有 usage 时提供 |
| 定时探测 | 是 | 是 | 流式成功时提供 | 有 usage 时提供 |

三种来源不设置系数。真实调用已经到达上游后，除 429 外的一次成功或失败都按一个样本计入；探测不是只改状态的虚拟请求，也必须进入同一份共享样本。

### 6.2 手动测试

手动测试满足以下条件即写入共享样本：

1. 全局智能调度已开启。
2. 能确定测试使用的原始模型。
3. 该渠道模型至少关联一条启用、参与调度且命中分组策略的路由。
4. 该 `(渠道, 模型)` 当前不在 429 冷却中。

不要求该分组选择定时探测或探索流量。一次测试只写入一份 `(渠道, 模型)` 样本，所有关联分组共享。

测试结果必须展示：

- 成功或失败。
- 总耗时。
- 是否计入智能调度样本及原因。
- 流式测试的首字时间。
- 可取得时的 TPS 和输出 token。

非流式成功测试不把总耗时冒充首字时间。

### 6.3 定时探测

定时探测对以下文本模型路由执行：正常路由和稳定性试放路由要求 `sample_mode=probe`；稳定性降级路由要求同时开启稳定性保护与 `degraded_probe_enabled`。降级探测与样本补充方式相互独立，可以在 `sample_mode=off` 或 `traffic` 时单独开启。

- 同一个 `(渠道, 模型)` 被多个分组覆盖时，只发送一次请求。
- 多个符合条件的策略探测间隔不同时，使用最短间隔。
- 探测发出前必须经过 429 总门禁；冷却中直接跳过，不产生伪造样本。
- 使用小型流式 Responses 请求，输出 token 上限为 16。
- 实际请求已发到上游后，无论成功或失败都写入样本。
- 本地参数转换失败、不支持协议或并发已满时跳过，不伪造上游失败。
- 探测占用渠道并发额度并产生真实上游成本。
- 探测日志带专用标记，不重复进入普通业务分钟统计。

降级探测连续成功数按 `(渠道, 模型)` 持久化，并在多实例之间共享。实际到达上游的非 429 稳定性失败会清零该次数，并立即把所有已开启降级探测的关联降级路由截止时间更新为 `max(原截止时间, 本次失败时间 + cooldown_minutes)`，不经过连续失败或保护失败窗口阈值；429 仍只进入独立限流冷却，未实际发出的探测仍按跳过处理，二者都不续期稳定性降级。当某个降级路由的共享连续成功数达到其分组 `recovery_success_threshold` 时，在该次探测结果事务中立即恢复保护前保存的优先级和权重、推进共享观测边界并刷新路由缓存，不等待 `cooldown_minutes` 到期或下一轮完整调度。协议成功即累计恢复成功；即使首字属于慢成功，也只通过抖动得分影响评分恢复路径，不把本次协议成功改判为探测失败。探测仍占用并发、写日志、计入共享样本并产生真实上游成本。

### 6.4 上游 429

429 表示上游并发或限流暂时达到上限，与渠道稳定性失败分开处理：

- 当前请求仍按现有重试规则尝试其他渠道。
- 若智能调度开启且 `smart_schedule_rate_limit_cooldown_seconds` 大于 0，记录该 `(渠道, 模型)` 的短时冷却，到期自动恢复。
- 冷却对所有关联分组生效，不修改任何分组的优先级、权重、稳定性状态或稳定性保护计时。
- 冷却在普通选路中是优先避让条件：同一已确定分组还有其他符合请求条件且未被本轮排除的候选时不能命中；分组只有该渠道或其他候选都已尝试、被排除或不可用时，可以作为最终兜底。已有渠道亲和、管理员手动测试、常规定时探测、降级恢复探测、统一探索和自适应备援仍不能命中该渠道模型。
- 冷却结束后由独立计时或下一次入口检查立即放行，原亲和记录可以继续复用，不等待完整调度。
- 同一渠道模型在冷却期间再次收到 429，只能延长到更晚的到期时间，不会缩短已有冷却。
- 429 仍进入普通错误日志和成功率统计，便于管理员查看上游限流情况；但不计入智能调度稳定性样本、失败惩罚、重试失败耗时和稳定性降级门槛。
- 本地渠道并发额度耗尽产生的跳过重试错误不属于上游 429，不触发本配置。

429 冷却不写数据库。Redis 已启用且可用时，冷却截止时间写入 Redis 有序集合，所有实例按 `(渠道, 模型)` 共享；Redis 未启用、访问失败或共享状态接管失败时，收到 429 的节点继续使用本地内存状态兜底。相同渠道模型的后续 429 使用“只延长、不缩短”的原子更新规则；Redis 共享截止时间晚于本地值时，本地同步到共享值。冷却写入必须携带本次请求读取到的调度控制版本，配置版本已变化时拒绝旧请求重新创建冷却。任意智能调度配置变更都会生成新控制版本并统一清空现有本地与 Redis 冷却；选路只读取与当前本地控制版本一致的 Redis 数据，其他修订的数据一律忽略。Redis 丢失版本键或恢复旧快照时，收到新 429 的实例必须重新校验数据库控制版本，再通过 Redis 比较并交换接管共享状态、清除归属不明的旧冷却并写入本次冷却；比较失败时不得覆盖并发产生的新状态。

例如：默认冷却 30 秒时，`channel-2 + gpt-4o` 收到 429，接下来 30 秒各分组都会优先选择其他可用路由；某个分组只有这一路由，或本轮其他路由都不可用时，仍可选择它兜底。`channel-2 + claude-3` 不受影响，30 秒后 `gpt-4o` 自动恢复正常候选优先级。若设置为 0，系统只保留现有重试和日志行为，不创建冷却。

### 6.5 滚动保留与恢复边界

- 共享样本表唯一键为 `(渠道, 模型)`。
- 每个共享观测键持久化 `observation_since`，表示最近一次稳定性恢复后的当前观测起点。
- 单键最多保留 1500 个滚动样本。
- 同一来源和同一请求 ID 重复写入时去重。
- 保留时长按性能窗口和稳定性评分窗口中的较大值决定。
- 性能评分的实际起点为 `max(当前时间 - 性能窗口, observation_since)`；稳定性和抖动评分的实际起点为 `max(当前时间 - 稳定性评分窗口, observation_since)`。
- 渠道监控主表和智能调度看板使用同一边界。主表汇总多个模型时，先对每个 `(渠道, 模型)` 应用各自边界，再汇总剩余数据。
- 推进边界不删除或改写滚动原始样本、消费日志、错误日志、分钟聚合和延迟分桶。今日统计、按日趋势、历史明细和长期模型统计不应用该边界，仍可读取恢复前的数据。
- 边界后没有新样本时，样本数显示 `0`，首字、TPS、成功率、缓存率和最后请求时间显示 `-`，不能把“无数据”显示为 `0`、`0ms` 或 `0%`。
- 看板将窗口内生产观测与手动测试/定时探测样本明确区分：首字 P50 和 TPS 来自生产分钟聚合，稳定性按评分实际使用的合并窗口展示，手动/探测样本单独显示数量，避免把测试样本误认为全部生产流量。

### 6.6 样本不足

样本不足不等于渠道不稳定：

- 在 `priority_weight` 中排在所有有效评分渠道之后，仍保留独立基础优先级。
- 未硬降级的健康备用只要仍有任一启用指标的样本欠账，就必须保留在统一采样候选中。
- 选择探索流量时，统一采样对象可以临时进入最高优先级层取得少量真实请求。
- 选择定时探测时，通过主动探测补样本。
- 选择关闭时，只等待真实业务或手动测试补样本。
- 健康备用达到最少样本后停止接收采样流量，但继续作为成熟接管候选；采样资格和接管资格不能混为一谈。

探索流量和稳定性释放都可以配置独立的最大预估 Prompt Token。该阈值只用于减少大上下文请求突然切到特殊路由造成的缓存损失，不代表特殊路由永远不能接收大请求；没有其他候选时仍必须允许它们兜底。

---

## 7. 评分规则

### 7.1 策略

| 策略 | 必需数据 | 行为 |
| --- | --- | --- |
| `smart` | 当前可比较的成本、首字和 TPS | 不可比较的动态指标先剔除，其余指标重新归一后合并 |
| `ratio` | 成本倍率 | 首字和 TPS 有足够可比样本时加入，否则对剩余指标重归一 |
| `first_token` | 至少配置数量的渠道达到首字最少样本数 | 首字越低越好；只有一条有效渠道时不评分切主 |
| `tps` | 至少配置数量的渠道达到 TPS 最少样本数 | TPS 越高越好；只有一条有效渠道时不评分切主 |

稳定性开启且稳定性占比大于 0 时，稳定性样本也必须达到 `min_samples`。

首字、TPS 和稳定性等相对观测指标只有达到 `adaptive_sampling_min_comparable_channels` 个有效渠道时才可比较。只有一个渠道有足够样本时，该指标状态为“不可比较”，不能因同池最大值等于最小值而自动得到满分；系统保留上一轮正常主渠道并继续补充其他渠道样本。样本充足且健康的备用仍保留在正常选主候选中，不能因为不再需要采样而被过滤。

### 7.2 Min-Max 归一化

成本倍率和首字越低越好：

```text
归一化分 = (同池最大值 - 当前值) / (同池最大值 - 同池最小值)
```

TPS 越高越好：

```text
归一化分 = (当前值 - 同池最小值) / (同池最大值 - 同池最小值)
```

只有一个有效值时不执行 Min-Max，该项记为“不可比较”且归一化分为空。有效渠道数量达到配置要求且最大值等于最小值时，该项才统一记为 1。归一化结果范围为 0–1，页面按 0–100 分展示。

### 7.3 业务分和最终分

```text
业务分 = Σ(可用业务指标归一化分 × 该指标有效占比)

最终分 = 业务分 × (1 - 稳定性占比)
       + 稳定性分 × 稳定性占比
```

缺失的可选指标从本轮有效占比中剔除，其余指标重新归一到 100%。缺失必需指标时不生成有效最终分。

### 7.4 Min-Max 的已知边界

本版继续使用现有 Min-Max，不增加“相对权重拉伸”或“得分曲线指数”配置。管理员需要知道它表达的是候选中的相对位置，而不是原始差距大小。

例 1：只有成本 1.00 和 1.01 两条渠道时，归一化后分别是 100 和 0，虽然成本只差 1%。

例 2：加入成本 2.00 的第三条渠道后，前两条变为 100 和约 99。原始数据没变，但比较范围变了。

例 3：首字 1000ms 和 1050ms 的两条渠道也会得到 100 和 0，虽然只差 50ms。

因此本版使用“主渠道切换分差”减少频繁切换，并在执行记录中同时展示原始值、比较范围和贡献，避免只看总分误判。后续若要增加原始指标改善门槛，应作为独立需求评审。

### 7.5 评分快照

每条执行明细必须保存：

- 策略、应用方式、最少样本数和观测范围。
- 性能窗口和稳定性评分窗口。
- 成本、首字、TPS、稳定性原始值及样本数。
- 同池各指标最小值、最大值和可比较渠道数。
- 配置占比、缺失数据处理后的有效占比。
- 各指标归一化分、业务贡献、稳定性贡献和最终分。
- 评分第一、当前主渠道、实际主渠道和选择原因。
- 基础排名、基础 P/W、当前应用 P/W、临时流量类型和比例。

缓存命中率可以作为观察信息展示，但必须标注“未参与评分”。

---

## 8. 主渠道决策

### 8.1 三个解释字段

- **评分第一**：只按本轮最终得分排序后的第一名。
- **实际主渠道**：应用当前主渠道保护、切换分差、固定意图和稳定性状态后，智能调度选择的主要渠道。
- **实际最高优先级层**：通过分组策略和参与状态准入后，官方选路真正最先访问的全部路由，可能包含临时采样或稳定性释放路由，但绝不包含未参与路由。

这不是三套状态。它们用于解释同一轮决策的不同阶段。

### 8.2 选择顺序

1. 过滤禁用、未参与和处于稳定性保护的路由。
2. 计算有效评分并得到评分第一。
3. 管理员固定且当前未降级时，固定渠道直接成为实际主渠道。
4. 没有固定时，若当前主渠道仍有效且挑战渠道领先未达到切换分差，保留当前主渠道。
5. 达到切换分差、强制重置或当前主渠道失效时，使用评分第一。
6. 同分时优先保留当前主渠道或上一轮相对顺序，再按渠道 ID 稳定排序。

### 8.3 为什么评分第一不一定是实际主渠道

示例：当前主渠道 B 得分 90，A 得分 92，切换分差为 3。A 只领先 2 分，所以页面显示：

```text
评分第一：A
实际主渠道：B
原因：挑战渠道未达到 3 分切换分差
```

如果 B 被管理员固定，即使 A 得分更高，实际主渠道仍是 B；如果 B 允许稳定性降级且已降级，实际主渠道改为当前健康渠道，但固定意图仍保留到到期。

---

## 9. 路由应用方式

### 9.1 仅权重 `weight`

- 保留每条参与路由原有优先级。
- 只在相同优先级层内比较和分配权重。
- 同层少于两条可调渠道时不修改。
- 同层实际主渠道取得 `primary_traffic_percent`，其余权重按得分比例分配。
- 分配总权重使用 1000；最低有效分按 0.01 参与分配，避免健康渠道被分到无法使用的负值。
- 低优先级层在高优先级可用时不会取得普通首请求。
- 统一探索流量和自适应备援不在此模式执行。

该模式适合管理员已经人工设计好优先级层，只希望系统调整同层流量的场景。

### 9.2 优先级分层 + 权重 `priority_weight`

#### 9.2.1 基础排名

正常参与路由按以下顺序生成基础排名：

1. 当前主渠道切换保护后的自动主渠道。
2. 其余样本有效渠道按最终分降序。
3. 同分优先保留上一轮相对顺序，再按渠道 ID。
4. 样本不足渠道排在所有有效评分渠道之后。
5. 样本不足渠道之间优先上一轮相对顺序，再按渠道 ID。
6. 稳定性降级渠道不进入正常排名，统一进入 `P0/W0`。

管理员固定只改变当前应用的优先级，不改变评分生成的基础排名和基础 P/W。固定期间仍持续更新真实的自动排名；解除固定或固定到期时，必须立即回到当时的基础 P/W，不等待下一轮调度。

#### 9.2.2 唯一优先级

有 `N` 条正常参与路由时：

```text
第 1 名 -> PN / W1000
第 2 名 -> P(N-1) / W1000
...
第 N 名 -> P1 / W1000
稳定性降级 -> P0 / W0
```

十条健康渠道会形成 `P10` 到 `P1` 十个独立优先级，不再只有第一备用和第二备用。渠道数量变化时，具体 P 值可以变化；产品稳定含义是基础排名和相对顺序。

#### 9.2.3 未参与路由

全局智能调度开启后，命中策略及模型范围的池中，未参与路由不进入候选集合：

- 不参与首请求分流。
- 不参与普通重试、同渠道快速重试或请求级兜底。
- 旧渠道亲和命中该路由时按无资格处理。
- 即使 Ability 上仍存在历史 P/W，也不得据此恢复流量。

页面仍展示未参与路由用于管理员重新开启参与，但预计流量固定为 `0%`，且不能把它计入“实际最高优先级层”。

---

## 10. 统一真实流量采样

### 10.1 通用规则

- 同一调度池同一时刻最多一条业务采样渠道；`P0/W10` 稳定性释放是独立恢复状态，不占用业务采样名额。
- 常规探索和自适应备援共用一条候选队列；自适应条件满足时暂停常规探索，自适应结束时立即恢复。
- 业务采样渠道提升到实际最高优先级层，以受控的小权重取得真实请求。
- 基础排名、基础优先级和基础权重始终保留，临时状态结束后恢复基础值。
- 固定主渠道存在时仍可采样，但固定渠道保留剩余大部分流量。
- 已硬降级、稳定性试放或处于 429 冷却的渠道不能被业务采样提权。
- 样本充足的健康备用不取得采样权重，但继续参与成熟候选接管判断。

### 10.2 常规探索采样

触发条件：

- `sample_mode=traffic`。
- `apply_mode=priority_weight`。
- 至少有一条启用、参与、未硬降级、未处于 429 冷却且存在启用指标样本欠账的备用路由。
- 当前未进入自适应备援状态。

目标比例取 `exploration_traffic_percent`。标准情况下使用 10000 个权重单位：

```text
探索 3%：主渠道 W9700，探索渠道 W300
```

探索持续到当前候选样本补足、策略变化、渠道退出参与、渠道禁用、进入稳定性保护、429 冷却或自适应备援接管。当前候选结束后，只要池内还有欠账渠道，就在下一次 1 秒事件刷新中继续采样下一个候选，而不是等下一轮完整调度。

### 10.3 候选排序与即时切换

业务采样先执行第 5.5 节的候选分层，再使用策略配置的顺序：

- `sampling_order=priority_weight`：基础优先级降序、基础权重降序。
- `sampling_order=ratio`：换算后的成本倍率升序。
- 同序时：当前仍有欠账的采样对象优先，其次是最久没有新增有效样本、最久没有被采样、渠道 ID 升序。

临时提权后的当前 P/W 和候选已经保持采样的时间都不参与排序。每次有效样本写入后按池合并 1 秒事件；当前对象达到所需样本时，同一次刷新按以下顺序执行：

1. 撤回当前对象的采样覆盖并恢复其基础 P/W。
2. 将它标记为成熟候选，执行健康比例、最少可比渠道数和切换分差检查。
3. 若满足接管条件则立即切换正常主渠道；否则保持当前主渠道。
4. 若还有其他欠账候选，按 `sampling_order` 选择下一条并应用常规探索或自适应预算。

达到样本数只会停止补量并触发接管检查，不等于无条件成为主渠道。样本充足但自身处于软压力、没有足够可比对象或未达到切换分差时，仍保留为备用。

### 10.4 固定主渠道最高层下的权重换算

若固定主渠道或其他参与路由组成的实际最高层已有总权重 `H`，目标临时比例为 `p`：

```text
临时权重 = max(1, round(H × p / (1 - p)))
```

例如固定主渠道最高层总权重 `W1000`，目标 3%，临时渠道使用约 `W31`，预计比例约 3.01%。最高层总权重为 0 时无法安全计算，跳过临时流量并记录原因。

### 10.5 统一采样与稳定性释放的请求上限

统一采样流量和稳定性释放都属于“可能导致上游缓存切换”的特殊路由。页面配置单位为 K Token，`1K = 1000 Token`；接口与运行时比较值使用实际 Token 数。每次新请求或普通重试选路时，系统先估算本次 Prompt Token，再分别检查候选路由的上限。估算优先使用已解析的 Prompt Token；无法取得时可以按统一规则由请求体大小估算，两者都没有时不因上限主动过滤：

1. 常规探索和自适应备援路由都比较 `exploration_max_prompt_tokens`；稳定性释放路由比较 `stability_release_max_prompt_tokens`。
2. 配置为 `0` 时该类路由不做上限过滤；未取得 Prompt Token 估算值时也不因上限主动排除。
3. 估算值超过某类路由的上限时，只把该路由从本次“优先候选”中暂时后移，优先尝试队列中的普通路由或未超限的特殊路由。它仍保留在本轮重试队列中，不删除、不改变 P/W，也不触发稳定性状态变化。
4. 如果过滤后没有其他可用候选，必须恢复被后移的特殊路由作为兜底；即使估算值超过上限，也要发出请求，不能因为预估阈值直接返回无可用渠道。
5. 当前渠道已经完成选路，尤其请求已经发往上游后，后续得到更精确的 Token 数或发现超过上限，也不能中断当前请求、回收请求或临时切换渠道。精确估算只影响下一次重试选路。
6. 每次普通重试都重新执行一次软过滤。若特殊路由本次被后移，仍按第 2.5 节的队列循环规则在普通候选耗尽后参与；若只有该特殊路由，则会按普通 `RetryTimes` 继续重试它。
7. 稳定性降级的 `P0/W0` 只是最后兜底状态，不属于稳定性释放，不使用这两个上限。
8. 定时探测和管理员手动测试的目标是补充样本，默认不受真实业务请求上限过滤；它们发出的请求仍按各自的样本和稳定性规则记录。

这里的“上限”是减少大上下文突然换渠道的一种概率性优化，不能承诺完全避免切换。选路应当在可过滤时尽量过滤，无法过滤时保证请求继续完成。

---

## 11. 稳定性保护

### 11.1 稳定性得分

```text
基础稳定性分 = 1 - (最终失败完整惩罚 + 重试失败耗时惩罚) / 稳定性总样本数

允许慢成功数 = jitter_tolerance_percent 为 0 时取 0
               否则 max(1, floor(成功流式首字样本数 × jitter_tolerance_percent))

超额慢成功数 = max(慢成功数 - 允许慢成功数, 0)

最终稳定性分 = clamp(基础稳定性分 - 超额慢成功数 / 稳定性总样本数, 0, 1)
```

- 最终失败按 1 次完整失败惩罚。
- 快速重试失败按 `fast_failure_penalty_percent` 惩罚。
- 符合原有重试规则且耗时不超过 `fast_failure_seconds` 的失败，可以先按 `fast_failure_same_channel_retry_count` 在当前渠道额外重试；每次重试前按 `fast_failure_same_channel_retry_delay_ms` 等待，该额度不消耗普通 `RetryTimes`，用尽后才切换渠道。普通跨渠道重试不等待，请求取消会中止等待。
- 上游 429 不参与上述稳定性惩罚和稳定性样本计数，但仍保留在普通错误统计中。
- 从快速失败界限到慢失败界限之间线性增加。
- 达到慢失败界限按完整失败惩罚。
- 成功样本不扣分，但异常慢成功可能通过抖动保护扣分。

稳定性分是软评分输入，不是正常路由进入硬保护的触发器。只要最近保护失败窗口没有达到连续失败或窗口失败率阈值，路由仍保持正常状态，由评分和流量分配逐步降低其流量。

“20 个样本允许 1 次慢成功”不是写死规则。容忍数量的分母是成功流式首字样本数，实际扣分的分母是稳定性总样本数。容忍比例大于 0 时至少允许 1 次，设置为 0 时不保留最低容忍。例如默认 5%：20 个有效首字样本允许 1 次，40 个允许 2 次，10 个仍允许 1 次。

例如窗口内有 100 个稳定性样本，基础稳定性分为 98%；其中有 100 个成功流式首字样本、8 次慢成功、容忍比例 5%，则允许 5 次，超额 3 次，最终稳定性分为 `98% - 3/100 = 95%`。慢成功仍然是成功，不直接触发 `P0/W0`。

### 11.2 首字抖动阈值

首字慢成功使用固定阈值，不学习或叠加历史基线。

```text
慢成功 = 首字耗时 >= jitter_slow_threshold_seconds
```

默认阈值为 10 秒。达到阈值仍是成功请求，只参与抖动处罚；请求失败上限由“上游响应等待时间”控制。慢成功写入后按池合并 1 秒更新滚动稳定性得分、看板和自适应延迟压力，不等待完整调度。

### 11.3 状态机

```text
正常
  | 连续失败达到阈值，或保护失败窗口内失败率达到阈值
  v
降级 P0/W0
  |-- 降级探测连续成功达标 --> 正常并恢复保护前 P/W
  | 冷却到期且未提前恢复
  v
试放：P0/W10
  |-- 连续成功或恢复评分达标 --> 正常并立即恢复保护前 P/W
  |-- 任意实际到达上游的非 429 稳定性失败 --> 立即再次降级并重新冷却
  `-- 尚未达到恢复条件 --> 继续试放
```

`P0/W0` 是“最后兜底”，不是禁用：只要存在 `P1` 以上可用路由，正常首请求不会进入；所有高层不可用或重试到最后时，仍允许官方选路使用。

稳定性释放（本文此前称“稳定性试放”）保持现有小流量方式，不自动提升到主渠道同层。它可以通过失败重试、定时探测和手动测试取得恢复样本；真实业务重试选路时使用独立的 `stability_release_max_prompt_tokens` 软过滤，具体见第 10.5 节。试放失败不等待分钟级评分下降，也不经过正常路由的连续失败或窗口失败阈值，首个有效失败即重新降级。冷却到期由独立计时器立即进入试放，恢复条件达标也在当前结果事件中立即恢复，不依赖一次性完整调度任务。

降级探测连续成功数按 `(渠道, 模型)` 持久化和跨实例共享。协议成功即增加连续成功数，慢成功不会被改判为失败；抖动处罚只影响滚动稳定性得分和“恢复评分达标”路径。达到各分组的 `recovery_success_threshold` 后，关联路由分别在同一结果事务中恢复；实际到达上游的非 429 失败则立即清零共享成功数并从失败时刻续满冷却。

无论通过降级探测提前恢复、稳定性释放达标恢复，还是管理员手动解除保护，恢复事务都必须把恢复时刻写为共享 `(渠道, 模型)` 的 `observation_since`，并清除恢复前的运行时失败队列、连续失败数和恢复成功数。`stability_since` 只描述单条路由当前降级或试放阶段，不再承担长期可见数据重置职责。恢复后所有关联分组的性能与稳定性计算都只读取共享边界之后的新样本；边界之后并发产生的新失败仍需保留。原始样本、日志和分钟聚合不删除，因此历史统计仍完整，但旧失败不会让刚恢复且尚无新请求的渠道立即再次降级。

### 11.4 运行时即时保护

调度任务之外，所有开启稳定性保护的正常参与路由、临时流量、稳定性试放或允许降级的固定主渠道若收到真实上游失败，应在当前请求结束时更新保护失败窗口，不等待下一轮定时任务：

- 已经发到上游的生产请求、管理员手动渠道测试和智能调度定时探测失败都可以触发；普通自动渠道维护测试、本地跳过重试错误和最终重试汇总不触发。
- 上游 429 只触发第 6.4 节的短时冷却，不进入稳定性即时保护。
- 正常路由在 `consecutive_failures >= consecutive_failure_threshold`，或最近 `burst_failure_window_minutes` 分钟内取最近 `burst_failure_window_requests` 次请求后的失败率达到 `burst_failure_threshold_percent` 时进入 `P0/W0`。一次成功清零连续失败数并进入失败率分母，不清除仍处于滚动窗口内的失败；429 不进入窗口。
- `min_samples` 只约束评分、探索和统计可用性，不约束突发硬保护。低流量渠道即使刚恢复且样本不足，连续失败或窗口失败率达到保护阈值后也必须及时降级。
- 稳定性试放中的任意有效失败立即重新降级；已降级路由的定时恢复探测失败立即续满冷却，不再经过正常路由阈值。
- 命中智能调度策略范围的渠道亲和只允许命中当前 `(分组, 模型)` 池的实际最高优先级；调度切主、稳定性降级或管理员固定变化后，旧亲和记录暂时失效但不删除，路由重新回到最高层后可以继续复用。
- 全局智能调度开启后，命中分组策略及模型范围的渠道亲和必须通过参与状态门禁，未参与或已取消参与的路由视为无效亲和。未配置分组和策略外模型池保留官方亲和行为。开启内存缓存时，纳管路由的参与状态读取同一份渠道缓存快照，请求热路径不得查询数据库。
- 判断按共享观测键 `(渠道, 模型)` 完成一次，然后应用到所有命中策略且存在该路由的分组，避免同一失败被重复计数。
- 固定主渠道明确选择“不允许稳定性降级”时，固定期内跳过该自动保护；允许降级时保留固定意图并按第 12.2 节处理。
- 允许降级的固定主渠道达到突发保护阈值后进入保护时，必须同时撤回池内统一探索或自适应备援提权，避免临时路由独占最高层。
- 即时保护失败必须写系统错误日志；数据库更新成功后立即刷新官方路由缓存。

运行时保护状态按共享观测键 `(渠道, 归一化模型)` 保存，配置修订号变化或稳定性恢复时清空。多实例部署启用 Redis 时必须共享保护失败窗口；未启用 Redis 时只能保证单实例内的精确突发计数，不得再把分钟级稳定性评分当作跨实例硬保护的替代品。

统一采样、自适应备援和慢成功得分复用同一批请求完成事件，但使用各自的样本欠账、`adaptive_sampling_window_minutes`、`adaptive_sampling_window_requests` 和请求占比。每个事件先写入或确认可查询的请求级观测，再对关联池合并 1 秒刷新；刷新只应用滚动得分、软压力、成熟候选判断和临时采样覆盖。若同一事件已经触发硬保护，硬保护优先，软事件不得把已降级路由重新提权。没有新请求时不需要为了窗口自然滑动主动改权重；下一次请求到来时先淘汰过期观测，再按最新时间和请求数上限取最近请求刷新。

---

## 12. 固定主渠道

### 12.1 设置规则

- 唯一范围为 `(分组, 模型)`，同池只能固定一条路由。
- 固定时间单位为分钟，`0` 表示解除，最大 525600 分钟。
- 仅允许固定已启用、已参与且当前可固定的路由。
- 新固定会解除同池旧固定。
- 固定渠道的基础固定优先级必须高于池内所有已参与且可用的常规 Ability。统一探索或自适应备援可以把一条欠账渠道临时提升到同一最高层并取得受控流量；固定渠道仍是实际主渠道并保留剩余大部分流量。
- 页面默认勾选“允许稳定性降级”，管理员可取消。
- 选择当前处于稳定性降级或试放状态的路由时，不直接失败：页面必须弹出二次确认，明确说明继续操作会立即解除当前保护并固定为主渠道。
- 未确认时后端拒绝覆盖保护且不修改任何路由数据；确认后，解除保护和固定主渠道必须在同一事务内完成。
- 确认解除当前保护不等于永久关闭稳定性保护。若仍勾选“允许稳定性降级”，该路由后续再次达到阈值时仍可重新进入保护。

### 12.2 允许稳定性降级

固定意图和当前路由状态分开保存：

1. 固定渠道健康时作为主渠道。
2. 固定期内变得不稳定时进入 `P0/W0`，固定意图仍保留。
3. 其他健康渠道暂时成为实际主渠道。
4. 固定期内完成试放并恢复后，固定渠道重新成为主渠道。
5. 固定到期仍不稳定时，只清除固定意图，继续保持当前降级或试放状态。
6. 之后完全按正常调度恢复，不强行恢复不安全的旧路由值。

固定渠道被禁用、对应 Ability 被禁用、移出当前分组模型池或删除时，系统必须在同一事务内撤回该池仍存在的统一采样提权，避免临时路由因失去固定主渠道而独占最高层。普通禁用只让固定意图暂时失效，不提前删除到期时间；固定期内重新启用后立即恢复为固定主渠道。移出参与、移池或删除则按对应生命周期规则清除已经不存在的固定路由状态。

### 12.3 不允许稳定性降级

固定期内不因自动稳定性判定降级该渠道。管理员明确承担固定期间的可用性风险；渠道或 Ability 被官方禁用时仍无法接收请求。

### 12.4 到期和解除

- 健康状态到期：清除固定意图，立即恢复当前基础 P/W，由已经保存的正常排名接管。
- 保护状态到期：清除意图但不解除保护，把保护恢复目标改为固定前的基础值。
- 管理员提前解除与到期使用同一安全规则。

---

## 13. 任务、事务与失败保留

### 13.1 任务触发

- 管理员手动执行。
- 管理员强制重置。
- 固定、参与状态、成本或相关配置修改后触发一次新的调度任务。
- 有效生产请求、手动测试或定时探测完成后，触发受影响池的统一采样、自适应备援、慢成功得分和成熟候选轻量刷新，不排入完整评分任务。
- 稳定性硬保护和 429 在结果到达时立即执行；探测恢复、失败续期、冷却结束试放和冷却放行由结果事件或独立计时器执行。

同类型任务由系统任务租约和去重机制控制，避免多节点重复执行。

请求事件触发的软刷新读取上一轮完整调度已经保存的基础排名和基础 P/W，只在受影响的 `(分组, 模型)` 池内原子更新样本欠账、滚动稳定性得分、健康状态、成熟候选判断与临时采样覆盖。同池事件合并 1 秒；合并期间继续到达的事件至少再触发一次刷新。完整调度只负责重新评分和基础快照，不承担软刷新职责。

### 13.2 执行流程

1. 读取全局配置并记录控制版本。
2. 清理已到期固定意图，并读取但不覆盖仍有效的运行时保护和采样状态。
3. 读取 Ability、渠道、路由状态、成本倍率和共享观测数据。
4. 按 `(分组, 模型)` 建池。
5. 消费当前滚动稳定性值和硬保护快照，不通过完整任务代替事件保护或恢复。
6. 评分、选择正常主渠道、生成基础排名和基础 P/W。
7. 为整池生成基础更新计划，保留固定、硬保护、稳定性释放和运行时采样覆盖层的所有权边界。
8. 校验配置版本、路由修订号、参与状态、渠道状态和原 Ability 值。
9. 在一个数据库事务内写入整池基础快照。
10. 成功后刷新渠道缓存并保存执行明细。
11. 重放任务执行期间积累的软事件，基于新基础快照重新应用统一采样、自适应备援和抖动结果。

### 13.3 整池原子性

同一 `(分组, 模型)` 的全部路由必须在一个事务中提交：

- 任一路由 guard 冲突，整池零写入。
- 任一路由数据库写入失败，整池回滚。
- 一个池失败不阻止其他池继续执行。
- 失败池继续使用上一轮完整结果，不能出现一半新优先级、一半旧优先级。

任务结束提交也必须原子完成：任务终态、结果摘要、完整执行明细和对应任务租约删除使用同一数据库事务。终态 guard 或租约校验失败、明细写入失败、租约删除失败时全部回滚，不能出现“任务显示完成但明细缺失”或“任务已结束但租约仍残留”的半完成状态。

### 13.4 配置变化

任务读取配置后，管理员若修改策略、参与状态或固定状态：

- 旧任务检测控制版本或路由修订号冲突。
- 旧任务放弃该池全部写入。
- 页面记录失败阶段为“配置冲突”。
- 新配置触发或等待下一轮任务重新计算。

管理员修改任意智能调度配置时，系统必须在保存配置的同一事务内撤回旧配置产生的统一探索、自适应备援和稳定性试放流量。探索和自适应覆盖恢复基础 P/W；试放恢复为 `P0/W0` 的降级保护。该清理不解除硬保护、不丢失仍在有效期内的固定主渠道意图，并在清理后立即重新置顶仍满足条件的固定主渠道。普通渠道能力变化且池内没有有效固定意图时不得误撤回采样流量。配置与清理任一步失败时整笔保存回滚；成功后触发完整调度和一次软事件刷新，分别生成新基础快照和新采样覆盖。

智能调度配置保存使用控制版本比较并交换。设置接口返回控制版本，管理端打开表单时保存该版本并在提交时原样带回；同一旧版本上的并发保存或之后提交的旧标签页只有第一笔成功，后到请求返回 HTTP `409`，且服务节点立即从数据库刷新全部智能调度配置缓存；管理端收到冲突后刷新数据并关闭旧表单，重新打开即可基于当前版本重试。

Redis 429 冷却版本同步发生在数据库事务提交之后，不能与三种数据库共同组成跨存储事务。每个新控制版本都清空旧冷却。若同步失败，数据库配置和临时流量清理保持已提交状态，接口明确返回“设置已保存但冷却同步失败”，管理审计记录失败原因，并且不得创建强制重置任务。管理员重试当前设置后重新推进 Redis 版本；较旧请求不能覆盖更新版本或误清更新冷却，选路也不会读取修订号不一致的 Redis 数据。

### 13.5 失败记录

失败明细至少包含：

- 失败阶段：读取、计划、写入、配置冲突、缓存刷新或明细保存。
- 分组、模型、渠道和错误摘要。
- 本轮原计划的评分与 P/W。
- 上一轮成功生效时间、优先级和权重。
- “上一轮结果仍在生效”的明确提示。

### 13.6 并发与锁顺序

以下是实现必须遵守的并发不变量，不作为管理员配置项：

- 单进程内，管理员状态变更、自动状态变更、标签批量状态变更、渠道删除及渠道依赖数据写入始终使用同一渠道状态互斥锁；关闭内存缓存时也不能绕过，SQLite 下依靠该锁避免删除与重建交错。
- 涉及渠道及其依赖数据的数据库事务统一按“Channel → 倍率监控配置 → 池路由状态 → 共享样本状态 → Ability”顺序取锁；只使用其中部分表时仍保持相对顺序。渠道、监控配置按渠道 ID 升序，路由状态按 `channel_id、group_name、model_name`，Ability 按 `channel_id、group、model` 稳定排序。
- 自动状态变更按“Channel → 倍率监控配置 → 池路由状态 → Ability”加锁，并同时校验渠道状态快照和监控配置修订号；恢复渠道或标签批量启用 Ability 后，必须在同一事务内重新置顶仍有效的固定主渠道。分组倍率旧计划按“相关 Option → Channel → 倍率监控配置”校验，并逐分组比较计划时的倍率和系数，管理员中途修改的分组必须跳过旧计划写入。
- 渠道删除按“Channel → 倍率监控配置 → 池路由状态 → 共享样本状态 → Ability”删除，避免与状态变更、路由重建及共享样本写入形成反向等待。
- 渠道重建 Ability 的独立事务必须先锁定并重新读取 Channel；若渠道已被并发删除则直接失败，禁止重新创建孤立 Ability。旧池和新池的路由状态使用统一稳定顺序预锁，再更新 Ability。
- 固定、解除固定、固定到期和参与状态变更按“受影响池 Channel → 池路由状态 → Ability”加锁。所有入口不得形成“先 Ability、后 Channel”的反向锁序。
- 整池调度写入按“控制版本 Option → 受影响池 Channel → 池路由状态 → Ability”加锁并执行整池 guard；渠道状态、池成员、路由修订号和 Ability 原值必须来自同一事务快照。
- 仅锁倍率监控配置的失败、余额记录不得在后续再请求 Channel、路由状态或 Ability；仅锁池路由状态的事务不得在后续再请求 Channel 或倍率监控配置。
- 控制版本记录在第一次读取或第一次调度时使用唯一键原子创建；并发首用只能得到一条记录，不能因记录尚不存在而失去配置保护。
- 运行时稳定性保护同样先锁控制版本并校验请求读取到的版本，再按“Channel → 池路由状态 → Ability”写入；配置提交后的旧请求不得重新降级路由。
- 渠道缓存刷新必须从一个数据库事务读取 Channel、路由状态和 Ability，完整构建后一次发布；任一查询失败继续保留上一份可用快照，不能发布空快照或跨轮次混合快照。

---

## 14. 管理端页面

### 14.1 页面入口

渠道监控主页面提供两个独立记录入口：

- **余额和倍率更新记录**：只查看余额、倍率同步和相关渠道操作。
- **智能调度记录**：只查看智能调度任务和逐路由决策。

智能调度面板不再提供模型测试。渠道测试继续放在渠道管理的测试入口。

页面上每一个可配置字段都必须提供悬停说明，至少说明业务含义、单位、允许范围、默认值、何时生效以及由请求事件刷新还是一次性完整调度生效。组合约束必须在相关字段的悬停说明中直接写明，例如“最大备援预算同时决定主渠道最低保留比例”“自适应期间暂停探索”“采样顺序读取基础 P/W 而非临时 P/W”；不能只依赖本文档或提交后的校验错误解释。

### 14.2 分组列表

- 左侧按分组成本倍率从低到高默认排序。
- 显示分组倍率、策略、参与模型数、路由数和异常数。
- 分组多时允许搜索和滚动，不依赖浏览器原生下拉框。
- 选择状态清晰，不因右侧数据刷新丢失当前分组。

### 14.3 调度池总览

右侧面向生产环境一个池几十条渠道的场景，使用紧凑表格或虚拟列表，不使用大卡片堆叠。每条路由至少展示：

- 渠道名称和 ID。
- 参与、禁用、固定、硬降级、试放、常规探索和自适应备援状态。
- 最终分和评分排名。
- 基础排名、基础优先级和基础权重。
- 当前应用优先级和权重。
- 探索流量和稳定性释放的最大预估 Prompt Token，以及本次请求是否因软过滤后移。
- 预计流量，仅按实际最高优先级层计算。
- 成本倍率、窗口内生产首字 P50、TPS、稳定性和样本数，以及单独标识的手动测试/定时探测样本数。
- 最近调度时间和最近调整原因。

缓存命中率继续从渠道视图的缓存率功能查看，不复制为调度评分字段，也不参与本表排序和自动决策。

池头部必须单独显示：

- 评分第一。
- 实际主渠道。
- 实际最高优先级层及同层预计比例。
- 当前固定意图。
- 主渠道软健康状态、错误率、首字告警成功请求占比，以及用于解释与恢复的风险/健康请求占比。
- 当前采样类型、采样渠道、目标比例、`sampling_order` 和样本欠账。
- 性能窗口、稳定性评分窗口、保护失败窗口和样本共享口径。

### 14.4 路由详情

详情面板分区展示：

1. **当前路由**：基础 P/W、当前 P/W、预计流量、路由所有者，以及统一采样/稳定性释放上限。
2. **评分计算**：原始数据、各指标有效样本数、可比渠道数、不可比较原因、归一化分、有效权重、贡献和最终分。
3. **决策解释**：评分第一、实际主渠道、最高层、切换分差和调整原因。
4. **稳定性**：成功、失败、失败耗时、基础稳定性分、慢成功数、允许数、超额数、最终稳定性分、保护失败窗口、恢复探测连续成功数和状态机时间。
5. **人工控制**：参与开关、固定分钟、允许稳定性降级、解除保护。
6. **未参与路由**：明确显示“无流量资格”和 `0%` 预计流量，只保留参与开关，不提供独立 P/W 编辑。

### 14.5 执行记录

列表默认按时间倒序，支持状态、分组、模型和渠道筛选。任务摘要展示：

- 触发方式、任务状态、开始时间、耗时。
- 总数、计划数、更新数、未变化数、跳过数和失败数。
- 性能窗口、稳定性评分窗口、保护失败窗口和策略快照。

展开单条渠道后展示：

- 本轮输入数据和样本口径。
- 完整评分公式代入结果。
- 基础排名与基础 P/W。
- 当前应用 P/W 和临时流量。
- 评分第一、实际主渠道、最高层及原因。
- 调整前后 P/W。
- 失败阶段和错误。
- 上一轮仍生效的时间与 P/W。

记录不能只显示“已更新”或“已跳过”，必须回答“根据什么数据得到什么结果，为什么调整或不调整”。

逐路由执行明细必须完整写入独立明细表，不设置 500 条静默截断。任务摘要与逐路由明细分开保存，失败、已调整、未变化和跳过记录都必须可筛选、可核对。

### 14.6 渠道监控主表窗口与空状态

渠道监控主表顶部的“近 N 分钟”由后端确定最终口径：

- 智能调度实际启用时，固定使用智能调度的性能窗口，隐藏手动分钟输入，只显示当前“近 N 分钟”；响应返回 `range_source=smart_schedule`。
- 智能调度关闭时，显示手动分钟输入，允许 `1–1440` 分钟并保留管理员最近一次手动值；响应返回 `range_source=manual`。
- 性能接口始终返回最终采用的 `range_minutes`，前端不能用提交值覆盖后端结果。
- 两种模式都按第 6.5 节应用每个 `(渠道, 模型)` 的 `observation_since`。恢复后尚无新数据时，样本数显示 `0`，其余样本型指标显示 `-`，不显示误导性的 `0%`。
- 渠道名称、状态、余额、成本倍率、优先级、权重和并发等非样本数据不因恢复而清空。
- 今日统计、按日趋势、成功率/缓存率历史明细和长期模型统计继续展示完整保留数据，不受主表恢复边界影响。

---

## 15. API 与持久化

### 15.1 管理接口

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/channel_monitor/schedule` | 查询路由、共享指标、稳定性和当前决策 |
| `POST` | `/api/channel_monitor/schedule/run` | 手动执行调度 |
| `PUT` | `/api/channel_monitor/channel/:id/schedule/routes` | 批量设置渠道参与状态 |
| `PUT` | `/api/channel_monitor/channel/:id/schedule/route` | 设置单路由参与状态 |
| `PUT` | `/api/channel_monitor/channel/:id/schedule/route/primary` | 固定、续期或解除主渠道 |
| `POST` | `/api/channel_monitor/channel/:id/schedule/route/stability/clear` | 手动解除稳定性保护 |
| `GET` | `/api/channel_monitor/tasks?kind=schedule` | 查询智能调度执行记录 |

所有写接口仅管理员可用，参数同时在前后端校验，并写管理审计。

### 15.2 固定请求

```json
{
  "group": "vip",
  "model": "model-a",
  "duration_minutes": 30,
  "allow_stability_degrade": true,
  "confirm_stability_override": false
}
```

未传 `allow_stability_degrade` 时后端也按 `true` 处理。
`confirm_stability_override` 仅在用户通过二次确认后传 `true`。受保护路由未确认时返回 `smart_schedule_route_stability_confirmation_required`，前端据此展示确认弹窗，不显示普通错误提示。

### 15.3 核心持久化

路由状态保存：

- 参与状态和修订号。
- 最近调度状态、错误、得分、P/W 和时间。
- 不可变评分快照。
- 稳定性状态、冷却时间和恢复值。
- 滚动稳定性得分、慢成功计数及最近事件刷新时间。
- 基础排名、基础 P/W。
- 临时采样类型、当前候选、目标比例、样本欠账、软健康状态和最近采样时间。
- 探索流量和稳定性释放的最大预估 Prompt Token 及其配置修订号。
- 固定到期时间、允许降级选项和内部恢复值。

分组策略本身保存 `sampling_order`、`exploration_max_prompt_tokens`、`stability_release_max_prompt_tokens` 和全部自适应窗口/比例字段；路由状态保存任务执行时采用的快照，避免配置修改后历史重试记录被重新解释。

共享样本保存 `(渠道, 模型)` 的滚动原始样本、聚合结果、降级探测连续成功数和 `observation_since`。恢复成功数必须持久化并在多实例间共享；恢复操作只原子推进观测边界并清零运行时计数，不删除样本或分钟聚合。路由状态中的阶段时间继续只服务于降级和试放状态机。执行记录完整、单独保存逐路由决策，任务摘要不内嵌大明细，任务列表仍按任务分页。

---

## 16. 典型场景

### 16.1 十条健康渠道

`priority_weight` 下，最终排名为 A 到 J：

```text
A P10/W1000
B P9/W1000
C P8/W1000
...
J P1/W1000
```

普通首请求进入 A；重试按 B、C 等顺序后移，不再出现多个“第一备用”。当 C 也失败且普通重试次数仍有余额时，下一轮回到 A，再按 B、C 循环。

### 16.2 样本不足探索

A 是 `P5` 主渠道，E 样本不足且基础为 `P1`，探索比例 3%：

```text
基础：A P5/W1000，E P1/W1000
临时：A P5/W9700，E P5/W300
```

E 样本补足后的下一次 1 秒事件刷新立即恢复 `P1/W1000`，标记为成熟候选并执行接管条件检查；接管条件不足时继续保留当前主渠道，不等待也不强制触发一轮完整评分。

### 16.3 统一采样顺序与切换

A 是主渠道，B 基础 `P4/W1000`，C 基础 `P3/W1000`，两条都有样本欠账，常规探索比例为 3%。

- `sampling_order=priority_weight` 时先采样 B，临时流量为 A 97%、B 3%。
- B 达到最少样本后的下一次 1 秒刷新撤回 B 的采样覆盖，立即改为 A 97%、C 3%。
- 如果改成 `sampling_order=ratio`，且 C 倍率 0.8、B 倍率 1.0，则先采样 C。
- 不存在“每 10 分钟轮换”；候选仍有欠账时可以持续采样，达到要求才切换。

### 16.4 所有高层不可用

A、B、C 均因稳定性降级为 `P0/W0`。官方选路仍可在最后层选择它们作为兜底；如果管理员要求绝对禁止某渠道，必须禁用渠道或 Ability，不能依赖稳定性降级。

### 16.5 固定且允许降级

B 固定 30 分钟，10 分钟后降级；A 临时成为实际主渠道。B 在第 20 分钟恢复后重新成为固定主渠道。若 B 到第 30 分钟仍未恢复，则固定意图清除，B 继续保持保护状态。

### 16.6 严格流量准入

全局智能调度已开启，`vip` 已保存策略，A 参与调度且为 `P10/W1000`，C 未参与但 Ability 中残留 `P20/W500`：

```text
评分第一：A
智能实际主渠道：A
官方实际最高层：A
C 预计流量：0%
```

若 A 在 `vip` 策略仍生效时取消参与，则当前纳管池没有可用参与候选并直接返回无可用渠道；系统不会使用 C，也不会回退渠道默认 P/W。若删除 `vip` 分组策略，则该池退出智能调度范围，A、C 重新按官方 Ability 或渠道默认 P/W 参与选路。

### 16.7 配置中途变化

任务读取旧策略后，管理员关闭 B 的参与开关。任务写入时发现修订号变化，整个池回滚；A、B、C 继续使用上一轮结果，执行记录显示“配置冲突，上一轮结果仍在生效”。

### 16.8 智能调度重试顺序

以下示例均假设请求已经确定为 `vip + model-a`，首请求按正常选路选中 A，普通 `RetryTimes` 仍有余额：

| 场景 | 当前有效队列 | 最终尝试顺序 |
| --- | --- | --- |
| A 成功 | `A` | `A`，成功后结束 |
| 只有 A，A 失败，`RetryTimes=3` | `A` | `A -> A -> A -> A`，用尽后结束 |
| A、B、C 依次失败，`RetryTimes=5` | `A -> B -> C` | `A -> B -> C -> A -> B -> C` |
| A 快速失败，同渠道额外重试 `2` 次后仍失败 | `A -> B -> C` | `A -> 等待 -> A -> 等待 -> A -> B -> C ...`；同渠道额度不消耗普通 `RetryTimes`，普通跨渠道重试不等待 |
| B 被禁用或模型不匹配 | `A -> C` | `A -> C -> A -> C ...`；B 不因回到队首而重新加入 |
| B 处于 429 冷却，A/C 仍可用 | `A -> C` | 优先循环 A/C；只有 A/C 都已被本轮排除或不可用时，B 才作为最终兜底 |
| A 在第一轮失败后即时降级为 `P0/W0` | 第一轮 `A(P3) -> B(P2) -> C(P1)` | 第一轮按旧快照尝试；下一轮重新排序为 `B(P2) -> C(P1) -> A(P0)` |
| 本轮没有任何真正可用渠道 | 空 | 不再循环，直接返回无可用渠道 |
| 上游错误不可重试或请求指定单渠道 | 队列不生效 | 按现有规则结束，不切换到其他渠道 |
| 智能调度关闭 | 由渠道菜单 P/W 和官方重试规则决定 | 保留现有行为，不套用本节队列 |

同一优先级有多个渠道时，首请求仍按权重分流；重试只排除本轮已经尝试的渠道，并在同层按权重无放回选择，直到该层耗尽后再进入下一层。重试次数耗尽时无论队列是否回到队首都立即结束。

### 16.9 统一采样请求上限是软过滤

假设普通渠道为 A、B，样本不足探索渠道为 E，稳定性释放渠道为 S：

| 场景 | 配置与请求估算 | 选路和重试行为 |
| --- | --- | --- |
| E 超过探索上限，但有普通候选 | E 上限 `8000`，请求估算 `12000` | E 在本次优先候选中后移，优先 `A -> B -> E`；它仍可能在普通候选耗尽后被尝试 |
| S 超过稳定性释放上限，但 E 未超限 | S 上限 `8000`，E 上限 `16000`，请求估算 `12000` | S 后移，E 按自身规则保留；两类阈值互不影响 |
| 当前已经选中 E，随后发现估算超过上限 | E 已完成选路并已发上游请求 | E 继续完成当前请求，不中途切换；只有下一次重试才重新软过滤 |
| E 和 S 都超过各自上限，且没有普通候选 | 请求估算高于两类阈值 | 仍允许 E/S 中的当前队列候选兜底，不能因预估超限直接失败；后续按循环队列重试 |
| 只有 E 且超过探索上限 | E 上限 `8000`，请求估算 `12000` | 仍选择 `E -> E -> E ...`，上限不会把唯一候选变成失败 |
| 任一上限配置为 `0` | `0` 表示无限制 | 该类路由不参与超限过滤，仍按正常 P/W 和队列顺序选择 |

上限是为了减少大上下文突然切渠道导致的缓存损失，只能过滤一部分候选，不能承诺完全避免渠道切换。它不改变请求是否应该重试，也不改变稳定性样本、计费和最终失败判定。

### 16.10 稳定性恢复后的观测重置

性能窗口和稳定性评分窗口均为 60 分钟。`channel-1 + model-a` 在 10:30 完成稳定性恢复，恢复前 60 分钟已有 40 个请求样本：

1. 40 个样本、对应日志和分钟聚合仍保留，今日统计、历史明细和长期模型统计继续可见。
2. `observation_since` 推进到 10:30，智能调度和渠道监控主表的实际统计起点变为 `max(窗口起点, 10:30)`。
3. 10:30 之后尚无新请求时，所有关联分组的调度输入样本数为 0；主表显示样本数 `0`，首字、TPS、成功率和缓存率显示 `-`。
4. 10:34 收到一个新请求后，只用该请求及之后的数据重新计算；恢复前 40 个样本不会重新进入当前窗口。
5. 若此时关闭智能调度并把主表手动范围设为 15 分钟，实际起点为 `max(当前时间 - 15 分钟, 10:30)`；切换窗口不会绕过恢复边界。

### 16.11 突发失败保护

渠道 A 在稳定性评分窗口内已有 1000 次成功，最近 1 分钟内依次完成 2 次成功和 2 次非 429 上游失败，策略配置为保护失败窗口 1 分钟、最多统计 4 次请求、连续失败阈值 100 次、窗口失败阈值 50%：

1. 第 2 次失败完成时，最近 4 次请求中失败率为 `2/4=50%`，立即把 A 降级到 `P0/W0`，不等待下一轮调度，也不因 1000 次历史成功稀释本次突发故障。
2. 若 3 次失败之间各有一次成功，连续失败计数会被清零，但第 3 次失败仍会达到 30 秒窗口累计阈值并触发保护。
3. 若 A 只有分钟级稳定性低分，但最近 30 秒没有达到任一失败阈值，A 不进入硬保护；其得分和正常流量仍会因稳定性较低而下降。
4. 降级到期进入 `P0/W10` 试放后，首个有效失败立即重新降级；保护窗口自然清空不能自动恢复正常流量。

### 16.12 单渠道样本不产生相对满分

A 有 100 个首字样本，B、C 都没有达到最少样本。首字策略要求至少 2 条可比渠道：

```text
首字可比渠道：1/3
A 首字归一化分：不可比较
正常主渠道：保留上一轮
采样候选：B 或 C
```

A 不会因为是唯一有首字数据的渠道得到 100 分。B、C 中未硬降级且有欠账的渠道仍进入统一采样队列；任一达到要求后，池内至少有两条可比渠道，才恢复首字相对评分。

### 16.13 主渠道半可用时的自适应备援

最少样本为 5，A 是主渠道；B 缺 5 个样本，C 缺 3 个。配置为基础预算 3%、最大预算 30%，因此正常单主渠道场景下 A 最低保留 70%；A 的首字告警成功请求占比已达到 10% 并独立触发，当前首字延迟压力为 0.5：

```text
样本需求 = 5 / 5 = 1
备用预算 = 1 × (3% + 0.5 × (30% - 3%)) = 16.5%
主渠道 A = 83.5%
当前采样候选 = B 16.5%
```

这不是给 A 增加 16.5% 流量，而是从 A 的现有流量中切出 16.5% 验证备用。B 补足后在下一次 1 秒刷新切换到 C；B 自身满足健康比例、可比渠道数和切换分差时才可以接管。A 恢复健康后，同一次刷新撤回自适应预算；若 `sample_mode=traffic` 且 C 仍有欠账，则立即恢复常规 3% 探索。

### 16.14 慢成功如何计算稳定性

窗口内有 100 个稳定性样本，失败耗时惩罚计算出的基础稳定性分为 98%；其中 100 个成功流式首字样本里有 8 次达到慢成功阈值，容忍比例为 5%：

```text
允许慢成功 = max(1, floor(100 × 5%)) = 5
超额慢成功 = 8 - 5 = 3
最终稳定性分 = 98% - 3 / 100 = 95%
```

8 次请求仍全部是协议成功，只有超出的 3 次参与扣分；它们不直接触发硬降级。若这些请求来自降级探测，协议成功仍累计恢复连续成功数，抖动只影响稳定性得分恢复路径。

### 16.15 降级探测恢复与 429

A 在 10:00 降级为 `P0/W0`，降级探测间隔 30 秒，恢复阈值为连续成功 2 次：

1. 10:00:30 第一次探测成功，真实结果写入共享样本，持久化成功数为 1。
2. 10:01:00 第二次探测成功，成功数达到 2，A 在当前结果事务中立即恢复保护前 P/W，不等待 10 分钟完整调度。
3. 如果第二次是非 429 失败，成功数立即清零，并把冷却截止时间延长到不早于“10:01:00 + 冷却时间”。
4. 如果探测前 A 处于 429 冷却，探测被总门禁拦截，不发真实请求、不写伪造样本，也不续期稳定性降级；429 到期后立即重新允许后续探测。

---

## 17. 验收标准

### 17.1 配置

- 性能和稳定性评分窗口均接受 1–43200 的任意整数分钟；性能窗口默认 60，稳定性评分窗口默认 5。
- 保护失败窗口分钟数接受 1–60 的整数、请求数接受 1–1000 的整数，默认 `1 分钟 + 100 次`；窗口失败阈值接受大于 0% 且不超过 100% 的百分比；连续失败和恢复成功阈值仍接受 1–100。
- 固定主渠道默认勾选允许稳定性降级，前后端默认一致。
- `sampling_order` 可保存、回显并只接受 `priority_weight` 或 `ratio`；探索和自适应使用同一值。
- 不接受、不输出、不迁移五个 `priority_sampling_*` 字段，也不出现探索租约或轮转间隔配置。
- 必填的首字告警请求占比、请求比例窗口分钟数和请求数、恢复健康比例及切换确认健康比例可分别保存、回显和校验；错误率超过错误告警阈值或首字告警请求占比达到配置值时可独立进入压力状态，两个信号按 OR 判定。
- 首字告警请求占比与恢复健康比例之和必须大于 `100%`；恢复阈值和风险/健康请求占比的展示口径保持不变。
- `adaptive_sampling_primary_min_percent` 已移除；旧配置中的该字段兼容忽略且不再输出，主渠道最低保留比例由 `100% - adaptive_sampling_max_percent` 自动推导；自适应开启时必须使用 `priority_weight` 应用方式。
- `exploration_max_prompt_tokens` 与 `stability_release_max_prompt_tokens` 可分别保存、回显和校验；页面按 K Token 展示，默认分别为 `50K` 与 `0K`，接口值必须为 `0` 或 `1000` 的整数倍且不超过 `1000000 Token`，`0` 表示无限制。
- `degraded_probe_enabled` 可保存和回显，默认关闭；开启时与常规探测复用 `probe_interval_minutes`。
- 不出现相对权重拉伸和得分曲线指数配置。

### 17.2 样本共享

- 同一渠道模型在两个分组产生的业务请求进入同一观测键。
- 手动测试只写一份共享样本，两个关联分组都能读取。
- 定时探测和手动测试按相同样本权重计入稳定性。
- 定时探测发出真实上游调用，除 429 外的成功或失败结果按同等权重计入共享样本。
- 降级探测关闭时不请求 `P0/W0` 路由；开启时按配置间隔请求，连续成功数持久化并跨实例共享，达标后在结果事务中立即恢复，不等待完整调度；每次实际到达上游的非 429 稳定性失败都会清零成功数并从失败时刻续满分组冷却时间。
- 429 冷却期间手动测试、常规探测和降级探测均不发出请求；到期后立即恢复入口资格。
- 任一关联路由恢复时，只推进一份 `(渠道, 模型)` 共享观测边界，不按分组复制边界。
- 恢复不删除原始样本、日志、分钟聚合或延迟分桶，恢复前数据仍能用于历史和长期统计。
- 流式测试展示首字、总耗时和 TPS；非流式不伪造首字。

### 17.3 唯一优先级

- 十条正常路由生成十个唯一基础优先级。
- 样本有效渠道全部排在样本不足渠道之前。
- 样本不足渠道仍有独立优先级。
- 任一动态指标只有一条渠道达到最少样本时显示“不可比较”，不生成单渠道相对满分，也不据此切换主渠道。
- 样本充足的健康备用停止采样后仍保留成熟接管资格。
- 所有稳定性降级渠道统一为 `P0/W0`。
- 不再产生“第一备用多个渠道”的产品角色。

### 17.4 临时流量

- 统一采样渠道与实际最高层同优先级并取得配置的小比例真实流量。
- 常规探索和自适应备援共用候选资格、`sampling_order` 和 Token 上限；自适应生效时暂停探索，结束时在同一次事件刷新恢复探索。
- 每个池一次只提升一条未硬降级且有样本欠账的渠道；已硬降级或试放渠道不被业务采样提权。
- 当前候选达到最少样本后，在下一次 1 秒事件刷新切换到下一个欠账渠道，不等待一次性完整调度任务。
- 临时状态结束后精确恢复基础 P/W。
- 未参与路由始终为 `0%`，不进入临时流量权重换算。
- 请求估算值超过探索或稳定性释放上限时只后移特殊路由，不阻断请求；无其他候选时特殊路由仍可兜底。
- 请求已发往上游后，即使后续精确 Token 超过上限也不切换当前渠道；下一次重试才重新过滤。

### 17.5 固定和稳定性

- 两种应用模式下，固定路由都高于全部已参与且可用的常规路由。
- 允许降级时，降级不清除固定意图；固定期内恢复后重新固定。
- 固定到期仍不稳定时只清除意图，继续保护。
- 不允许降级时，固定期内不触发自动稳定性降级。
- 新固定当前受保护路由时先展示风险确认；取消不写数据，确认后原子地解除保护并完成固定。
- 高优先级全部不可用时，`P0/W0` 仍保留最后兜底能力。
- 正常路由的连续失败或保护窗口失败率达到配置阈值时立即触发保护，不受分钟级评分和 `min_samples` 限制。
- 成功会清零连续失败数并进入窗口分母，但不会提前移除保护窗口内的失败；时间和请求数上限共同决定实际统计集合。
- 试放路由的首个有效失败立即重新降级，降级期间恢复探测失败立即续满冷却。
- 429、普通自动渠道维护测试、本地跳过错误和最终重试汇总不会误触发稳定性即时保护；管理员手动测试和智能调度定时探测失败可以进入同一保护判定。
- 429 冷却作为 `(渠道, 模型)` 独立避让状态：普通选路无其他可用候选时允许最终兜底，亲和、手动测试、全部探测和全部采样继续阻断，不修改稳定性状态。
- 分钟级稳定性评分降低但保护失败窗口未达标时，正常路由不会进入 `P0/W0`。
- 慢成功允许数按成功流式首字样本数计算，超额数按稳定性总样本数扣分；慢成功仍累计协议成功，不直接触发硬保护。
- 慢成功得分按池合并 1 秒刷新；探测恢复、探测失败续期、冷却结束试放和 429 放行均不等待完整调度。
- 自动或手动恢复后，性能和稳定性计算都不再读取共享边界前的数据；运行时短期失败和恢复成功计数已清空，恢复后的新失败仍正常计入。

### 17.6 事务和失败

- 同池全部更新在一个事务内提交。
- 任一 guard 或写入失败时整池零写入。
- 一个池失败不影响其他池成功。
- 配置变化时旧任务不覆盖新配置。
- 完整调度只提交评分、正常选主、基础排名和基础 P/W；执行期间到达的软事件在基础快照提交后重放，不能被完整调度覆盖。
- 统一采样、自适应备援和抖动软事件按池合并 1 秒且不丢事件；硬保护和 429 立即执行，不进入 1 秒等待。
- 失败记录显示阶段、原因和上一轮仍生效值。
- 智能调度开启时重试按 `(分组, 模型)` 队列顺序执行；当前轮候选耗尽且 `RetryTimes` 未用尽时回到队首。
- 智能调度开启时，命中策略及模型范围的池必须在首请求、亲和、同渠道快速重试和普通重试中排除未参与和已取消参与的路由，候选为空时不回退默认 P/W；未配置分组和策略外模型池继续使用官方选路。
- 只有一条渠道时失败重试会再次选择该渠道；没有任何可用渠道时直接结束，不产生空转循环。
- 同渠道快速失败重试先于普通队列，按配置的毫秒间隔等待，使用独立额度且不消耗普通 `RetryTimes`；请求取消时等待立即结束。
- 固定主渠道与自动禁用或恢复并发执行时，不出现 Channel/Ability 反向加锁死锁。
- 控制版本记录不存在时，读取和调度都能原子初始化；重复或并发首用只保留一条唯一记录。

### 17.7 页面

- 分组默认按成本倍率从低到高排序。
- 大量渠道使用紧凑、可滚动、可筛选的展示。
- 页面同时显示评分第一、实际主渠道和官方最高层。
- 路由详情可完整复算评分。
- 余额倍率记录和智能调度记录使用独立按钮进入。
- 执行记录清楚说明输入、计算、结果、调整原因和失败保留。
- 智能调度面板不显示模型测试。
- 智能调度实际启用时，主表隐藏手动分钟输入并使用性能窗口；关闭时恢复 `1–1440` 分钟手动输入和最近一次手动值。
- 性能接口返回权威的 `range_minutes` 和 `range_source`，页面显示与后端实际查询一致。
- 恢复后没有边界内新样本时，主表显示样本数 `0` 和指标 `-`，不显示 `0%`；非样本字段以及今日、历史和长期统计不被清空。
- 每个可配置字段都有页面悬停说明，明确业务含义、单位、范围、默认值、生效时机、事件刷新或完整调度关系及组合约束。
- 看板展示主渠道软健康状态、错误率、首字告警成功请求占比、风险/健康请求比例、采样类型、当前采样渠道、样本欠账、采样顺序和未切换原因。

### 17.8 数据库与工程

- SQLite、MySQL 和 PostgreSQL 均可迁移和执行。
- V2 不读取旧版三层角色、旧探索状态、五个 `priority_sampling_*` 字段、轮数阈值或探索租约字段。
- 开发数据工具只能在显式开发环境、显式数据库和确认令牌下运行。
- 验收数据重复写入幂等，不清空或覆盖其他业务数据。

---

## 18. 开发环境验收数据

使用 `cmd/channel-smart-schedule-seed` 写入真实表结构数据，不伪造任务结果。数据集包括：

- 10 个隔离验收渠道。
- 2 个分组和 2 个模型。
- 40 条 Ability 和参与路由。
- 20 份 `(渠道, 模型)` 共享样本。
- 稳定高速、低成本、轻微失败、高成本、慢响应、首字抖动、近期不稳定、慢失败和样本不足等场景。

写入后必须配置验收分组策略并通过真实调度任务生成评分、路由和执行记录。执行保护和命令见 `docs/downstream/channel-monitor/development-seed.md`。

本轮开发验收完成后，应在本文附加：

- 使用的开发数据库和服务地址。
- 写入渠道 ID。
- 实际任务 ID。
- 至少一个唯一优先级池、一个共享样本、一个临时流量或稳定性场景的核对结果。

本轮开发环境已完成真实验收：

- 数据库：本机开发 MySQL；服务：`http://127.0.0.1:3001`。
- 验收渠道 ID：`9-18`；验收分组：`调度验收-低成本`、`调度验收-高可靠`。
- 最新真实调度任务：`systask_eemgCApnKQzxgDWeeGEZKwnvbWzkQlb1`，状态成功，总路由 40，计划 24，更新 40，跳过 0，失败 0，并保存 40 条逐路由执行明细。
- `调度验收-低成本 / smart-seed-chat-fast` 池验证：实际最高层为 `P6`，渠道 10 使用 `W9700` 承接约 97%，渠道 18 以“样本不足补量”使用 `W300` 承接约 3%。
- 共享样本验证：渠道 9 的 `smart-seed-chat-fast` 在两个分组读取同一份 24 次样本，首字约 239 ms、TPS 58.00。
- 执行记录验证：页面可展开评分输入、归一化、权重、业务/稳定性贡献、选择原因、调整原因和上一轮生效 P/W；稳定性试放不足记录显示为“稳定性试放样本不足（0/5）”。

---

## 19. 明确不兼容项

### 19.1 删除项

本功能仍在开发阶段，V2 直接替换旧设计：

- 删除“主渠道 / 第一备用 / 第二备用”三层角色口径。
- 删除旧探索状态、租约和轮转持久化字段；保留当前契约中的 `exploration_traffic_percent` 与 `exploration_max_prompt_tokens`。
- 删除 `priority_sampling_enabled`、`priority_sampling_interval_minutes`、`priority_sampling_base_percent`、`priority_sampling_decay_percent`、`priority_sampling_min_percent`，不读取、不迁移旧值。
- 删除自适应采样的进入、恢复和切换确认轮数字段；新策略必须显式提交独立的窗口分钟数、窗口请求数及三项请求占比字段。
- 删除并明确拒绝 `adaptive_sampling_enter_request_percent`，不读取、不输出、不迁移；新策略必须提交 `adaptive_sampling_first_token_warning_request_percent`，不提供旧配置兼容。
- 删除探索租约和按固定轮转时间切换候选的产品语义；采样对象完全由当前窗口欠账、候选层和 `sampling_order` 决定。
- 删除未参与路由人工 P/W 接口、页面输入和运行时覆盖语义；不读取该接口的旧用法，也不允许已纳管池中的未参与路由绕过流量准入。
- 已纳管池没有参与路由时不回退默认 P/W 或官方亲和；没有命中策略范围的池保留官方选路与亲和行为。
- 使用“基础排名 + 基础 P/W + 当前 P/W + 临时流量类型”统一表达。
- 不回填旧智能调度执行明细。
- 上线 V2 前允许清理旧路由状态、旧执行明细和旧测试数据，再从当前 Ability 与新配置初始化。
- 删除渠道时只清理会影响运行的倍率监控配置、路由状态、共享样本状态和 Ability；倍率历史、成本事实、分钟观测和既有任务执行明细作为审计历史保留。

普通渠道、Ability、业务日志和非智能调度配置不属于清理范围。

### 19.2 V2.4 落地边界

V2.4 已按本节目标完成实现，运行时边界如下：

1. 策略只使用 `sample_mode`、`exploration_traffic_percent`、`exploration_max_prompt_tokens` 和必填的 `sampling_order`；五个 `priority_sampling_*` 字段、旧轮数字段、旧统一风险进入字段及探索租约字段会被明确拒绝，不读取、不输出、不迁移。自适应进入由“错误率超过错误告警阈值”与“首字告警成功请求占比达到独立阈值”按信号级 OR 触发。
2. 统一采样、自适应备援和抖动稳定性使用按池合并的 1 秒事件刷新；采样对象补足、失去资格或进入 429 冷却后立即重选，不等待完整调度。
3. 自适应备援退出时在同一次刷新中撤回覆盖，并在 `sample_mode=traffic` 且仍有欠账时立即恢复常规探索。
4. 降级探测连续成功次数按 `(渠道, 模型)` 持久化并跨实例共享；达到次数阈值的结果事务立即恢复，试放稳定性分达到阈值时也由运行时刷新恢复。
5. 429 冷却是 `(渠道, 模型)` 独立避让状态，普通选路优先跳过但无其他可用候选时允许兜底，亲和、手动测试、常规/降级探测和全部采样继续拦截；独立到期回调立即恢复正常入口资格并触发池刷新。
6. 完整调度只提交评分、正常选主、基础排名和基础 P/W，不接管临时采样、固定、硬保护、试放或滚动运行时状态；每个成功提交的池都会立即排队重放软事件。
7. 运行时健康、滚动稳定性、采样欠账、候选、顺序和最近采样时间均持久化；多实例写入使用控制版本、路由修订及池级行锁防止旧快照覆盖新状态。
8. 探索请求上限和稳定性释放请求上限在页面中统一以 K Token 输入和回显，默认分别为 `50K` 与 `0K`；接口和持久化仍使用实际 Token 数，只接受 `0` 或 `1000` 的整数倍，不兼容旧的非整 K 配置值。
9. 全局智能调度开启后，命中分组策略及模型范围的池以路由参与状态作为请求流量门禁；未通过门禁的 Ability 不参与首请求、亲和或任何重试。删除人工未参与路由 P/W 接口，纳管候选为空时直接返回无可用渠道，不兼容默认 P/W 回退；未命中策略范围的池继续使用官方选路。

---

## 20. 关联文档

- `docs/downstream/channel-monitor/smart-scheduling.md`
- `docs/downstream/channel-monitor/api-and-operations.md`
- `docs/downstream/channel-monitor/development-seed.md`
- `docs/downstream/relay-reliability/README.md`
