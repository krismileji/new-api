# 智能调度产品需求文档

**文档版本**：V2.0 评审稿

**更新时间**：2026-08-02

**文档状态**：本轮实现基线，待产品审核

**适用范围**：渠道监控中的文本模型智能调度、样本补充、稳定性保护、人工控制、执行记录和开发验收。

**口径说明**：本文是 V2 的唯一产品口径。旧版“主渠道 / 第一备用 / 第二备用”三层设计和旧探索状态不再适用，不要求兼容旧智能调度数据。

---

## 1. 背景与目标

### 1.1 背景

同一个模型通常会配置多个上游渠道。渠道在成本倍率、首字、TPS 和稳定性方面不同，而且上游短时波动是常态。仅依靠管理员长期手工设置优先级和权重，会出现以下问题：

- 高优先级渠道长期吃满流量，低优先级渠道没有新鲜的真实请求样本。
- 评分最高渠道频繁切换时，上游缓存命中率容易下降。
- 同一渠道模型在不同分组产生的数据重复学习或互相割裂。
- 渠道短时不稳定时，系统容易在降级、恢复和固定主渠道之间产生冲突。
- 管理员只能看到结果，无法确认本轮用了什么数据、怎么算分、为什么调整。

### 1.2 产品目标

1. 以 `(分组, 模型)` 作为独立调度池，独立决定路由顺序。
2. 以 `(渠道, 模型)` 作为观测键，在所有关联分组之间共享真实请求、手动测试和定时探测样本。
3. 使用成本倍率、首字、TPS 和稳定性生成可解释的评分。
4. 让评分最高且满足切换条件的渠道获得最多流量，同时控制无意义的主渠道切换。
5. 在优先级分层模式下，为每条正常路由保留独立优先级，并用少量轮转流量更新低优先级渠道样本。
6. 对不稳定渠道自动降级、冷却、试放和恢复，并保留最终兜底能力。
7. 支持管理员按分钟固定主渠道，并自行决定固定期间是否允许稳定性降级；默认允许。
8. 任务失败或配置变化时保留上一轮完整结果，并提供可追溯的执行记录。
9. 不重写官方请求选路逻辑，只更新官方 `Ability.Priority` 和 `Ability.Weight`。

### 1.3 非目标

- 不把缓存命中率加入评分或自动切换条件。
- 不增加分组级暂停；异常时使用全局开关、渠道参与开关或单路由参与开关。
- 不增加模拟运行模式。
- 不针对单个用户、提示词或请求内容实时重新评分。
- 不为图片、音频、视频等非文本能力强制生成文本探测请求。
- 不保证临时采样百分比在低请求量下精确命中，只保证官方权重对应的目标比例。

---

## 2. 核心模型

### 2.1 三个维度

| 维度 | 唯一键 | 用途 | 是否跨分组共享 |
| --- | --- | --- | --- |
| 调度池 | `(分组, 模型)` | 排名、主渠道、优先级、权重、固定意图和任务事务 | 否 |
| 观测键 | `(渠道, 模型)` | 业务请求、手动测试、定时探测、首字、TPS 和稳定性样本 | 是 |
| 路由状态 | `(渠道, 分组, 模型)` | 参与状态、基础路由值、临时流量、稳定性状态和最近执行结果 | 否 |

### 2.2 共享样本原则

同一个渠道访问同一个上游模型时，不论请求来自哪个用户分组，上游性能都属于同一个观测对象。因此：

- `vip + model-a` 的真实请求会更新 `(渠道 1, model-a)`。
- `default + model-a` 读取同一份 `(渠道 1, model-a)` 数据。
- `vip + model-b` 不共享 `model-a` 的样本。
- 原始业务日志仍保留实际请求分组，便于审计；智能调度读取时再按渠道和模型聚合。

### 2.3 官方请求路径

```text
智能调度任务
  -> 读取策略和观测数据
  -> 按 (分组, 模型) 生成完整计划
  -> 整池更新 Ability.Priority / Ability.Weight
  -> 刷新渠道缓存
  -> 官方选路处理真实请求
```

官方选路顺序保持不变：

1. 过滤分组、模型、请求路径、渠道状态、Ability 状态和排除渠道。
2. 从最高 `Priority` 层选择。
3. 同一优先级层内按 `Weight` 分流。
4. 重试时进入后续优先级层。

优先级决定请求先进入哪一层；权重只决定同层渠道之间的比例。

---

## 3. 权限与控制边界

### 3.1 管理员能力

管理员可以：

- 开关全局智能调度。
- 配置调度间隔、性能窗口和稳定性窗口。
- 新增、修改和删除分组策略。
- 设置整个渠道或单条路由是否参与调度。
- 对未参与路由手动设置优先级和权重。
- 手动执行或强制重置调度。
- 固定、续期或解除主渠道。
- 选择固定期间是否允许稳定性降级，默认勾选允许。
- 手动解除稳定性保护。
- 查看评分明细、路由详情和完整执行记录。

### 3.2 普通用户

普通用户不配置、不查看智能调度。普通请求只读取已生效的官方路由值。

### 3.3 路由值所有权

| 路由状态 | 优先级和权重所有者 | 调度任务行为 |
| --- | --- | --- |
| 参与调度 | 智能调度 | 可以按策略修改 |
| 未参与调度 | 管理员 | 永不修改 |
| 渠道或 Ability 禁用 | 官方状态 | 不修改路由，仅记录跳过 |
| 管理员固定 | 管理员固定意图 | 固定优先于正常评分计划 |
| 稳定性保护 | 稳定性状态机 | 按降级、试放或恢复规则修改 |

关闭参与状态时保留该路由当时已经生效的优先级和权重，不自动恢复渠道默认值。管理员之后可以直接编辑。

---

## 4. 全局配置

| 页面字段 | API 字段 | 默认值 | 范围 | 说明 |
| --- | --- | ---: | ---: | --- |
| 智能调度 | `smart_schedule_enabled` | 关闭 | 布尔值 | 没有有效分组策略时视为关闭 |
| 调度间隔 | `smart_schedule_interval_minutes` | 10 分钟 | 1–525600 | 自动任务间隔 |
| 性能窗口 | `smart_schedule_performance_window_minutes` | 60 分钟 | 1–43200 | 成本之外的首字和 TPS 评分窗口 |
| 稳定性窗口 | `smart_schedule_stability_window_minutes` | 60 分钟 | 1–43200 | 成功、失败、失败耗时和抖动窗口 |
| 429 冷却时间 | `smart_schedule_rate_limit_cooldown_seconds` | 30 秒 | 0–300 | 上游返回 429 后，按“渠道 + 模型”临时停止进入新请求；0 表示关闭 |
| 分组策略 | `smart_schedule_group_policies` | 空 | 最多 100 组 | 每个分组只能有一份完整策略 |
| 强制重置 | `smart_schedule_force_reset` | 一次性命令 | 布尔值 | 忽略切换分差，重算当前完整计划 |

配置值使用任意整数分钟，不再限制为 15、60、360、1440 等固定档位。

关闭全局调度时：

- 不创建新的调度和定时探测任务。
- 保留已生效的基础路由结果。
- 清除探索或低优先级轮转产生的临时提权，恢复基础路由值。
- 不删除样本和历史执行记录。

---

## 5. 分组策略

### 5.1 基础字段

| 字段 | 默认值 | 范围或枚举 | 说明 |
| --- | ---: | --- | --- |
| `group` | 无 | 1–64 字符 | 分组唯一键 |
| `models` | 空 | 最多 100 个 | 空表示分组内全部模型 |
| `model_order` | 空 | 最多 100 个 | 只影响看板顺序 |
| `strategy` | `smart` | `smart/ratio/first_token/tps` | 评分策略 |
| `apply_mode` | `priority_weight` | `priority_weight/weight` | 路由应用方式 |
| `min_samples` | 5 | 1–100000 | 必需指标的最少样本数 |

### 5.2 评分字段

| 字段 | 默认值 | 范围 | 说明 |
| --- | ---: | ---: | --- |
| `scoring.stability_percent` | 50% | 0–100% | 稳定性在最终得分中的占比 |
| `scoring.primary_traffic_percent` | 90% | 51–99% | 仅权重模式中的主渠道目标流量 |
| `scoring.primary_switch_threshold_percent` | 3% | 0–100% | 挑战渠道至少领先多少分才切主渠道 |
| `scoring.smart.cost_ratio_percent` | 40% | 0–100% | 综合评分成本占比 |
| `scoring.smart.first_token_percent` | 40% | 0–100% | 综合评分首字占比 |
| `scoring.smart.tps_percent` | 20% | 0–100% | 综合评分 TPS 占比 |
| `scoring.ratio.cost_ratio_percent` | 70% | 0–100% | 成本策略成本占比，必须大于 0 |
| `scoring.ratio.first_token_percent` | 20% | 0–100% | 成本策略可选首字占比 |
| `scoring.ratio.tps_percent` | 10% | 0–100% | 成本策略可选 TPS 占比 |

`smart` 和 `ratio` 内部的三个指标占比都必须合计 100%。

### 5.3 稳定性与抖动字段

| 字段 | 默认值 | 范围 | 说明 |
| --- | ---: | ---: | --- |
| `stability_enabled` | 开启 | 布尔值 | 稳定性评分和保护总开关 |
| `degrade_stability_score` | 90% | 0–100% | 低于该值进入降级 |
| `recovery_stability_score` | 95% | 大于降级值且不超过 100% | 达到该值解除试放 |
| `fast_failure_penalty_percent` | 40% | 0–100% | 快速重试失败的单次惩罚 |
| `fast_failure_seconds` | 1 秒 | 大于 0 且小于 60 | 快速失败界限 |
| `slow_failure_seconds` | 10 秒 | 大于快速失败且不超过 60 | 完整失败惩罚界限 |
| `cooldown_minutes` | 30 分钟 | 1–525600 | 降级冷却时间 |
| `jitter_enabled` | 开启 | 布尔值 | 首字慢成功保护 |
| `jitter_tolerance_percent` | 5% | 0–50% | 窗口内允许的慢成功比例 |
| `jitter_threshold_multiplier` | 5 倍 | 大于 1 且不超过 20 | 相对基线阈值 |
| `jitter_absolute_tolerance_seconds` | 10 秒 | 0–60 | 绝对容差 |
| `jitter_baseline_minutes` | 60 分钟 | 1–43200 | P50 基线学习周期 |

### 5.4 样本补充字段

| 字段 | 默认值 | 范围或枚举 | 说明 |
| --- | ---: | --- | --- |
| `sample_mode` | `off` | `off/traffic/probe` | 关闭、探索流量、定时探测 |
| `exploration_traffic_percent` | 3% | 大于 0 且不超过 20% | 样本不足探索的目标流量 |
| `probe_interval_minutes` | 10 分钟 | 1–525600 | 定时探测间隔 |

探索流量只允许用于 `priority_weight`。

### 5.5 低优先级轮转字段

| 字段 | 默认值 | 范围 | 说明 |
| --- | ---: | ---: | --- |
| `priority_sampling_enabled` | 开启 | 布尔值 | 是否轮转健康的低优先级渠道 |
| `priority_sampling_interval_minutes` | 10 分钟 | 1–1440 | 单个渠道保持采样的时间 |
| `priority_sampling_base_percent` | 3% | 0.1–20% | 第 2 名的目标采样比例 |
| `priority_sampling_decay_percent` | 70% | 1–100% | 每降低一名保留的比例 |
| `priority_sampling_min_percent` | 0.5% | 0.01–5% | 最低目标比例，不得高于基础比例 |

轮转配置只在 `priority_weight` 下生效，切换到其他模式时保留配置但不执行。

---

## 6. 样本与统计口径

### 6.1 样本来源

| 来源 | 是否等价计数 | 可提供稳定性 | 可提供首字 | 可提供 TPS |
| --- | --- | --- | --- | --- |
| 真实业务请求 | 是 | 是 | 流式请求可提供 | 有 usage 时可提供 |
| 手动渠道测试 | 是 | 是 | 选择流式时提供 | 有 usage 时提供 |
| 定时探测 | 是 | 是 | 流式成功时提供 | 有 usage 时提供 |

三种来源不设置系数。一次成功或失败都按一个样本计入。

### 6.2 手动测试

手动测试满足以下条件即写入共享样本：

1. 全局智能调度已开启。
2. 能确定测试使用的原始模型。
3. 该渠道模型至少关联一条启用、参与调度且命中分组策略的路由。

不要求该分组选择定时探测或探索流量。一次测试只写入一份 `(渠道, 模型)` 样本，所有关联分组共享。

测试结果必须展示：

- 成功或失败。
- 总耗时。
- 是否计入智能调度样本及原因。
- 流式测试的首字时间。
- 可取得时的 TPS 和输出 token。

非流式成功测试不把总耗时冒充首字时间。

### 6.3 定时探测

定时探测仅对 `sample_mode=probe` 的文本模型执行：

- 同一个 `(渠道, 模型)` 被多个分组覆盖时，只发送一次请求。
- 多个策略探测间隔不同时，使用最短间隔。
- 使用小型流式 Responses 请求，输出 token 上限为 16。
- 实际请求已发到上游后，无论成功或失败都写入样本。
- 本地参数转换失败、不支持协议或并发已满时跳过，不伪造上游失败。
- 探测占用渠道并发额度并产生真实上游成本。
- 探测日志带专用标记，不重复进入普通业务分钟统计。

### 6.4 上游 429

429 表示上游并发或限流暂时达到上限，与渠道稳定性失败分开处理：

- 当前请求仍按现有重试规则尝试其他渠道。
- 若智能调度开启且 `smart_schedule_rate_limit_cooldown_seconds` 大于 0，记录该 `(渠道, 模型)` 的内存冷却，到期自动恢复。
- 冷却对所有关联分组生效，不修改任何分组的优先级、权重、稳定性状态或稳定性保护计时。
- 同一渠道模型在冷却期间再次收到 429，只能延长到更晚的到期时间，不会缩短已有冷却。
- 429 仍进入普通错误日志和成功率统计，便于管理员查看上游限流情况；但不计入智能调度稳定性样本、失败惩罚、重试失败耗时和稳定性降级门槛。
- 本地渠道并发额度耗尽产生的跳过重试错误不属于上游 429，不触发本配置。

例如：默认冷却 30 秒时，`channel-2 + gpt-4o` 收到 429，接下来 30 秒所有分组的新请求都不会选择这条路由；`channel-2 + claude-3` 不受影响，30 秒后 `gpt-4o` 自动回到候选池。若设置为 0，系统只保留现有重试和日志行为，不创建冷却。

### 6.5 滚动保留

- 共享样本表唯一键为 `(渠道, 模型)`。
- 单键最多保留 1500 个滚动样本。
- 同一来源和同一请求 ID 重复写入时去重。
- 保留时长按性能窗口和稳定性窗口中的较大值决定。
- 评分读取性能窗口；稳定性和抖动读取稳定性窗口。

### 6.6 样本不足

样本不足不等于渠道不稳定：

- 在 `priority_weight` 中排在所有有效评分渠道之后，仍保留独立基础优先级。
- 选择探索流量时，可以临时进入最高优先级层取得少量真实请求。
- 选择定时探测时，通过主动探测补样本。
- 选择关闭时，只等待真实业务或手动测试补样本。

---

## 7. 评分规则

### 7.1 策略

| 策略 | 必需数据 | 行为 |
| --- | --- | --- |
| `smart` | 配置占比大于 0 的成本、首字和 TPS | 合并三个业务指标 |
| `ratio` | 成本倍率 | 首字和 TPS 有足够可比样本时加入，否则对剩余指标重归一 |
| `first_token` | 首字样本达到最少样本数 | 首字越低越好 |
| `tps` | TPS 样本达到最少样本数 | TPS 越高越好 |

稳定性开启且稳定性占比大于 0 时，稳定性样本也必须达到 `min_samples`。

### 7.2 Min-Max 归一化

成本倍率和首字越低越好：

```text
归一化分 = (同池最大值 - 当前值) / (同池最大值 - 同池最小值)
```

TPS 越高越好：

```text
归一化分 = (当前值 - 同池最小值) / (同池最大值 - 同池最小值)
```

同池最大值等于最小值时，该项统一记为 1。归一化结果范围为 0–1，页面按 0–100 分展示。

### 7.3 业务分和最终分

```text
业务分 = Σ(可用业务指标归一化分 × 该指标有效占比)

最终分 = 业务分 × (1 - 稳定性占比)
       + 稳定性分 × 稳定性占比
```

缺失的可选指标从本轮有效占比中剔除，其余指标重新归一到 100%。缺失必需指标时不生成有效最终分。

### 7.4 Min-Max 的已知边界

本版继续使用现有 Min-Max，不增加“相对权重拉伸”或“得分曲线指数”配置。管理员需要知道它表达的是候选中的相对位置，而不是原始差距大小。

例 1：只有成本 1.00 和 1.01 两条渠道时，归一化后分别是 100 和 0，虽然成本只差 1%。

例 2：加入成本 2.00 的第三条渠道后，前两条变为 100 和约 99。原始数据没变，但比较范围变了。

例 3：首字 1000ms 和 1050ms 的两条渠道也会得到 100 和 0，虽然只差 50ms。

因此本版使用“主渠道切换分差”减少频繁切换，并在执行记录中同时展示原始值、比较范围和贡献，避免只看总分误判。后续若要增加原始指标改善门槛，应作为独立需求评审。

### 7.5 评分快照

每条执行明细必须保存：

- 策略、应用方式、最少样本数和观测范围。
- 性能窗口和稳定性窗口。
- 成本、首字、TPS、稳定性原始值及样本数。
- 同池各指标最小值、最大值和可比较渠道数。
- 配置占比、缺失数据处理后的有效占比。
- 各指标归一化分、业务贡献、稳定性贡献和最终分。
- 评分第一、当前主渠道、实际主渠道和选择原因。
- 基础排名、基础 P/W、当前应用 P/W、临时流量类型和比例。

缓存命中率可以作为观察信息展示，但必须标注“未参与评分”。

---

## 8. 主渠道决策

### 8.1 三个解释字段

- **评分第一**：只按本轮最终得分排序后的第一名。
- **实际主渠道**：应用当前主渠道保护、切换分差、固定意图和稳定性状态后，智能调度选择的主要渠道。
- **实际最高优先级层**：官方选路真正最先访问的全部路由，可能包含临时采样或未参与调度的人工路由。

这不是三套状态。它们用于解释同一轮决策的不同阶段。

### 8.2 选择顺序

1. 过滤禁用、未参与和处于稳定性保护的路由。
2. 计算有效评分并得到评分第一。
3. 管理员固定且当前未降级时，固定渠道直接成为实际主渠道。
4. 没有固定时，若当前主渠道仍有效且挑战渠道领先未达到切换分差，保留当前主渠道。
5. 达到切换分差、强制重置或当前主渠道失效时，使用评分第一。
6. 同分时优先保留当前主渠道或上一轮相对顺序，再按渠道 ID 稳定排序。

### 8.3 为什么评分第一不一定是实际主渠道

示例：当前主渠道 B 得分 90，A 得分 92，切换分差为 3。A 只领先 2 分，所以页面显示：

```text
评分第一：A
实际主渠道：B
原因：挑战渠道未达到 3 分切换分差
```

如果 B 被管理员固定，即使 A 得分更高，实际主渠道仍是 B；如果 B 允许稳定性降级且已降级，实际主渠道改为当前健康渠道，但固定意图仍保留到到期。

---

## 9. 路由应用方式

### 9.1 仅权重 `weight`

- 保留每条参与路由原有优先级。
- 只在相同优先级层内比较和分配权重。
- 同层少于两条可调渠道时不修改。
- 同层实际主渠道取得 `primary_traffic_percent`，其余权重按得分比例分配。
- 分配总权重使用 1000；最低有效分按 0.01 参与分配，避免健康渠道被分到无法使用的负值。
- 低优先级层在高优先级可用时不会取得普通首请求。
- 探索流量和低优先级轮转不在此模式执行。

该模式适合管理员已经人工设计好优先级层，只希望系统调整同层流量的场景。

### 9.2 优先级分层 + 权重 `priority_weight`

#### 9.2.1 基础排名

正常参与路由按以下顺序生成基础排名：

1. 当前有效的固定主渠道。
2. 当前主渠道切换保护后的实际主渠道。
3. 其余样本有效渠道按最终分降序。
4. 同分优先保留上一轮相对顺序，再按渠道 ID。
5. 样本不足渠道排在所有有效评分渠道之后。
6. 样本不足渠道之间优先上一轮相对顺序，再按渠道 ID。
7. 稳定性降级渠道不进入正常排名，统一进入 `P0/W0`。

#### 9.2.2 唯一优先级

有 `N` 条正常参与路由时：

```text
第 1 名 -> PN / W1000
第 2 名 -> P(N-1) / W1000
...
第 N 名 -> P1 / W1000
稳定性降级 -> P0 / W0
```

十条健康渠道会形成 `P10` 到 `P1` 十个独立优先级，不再只有第一备用和第二备用。渠道数量变化时，具体 P 值可以变化；产品稳定含义是基础排名和相对顺序。

#### 9.2.3 未参与人工路由

未参与路由继续使用管理员设置的绝对优先级：

- 高于智能第 1 名时，人工路由先承接请求。
- 与智能路由同层时，按双方权重分流。
- 低于智能路由时，只在前层不可用或重试时使用。

页面必须展示“实际最高优先级层”，不能只显示智能调度选择的主渠道。

---

## 10. 临时真实流量

### 10.1 通用规则

- 同一调度池同一时刻最多一条临时流量渠道。
- 样本不足探索优先于健康低优先级轮转。
- 临时渠道提升到实际最高优先级层，以小权重取得真实请求。
- 基础排名、基础优先级和基础权重始终保留，临时状态结束后恢复基础值。
- 固定主渠道存在时仍可采样，但固定渠道保留剩余大部分流量。

### 10.2 样本不足探索

触发条件：

- `sample_mode=traffic`。
- `apply_mode=priority_weight`。
- 至少有一条正常、参与、非保护且必需样本不足的路由。

目标比例取 `exploration_traffic_percent`。标准情况下使用 10000 个权重单位：

```text
探索 3%：主渠道 W9700，探索渠道 W300
```

探索持续到样本补足、策略变化、渠道退出参与、渠道禁用或进入稳定性保护。

### 10.3 低优先级轮转

候选必须满足：

- 已参与且渠道与 Ability 均启用。
- 样本充足并完成评分。
- 稳定性正常。
- 不是实际主渠道和固定渠道。
- 当前没有样本不足探索。

选择最久未轮转的渠道；从未轮转的优先；再按基础排名和渠道 ID。每条渠道保持 `priority_sampling_interval_minutes` 后轮换。

基础排名为 `r` 时：

```text
目标流量(r) = max(最小比例, 基础比例 × 递减比例^(r-2))
```

默认值 `基础 3% / 递减 70% / 最小 0.5%` 的示例：

| 排名 | 被轮转时的目标流量 |
| ---: | ---: |
| 2 | 3.00% |
| 3 | 2.10% |
| 4 | 1.47% |
| 5 | 1.03% |
| 6 | 0.72% |
| 7 及以后 | 最低不小于 0.50% |

比例是该渠道被选中期间的目标比例，不是全天平均比例。

### 10.4 人工最高层下的权重换算

智能调度不能修改未参与路由的人工权重。若实际最高层已有总权重 `H`，目标临时比例为 `p`：

```text
临时权重 = max(1, round(H × p / (1 - p)))
```

例如人工最高层总权重 `W1000`，目标 3%，临时渠道使用约 `W31`，预计比例约 3.01%。最高层总权重为 0 时无法安全计算，跳过临时流量并记录原因。

---

## 11. 稳定性保护

### 11.1 稳定性得分

```text
稳定性分 = 1 - 总惩罚 / 总样本数
```

- 最终失败按 1 次完整失败惩罚。
- 快速重试失败按 `fast_failure_penalty_percent` 惩罚。
- 上游 429 不参与上述稳定性惩罚和稳定性样本计数，但仍保留在普通错误统计中。
- 从快速失败界限到慢失败界限之间线性增加。
- 达到慢失败界限按完整失败惩罚。
- 成功样本不扣分，但异常慢成功可能通过抖动保护扣分。

“20 个样本允许 1 次慢成功”不是写死规则。允许数量按窗口实时计算：

```text
允许慢成功数 = floor(窗口样本数 × jitter_tolerance_percent)
```

容忍比例大于 0 时至少允许 1 次。例如默认 5%：20 个样本允许 1 次，40 个允许 2 次，10 个仍允许 1 次。

### 11.2 首字抖动阈值

只在健康状态、样本足够且稳定性达到恢复阈值时学习首字 P50 基线。

```text
慢成功阈值 = max(
  当前基线 × jitter_threshold_multiplier,
  当前基线 + jitter_absolute_tolerance_seconds
)
```

默认基线 2 秒时，5 倍为 10 秒，绝对容差为 12 秒，所以阈值是 12 秒。

基线按 `jitter_baseline_minutes` 的分钟级周期平滑学习；每次最多相对当前基线上下移动 10%，避免短时上游波动污染基线。

### 11.3 状态机

```text
正常
  | 稳定性低于降级阈值
  v
降级 P0/W0
  | 冷却到期
  v
试放：恢复保存优先级，权重最多 W10
  |-- 达到恢复阈值 --> 正常并进入下一轮完整排名
  |-- 低于降级阈值 --> 再次降级并重新冷却
  `-- 中间区间 ------> 继续试放
```

`P0/W0` 是“最后兜底”，不是禁用：只要存在 `P1` 以上可用路由，正常首请求不会进入；所有高层不可用或重试到最后时，仍允许官方选路使用。

稳定性试放保持现有小流量方式，不自动提升到主渠道同层。它可以通过失败重试、定时探测和手动测试取得恢复样本。

---

## 12. 固定主渠道

### 12.1 设置规则

- 唯一范围为 `(分组, 模型)`，同池只能固定一条路由。
- 固定时间单位为分钟，`0` 表示解除，最大 525600 分钟。
- 仅允许固定已启用、已参与且当前可固定的路由。
- 新固定会解除同池旧固定。
- 固定期间，固定渠道优先级必须高于池内所有启用 Ability，包括未参与智能调度的人工路由。
- 页面默认勾选“允许稳定性降级”，管理员可取消。

### 12.2 允许稳定性降级

固定意图和当前路由状态分开保存：

1. 固定渠道健康时作为主渠道。
2. 固定期内变得不稳定时进入 `P0/W0`，固定意图仍保留。
3. 其他健康渠道暂时成为实际主渠道。
4. 固定期内完成试放并恢复后，固定渠道重新成为主渠道。
5. 固定到期仍不稳定时，只清除固定意图，继续保持当前降级或试放状态。
6. 之后完全按正常调度恢复，不强行恢复不安全的旧路由值。

### 12.3 不允许稳定性降级

固定期内不因自动稳定性判定降级该渠道。管理员明确承担固定期间的可用性风险；渠道或 Ability 被官方禁用时仍无法接收请求。

### 12.4 到期和解除

- 健康状态到期：清除固定意图，恢复固定前保存值，再由下一轮正常调度接管。
- 保护状态到期：清除意图但不解除保护，把保护恢复目标改为固定前的基础值。
- 管理员提前解除与到期使用同一安全规则。

---

## 13. 任务、事务与失败保留

### 13.1 任务触发

- 按全局间隔自动执行。
- 管理员手动执行。
- 管理员强制重置。
- 固定、参与状态或相关配置修改后可触发新的调度任务。

同类型任务由系统任务租约和去重机制控制，避免多节点重复执行。

### 13.2 执行流程

1. 读取全局配置并记录控制版本。
2. 清理已到期固定意图和失效临时流量。
3. 读取 Ability、渠道、路由状态、成本倍率和共享观测数据。
4. 按 `(分组, 模型)` 建池。
5. 处理稳定性状态。
6. 评分、选择主渠道、生成基础排名。
7. 选择样本不足探索或低优先级轮转。
8. 为整池生成完整更新计划。
9. 校验配置版本、路由修订号、参与状态、渠道状态和原 Ability 值。
10. 在一个数据库事务内写入整池全部路由。
11. 成功后刷新渠道缓存并保存执行明细。

### 13.3 整池原子性

同一 `(分组, 模型)` 的全部路由必须在一个事务中提交：

- 任一路由 guard 冲突，整池零写入。
- 任一路由数据库写入失败，整池回滚。
- 一个池失败不阻止其他池继续执行。
- 失败池继续使用上一轮完整结果，不能出现一半新优先级、一半旧优先级。

### 13.4 配置变化

任务读取配置后，管理员若修改策略、参与状态、固定状态或人工路由值：

- 旧任务检测控制版本或路由修订号冲突。
- 旧任务放弃该池全部写入。
- 页面记录失败阶段为“配置冲突”。
- 新配置触发或等待下一轮任务重新计算。

### 13.5 失败记录

失败明细至少包含：

- 失败阶段：读取、计划、写入、配置冲突、缓存刷新或明细保存。
- 分组、模型、渠道和错误摘要。
- 本轮原计划的评分与 P/W。
- 上一轮成功生效时间、优先级和权重。
- “上一轮结果仍在生效”的明确提示。

---

## 14. 管理端页面

### 14.1 页面入口

渠道监控主页面提供两个独立记录入口：

- **余额和倍率更新记录**：只查看余额、倍率同步和相关渠道操作。
- **智能调度记录**：只查看智能调度任务和逐路由决策。

智能调度面板不再提供模型测试。渠道测试继续放在渠道管理的测试入口。

### 14.2 分组列表

- 左侧按分组成本倍率从低到高默认排序。
- 显示分组倍率、策略、参与模型数、路由数和异常数。
- 分组多时允许搜索和滚动，不依赖浏览器原生下拉框。
- 选择状态清晰，不因右侧数据刷新丢失当前分组。

### 14.3 调度池总览

右侧面向生产环境一个池几十条渠道的场景，使用紧凑表格或虚拟列表，不使用大卡片堆叠。每条路由至少展示：

- 渠道名称和 ID。
- 参与、禁用、固定、降级、试放、探索、轮转状态。
- 最终分和评分排名。
- 基础排名、基础优先级和基础权重。
- 当前应用优先级和权重。
- 预计流量，仅按实际最高优先级层计算。
- 成本倍率、首字、TPS、稳定性、样本数和缓存率观察值。
- 最近调度时间和最近调整原因。

池头部必须单独显示：

- 评分第一。
- 实际主渠道。
- 实际最高优先级层及同层预计比例。
- 当前固定意图。
- 当前临时流量渠道和目标比例。
- 性能窗口、稳定性窗口和样本共享口径。

### 14.4 路由详情

详情面板分区展示：

1. **当前路由**：基础 P/W、当前 P/W、预计流量和路由所有者。
2. **评分计算**：原始数据、样本数、比较范围、归一化分、有效权重、贡献和最终分。
3. **决策解释**：评分第一、实际主渠道、最高层、切换分差和调整原因。
4. **稳定性**：成功、失败、失败耗时、抖动基线、阈值、慢成功允许数和状态机时间。
5. **人工控制**：参与开关、固定分钟、允许稳定性降级、解除保护。
6. **未参与路由**：优先级、权重输入和保存按钮；参与调度时禁用编辑。

### 14.5 执行记录

列表默认按时间倒序，支持状态、分组、模型和渠道筛选。任务摘要展示：

- 触发方式、任务状态、开始时间、耗时。
- 总数、计划数、更新数、未变化数、跳过数和失败数。
- 性能窗口、稳定性窗口和策略快照。

展开单条渠道后展示：

- 本轮输入数据和样本口径。
- 完整评分公式代入结果。
- 基础排名与基础 P/W。
- 当前应用 P/W 和临时流量。
- 评分第一、实际主渠道、最高层及原因。
- 调整前后 P/W。
- 失败阶段和错误。
- 上一轮仍生效的时间与 P/W。

记录不能只显示“已更新”或“已跳过”，必须回答“根据什么数据得到什么结果，为什么调整或不调整”。

---

## 15. API 与持久化

### 15.1 管理接口

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/channel_monitor/schedule` | 查询路由、共享指标、稳定性和当前决策 |
| `POST` | `/api/channel_monitor/schedule/run` | 手动执行调度 |
| `PUT` | `/api/channel_monitor/channel/:id/schedule/routes` | 批量设置渠道参与状态 |
| `PUT` | `/api/channel_monitor/channel/:id/schedule/route` | 设置单路由参与状态 |
| `PUT` | `/api/channel_monitor/channel/:id/schedule/route/routing` | 修改未参与路由的 P/W |
| `PUT` | `/api/channel_monitor/channel/:id/schedule/route/primary` | 固定、续期或解除主渠道 |
| `POST` | `/api/channel_monitor/channel/:id/schedule/route/stability/clear` | 手动解除稳定性保护 |
| `GET` | `/api/channel_monitor/tasks?kind=schedule` | 查询智能调度执行记录 |

所有写接口仅管理员可用，参数同时在前后端校验，并写管理审计。

### 15.2 固定请求

```json
{
  "group": "vip",
  "model": "model-a",
  "duration_minutes": 30,
  "allow_stability_degrade": true
}
```

未传 `allow_stability_degrade` 时后端也按 `true` 处理。

### 15.3 人工路由请求

```json
{
  "group": "vip",
  "model": "model-a",
  "priority": 20,
  "weight": 1000
}
```

仅未参与路由可以提交。参与路由提交时返回明确业务错误。

### 15.4 核心持久化

路由状态保存：

- 参与状态和修订号。
- 最近调度状态、错误、得分、P/W 和时间。
- 不可变评分快照。
- 稳定性状态、冷却时间和恢复值。
- 抖动基线及更新时间。
- 基础排名、基础 P/W。
- 临时流量类型、开始时间、目标比例和最近轮转时间。
- 固定到期时间、允许降级选项和内部恢复值。

共享样本保存 `(渠道, 模型)` 的滚动原始样本和聚合结果。执行记录单独保存逐路由决策，任务摘要不因明细较多而失去分页能力。

---

## 16. 典型场景

### 16.1 十条健康渠道

`priority_weight` 下，最终排名为 A 到 J：

```text
A P10/W1000
B P9/W1000
C P8/W1000
...
J P1/W1000
```

普通首请求进入 A；重试按 B、C 等顺序后移，不再出现多个“第一备用”。

### 16.2 样本不足探索

A 是 `P5` 主渠道，E 样本不足且基础为 `P1`，探索比例 3%：

```text
基础：A P5/W1000，E P1/W1000
临时：A P5/W9700，E P5/W300
```

E 样本补足后恢复 `P1/W1000`，再按新得分参与下一轮排名。

### 16.3 健康低优先级轮转

B 基础排名 2，默认目标 3%；C 基础排名 3，目标 2.1%。每 10 分钟只选择其中一条进入主渠道层。B 和 C 不会同时稀释主渠道流量。

### 16.4 所有高层不可用

A、B、C 均因稳定性降级为 `P0/W0`。官方选路仍可在最后层选择它们作为兜底；如果管理员要求绝对禁止某渠道，必须禁用渠道或 Ability，不能依赖稳定性降级。

### 16.5 固定且允许降级

B 固定 30 分钟，10 分钟后降级；A 临时成为实际主渠道。B 在第 20 分钟恢复后重新成为固定主渠道。若 B 到第 30 分钟仍未恢复，则固定意图清除，B 继续保持保护状态。

### 16.6 人工路由覆盖

智能实际主渠道 A 为 `P10/W1000`，未参与路由 C 被管理员设置为 `P20/W500`：

```text
评分第一：A
智能实际主渠道：A
官方实际最高层：C
页面提示：人工路由优先级覆盖智能调度
```

如果管理员固定 A，固定期间 A 必须提升到高于 `P20`。

### 16.7 配置中途变化

任务读取旧策略后，管理员关闭 B 的参与开关。任务写入时发现修订号变化，整个池回滚；A、B、C 继续使用上一轮结果，执行记录显示“配置冲突，上一轮结果仍在生效”。

---

## 17. 验收标准

### 17.1 配置

- 性能和稳定性窗口均接受 1–43200 的任意整数分钟，默认 60。
- 固定主渠道默认勾选允许稳定性降级，前后端默认一致。
- 轮转五项配置可保存、回显、校验，模式切换不丢值。
- 不出现相对权重拉伸和得分曲线指数配置。

### 17.2 样本共享

- 同一渠道模型在两个分组产生的业务请求进入同一观测键。
- 手动测试只写一份共享样本，两个关联分组都能读取。
- 定时探测和手动测试按相同样本权重计入稳定性。
- 流式测试展示首字、总耗时和 TPS；非流式不伪造首字。

### 17.3 唯一优先级

- 十条正常路由生成十个唯一基础优先级。
- 样本有效渠道全部排在样本不足渠道之前。
- 样本不足渠道仍有独立优先级。
- 所有稳定性降级渠道统一为 `P0/W0`。
- 不再产生“第一备用多个渠道”的产品角色。

### 17.4 临时流量

- 探索渠道与实际最高层同优先级并取得配置的小比例真实流量。
- 样本不足探索存在时不同时执行健康轮转。
- 健康轮转按最久未轮转顺序选择，一次只提升一条。
- 轮转目标比例符合递减公式和最小比例。
- 临时状态结束后精确恢复基础 P/W。
- 人工最高层权重不被智能调度修改。

### 17.5 固定和稳定性

- 两种应用模式下，固定路由都高于全部启用路由，包括未参与人工路由。
- 允许降级时，降级不清除固定意图；固定期内恢复后重新固定。
- 固定到期仍不稳定时只清除意图，继续保护。
- 不允许降级时，固定期内不触发自动稳定性降级。
- 高优先级全部不可用时，`P0/W0` 仍保留最后兜底能力。

### 17.6 事务和失败

- 同池全部更新在一个事务内提交。
- 任一 guard 或写入失败时整池零写入。
- 一个池失败不影响其他池成功。
- 配置变化时旧任务不覆盖新配置。
- 失败记录显示阶段、原因和上一轮仍生效值。

### 17.7 页面

- 分组默认按成本倍率从低到高排序。
- 大量渠道使用紧凑、可滚动、可筛选的展示。
- 页面同时显示评分第一、实际主渠道和官方最高层。
- 路由详情可完整复算评分。
- 余额倍率记录和智能调度记录使用独立按钮进入。
- 执行记录清楚说明输入、计算、结果、调整原因和失败保留。
- 智能调度面板不显示模型测试。

### 17.8 数据库与工程

- SQLite、MySQL 和 PostgreSQL 均可迁移和执行。
- V2 不读取旧版三层角色和旧探索字段。
- 开发数据工具只能在显式开发环境、显式数据库和确认令牌下运行。
- 验收数据重复写入幂等，不清空或覆盖其他业务数据。

---

## 18. 开发环境验收数据

使用 `cmd/channel-smart-schedule-seed` 写入真实表结构数据，不伪造任务结果。数据集包括：

- 10 个隔离验收渠道。
- 2 个分组和 2 个模型。
- 40 条 Ability 和参与路由。
- 20 份 `(渠道, 模型)` 共享样本。
- 稳定高速、低成本、轻微失败、高成本、慢响应、首字抖动、近期不稳定、慢失败和样本不足等场景。

写入后必须配置验收分组策略并通过真实调度任务生成评分、路由和执行记录。执行保护和命令见 `docs/downstream/channel-monitor/development-seed.md`。

本轮开发验收完成后，应在本文附加：

- 使用的开发数据库和服务地址。
- 写入渠道 ID。
- 实际任务 ID。
- 至少一个唯一优先级池、一个共享样本、一个临时流量或稳定性场景的核对结果。

本轮开发环境已完成真实验收：

- 数据库：本机开发 MySQL；服务：`http://127.0.0.1:3001`。
- 验收渠道 ID：`9-18`；验收分组：`调度验收-低成本`、`调度验收-高可靠`。
- 最新真实调度任务：`systask_AsHMuetSoRYZ6nRfBss4fHNC8PZ6TWfj`，状态成功，总路由 40，计划 24，更新 24，跳过 16，失败 0。
- `smart-seed-chat-fast` 池验证：实际最高层为 `P6`，渠道 9 承接约 97%，渠道 18 以“样本不足补量”承接约 3%。
- 共享样本验证：渠道 9 的 `smart-seed-chat-fast` 在两个分组读取同一份 24 次样本，首字约 239 ms、TPS 58.00。
- 执行记录验证：页面可展开评分输入、归一化、权重、业务/稳定性贡献、选择原因、调整原因和上一轮生效 P/W；稳定性试放不足记录显示为“稳定性试放样本不足（0/5）”。

---

## 19. 明确不兼容项

本功能仍在开发阶段，V2 直接替换旧设计：

- 删除“主渠道 / 第一备用 / 第二备用”三层角色口径。
- 删除旧 `exploration_*` 持久化字段和前端依赖。
- 使用“基础排名 + 基础 P/W + 当前 P/W + 临时流量类型”统一表达。
- 不回填旧智能调度执行明细。
- 上线 V2 前允许清理旧路由状态、旧执行明细和旧测试数据，再从当前 Ability 与新配置初始化。

普通渠道、Ability、业务日志和非智能调度配置不属于清理范围。

---

## 20. 关联文档

- `docs/downstream/channel-monitor/api-and-operations.md`
- `docs/downstream/channel-monitor/development-seed.md`
- `docs/downstream/relay-reliability/README.md`
