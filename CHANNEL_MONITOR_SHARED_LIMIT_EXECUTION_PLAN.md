# 渠道共享限流与优先级保障：需求与执行计划

计划日期：2026-09-18。

状态：用户已确认并授权实施。共享限流、严格预留、管理页面和在线配置已实现；本地验证记录见第 11 节。部署和线上验收尚未执行。

## 1. 需求与设计基线

### 1.1 用户需求

- 多个物理渠道实际使用同一个上游账号或同一份上游额度，需要共用并发和 RPM 限制。
- 多个渠道之间存在优先级，需要保障某个或某几个重要渠道在资源紧张时优先获得额度。
- 配置和运行状态应纳入现有渠道监控。
- 本任务已由设计阶段进入实施与验证阶段。

### 1.2 本计划采用的设计建议

用户已确认以下设计；后续调整时先更新本文。

- 新增独立的“共享限流组”，管理员显式绑定物理渠道；第一版每个渠道最多加入一个共享限流组。
- 采用“共享总额度 + 组内资源优先级 + 高等级预留额度”，第一版实现严格预留。
- 组内资源优先级独立配置，不直接复用渠道路由优先级或智能调度生成的优先级。
- 保留原有单渠道限额；加入共享组后，同时受组级规则和单渠道限额约束。
- 等待时按照资源优先级选择可放行请求，同级按入队顺序处理；队列有长度和等待时间上限。
- 第一版不实现低等级借用高等级预留额度，不中断已发送的低优先级请求。
- 第一版不增加用户、令牌、业务分组级优先级，不支持一个渠道叠加多个共享限流组。

这里保障的是经过指定渠道的流量。如果普通请求和重要请求都会被路由到同一个高优先级渠道，两者会获得相同的资源等级。需要按用户或业务重要性区分时，应另行设计请求级优先级。

## 2. 实施前的基线与接入位置

| 位置 | 当前行为 | 接入要求 |
| --- | --- | --- |
| `service/channel_concurrency.go` | 按物理渠道 ID 管理并发租约、最近 60 秒 RPM；支持 Redis 和进程内实现 | 增加组级及等级约束，保留单渠道接口的兼容入口 |
| `controller/channel_concurrency.go` | 限流后重选渠道；无替代渠道时按配置等待，当前每 100ms 重试 | 识别渠道、共享组、等级预留等拒绝原因，接入有界优先级等待 |
| `controller/relay_retry_routing.go` | 按当前请求排除已失败或满载渠道 | 避免共享组总量耗尽时反复尝试同组成员，同时保留原路由权限边界 |
| `model/channel_ratio_monitor.go` | 保存单渠道并发、RPM 和配置修订号 | 保留现有单渠道配置，新组关系和规则独立保存 |
| `model/channel_logical_group.go` | 逻辑组服务于调度、探测和模型检测，物理渠道额度仍独立 | 不改变逻辑组的既有含义，不自动把逻辑组当作限流组 |
| `service/channel_concurrency.go`、`model/channel_monitor_current_rpm.go` | 监控快照会以消费日志计数补充 RPM 展示，口径不同于准入计数 | 分开返回限流 RPM 与消费日志请求数 |
| `web/src/features/channel-monitor/components/edit-channel-concurrency-limit-dialog.tsx` | 编辑单渠道并发、RPM | 保留单渠道入口，增加共享组归属、等级及组配置入口 |
| 探测、手动测试、模型检测入口 | 入口不同，部分已经获取业务租约，部分使用其他跟踪租约 | 逐条核对实际发送路径，覆盖绕过路径并避免重复计数 |

实施前重新核对工作区、上述调用链和本地 `upstream/main`。文档中的入口定位不替代实施时的代码检查。

## 3. 资源模型与限流规则

### 3.1 共享限流组

- 一个组代表管理员确认共用的一份上游资源，包含名称、总并发、总 RPM、成员和优先级档位。
- 不根据 Base URL、渠道类型、标签或逻辑组自动合并。同一地址的不同账号可能有独立限额，不同地址也可能共用同一账号限额。
- 组内所有成员的所有适用请求合计计数，不按模型、用户分组或路由轮次另开额度。
- 组并发或 RPM 为 `0` 时，该维度不设组级上限；该维度的预留值必须为 `0`。
- 单渠道上限为 `0` 时，只取消单渠道的额外上限，仍须执行组级及等级规则。
- 未加入共享组的业务渠道保持现有行为。手动测试也登记物理渠道租约，避免加入组时漏掉已在途的探测；因此手动测试同时受单渠道限额约束。

### 3.2 优先级与严格预留

- 资源优先级数值越大，优先级越高。相同等级的渠道共同使用该档位的保障，不等于每个渠道分别获得一份保障。
- 预留额度按等级配置，分别设置预留并发和预留 RPM；两项可以单独为零。
- 高等级可以使用未被占用的公共额度及低等级可用空间，低等级不能借用高等级的预留额度。
- 预留保护只针对较低等级；中等级的预留不构成对更高等级的限制。
- 严格优先允许低等级在持续高负载下等待或超时，不额外承诺低等级最低吞吐。
- 不抢占已经放行的请求，不返还已使用的 RPM；高等级自身消耗完可用额度后仍需等待。

对于并发和 RPM，分别执行以下同一套约束。设该维度的组总上限为 `L`，等级 `p` 的当前用量为 `U[p]`，预留为 `R[p]`：

```text
所有等级总用量 <= L

对每个优先级分界 t：
  优先级 <= t 的所有用量之和 <= L - 优先级 > t 的所有预留之和
```

检查应在把本次请求计入后的状态执行，并检查所有受影响的分界；不能只检查请求自身所在档位。否则多个低等级分别通过局部检查后，仍可能合计挤占高等级的预留。

配置校验要求：额度为有界非负整数；每个维度所有等级预留之和不得超过该维度总上限；复用现有并发、RPM 上限常量。档位、成员和队列数量也必须有明确上限，具体值在实施阶段确定并统一前后端校验。

### 3.3 两档示例

以下数字仅用于说明行为，不作为自动生成的生产默认值。

| 项目 | 示例值 |
| --- | --- |
| 共享限流组 | 上游账号 A |
| 成员 | 渠道 A、B、C、D |
| 总并发 / 总 RPM | 10 / 300 |
| 高优先级 | A、B，资源优先级 100 |
| 普通优先级 | C、D，资源优先级 0 |
| 高等级合计预留 | 3 并发 / 90 RPM |

- C、D 合计最多使用 7 并发、210 RPM，并且仍受整个组剩余额度限制。
- C、D 已占用 7 并发时，A、B 仍可合计获得剩余 3 个并发。
- C、D 在窗口内已使用 210 RPM 时，仍有 90 RPM 不会被普通渠道使用。
- 普通渠道无流量时，A、B 合计可以用满 10 并发、300 RPM。
- 高等级无流量时，预留的 3 并发、90 RPM 保持空闲。
- A、B 的保障为合计保障；分别保障时需要不同档位或后续独立的成员保障能力。

### 3.4 多档示例

总并发为 10，高等级预留 3，中等级预留 2，低等级不预留：

```text
低等级用量 <= 5
低等级用量 + 中等级用量 <= 7
所有等级用量 <= 10
```

中等级已占用 7 时，即使低等级当前为 0，低等级也不能继续进入。高等级没有请求时，中、低等级仍不得使用高等级预留的 3 个并发。RPM 按相同规则单独计算。

### 3.5 后续可选策略

| 策略 | 低等级可否借用空闲预留 | 高等级突然到来时 |
| --- | --- | --- |
| 严格预留，第一版 | 不可以 | 预留不会被低等级占用，但仍受自身用量和总上限约束 |
| 空闲借用，后续评估 | 可以 | 优先取得后续释放的额度，无法保证立即放行；RPM 可能要等窗口过期 |

借用策略不能与“预留额度随时可用”混为同一承诺。

## 4. 请求生命周期、等待与重试

### 4.1 准入与释放

1. 先执行原有鉴权、模型权限、请求校验和选路，确定实际物理渠道。
2. 读取该渠道的共享组、资源等级和有效配置版本；本地直接响应不占用上游额度。
3. 在同一次原子判断中检查单渠道上限、组总上限、等级累计上限及等待顺序。
4. 全部通过后，一次性登记并发租约和 RPM；失败不能留下部分占用。
5. 向上游发送请求，流式响应和实时连接在其实际连接生命周期内持有并发租约。
6. 请求结束、异常退出或取消时幂等释放并发；RPM 留在滑动窗口内，不因失败或完成自动返还。

RPM 沿用“成功准入的一次上游尝试”口径：被限流、队列等待不计数；每次获得准入的新重试重新计数。准入后在本地发送前失败，第一版保守保留该次 RPM，不引入不可靠的发送判定和返还流程。适配器内部如有额外重发，实施时必须审计其计数边界。

异步生成任务的并发第一版保持当前提交请求生命周期，不宣称覆盖上游后台任务的整个执行时长。若上游限制的是后台运行任务数，需要单独接入任务完成与取消生命周期。

### 4.2 有界优先级等待

- 优先尝试现有路由规则允许且有额度的其他渠道；没有替代渠道时再等待。指定渠道请求保持指定渠道约束。
- 复用现有 `ChannelMonitorChannelConcurrencyWaitSeconds` 作为第一版等待超时来源，`0` 表示不等待；不为每次重选重新计算完整超时。
- 组内集中记录等待元数据，优先级高的可执行请求先获得资格，同级按入队顺序处理。
- 新到请求不能绕过已经等待且可执行的同级或更高等级请求；最高等级请求因自身渠道上限暂时不可执行时，不阻塞其他仍可执行的请求。
- 队列必须有总量上限、请求截止时间、节点归属和清理机制；队列满或等待超时返回 429，客户端取消及时出队。
- 等待不占用并发与 RPM；队列领取和额度占用必须原子完成，不能先通知某个节点后再让所有节点自由争抢。
- 多节点下，等待顺序由共享状态决定，不能通过各进程内独立轮询推断优先级。
- 请求切换渠道或组时撤销旧等待记录；同一请求不得在多个组同时获得放行资格。

### 4.3 拒绝原因与重选

至少区分单渠道并发、单渠道 RPM、共享组并发、共享组 RPM、等级预留保护、排队超时、队列已满和运行状态不可用。

- 共享组总量耗尽时，同组换渠道不能绕过限制，可在当前请求内暂缓尝试整组。
- 单渠道限额或某等级额度耗尽时，组内其他符合条件的成员仍可能可用，不能一律排除整组。
- 临时排除有恢复条件，不能跨整个请求永久沿用一次过时的满载判断。
- 切换渠道仍遵守原有模型、令牌、用户分组、指定渠道和跨组重试权限，不为获取高等级额度扩大候选范围。
- 本地限流不能计为上游故障，不能触发自动禁用、上游故障冷却或健康降级。
- 限流等待与重选保持现有预扣费、退款和结算的幂等性，不额外制造重复收费或漏退款。

## 5. 配置、存储与一致性

### 5.1 建议数据结构

名称为拟定名称，实施时统一最终模型、字段和接口契约。

| 对象 | 主要字段 | 约束 |
| --- | --- | --- |
| `ChannelLimitGroup` | ID、名称、总并发、总 RPM、运行状态、revision、时间戳 | ID 稳定；版本单调递增；配置更新不能重置运行计数 |
| `ChannelLimitGroupTier` | 组 ID、资源优先级、预留并发、预留 RPM | 组内优先级唯一；规则整体校验和提交 |
| `ChannelLimitGroupMember` | 组 ID、物理渠道 ID、资源优先级 | 物理渠道 ID 唯一；等级必须存在于组内 |

- 采用独立下游模型和关系表，保留 `ChannelRatioMonitor` 中现有单渠道限额。
- 管理变更在数据库事务内完成，使用 revision 防止旧表单覆盖新配置；必要的行锁统一使用 `lockForUpdate(tx)`。
- 使用 GORM 的跨数据库能力，不引入仅某一种数据库可用的类型、SQL 或默认值。
- 运行时配置采用组级完整版本快照，成员关系和等级规则不能分批生效。
- 可选标量请求字段使用指针区分缺失与显式零值；JSON 编解码通过 `common.*`。

### 5.2 Redis 与单实例行为

- 多实例使用同一 Redis；未启用 Redis 时仅支持单实例，在同一锁内完成判断和登记。
- 复用单渠道计数作为唯一用量来源。所有渠道始终记录并发与最近 60 秒 RPM，包括未设置 RPM 上限的渠道；共享组按当前成员及优先级汇总，探测单独归入内部等级 `-1`。
- 并发租约保持现有渠道机制：每 30 秒续期，失联两分钟后回收；释放始终操作物理渠道，与配置修改无关。无需另设共享组人工恢复流程。
- Redis 脚本使用 Redis 时间，组、等级、单渠道限额与等待队列在一次 Lua 操作内判断和登记。采用项目既有单 Redis 客户端，不支持 Redis Cluster。
- 同一次准入重试使用稳定尝试 ID，避免重复占用和重复 RPM。释放只回收并发。
- Redis 连接故障时报错，不退化为各实例独立放行。运行状态丢失的语义与既有单渠道限流一致；内存状态不跨进程持久化。配置缓存可从数据库重建，重建不会清空仍然存在的渠道计数。

### 5.3 配置变更与生命周期

- 所有配置均支持不停机更新，包括总量、成员、资源等级和预留值。无需暂停、排空或等待一分钟。
- 新组保存后直接启用。下调上限时已有请求自然完成，新增请求按最新限额等待或拒绝；上调上限立即增加可用空间。
- 成员加入、退出或换组后，组用量按当前成员计算。渠道已有在途和最近一分钟记录跟随渠道参与新组汇总，不能因移出再加入而重置。
- 等级变化后，渠道现有用量按新等级汇总，等待队列也使用新优先级；原请求仍只释放自己所属物理渠道。
- 删除组直接恢复成员的独立限流，保留渠道计数；删除渠道本身前仍需先解除组成员关系。
- 暂停仅是管理员主动停止新准入的开关，可随时恢复，不作为编辑配置的前置步骤。渠道复制不继承共享组绑定，逻辑组变化不影响共享关系。
- 数据库提交后原子发布完整配置，发布前请求继续使用旧规则；发布失败明确显示未生效并允许重新保存。配置版本保证旧表单或晚到发布不会覆盖较新的配置。

## 6. 管理接口与渠道监控

### 6.1 实际接口

统一使用现有 `/api/channel_monitor` 路由前缀和对应管理权限：

| 接口 | 用途 |
| --- | --- |
| `GET /limit-groups` | 批量返回组配置、总用量、各等级用量、成员用量、等待数量与运行状态 |
| `POST /limit-groups` | 创建组及完整等级配置 |
| `PUT /limit-groups/:id` | 携带 revision 整体在线更新配置 |
| `DELETE /limit-groups/:id` | 在线删除，保留物理渠道计数 |

首版通过组配置统一管理成员，避免额外的渠道绑定接口与组保存接口产生两个写入源。现有单渠道并发设置接口保持兼容。详情和实时数据合并在列表接口内；拒绝原因由请求响应返回，首版未增加历史拒绝原因统计。

权限需逐成员验证，不能借组接口访问或修改无权限的渠道。审计记录操作者、前后配置、成员变化、revision 和发布结果，不记录上游密钥。

### 6.2 页面行为

- 渠道监控增加共享限流组管理入口，支持配置名称、总量、成员、等级和预留值。
- 渠道行展示所属共享组、资源等级及渠道自身用量；加入组后保留单渠道额外上限编辑。
- 组详情展示总并发、限流 RPM、各等级用量与预留、等待数量、配置生效状态和具体限流原因。
- 明确显示“严格预留会让空闲保障额度暂时闲置”，同时呈现从配置推导出的各低等级累计上限。
- 限流 RPM 直接来自准入状态；消费日志请求数使用独立字段和名称，不覆盖限流指标。
- 确认后才能把保存中的表单更新为实际生效配置；发布失败、版本冲突均需有可理解的反馈。
- 组列表及渠道列表批量获取配置和状态，避免每行产生独立查询。
- 本功能属于下游功能，新增用户可见文本按 `.agents/downstream.md` 使用简体中文，不修改 locale 文件。

## 7. 探测和其他上游调用

- 逐项核对正常转发、故障重试、流式请求、实时连接、异步任务提交、手动测试、状态探测、智能调度探测和模型检测。
- 会消耗这份上游推理额度的调用统一进入共享限流；已经持有租约的路径不得重复获取和计数。
- 自动后台探测默认使用低于业务请求的资源等级，不因被探测渠道是高等级而抢占业务预留。
- 后台任务遇到限流可跳过或按自身预算等待，不能无限堆积；跳过不记为上游失败。
- 手动测试仍受共享上限约束，资源等级的默认值在实施契约中明确，不提供隐式绕过限制。
- 余额、模型列表等管理 API 是否消耗相同上游额度取决于提供商契约，不把所有 HTTP 请求无差别计入模型推理池。

## 8. 分阶段执行计划

各阶段按顺序推进。完成时更新本节复选框，并记录实际改动和验证证据；不能仅以代码已写完作为验收完成。

### 阶段一：核对接入点与冻结契约

- [x] 记录工作区基线和 `upstream/main`，识别计划改动文件的上下游归属。
- [x] 核对所有获取、续期、释放租约及实际发送上游的路径，列出需补接的入口。
- [x] 固定优先级取值范围、档位数、组成员数、队列上限及默认值，复用现有额度与等待时间限制。
- [x] 固定状态码、拒绝原因、RPM 口径、接口字段和队列出队规则。
- [x] 明确在线配置发布、渠道计数保留和首次启用的行为。

交付：接口与行为契约，以及最小改动清单。优先新增同包下游文件，不复制现有转发逻辑。

### 阶段二：配置模型、管理服务与迁移

- [x] 新增组、等级、成员模型和约束；实现整体校验、事务保存、revision 与管理审计。
- [x] 实现管理接口和生效状态查询，不在这一阶段向旧节点提前启用新限制。
- [x] 接入渠道删除、复制及组退出流程，确保关系和既有配置不会意外丢失。
- [x] 编写真实 SQLite、MySQL、PostgreSQL 的配置持久化、唯一性和迁移验证。

交付：可保存和查询的共享组配置，以及三数据库验证记录。

### 阶段三：共享限流与资源预留

- [x] 实现组级、等级累计及单渠道约束的原子准入；Redis 与内存版本行为一致。
- [x] 实现尝试 ID 幂等、租约续期和释放、RPM 窗口清理及快照查询。
- [x] 实现配置原子发布、成员/等级在线修改，保留渠道用量。
- [x] 覆盖两档和三档示例、双重限制、长流、失败与重复释放等确定性测试。

交付：单入口可验证的共享准入能力，以及真实 Redis 下的原子性和故障验证。

### 阶段四：优先级等待、重选与入口整合

- [x] 实现跨实例有界等待、同级 FIFO、取消/超时/节点消失清理和原子领取。
- [x] 接入重选逻辑，根据拒绝原因区分渠道排除与组级暂缓。
- [x] 接入业务转发、重试和适用探测路径，证明既无绕过也无重复计数。
- [x] 验证路由权限、指定渠道、预扣退款和上游健康状态不被限流流程破坏。

交付：真实请求链路能够共享限额，并在竞争时兑现等级优先和预留保障。

### 阶段五：渠道监控与管理交互

- [x] 实现共享组管理、成员等级配置、预留校验及派生上限说明。
- [x] 增加组和渠道运行状态、等待信息、限流原因、配置生效状态。
- [x] 区分限流 RPM 与消费日志请求数，并保持现有未分组渠道展示兼容。
- [x] 补充关键行为和表单交互测试，完成类型检查、相关文件 lint 和构建。

交付：管理员能完成配置并解释请求为何被放行、等待或拒绝。

### 阶段六：联合验收、发布与交接

- [x] 完成本地自动化验收，记录第 9 节覆盖范围、真实数据库与 Redis 版本、命令和结果；线上验收随部署执行。
- [ ] 在未启用共享组的状态升级全部转发节点；旧节点不能继续处理已启用共享组的流量。
- [ ] 首次在线启用并核对成员已有请求和 RPM 已纳入汇总。
- [ ] 验证停用和回退流程；回退到不理解共享限流的旧版本前，解除相应流量对共享保障的依赖。
- [x] 更新使用文档、限制说明和验证记录，检查 diff 范围与空白错误。

交付：具备验证证据和在线配置说明的实现。部署属于独立动作，不能把本计划的保存视为部署指令。

## 9. 验收矩阵

| 编号 | 场景 | 必须满足的结果 |
| --- | --- | --- |
| SL-01 | 多个渠道共用一组额度 | 成员合计不超过总并发和总 RPM；不同组独立计数 |
| SL-02 | 普通渠道先用满两档示例的 7 / 210 | 新普通请求被阻止，高等级仍可使用未被其自身消耗的预留空间 |
| SL-03 | 高等级空闲或普通等级空闲 | 普通等级不能借用预留；高等级可使用公共额度直到总上限 |
| SL-04 | 三档叠加 | 同时检查全部累计上限，中等级已占 7 时低等级不能进入 |
| SL-05 | 单渠道和组限额同时配置 | 任一约束达到上限均拒绝；失败不留下任何部分占用 |
| SL-06 | 并发释放、流式结束、重复释放 | 正确释放一次；RPM 不随并发释放；长流不被提前视为结束 |
| SL-07 | 窗口到期、拒绝、取消、上游失败及重试 | 计数符合准入口径；等待不计数；新的上游尝试重新计数 |
| SL-08 | 高低等级并发等待与新请求到达 | 可执行的高等级先执行，同级 FIFO；新到请求不能插队 |
| SL-09 | 队列满、超时、断连及节点退出 | 有界返回或清理，不留长期幽灵等待项，不重复领取额度 |
| SL-10 | 重选同组成员、不同组及指定渠道 | 无共享额度绕过；保留权限约束；可用成员不被错误整体排除 |
| SL-11 | 探测和正常请求混合 | 同一上游额度正确合计，后台探测不占高等级预留，不重复计数 |
| SL-12 | 限流失败与业务扣费、健康管理 | 不重复收费或漏退款，不误判上游故障或禁用渠道 |
| SL-13 | 降低上限、移动成员、修改等级和删除 | 在线生效，渠道占用保留，修改不能重置 RPM |
| SL-14 | 多实例竞争、Redis 响应丢失与状态异常 | 原子准入、尝试幂等；故障不转为各节点独立放行 |
| SL-15 | Redis 与内存版本运行同一组输入 | 单实例下计数、预留、等待和释放的外部行为一致 |
| SL-16 | 页面配置、版本冲突及运行状态 | 展示实际生效版本，派生上限准确，限流 RPM 不被日志数量覆盖 |
| SL-17 | 未分组渠道、逻辑组和渠道复制 | 保持原行为，逻辑组变动不改变限流归属，复制不隐式加入组 |
| SL-18 | 新旧节点并存、首次启用与回退 | 旧节点不能绕过新规则，启用和回退均处理既有占用 |

### 9.1 数据库验证要求

- 必须使用真实 SQLite、MySQL >= 5.7.8、PostgreSQL >= 9.6 实例，不以 mock、单一数据库或编译通过替代。
- 每种引擎至少覆盖一个支持版本；涉及版本特性时覆盖最低支持版本。
- 同时验证新建数据库，以及由最新发布版本建立、包含代表性数据的数据库升级；补充当前下游已有渠道监控数据的升级场景。
- 每种场景至少运行两次启动/迁移，检查幂等性、数据保留、成员唯一性和索引约束。
- 若实现触及日志库共有路径，也覆盖独立配置的日志数据库。
- 记录准确版本、完整可复现命令和结果；缺少任一必需验证时明确记录阻塞，不能宣称数据库兼容或实现完成。

### 9.2 验证方式与命令记录

- Go 新增或重写测试采用 `testify/require` 做准备与致命断言、`testify/assert` 做非致命值断言。
- 使用有业务含义的显式输入、可控时钟和同步屏障验证窗口与竞争；避免随机压力循环、固定睡眠或仅断言日志。
- 真实 Redis 验证覆盖跨客户端竞争、幂等、排队领取和在线配置；模拟 Redis 不能单独证明多节点行为。
- 实施后先运行受影响包的定向测试，补充与变更相关的既有回归及构建，不为文档创建运行完整业务测试。
- 前端在 `web/` 执行 `bun run typecheck`、受影响文件 lint、相关 Vitest 测试及 `bun run build`；实施前读取适用前端规范和技能。
- 若修改 `relaykit/` 或其公开 API，必须进入该模块，以 `GOWORK=off` 单独执行 `go build ./...`。
- 收尾执行 `git diff --stat`、`git diff --check`；新文件另行检查内容和空白，不能因未跟踪文件未出现在普通 diff 中而漏检。

当前验证记录见第 11 节；下述记录区分实际执行的测试、代码路径核对和未执行的部署动作。

## 10. 下游维护与完成标准

- 遵循 `.agents/downstream.md`，优先新增下游文件和窄接入点，避免与功能无关的重构、依赖升级及整文件格式化。
- 任何必须修改的上游归属文件，都在实施记录中写明理由和最小接入范围。
- 不修改 new-api、QuantumNous 的名称、版权、元数据或归属信息。
- 本文执行项及验收矩阵全部完成、必需验证证据齐全后，才把实现状态更新为完成；尚未部署时另行注明。
- 后续进度记录应包含：日期、完成阶段、实际修改文件、验证命令与结果、剩余事项及阻塞。

## 11. 实施与验证记录（2026-09-18）

### 11.1 实际实现

- 基线 HEAD：`b561fb0b2abd0eb532d71a226e87b58c4dd9fe08`；对照 `upstream/main`：`67a0585d0f252dfca445c11b7600971b7eeb8eea`。
- 新增 `model/channel_limit_group.go`：组、等级、成员、全局配置版本四张表，事务整体更新、版本冲突检查、成员唯一约束。
- 新增 `service/channel_limit_{group,runtime,monitor,probe}.go` 与 `channel_limit_runtime.lua`：Redis / 内存共享准入、严格累计预留、FIFO 等待、快照和在线配置原子发布。
- `controller/channel_concurrency.go` 接入共享等待与整组满额排除；转发期间传递租约上下文，完成后恢复原请求上下文，避免取消状态进入后续重试。
- 新增管理接口及页面。组、成员、等级用量一起读取；消费日志请求数与准入 RPM 分开显示。新增文案遵循下游简体中文规则，没有修改 locale 或依赖文件。
- 手动测试统一登记物理租约，状态/调度/模型探测传递已持有的租约。探测资源等级为 `-1`，低于全部业务等级。
- AWS Bedrock 的共享组请求关闭 SDK 内部自动重试，失败交回网关重新准入；未分组请求保持原 SDK 重试行为。
- 成员、等级、预留和总上限均在线修改，删除无需暂停；共享组直接汇总现有渠道计数，移除排空及人工恢复流程。
- 使用文档：[共享上游限流](docs/downstream/channel-monitor/shared-limits.md)。

最终实现采用单渠道计数汇总共享组用量，不另维护组/等级租约。配置在线生效，取消暂停排空与人工恢复接口。详情和实时状态合并在列表接口；未增加历史拒绝原因统计。只支持项目现有单 Redis 客户端，未支持 Redis Cluster。

### 11.2 验收覆盖范围

| 验收项 | 已执行的证据 |
| --- | --- |
| SL-01–05 | 内存、模拟 Redis、真实 Redis 对照测试：共享总量、两档/三档累计预留、单渠道额外上限、拒绝无部分占用 |
| SL-06–07 | 幂等准入/重复释放、RPM 不返还、RPM 窗口过期时长请求仍占并发；流式/任务释放边界另做调用链核对 |
| SL-08–09 | 可执行高等级优先、同级 FIFO、等待取消/截止时间、256 项队列边界与取消后复用；失联等待项的 15 秒清理由运行时过期逻辑执行 |
| SL-10–12 | 控制器验证整组排除、权限内替代渠道、指定渠道 429、无健康禁用/渠道扣额副作用；复跑现有探测、重试与预扣费回归；核对 `Relay` 原有失败退款 defer 未被绕过 |
| SL-13 | 在线创建、编辑、删除、换组及等级变更；在途并发和历史 RPM 保留；等待项应用新等级 |
| SL-14–15 | Redis 8.8.0 两个独立客户端、同步起跑的 12 个低等级请求只准入 7 个；数据库不可用时重建失败、配置缓存重建保持用量；基础规则同时覆盖内存实现 |
| SL-16 | 表单在线编辑、绑定限制、配置冲突、运行中删除确认、真实等级用量和 RPM 口径展示 |
| SL-17 | 未分组限流的既有回归通过；逻辑组仍走原选路；复制只创建新物理渠道，不写共享组成员表 |
| SL-18 | 首次启用与删除的本地状态流转已测试；混合版本节点部署、线上首次启用及回退演练未执行，按使用文档安排 |

测试不访问真实付费 AI 上游。SDK 失败重试测试使用 HTTP 客户端边界模拟；跨客户端 Redis 原子性测试不等同于完整多节点部署演练。没有进行全量压测或所有提供商的端到端验证。

### 11.3 真实数据库与 Redis

| 引擎 | 实际版本 | 结果 |
| --- | --- | --- |
| SQLite | 3.50.4 | 新库、发布版代表性数据升级、重复迁移、持久化及唯一约束通过 |
| MySQL | 5.7.44 | 同上；测试数据库使用 utf8mb4 |
| PostgreSQL | 9.6.24 | 同上；使用真实数据库行锁路径 |
| Redis | 8.8.0 | 多客户端原子准入、优先级队列、在线配置与计数保留通过 |

升级夹具来自发布标签 `v1.0.0-rc.37` 的渠道表结构快照，插入旧渠道密钥、分组、已用额度和当前下游单渠道限额数据，再迁移新增共享限流表。每个场景重复迁移至少两次，保存配置后再次迁移，验证旧数据、渠道名称索引、渠道唯一成员关系与组内等级唯一性。此处验证的是受影响模型的代表性升级，不是启动整套旧版本应用并导入生产全库。日志库不使用本次新增表，没有修改日志库 schema 或共享迁移实现。

复现环境必须是专用测试数据库；测试会删除相应夹具表，Redis 测试会清空专用实例的 DB 14。使用以下临时容器，未使用现有业务 MySQL / Redis：

```powershell
docker run -d --name new-api-shared-limit-mysql57 -e MYSQL_ROOT_PASSWORD=shared-limit-test -e MYSQL_DATABASE=shared_limit_test -p 127.0.0.1:23357:3306 mysql:5.7.44
docker run -d --name new-api-shared-limit-postgres96 -e POSTGRES_PASSWORD=shared-limit-test -e POSTGRES_DB=shared_limit_test -p 127.0.0.1:25496:5432 postgres:9.6.24
docker run -d --name new-api-shared-limit-redis -p 127.0.0.1:26379:6379 redis:8.8.0
docker exec new-api-shared-limit-mysql57 mysql -uroot -pshared-limit-test -e 'ALTER DATABASE shared_limit_test CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;'

$env:TEST_SHARED_LIMIT_MYSQL_DSN='root:shared-limit-test@tcp(127.0.0.1:23357)/shared_limit_test?charset=utf8mb4&parseTime=True&loc=Local'
$env:TEST_SHARED_LIMIT_POSTGRES_DSN='host=127.0.0.1 port=25496 user=postgres password=shared-limit-test dbname=shared_limit_test sslmode=disable'
$env:TEST_SHARED_LIMIT_REDIS_ADDR='127.0.0.1:26379'

go test ./model -run '^TestChannelLimitGroupDatabaseMatrix$' -count=1 -v
go test ./model ./service ./controller -run 'Test(ChannelLimitGroup|SharedLimit|Acquire.*ChannelConcurrency|GetChannelMonitorConcurrency|.*ProbePolicy.*|.*ModelDetectorRelay.*|.*ChannelTest.*|.*ChannelStatusProbe.*|.*SmartScheduleProbe.*|PrepareRelayBilling.*|.*RelayRetry.*)' -count=1
go test ./relay/channel/aws -count=1
go build ./...
```

以上测试与构建通过。数据库测试主动插入重复记录验证唯一约束，因此 verbose 日志内对应的 duplicate/unique constraint 错误是预期断言，不是迁移失败。Redis 实际测试使用当日 `redis:latest` 镜像，运行时版本已核实为 8.8.0；上方复现命令固定该版本。

### 11.4 前端检查

在 `web/` 执行：

```powershell
bun run typecheck
bun run test src/features/channel-monitor/lib/__tests__/limit-group.test.ts src/features/channel-monitor/components/__tests__/channel-limit-group-editor.test.tsx src/features/channel-monitor/components/__tests__/channel-limit-groups-dialog.test.tsx src/features/channel-monitor/lib/__tests__/query-options.test.ts src/features/channel-monitor/components/__tests__/channel-view-layout.test.tsx
bunx oxlint -c .oxlintrc.json src/features/channel-monitor/api-limit-groups.ts src/features/channel-monitor/lib/limit-group.ts src/features/channel-monitor/lib/__tests__/limit-group.test.ts src/features/channel-monitor/components/channel-limit-group-editor.tsx src/features/channel-monitor/components/channel-limit-groups-dialog.tsx src/features/channel-monitor/components/__tests__/channel-limit-group-editor.test.tsx src/features/channel-monitor/components/__tests__/channel-limit-groups-dialog.test.tsx src/features/channel-monitor/components/__tests__/channel-view-layout.test.tsx src/features/channel-monitor/index.tsx src/features/channel-monitor/components/channel-monitor-channel-view.tsx src/features/channel-monitor/components/edit-channel-concurrency-limit-dialog.tsx src/features/channel-monitor/lib/query-options.ts
bun run build
```

结果：类型检查、所涉文件 lint、5 个测试文件共 52 项测试、生产构建通过；新增前端文件的 oxfmt 格式检查通过。未进行浏览器像素截图验收。

### 11.5 下游改动与交接

新增实现主要位于下游独立文件。修改的上游归属文件共三处：

| 文件 | 必要原因 |
| --- | --- |
| `model/main.go` | 在既有启动迁移列表注册四个新增模型，没有另建并行迁移入口 |
| `controller/channel-test.go` | 在实际发送测试请求前接入物理租约，并使用已持有租约标记避免重复计数 |
| `relay/channel/aws/relay-aws.go` | 两种 AWS 客户端构造均挂接共享限流重试策略，避免 SDK 内部重发绕过 RPM |

其余既有改动文件对照 `upstream/main` 均为下游文件。项目名称、作者信息和版权头保留；没有依赖升级或 locale 变更。`relaykit/` 及其公开接口未修改。

部署尚未执行；没有自动创建或启用实际业务共享组。根目录原有未跟踪 `%SystemDrive%/` 目录未改动。提交前检查普通 diff 和新增文件，排除生成产物与无关改动。

收尾结果：`git diff --stat` 已审阅，`git diff --check` 及新增文件单独的空白检查通过；Lua 文件仅有 Git 的 LF/CRLF 提示。三个专用测试容器及其匿名数据卷已删除，既有业务容器未改动。代码和验证记录纳入本次提交；尚未部署。

### 11.6 在线配置修正（2026-09-18）

按用户要求，所有配置恢复为不停机编辑，不采用暂停排空或人工恢复流程。共享用量直接汇总物理渠道的并发和 RPM，修改成员/等级时不搬迁或清零计数。在线创建、删除、变更总量、预留及等级均即时发布；原请求继续执行，后续准入按最新配置判断。

成员优先级保留项目统一的 Base UI `Select`。新组默认启用，启用期间所有字段可编辑，运行中的组也可以删除。先验证旧实现无法通过在线编辑/创建回归，再修改实现。

本轮复跑 SQLite 3.50.4、MySQL 5.7.44、PostgreSQL 9.6.24 的新库及代表性升级、重复迁移与唯一约束矩阵，全部通过。Redis 8.8.0 验证在线配置、历史用量保留及跨客户端原子准入。测试命令沿用 11.3，新增在线配置回归位于 `service/channel_limit_live_config_test.go`。

本轮后端定向回归命令（根目录）：

```powershell
$env:TEST_SHARED_LIMIT_REDIS_ADDR='127.0.0.1:26379'
go test ./service ./controller ./relay/channel/aws -run 'Test(SharedLimit|ChannelLimit|.*ChannelConcurrency.*|.*ChannelRPM.*|GetChannelMonitorConcurrency|.*ProbePolicy.*|.*ModelDetectorRelay.*|.*ChannelTest.*|.*ChannelStatusProbe.*|.*SmartScheduleProbe.*|.*RelayRetry.*|.*Shared.*)' -count=1
go build ./...
```

结果：全部通过。前端复跑 11.4 的 5 个测试文件共 52 项通过；类型检查、本轮 6 份 TS/TSX 文件的 lint/格式检查及生产构建通过。原先“必须排空”和“必须人工恢复”的测试已按新契约替换为在线创建、修改、删除、配置缓存重建和恢复准入时计数不变的行为测试。新增测试覆盖修改等级后队列按新优先级执行、成员移出后不阻塞其他请求。

本轮修改均在下游文件，未扩大 11.5 列出的上游接入范围；数据库结构与依赖版本未变，未修改 locale 或 `relaykit/`。尚未部署。
