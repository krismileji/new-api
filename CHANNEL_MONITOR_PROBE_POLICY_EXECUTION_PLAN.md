# 渠道自动探测策略、小输入响应与周期监测执行计划

计划日期：2026-09-16；实施更新：2026-09-17。

状态：核心实现及本地验证已完成，未部署。三数据库和前端验证、8 个已复现的基线测试失败见第 11 节；未执行多节点故障注入或容量压测。

调研基线：当前工作区 `HEAD=eccbf584e`，本地 `upstream/main=0ed497f06`。实施前重新核对基线与工作区状态。

## 1. 需求与设计口径

### 1.1 用户已确认

- 在物理渠道上配置禁止自动探测，保留手动检测。
- 用户请求命中禁止自动探测的渠道时，可另外开启小输入请求自定义响应：输入小于配置阈值时，在本地返回指定内容，不请求上游。
- 输入阈值按估算输入 token 数判断，`1k = 1000 tokens`。
- 状态监测、分组监控开启后，针对禁止自动探测的渠道，按照各自配置的探测周期计算监测数据；每个周期使用 Redis 数据。
- 上述周期均值只作为禁止自动探测渠道的替代监测，不整体替换其他渠道的实际探测。
- 已创建根目录执行计划；用户随后要求“开始逐步修改”，本轮据此实施。

### 1.2 本计划采用的实施默认值

以下是设计建议，不额外标记为用户已确认；后续用户调整时先更新本文，再实施。

- 周期数据仅取真实上游业务请求，排除自动探测、手动测试、模型检测和本地自定义响应。关于是否混入实际探测请求，用户尚未单独答复，先采用此默认值。
- 小输入响应按渠道分别配置，默认关闭；默认表单阈值为 `1k`，保存为整数 `1000`。
- 本地自定义响应不产生上游成本，用户收费默认为零；请求仍经过正常鉴权、模型权限、请求校验和用户限流。
- 常规文本协议覆盖 Chat Completions、Responses、Claude Messages、Gemini 文本生成的非流式和流式响应。其他输出契约不兼容的请求不匹配本地文本响应规则，沿用原请求处理并说明支持范围。
- 混合分组中的实际探测结果和禁探测成员的业务周期数据分别展示，不合成含义不明的平均数。

## 2. 当前实现与接入约束

| 现有位置 | 调研结果 | 本次接入要求 |
| --- | --- | --- |
| `model/channel_ratio_monitor.go` | 下游渠道监控模型，已承载物理渠道的倍率、上游配置、并发与 RPM 设置 | 在此保存独立探测策略，避免将能力限制放进逻辑组共享配置 |
| `controller/channel_status_probe.go`、`controller/channel_status_probe_worker.go` | 状态探测区分 `scheduled/manual`，逻辑组会选择实际物理成员；关闭周期探测仍能手动执行 | 自动候选筛选和实际发送前检查渠道策略；禁探测渠道开启监测时走 Redis 周期数据 |
| `controller/channel_group_monitor_worker.go` | 分组探测复用业务路由、重试与并发重选 | 自动任务中的每次选择、重选都排除禁探测渠道；不能只过滤模型下拉候选 |
| `controller/channel_ratio_monitor_schedule_probe.go` | 智能采样与降级恢复都可能发送探测 | 两类自动任务都受策略约束 |
| `controller/channel-test.go`、`controller/system_task_handlers.go` | 定时健康检查与手动批量测试共用实现，任务载荷尚无独立触发来源字段 | 显式传递来源，不能用后台执行、通知开关或函数名推断是否手动 |
| `service/channel_model_detection_scheduler.go`、`controller/channel_model_detector_fixed_executor.go` | 定时模型检测有独立调度和发送路径，执行记录已有触发来源 | 防止该路径绕过禁止自动探测；手动模型检测继续实际调用 |
| `pkg/channelprobe/response.go`、`router/relay-router.go` | 现有本地探针按固定输入匹配，只覆盖 Chat Completions/Responses；中间件在 `Distribute()` 之前执行 | 新规则需知道实际物理渠道，不能直接塞进现有前置中间件；可复用响应格式构造 |
| `controller/relay.go` | 预扣费早于重试循环内渠道选择；并发处理也可能切换渠道 | 本地命中、实际选路、预扣退款、并发/RPM 必须一起处理 |
| `service/token_counter.go` | `EstimateRequestToken` 在 `CountToken=false` 时直接返回 `0` | 本地响应条件不能把“未统计”当作零输入；需要独立且有有效性标记的输入估算 |
| `service/channel_monitor_redis_shared_projection.go` | 现有共享实时数据按分钟存储，主要窗口 TTL 为 48 小时 | 无法准确拆分 30 秒、45 秒等周期，也不能直接满足 30 天展示 |
| `service/channel_monitor_redis_route_health.go` | 有按秒定位的请求样本，但保留时间和条数受智能调度设置控制 | 可复用事件/聚合规则，不能依赖有限采样得到完整周期数据 |
| `controller/channel_group_monitor.go`、`controller/channel_status_probe.go` | 现有监控主要根据实际探测执行记录生成状态条、成功率和性能值 | 新增明确的数据来源和周期指标，业务采样不伪造成一次探测执行 |

## 3. 渠道策略和配置接口

### 3.1 建议字段

字段名为计划中的接口契约，实施时在前后端统一落定。

| 字段 | 类型 / 默认值 | 语义 |
| --- | --- | --- |
| `auto_probe_disabled` | bool / false | 禁止该物理渠道接受系统自动探测 |
| `small_input_response_enabled` | bool / false | 开启小输入请求本地响应，仅在前一开关开启时生效 |
| `small_input_threshold_tokens` | 整数 / 未启用时可为 0 | 严格小于该输入 token 数时命中 |
| `small_input_response_text` | text / 空 | 返回给客户端的自定义文本 |
| `probe_policy_revision` | int64 / 0 | 独立乐观锁版本，不复用余额、并发或探测任务的版本 |
| `probe_policy_updated_at` | int64 / 0 | 策略更新时间与监测模式变更边界 |

- 优先扩展下游 `ChannelRatioMonitor`，在同包新文件实现保存和查询；已有迁移入口已经包含此模型，不为新增字段另外改写迁移框架。
- 不使用 `gorm:"default:true"`；旧记录、没有监控记录的渠道都按两个开关关闭解释。
- `small_input_response_enabled=true` 必须同时满足 `auto_probe_disabled=true`、阈值和文本合法。
- 取消禁止自动探测时，在同一次保存中关闭小输入响应，保留阈值和文本供下次编辑；不保留隐式生效的联动状态。
- 阈值范围建议为 `1..1,000,000 tokens`，复用现有本地探针 token 上限概念；前端以 k 展示，最多三位小数，精确转换为整数 token。拒绝零、负数、NaN、无穷大、超限和非整数 token。
- 文本非空白，长度上限复用 `channelprobe.MaxResponseTextLength`；保存原始换行和有效空格，作为文本处理，不执行模板、脚本或 HTML。
- 请求 DTO 用 `*bool`、`*int`、`*int64` 等区分缺失与显式值，缺参数或版本冲突应明确报错。

### 3.2 接口与权限

- 新增 `GET/PUT /api/channel_monitor/channel/:id/probe-policy`，复用 Root 权限。
- 返回上述配置、实际生效状态与修订号；PUT 只更新本功能字段，不覆盖倍率、余额、上游凭据、并发或 RPM。
- 总览接口按需补充开关、模式和修订号，避免列表为每行单独请求配置。
- 保存时沿用渠道依赖记录的事务锁和 `lockForUpdate(tx)`；记录操作者、前后配置、修订号的管理审计。
- 配置更新后刷新页面与本节点监测索引，其他节点每 5 秒读取配置；自动发送直接读主库策略，过期快照不能授权新的自动发送。
- 渠道删除沿用监控记录清理，并移除新增 Redis 索引；渠道复制默认复制显式策略，逻辑分组的加入、退出不覆盖物理策略。

## 4. 自动与手动请求边界

| 入口 | 禁止自动探测时 |
| --- | --- |
| 周期状态探测 | 不发送；开启监测的目标按原周期生成业务观测数据 |
| 定时分组探测 | 排除禁探测的物理成员；对应成员使用业务周期观测 |
| 智能调度探测、降级恢复探测 | 跳过禁探测成员，不制造成功/失败样本 |
| 后台定时渠道健康测试、定时模型检测 | 跳过禁探测成员 |
| 手动立即检测、测试连接、手动批量测试、手动模型检测 | 保留真实检测；不被小输入响应替代 |
| 用户正常业务请求 | 允许路由进入该渠道，再判断是否符合小输入响应规则 |
| 余额、倍率、模型列表同步 | 使用其独立配置；不属于模型推理探测 |

实施规则：

1. 在任务创建处确定 `scheduled/manual`，经任务载荷、重试、逻辑组选择和最终执行一直传递。手动任务只对本轮生效，不打开未来自动任务。
2. 统一策略判断独立于“模型是否支持文本探测”，不要用禁止探测覆盖协议能力判断，否则会误禁手动检测和被动模型配置。
3. 生成自动任务与筛选候选时尽早过滤；每个实际物理渠道发送前再次检查，覆盖领取后修改配置、并发重选、故障切换和多实例缓存延迟。
4. 因策略跳过不占上游重试次数、不创建上游费用、不触发自动禁用或恢复计数。没有允许探测的成员返回可解释的跳过原因。
5. 禁止开关不删除原有模型、周期、展示窗口和历史。解除禁止后从正常的下一个周期调度，不补发暂停期间的探测。
6. 已经发送的请求正常收尾，记录真实结果与成本；尚未发出的自动尝试受新策略约束。自动任务无法读取可靠策略时跳过并记录本地原因，不能视为上游失败。
7. 路由稳定性保护按现有冷却与真实业务试放机制恢复；整体自动禁用的渠道停止后台测试恢复后，页面提示人工检测并决定是否启用。

## 5. 小输入请求自定义响应

### 5.1 匹配条件与边界

```text
普通用户业务请求
且完成鉴权、模型权限、请求格式和用户限流校验
且实际选中的物理渠道 auto_probe_disabled = true
且 small_input_response_enabled = true
且输入估算有效，input_tokens < small_input_threshold_tokens
且协议允许返回本地文本响应
→ 返回该渠道配置的内容，不向此渠道发送上游请求
```

- 比较符固定为 `<`。阈值 2k 时，1999 命中，2000、2001 不命中。
- 输入指完整客户端上下文，包括 system/developer、历史消息、当前输入及适用的工具定义，不仅是最后一条 user 消息。缓存输入仍属于输入总长度，不采用扣除缓存后的计价 token 数。
- 使用请求模型对应的本地 tokenizer，对完整文本上下文 JSON 计数，包含角色、历史、system 和工具定义。现有计费元信息未完整覆盖所有协议上下文，因此这里独立估算；不称为上游精确计数。模型映射和参数覆盖可能改变实际用量，不用响应后 usage 倒推本次匹配。
- 即使全局关闭计费 token 统计，本规则开启时也独立取得有效估算；关闭本规则时不为它增加全量文本计数开销。
- 无法完整计数的引用式上下文、未知多模态内容不能按零处理；不额外请求上游补全计数，沿用普通业务转发并记录未匹配原因。
- 手动检测、模型检测、自动任务不进入该业务规则。禁止自动探测通过跳过实现，不能通过返回自定义成功来伪装探测。

### 5.2 接入位置和重试

- 规则定义及匹配逻辑放下游独立文件；在 `controller/relay.go` 的实际选路链挂窄入口。
- 在每次渠道选择和并发重选之后，以本次实际物理渠道重新判定；不能沿用第一次候选的开关、文本或归属。
- 本地命中应在上游发送、上游并发/RPM 消耗、渠道余额预估占用、正常上游性能计时之前完成。并发重选循环也需要相同检查点。
- 首轮在鉴权和请求校验后，读取 `Distribute()` 已选中的物理渠道，在计价预扣前判断；未命中继续原链路。后续重试和并发重选，在新候选消耗上游配额前再次判断。
- 若走到重试时已经发生预扣，显式结束零收费分支并退还未结算预扣，覆盖钱包、令牌、订阅与追加预留；通过现有计费会话收口，禁止自行增减余额。当前退款异步执行，失败必须有记录与可重试处理，不能以已调用退款当作资金已返还。
- 当前 `Relay` 的退款 defer 仅在 `newAPIError != nil` 时工作；本地成功直接 `return` 不会退款，实施时必须补齐该路径，不能只增加一个早返回。
- 已有上游失败尝试的真实成本保留；最终本地响应不制造一次新的上游成本。客户端已收到上游内容时不能再切换为一份本地成功响应。
- 一旦决定输出并开始写本地响应，本请求终止后续选路；客户端断连不触发上游补发。

### 5.3 协议和观测

- 响应遵守客户端请求的协议，不能按渠道类型返回另一种格式。支持非流式及各协议正确的流式开始、内容、结束事件。
- 可复用 `pkg/channelprobe` 的格式构造，传入本功能的内容与用量；不要继承旧探针的模拟延迟、固定输入/缓存 token 默认值。
- 本地输出的 usage 按实际本地估算填充，缓存和推理用量不伪造；以响应头或合法的协议扩展明确 `local_response`。不会仅凭 token 数认定请求是探针。
- 配置内容仍受客户端输出 token 上限约束；超出时按协议安全截断并给出对应结束原因，不忽略客户端上限，也不把截断响应标记为完整输出。
- 受严格 JSON Schema、强制工具调用、异步任务、Realtime 或二进制输出约束的请求，在没有合法响应实现时不匹配；配置界面明确支持范围。
- 现有全局固定输入探针先于选路生效；保留其明确的优先顺序，新规则只处理继续进入选路的请求，不改变旧开关的语义。
- 新增独立观测来源 `local_response`，记录请求 ID、实际选中渠道、模型、估算输入、命中阈值、策略版本和本地来源；默认不记录完整用户输入。
- 本地响应 `request_dispatched=false`、`scheduling_eligible=false`、`runtime_protection_eligible=false`、上游成本为零；不计入上游成功率、首字/TPS、缓存率、稳定性恢复和业务周期均值。
- 独立展示本地响应次数；不能只改 source 名称却让来源兜底逻辑继续按 `business` 入样。

## 6. 禁探测渠道的 Redis 周期监测

### 6.1 监测目标与混合分组

| 情况 | 数据与行为 |
| --- | --- |
| 状态监测开启，物理渠道禁止自动探测 | 按该渠道状态监测模型与周期读取业务观测，显示“业务采样” |
| 状态监测未开启 | 不生成新的周期观测，历史可查看 |
| 逻辑渠道组全部相关成员禁探测 | 对该模型按实际成员业务数据聚合；保留物理归属，不发送探测 |
| 逻辑渠道组部分成员禁探测 | 自动探测只选允许成员；禁探测成员的业务观测单独展示，组探测成功不能解释为该成员已被探测 |
| 分组监控开启，相关渠道全部禁探测 | 以该业务分组 + 配置模型的真实业务数据生成周期结果，作为该目标主监测来源 |
| 分组内混有两类渠道 | 保留允许成员的实际探测结果，并展示禁探测成员的业务周期指标、成员数与样本数；不合并两类来源 |

- 使用事件中实际业务分组和请求模型匹配监测目标，不根据当前渠道所属分组给历史请求补归属。
- 渠道属于多个分组时，一个业务请求只进入实际请求分组的周期统计；同一事件可以分别进入状态监测和对应分组监控，各自只计一次。
- 逻辑成员范围按事件发生时有效的关联修订处理，保留实际物理渠道 ID；成员迁移不重新归属旧周期。
- 禁探测渠道可选择实际支持的具体业务模型，不必通过“可自动发送文本探测”的校验；手动检测仍遵循其既有协议能力限制。
- 分组全部禁探测不再表示配置无效；它是有效的业务采样目标。模型是否存在与是否允许自动发送分开判断。

### 6.2 周期边界

复用现有 `interval_seconds`，范围保持 `30..86400` 秒；状态监测用自身配置，分组监控用分组监控配置。周期不是页面刷新间隔，也不是“最近一次请求之后的 N 秒”。

```text
周期长度 = Δ
period_start = floor(event.occurred_at / Δ) × Δ
period_end = period_start + Δ
周期区间 = [period_start, period_end)
```

- 事件按实际完成时间归属；跨周期长请求在结束周期统计，不能作为上一周期的失败或零耗时。
- 例如周期 60 秒：`12:00:00..12:00:59` 完成的请求构成一个周期，`12:01:00` 完成的进入下一个周期。
- 默认主指标使用最近一个已结束周期；当前周期可以预览，标记“采集中”及当前样本数。
- 模式、周期、模型或逻辑成员变更建立新的配置版本与生效时间。首个不完整周期明确 `partial`；旧周期继续按旧版本展示，不把 60 秒结果重解释为 300 秒结果。
- 启用前的业务事件不凭当前配置补成完整周期。迟到事件按其发生时间对应的配置版本更新，不按消费时的新配置归属。
- 保留原展示范围；一个展示格合并多个周期时合并原始总量和分母，再计算指标。跨展示格的长周期按周期结束点展示并注明完整起止时间，不按时间比例拆出虚假的子周期。

### 6.3 指标计算

保存总量、独立分母和来源，不只保存最终平均数。

| 指标 | 周期内的计算 |
| --- | --- |
| 状态监测的实际尝试成功率 | 成功上游业务尝试数 / 已完成上游业务尝试数 |
| 全禁探测分组的业务请求成功率 | 最终成功业务请求数 / 已完成业务请求数；按请求去重，保留分组的最终请求语义 |
| 混合分组中禁探测成员的成功率 | 显式标注“成员实际尝试成功率”，不能冒充全分组最终成功率 |
| 平均首字 | 有效首字耗时总和 / 首字样本数 |
| 平均 TPS | 有效输出 token 总数 / 有效生成时长总秒数，沿用现有 Redis TPS 加权口径 |
| 平均响应时间 | 有效上游尝试耗时总和 / 耗时样本数；标签明确为上游尝试耗时 |

- 性能样本沿用现有业务监控的有效性规则；缺首字、缺 usage、非流式无 TPS 的样本不补零，也不计入相应分母。
- 使用 `TPSMeasurement()` 的有效输出与生成时长，不用总响应时间替代生成时间，不简单平均每次 TPS。
- 客户端取消、未发往上游的本地错误、`FinalRetrySummary` 与真实尝试分别处理；最终失败摘要不能重复增加实际尝试失败数。
- 同一请求在 A 失败、B 成功：A 的状态监测体现真实失败，B 体现成功；分组最终成功只计一次。混合组的禁探测成员子集不承担整个分组的最终归因。
- 本地自定义响应以及系统探测一律排除；此前已经实际发送的业务失败尝试仍是独立真实样本。
- 请求此前发生上游失败、最终改为本地响应时，之前的尝试统计保留，最终请求标记为本地终结并从上游最终请求成功率的分子、分母一起排除；不把它记为上游成功或留成永远未完成。
- 分母为零返回 `null` 与原因，展示“暂无业务请求”或“暂无有效性能样本”，不显示成功率 0% / 100%。

加权验收样例：

```text
请求 A：首字 100ms，输出 10 tokens，生成耗时 1s
请求 B：首字 300ms，输出 90 tokens，生成耗时 3s
平均首字 = (100 + 300) / 2 = 200ms
平均 TPS = (10 + 90) / (1 + 3) = 25 tokens/s
```

这里不能把两次 TPS `10` 和 `30` 简单平均成 `20`。

### 6.4 Redis 数据方案

新增下游独立的周期投影，接入现有 Redis Stream 事件消费链。周期读取只从 Redis 获取聚合数据，数据库仅保存配置和既有真实执行历史。

1. 在配置缓存中建立“当前需要业务采样的状态目标 / 分组目标”索引，限定为开启监测且包含禁探测渠道的目标；不在每笔用户请求中查询全部配置。
2. 复用已发布到 Redis Stream 的业务事件，在消费者中按目标、配置版本、周期精确归桶；不从 60 秒分钟总量比例拆出 30 秒/45 秒数据。
3. 每个周期存储成功/失败计数、性能总量与分母、处理水位和覆盖信息。建议键形态为 `channel_monitor:v1:passive_period:{scope}:{subject}:{revision}:{start}`，作为新投影独立演进。
4. 来源过滤、逻辑成员映射与聚合使用同一事件快照。新增投影必须独立于智能调度的开启状态、保留分钟数和样本条数上限。
5. 写入沿用 consumer group 和租约机制，使用原子事务或 Lua 将幂等标记与计数一起提交。新增投影有独立消费应用标记，不能因共享投影已写成功而在崩溃重试时漏写。
6. 同一事件重放不重复累计；费用补结算事件不成为新请求。使用稳定业务请求/尝试身份和版本处理更新，必要时替换旧贡献，不能仅依赖可能变化的事件 ID。
7. 页面按周期边界用一个有界 Redis pipeline 读取“最近已结束周期”和当前桶；完整结果保留有限的进程内回退。覆盖心跳使用多节点上报与单租约认证，不另建重复周期快照，不扫描日志或逐卡查询 Stream。
8. 迟到事件在修正窗口内更新原周期并提高数据版本；页面可刷新已结束但尚未收敛的周期。过期修正和消费积压通过 coverage 暴露，不静默丢失也不重复计数。
9. 数据缺失时不向上游补发探测，不从数据库历史临时拼出“Redis 周期均值”。真实成本账本仍按原可靠账务链执行。

配置版本元数据的保留时间覆盖可修正周期；消费者无法还原事件发生时的目标范围时标记覆盖缺口，不改用当前配置猜测历史。计数与耗时累计检查有限值和整数边界，异常标记不完整，不让溢出产生负耗时或无穷大均值。

保留和容量要求：

- 精确周期桶至少覆盖当前周期、上一个完整周期和事件修正窗口，不能因为最长周期 24 小时而使所需数据提前过期。建议保留最近 48 小时的可修正数据，并以周期结束时间计算过期点。
- 现有状态展示可达 30 天，需要额外保存由 Redis 周期数据形成的分钟/小时/日展示汇总，至少覆盖 30 天及边界余量；不能直接套用现有 48 小时 TTL。
- 长期展示汇总保留总量和分母；细粒度桶过期后保留汇总的 coverage，明确不能再还原更细粒度。迟到修正须原子更新所有受影响层级。
- 幂等标记必须覆盖允许修改周期的全部时间；过了修正截止的事件不能重新累计到已汇总周期。其账务部分继续可靠处理，观测部分标记迟到影响。
- 明确活动目标数、每批 fan-out、每页周期数、返回字节数与过期索引清理上限；按实际允许的最短 30 秒周期和配置模型上限估算容量。
- 不将用户输入、完整响应或无限增长的请求明细保存在周期 key 中；元数据索引同样需要过期和清理。

### 6.5 无数据、异常与健康状态

返回 `source=redis_business`、`period_start/end`、`interval_seconds`、`config_revision`、各样本数、`data_cutoff_at`、`processed_at`、数据版本与既有 coverage 契约。

| 情况 | 页面含义 |
| --- | --- |
| 周期未结束 | 采集中 |
| 链路健康、完整覆盖且有效业务请求数为零 | 暂无业务请求 |
| 有样本、全部成功 | 本周期业务请求正常 |
| 有成功也有失败 | 本周期部分业务请求失败 |
| 有样本、全部失败 | 本周期业务请求失败 |
| Redis 不可用、消费者积压、样本丢失、配置版本缺失 | 数据不可用或覆盖不完整，显示具体原因 |
| 本地响应有次数、实际上游请求为零 | 本地响应次数单列，上游仍为暂无业务请求 |

- 链路健康心跳与覆盖区间独立存在；不能只凭 key 不存在或本周期没有事件判定“没有业务”。
- Redis 异常保留最后有效结果和时间，标注过期/不完整；不显示为实时完整结果，不误记成渠道故障。
- 状态监测的手动检测结果和业务周期状态分别展示，手动成功不会覆盖业务均值来源。
- 周期汇总只用于展示与既定监控用途，不重新发布成一条智能调度成功/失败样本；真实业务事件已经参与原有调度，不能重复计权。

## 7. 前端交互

- 渠道监控的渠道配置新增“禁止自动探测”开关；不要求先配置余额/倍率上游接口。
- 开启后展示“小输入请求返回自定义内容”开关、输入阈值（k tokens）与响应文本，说明严格小于和完整上下文估算。
- 渠道行展示“自动探测已禁用”；本地响应另有状态，避免用户把两个开关当成同一件事。
- 禁探测渠道的状态页把“启用周期探测”解释为“启用周期监测”，周期、模型、展示范围继续可编辑；保存后自动使用业务采样。
- 状态卡显示最近完整周期、首字/TPS/响应耗时、成功率及各自样本数；tooltip 说明周期起止、业务来源、覆盖状态。
- 分组全部禁探测时显示业务采样卡；混合分组按第 6.1 节并列来源，保留手动检测入口。
- 用户点一次手动检测只触发该轮真实请求；按钮不能被小输入本地响应“测试成功”。
- 首次开启、周期切换、无业务、Redis 不可用、部分覆盖等状态都有明确文本；保留旧记录的真实来源。
- 下游文案全部使用简体中文，不新增 i18n key 或修改 locale 文件。实施前按任务范围读取项目 UI 技能与 `web/AGENTS.md`。

## 8. 实施阶段与完成条件

### T1：配置模型与 API

- [x] 实现渠道策略字段、独立版本、校验、保存/查询与审计。
- [x] 覆盖无监控记录、版本冲突、显式 false、删除、复制、逻辑归组及跨实例缓存更新。
- [x] 完成 SQLite/MySQL/PostgreSQL 新建与升级迁移验证，确认旧配置和唯一约束保留。

完成条件：独立保存策略不会覆盖其他渠道监控设置；两个开关默认关闭。

### T2：统一自动探测限制

- [x] 将触发来源传到状态、分组、智能采样、恢复、健康测试、模型检测各自的最终发送点。
- [x] 过滤自动候选，并在排队后执行、重试与并发重选时重新校验实际物理渠道。
- [x] 处理旧版手动批量任务载荷的语义，避免升级中将已排队手动任务当自动，或用缺失字段放行自动任务。
- [x] 跳过不计失败、恢复、上游成本或上游重试；实际已发请求正确收尾。

完成条件：每一种自动入口针对禁探测渠道的上游请求数为零；对应手动入口能实际请求。

### T3：小输入本地响应

- [x] 实现完整输入估算及有效性，不受 `CountToken=false` 的零值影响。
- [x] 在真实选路和并发重选处接入统一匹配；覆盖禁用开关、阈值边界、映射与重试切换。
- [x] 覆盖四类文本协议的非流式与流式输出，以及输出约束不兼容时的普通转发。
- [x] 完成零收费终结与退款、并发/RPM/余额预估释放，保留此前真实尝试成本。
- [x] 独立记录本地响应来源和次数，验证不进入任何上游健康、性能、缓存或恢复统计。

完成条件：阈值命中时返回配置内容且该次上游请求数为零；手动检测不被拦截，资金与令牌额度无泄漏。

### T4：Redis 精确周期投影

- [x] 建立禁探测监测目标索引、版本与生效边界。
- [x] 接入 Stream 消费链，按精确周期累计原始总量和分母，支持幂等、费用更新不重复、迟到修正和跨实例恢复。
- [x] 实现周期快照、覆盖水位、空周期判定、30 天展示汇总及容量边界。
- [x] 覆盖最长周期、最短周期、非整分钟周期和智能调度关闭时的独立运行。

完成条件：周期数据完全来自 Redis；固定事件集能计算出明确正确的指标，不扫描数据库日志或发补偿探测。

### T5：监控接口与页面

- [x] 接入状态、分组总览，返回来源、样本数、周期与覆盖信息。
- [x] 处理物理渠道、逻辑组、全禁探测分组、混合分组；不混入其他模型/业务分组。
- [x] 实现配置表单、本地响应标识、业务周期卡片和手动检测结果展示。
- [x] 使用可访问的表单控件，覆盖保存失败、版本冲突、加载、空态和错误态。

完成条件：只有禁探测成员使用替代监测；用户可分辨实际探测、本地响应、业务采样及数据不足。

### T6：集成验证与交付

- [x] 完成第 9 节核心行为回归与实际三数据库验证，记录精确环境、命令与结果。
- [x] 执行前端类型检查、涉及文件 lint/format、受影响测试和生产构建。
- [x] 审查 upstream-owned 修改清单、`git diff --stat` 与 `git diff --check`。
- [x] 更新下游使用文档，记录旧任务兼容、配置默认值、Redis 保留与模式切换说明。

上线时先使所有消费者支持新增来源和周期投影，再更新请求节点，最后启用配置；旧节点不能继续发送被新策略禁止的自动请求，也不能因不认识 `local_response` 阻塞事件消费。回滚前关闭新增功能并排空相关事件，不在旧二进制中假设这些契约已经存在。

依赖顺序：`T1 → T2 → T3`；`T1 → T4`；`T2/T3/T4 → T5 → T6`。这是依赖关系说明，不要求启动额外子代理。

## 9. 验收矩阵与验证记录要求

### 9.1 必须保护的行为

| 类别 | 明确用例 |
| --- | --- |
| 默认行为 | 旧记录、未配置渠道两个开关关闭，原有请求与探测可运行 |
| 自动限制 | 每一种自动入口、逻辑成员切换、分组重试、并发重选均无禁用渠道外发 |
| 手动来源 | 单渠道、分组、批量、模型检测手动任务实际发送；同一共享函数不能误拦 |
| 配置生效 | 已入队任务执行前关闭；不同节点保存/执行；租约失效；开启后不补发积压 |
| 输入边界 | 阈值前一 token / 相等 / 后一 token，system 与历史消息计入，k 转换精确 |
| 不确定输入 | 全局不计 token、引用上下文、多模态缺计数、估算失败不按零命中 |
| 协议 | 四类文本协议非流式/流式、中文及换行、结束事件、断连、结构化/工具契约 |
| 实际路由 | A 允许自动、B 禁探测；首次选择和故障/并发重选分别命中对应配置 |
| 资金 | 首轮本地命中、已预扣后命中、钱包/订阅/令牌/追加预留、断连重复终结均无多扣多退 |
| 来源隔离 | 本地 100 次且真实上游 0 次，页面显示本地 100 次与上游无样本；不产生 100 次成功 |
| 周期切分 | 30、45、60、300、86400 秒；恰好在边界完成；跨周期长请求 |
| 平均公式 | 第 6.3 节样例为首字 200ms、TPS 25；缺值有独立分母；跨周期合并按原始总量 |
| 请求语义 | A 失败 B 成功，渠道实际尝试与分组最终请求成功分别准确；终局摘要不重复 |
| 数据完整性 | Redis 断开、写入丢失、消费者暂停、缺 key、重复消费、更新事件、迟到与修正截止 |
| 模式切换 | 禁探测开/关、监测开/关、周期修改、模型修改、成员迁移，首个周期 partial |
| 分组隔离 | 同模型多分组、同渠道多模型、全部禁探测及混合成员，不跨维度累计 |
| 长期展示 | 超过 48 小时仍有明确来源的汇总；30 天边界、TTL 清理及过期数据无假零 |

后端新写或重写测试使用 `require`/`assert`；固定输入、明确输出、真实网络边界用本地可控 upstream。计时相关测试注入时钟，不用 sleep 或随机请求堆量制造结果。

### 9.2 数据库矩阵

- 所有新增配置字段、更新事务、审计或退款涉及的数据库行为必须验证真实 SQLite、MySQL、PostgreSQL；内存 mock 或只成功编译不算完成。
- 每种数据库验证全新数据库，以及由最新已发布版本建立的代表性数据库升级；迁移/启动各重复至少两次，检查原有配置、数据、渠道唯一约束与索引。
- 特别验证取消开关的 false 更新、缺失/零值、中文多行响应、修订冲突以及并发保存不覆盖其他字段。
- 如果新增记录复用日志数据库，覆盖主库与单独日志库配置；不修改日志结构也需验证本地响应记录去向和归属。
- 每种引擎至少覆盖一个支持版本；依赖版本特性的 DDL/SQL 还要验证最低支持版本（MySQL 5.7.8、PostgreSQL 9.6）。
- 在交付记录填写实际引擎版本、DSN 的脱敏形式、测试命令、迁移次数和结果；不照抄其他计划的历史通过记录。

### 9.3 实施时的检查入口

以下为常用复跑入口；实际执行记录见第 11 节：

```powershell
# 仓库根目录：实际新增测试统一使用以下业务前缀或按最终名称调整
go test ./controller ./service ./model ./pkg/channelprobe -run 'TestChannelProbePolicy|TestChannelSmallInputResponse|TestChannelPassiveMonitor|TestChannelStatusProbe|TestChannelGroupMonitor|TestChannelSmartScheduleProbe' -count=1

# web/ 目录
bun run typecheck
bun run test src/features/channel-monitor
bun run build
```

另对实际修改的前端文件运行 `bunx oxlint -c .oxlintrc.json <文件列表>` 与 `bunx oxfmt --check <文件列表>`。三数据库矩阵和真实 Redis 集成测试必须单独列出实际执行命令，不能以此处的包测试替代。

若最终修改 `relaykit/` 或其公开 API，必须在该目录执行 `$env:GOWORK='off'` 后 `go build ./...`，并恢复进程环境；根模块通过不构成独立模块验证。

## 10. 下游落点与上游文件最小修改

- 主要业务放 `model/channel_probe_policy.go`、`service/channel_probe_policy.go`、`controller/channel_probe_policy.go`、`controller/channel_small_input_response.go`、`service/channel_monitor_passive_*.go` 等下游新文件，命名以实际职责收敛。
- 修改现有下游 `model/channel_ratio_monitor.go`、渠道监控路由、状态/分组/智能探测 worker、模型检测发送路径、Redis 投影接入与渠道监控前端。
- 根据当前本地 `upstream/main`，`controller/relay.go`、`controller/channel-test.go`、`controller/system_task_handlers.go` 为上游已有文件：预计分别需要选路后本地响应/退款接入、健康测试策略接入、手动/自动来源传递。逐项核对必要性并保持最小改动。
- `service/token_counter.go`、`service/billing_session.go`、`router/relay-router.go`、`model/main.go` 同为上游已有文件：默认复用现有能力；只有独立下游适配不足时才增加窄接口，记录具体原因。
- 不为本功能改动 `relaykit` DTO 中的渠道其他设置；探测策略是宿主监控配置。响应类型确有复用需求时保持模块独立。
- 不重写中继重试/计费/调度算法，不把数据库读取放进每笔监控补偿链，不复制整个上游实现规避小范围接入。
- 本轮实现范围仅为此功能；工作区同时存在其他任务的 pre-audit 等改动，交付记录中单独说明归属。

## 11. 实施交付记录（2026-09-17）

| 项目 | 记录 |
| --- | --- |
| 最终配置/API 契约 | 物理渠道五字段 PUT + 独立修订号；新增 passive 总览、history、versions 接口。使用方式见 `docs/downstream/channel-monitor/probe-policy.md` |
| 实际修改的 upstream-owned 文件及原因 | `controller/audit.go` 审计模板；`controller/channel-test.go` 自动发送门禁与手动来源；`controller/system_task_handlers.go` 任务来源兼容；`controller/relay.go` 本地匹配和终结；`controller/channel.go` 与 `model/channel.go` 原子复制；`model/main.go` 退款表迁移。详细说明见使用文档 |
| SQLite / MySQL / PostgreSQL 版本与结果 | 真实 SQLite 3.50.4、MySQL 5.7.44、PostgreSQL 9.6.24；新建、旧下游监控结构、已发布 v1.0.0-rc.35 代表性渠道/账户结构升级；重复迁移两次；配置、唯一性、复制、钱包/令牌/订阅退款、回滚与缓存重放验证 |
| Redis 版本、周期精度、幂等和故障恢复结果 | Redis 8.8.0；30/45/60/300/86400 秒、边界归桶、均值、稳定尝试幂等、过期修正、覆盖缺口、旧配置版本和不可用回退通过；未做实际多节点故障注入 |
| 后端与前端检查 | 新增行为及相关回归通过；model、pkg/channelprobe 完整包通过；service/controller 排除下列 8 个基线失败后完整包通过。Go build/vet 通过。前端 110 文件/644 测试、类型检查、14 个改动文件 lint/format、生产构建通过 |
| 已知限制或阻塞 | 全量回归存在下列 8 个已在修改前代码复现的失败；未运行生产多节点故障演练或容量压测。进程内最后有效周期回退不跨重启。旧历史列表有界读取最近 100 个索引项 |
| 部署状态 | 本轮未部署 |

测试环境为 Windows + Go 1.26.5。MySQL、PostgreSQL、Redis 使用本次独立容器，端口分别为 13397、15497、16497；Redis 集成使用 DB 13/14。退款表只在主库迁移，日志库 schema 未改动。新版配置测试冻结了旧下游结构 `eccbf584e` 及已发布 `v1.0.0-rc.35` 的渠道/账户结构，检查旧数据与唯一索引未丢失；各独立数据库场景重开连接，避免跨测试 DDL 重用 PostgreSQL 语句计划。

实际复跑命令（专用测试库，会重建用例负责的表）：

```powershell
$env:TEST_PROBE_POLICY_MYSQL_DSN='root:<测试密码>@tcp(127.0.0.1:13397)/probe_policy_test?charset=utf8mb4&parseTime=True&loc=Local'
$env:TEST_PROBE_POLICY_POSTGRES_DSN='host=127.0.0.1 port=15497 user=postgres password=<测试密码> dbname=probe_policy_test sslmode=disable'
$env:TEST_PROBE_POLICY_REDIS_ADDR='127.0.0.1:16497'
go test ./model -run 'TestChannel(ProbePolicy|SmallInputResponse)' -count=1 -v
go test ./service ./controller ./pkg/channelprobe -run 'TestChannel(ProbePolicy|PassiveMonitor|SmallInputResponse|ModelDetector|LocalResponse|MonitorEvent|StatusProbe|GroupMonitor)' -count=1
go build ./...
go vet ./service ./controller ./model ./pkg/channelprobe

# web/
bun run typecheck
bun run test src/features/channel-monitor src/features/group-monitor
bun run build
```

完整 `go test ./service ./controller ./model ./pkg/channelprobe -count=1` 中，新增策略要求真实存在的渠道记录，因此补齐了旧探测日志测试的两条渠道夹具。剩余 8 个失败在由修改前 `HEAD=eccbf584e` 导出的干净目录中复现，其中 recovery 用例需完整 controller 包才能复现：

```text
TestUpdateGroupedChannelAddressValidatesMembersAndAdvancesRevision
TestGetChannelMonitorRecoveryBeforeBackgroundCheck
TestProtectChannelSmartScheduleRuntimeFailureIgnoresMinimumSamples
TestProtectChannelSmartScheduleRuntimeFailureDoesNotRecountPersistedErrors
TestRunChannelSmartSchedulePersistsExecutionTimeScoreDetails
TestPlanChannelSmartScheduleUsesHysteresisAndForceReset
TestRunChannelSmartScheduleManualPrimaryAllowsStabilityDegrade
TestUpdateChannelMonitorSettingsValidatesAndPersists
```

排除已确认基线失败的完整回归命令：

```powershell
go test ./service ./controller -skip 'Test(UpdateGroupedChannelAddressValidatesMembersAndAdvancesRevision|GetChannelMonitorRecoveryBeforeBackgroundCheck|ProtectChannelSmartScheduleRuntimeFailureIgnoresMinimumSamples|ProtectChannelSmartScheduleRuntimeFailureDoesNotRecountPersistedErrors|RunChannelSmartSchedulePersistsExecutionTimeScoreDetails|PlanChannelSmartScheduleUsesHysteresisAndForceReset|RunChannelSmartScheduleManualPrimaryAllowsStabilityDegrade|UpdateChannelMonitorSettingsValidatesAndPersists)$' -count=1
```

结果：service 59.986 秒、controller 28.544 秒通过。未调整这些已有用例对应的业务规则。工作区的 pre-audit 功能、导航、生成路由与其他既有文档/补丁属于并行任务，已保留。

最终三数据库矩阵通过（7.835 秒）；最后一次针对性后端回归为 service 4.104 秒、controller 3.451 秒、pkg/channelprobe 1.131 秒；最后两处页面调整另复跑 5 文件/32 测试通过。修改文件格式、构建、vet 与 diff 检查通过。本次测试容器已停止。临时基线目录的递归清理被自动审批拦截（仅返回 blocked by policy），因此保留在系统临时目录；仓库内临时测试日志已移除。
