# 渠道状态监测设计

> 状态：已实施并进入联调验收（2026-08-11）
>
> 目标版本：下游渠道监控扩展
>
> 页面入口：`/channel-monitor`
>
> Tab 名称：**状态监测**

## 0. 实施摘要

- 已完成状态监测 Tab、每渠道卡片、配置、立即检测、执行记录、组合筛选与排序；分组和模型使用级联筛选，必须先选分组，模型候选只来自该分组下已配置的状态探测模型；搜索仅匹配渠道名称、备注和 ID，样本设置使用卡片标签展示；卡片可按分钟、小时或天展示滚动状态，范围分别为 `1..60` 分钟、`1..24` 小时、`1..30` 天，默认展示 60 分钟并按成本倍率从低到高排序。
- 已完成秒级固定延迟调度、多实例数据库租约、同渠道模型串行和渠道间并行，并复用真实渠道测试、并发限制、计费与消费日志链路。
- 智能调度样本写入失败时保留为 `pending`，Worker 每 30 秒重试近期记录，超过 24 小时标记失败；重试只补写样本，不再次请求上游。
- 执行记录默认保留 7 天，可配置为 `1..90` 天，复用渠道监控每日清理任务；删除渠道时同步清理配置、最新状态和执行历史。

## 1. 结论

在现有“渠道监控”中新增“状态监测”Tab，顺序为“渠道、状态监测、智能调度、分组、模型性能”。页面始终按“一渠道一卡片”展示全部渠道，包括尚未配置、已暂停和已禁用的渠道。

每个渠道单独配置：

- 是否启用周期探测。
- 要探测的一个或多个精确模型。
- 探测间隔，单位为秒。
- 状态条和平均指标的展示范围，由数值与分钟/小时/天单位组成，整体最多 30 天，默认 `60 分钟`。
- 是否把有效探测结果计入智能调度的 `(渠道, 模型)` 共享样本。

卡片展示渠道备注、成本倍率、最近一次首字和 TPS、配置窗口内的平均首字和平均 TPS、下次执行时间，并为每个已配置模型分别展示同一窗口的结果带。点击卡片主体打开执行记录抽屉；卡片菜单提供配置、立即检测、暂停或恢复。

本功能只负责探测和观测，**不自动启用或禁用渠道**。现有全局自动渠道测试继续负责自动禁用与恢复，二者不能混成同一套语义。

总响应耗时不在卡片展示，但仍保留在单次执行记录中用于故障排查。出于安全、隐私和存储成本考虑，不保存上游响应正文；执行记录只保存性能、用量、结果和经过脱敏、截断的错误信息。

## 2. 现有能力与约束

### 2.1 可直接复用的能力

- `controller/channel-test.go` 的真实渠道测试已经支持自动端点选择、流式请求、总响应耗时、首字时间、TPS、Usage、计费和消费日志。
- `GET /api/channel/test/:id` 已支持 `model`、`endpoint_type`、`stream` 和 `record_sample`。
- 手动测试能够通过 `recordManualChannelSmartScheduleProbeResult` 写入 `(渠道, 模型)` 共享样本。
- 智能调度定时探测会根据共享样本的 `LastTime` 跳过近期已经有样本的渠道模型，因此状态探测写入样本后可以自然减少重复的智能调度探测。
- 渠道并发租约已经由 `service.AcquireChannelConcurrency` 统一处理，本功能应复用，不能绕过生产渠道并发上限。
- 渠道监控是下游专用模块，界面和接口文案继续使用简体中文常量，不修改 locale 文件。

### 2.2 不能直接复用的部分

- 当前全局自动渠道测试按分钟配置，且可能自动禁用或启用渠道，不适合作为秒级、每渠道多模型的状态采集器。
- 当前系统任务调度器每 `15` 秒扫描一次，并且每次运行会留下任务记录。用它承载高频状态扫描会产生大量无业务价值的系统任务行，也无法稳定表达每渠道不同的秒级间隔。
- 现有模型性能视图统计业务流量，不能代替状态探测自身的窗口平均值和逐模型结果带。
- 智能调度共享样本当前只接受 `scheduled_probe` 和 `manual_test` 两种来源，需要增加明确的 `status_probe` 来源，避免审计时混淆。

## 3. 产品边界

### 3.1 本期必须完成

1. 新增“状态探测”Tab 和响应式卡片网格。
2. 每渠道配置启用状态、模型列表、秒级间隔、状态展示范围和智能调度样本开关。
3. 后台持久化调度，服务重启后继续执行，多实例不会重复探测同一渠道。
4. 保存每个模型的最新状态和分页执行记录。
5. 为每个配置模型展示当前配置窗口的状态条，并同时展示最近一次首字/TPS和窗口平均首字/TPS。
6. 提供状态筛选和“分组 → 模型”级联筛选；搜索支持渠道名称、备注和 ID，并支持成本倍率、平均首字和平均 TPS 排序。
7. 支持“立即检测”，请求异步排队，不占用浏览器长连接。
8. 复用渠道并发限制、测试计费、Usage 和日志链路。
9. 可选写入智能调度样本，并完整展示“已记录 / 未记录及原因”。
10. 历史数据自动清理，渠道删除时清理关联配置和状态。

### 3.2 本期明确不做

- 不根据状态探测结果自动启用、禁用渠道。
- 不改写渠道优先级、权重或默认模型。
- 不保存请求体、响应正文、密钥、认证头等敏感内容。
- 不把状态探测结果计入普通真实业务请求的分钟性能统计。
- 不承诺硬实时调度；配置间隔是目标间隔，实际开始时间会受请求耗时、并发上限和队列负载影响。
- 不用本功能替代现有全局自动渠道测试或智能调度探测。

## 4. 页面设计

### 4.1 Tab 与工具栏

Tab 顺序建议为：

```text
渠道 | 状态监测 | 智能调度 | 分组 | 模型性能
```

现有移动端 Tab 使用固定两列网格。增加第五项后应改为可横向滚动的紧凑 Tab 列表，避免三行 Tab 占据首屏，也避免文字挤压。

“状态监测”工具栏包含：

- 状态分段筛选：全部、异常、过期、正常、已暂停、未配置。
- 分组选择：默认显示“选择分组”，选择一个精确分组后才可使用模型筛选。
- 模型选择：未选择分组时禁用并显示“请先选择分组”；选择分组后只提供“全部模型”和该分组下已配置的状态探测模型。分组没有已配置探测模型时继续禁用并显示空状态。
- 排序选择：成本倍率、平均首字、平均 TPS，均支持双向排序。
- 搜索框：只匹配渠道名称、备注和 ID，不与分组、模型筛选混用。

状态分段筛选和控件区均靠右排列；桌面端的分组、模型、排序、搜索和刷新控件紧凑换行，移动端保持全宽纵向布局。切换分组时立即清空已选模型，避免保留不属于新分组的模型条件。顶部用一行紧凑统计展示各状态数量，不再额外套一层卡片。默认只在该 Tab 激活时请求和轮询状态数据。

### 4.2 卡片布局

```text
┌──────────────────────────────────────────────┐
│ ● 正常  渠道名称 #12              [⏸][↻][⚙] │
│ 备注：主线路，额度充足                         │
│ 最近首字 242 ms          最近 TPS 45.1       │
│ 近 60 分钟平均首字       近 60 分钟平均 TPS  │
│ 286 ms                  41.8                 │
│ 成本倍率 0.75 · 模型 3 个 · 每 300 秒       │
│ 近 60 分钟状态                               │
│ ● gpt-4.1       [■■□■■■■□□■■■] 成功 · 15:42 │
│ ● gpt-4.1-mini  [■■■■■■■□■■■■] 成功 · 15:41 │
│ ● o3-mini       [■■■■□□□■■■■■] 异常 · 15:40 │
│ [不计入样本]                       下次 04:18 │
└──────────────────────────────────────────────┘
```

布局规则：

- 手机单列、普通桌面两列、宽屏三列：`grid-cols-1 md:grid-cols-2 xl:grid-cols-3`。
- 卡片只作为重复渠道项；指标区使用无边框 `<dl>`，不在卡片里再嵌套指标卡片。
- 渠道名、模型名和错误原因必须允许截断并提供 Tooltip，不能撑破布局。
- 状态同时使用圆点、文字和可访问名称，不能只依赖颜色。
- 最近一次和窗口平均的首字/TPS使用等宽数字和稳定宽度，加载或内容变化不能导致卡片跳动。
- 未返回首字或 TPS 时显示 `-`，不能显示 `0`。非流式端点的 `-` 是“不可用”，不是性能为零。
- 展示范围由数值和单位共同决定：分钟 `1..60`、小时 `1..24`、天 `1..30`；状态条按所选单位展示对应数量的桶，默认 `60 分钟`，没有执行的桶显示为空档。
- 分钟桶和小时桶以服务器 Unix 时间对齐，天桶按北京时间自然日对齐。同一桶有多次执行时不丢弃数据：色块按该桶最严重的结果着色，Tooltip 展示成功、失败、限流、跳过数量及涉及模型。
- 每个已配置模型独占一条状态带；工具栏选择具体模型时只返回该模型。窗口随状态轮询向前滚动，不按自然整点重置。
- 卡片高度固定，模型状态区内部滚动，配置到上限 20 个模型时也不能撑高整行卡片。
- 配置菜单和立即检测是独立图标按钮，使用 Hugeicons 和 Tooltip。

卡片主体做成可聚焦的按钮区域，点击或按 Enter/Space 打开记录。菜单、开关不能嵌套在按钮内部，避免无效 HTML 和键盘冲突。

### 4.3 多模型状态条与平均指标

- 分组是模型筛选的前置条件；模型候选严格由总览接口的 `models_by_group[所选分组]` 派生，不另存一份可漂移的前端状态。
- 工具栏选择了具体模型：只保留配置了该模型的渠道，卡片只展示该模型的一条状态带。
- 工具栏为“全部模型”：按配置顺序展示全部模型状态带，每条状态带单独显示最近结果和完成时间。
- 平均首字和平均 TPS 只统计当前配置窗口内成功执行且对应指标非空的记录；不同指标各自使用自己的有效样本数，缺失值不按 `0` 参与。
- “全部模型”时使用所有当前展示模型的有效样本加权平均；选择具体模型后，平均值和排序都严格限定为该模型。
- 平均首字、平均 TPS 排序使用卡片当前窗口平均值；没有有效样本的渠道始终排在有值渠道之后。
- 最近首字和最近 TPS 来自卡片当前展示模型中完成时间最新的一次执行，不参与窗口平均值替代，也不因窗口缩短而隐藏。

### 4.4 执行记录抽屉

桌面使用右侧 Sheet，移动端使用接近全屏的 Drawer。内容包括：

- 渠道基本状态、当前配置、下次执行时间和连续失败数。
- 配置、立即检测、暂停或恢复操作。
- 模型、结果和触发方式筛选；桌面端紧凑右对齐，移动端保持全宽纵向布局。
- 服务端分页记录，默认每页 20 条，最大 100 条。
- 每条记录展示时间、模型、计划/手动触发、结果、总响应、首字、TPS、Usage、样本写入状态和错误摘要。
- 错误详情按行展开；不显示响应正文。

正在执行的轮次显示在历史列表顶部，但不伪造性能值。请求完成后用真实记录替换。

### 4.5 配置表单

字段顺序：

1. 启用周期探测：Switch。
2. 探测模型：多选 Combobox，精确模型名，至少一个，最多 20 个。
3. 探测间隔：数字输入，单位秒，提供 `60 / 300 / 900 / 3600` 快捷值。
4. 状态展示范围：整数输入配合分钟/小时/天 Toggle Group，切换单位时自动收敛到该单位上限。
5. 计入智能调度样本：Switch。
6. 负载预估：只读显示“每小时约 N 次请求”。

校验规则：

- `interval_seconds` 范围 `30..86400`，默认 `300`。
- `display_value + display_unit` 必须满足：分钟 `1..60`、小时 `1..24`、天 `1..30`；新配置和旧数据缺省值都归一化为 `60 分钟`。
- 开启智能调度样本时，间隔不得小于 `60` 秒，与现有智能调度最小一分钟探测口径保持一致。
- 模型名 Trim 后去重，保留大小写，单项最长 255 字符。
- 普通渠道只允许选择其当前支持的具体模型。
- 通配模型渠道允许手工输入具体模型，但后端仍须用现有模型匹配规则验证。
- 不可自动探测的模型在选择器中禁用并说明原因；非流式模型可以选择，但只承诺总响应耗时。

默认关闭“计入智能调度样本”。配置区必须明确提示：探测会真实请求上游、产生上游费用和本地测试消费记录；计入样本还可能影响智能调度的稳定性保护和路由决策。

配置保存使用修订号乐观锁。旧页面提交返回 HTTP `409`，前端刷新后要求重新编辑，不能静默覆盖新配置。

## 5. 状态定义

每个模型保存自己的最新状态，渠道卡片取最需要关注的状态。

| 状态 | 判定 | 颜色语义 |
| --- | --- | --- |
| 未配置 | 没有配置或没有模型 | 中性灰 |
| 已暂停 | 配置关闭，或周期任务因渠道手动禁用、自动禁用而暂停 | 中性灰 |
| 待检测 | 已启用但尚无完成记录 | 蓝/中性 |
| 正常 | 所有已配置模型都有新鲜的最近成功记录 | 绿色 |
| 部分异常 | 成功与失败混合，或存在配置/本地执行错误 | 黄色 |
| 异常 | 所有具有有效上游结果的已配置模型最近均失败 | 红色 |
| 已限流 | 最近上游结果为 429 | 黄色 |
| 已过期 | 超过两个目标间隔仍没有新的有效结果，且当前未执行 | 黄色 |

“检测中”是叠加状态，不覆盖上一轮健康结论。这样轮次执行期间不会把所有卡片短暂改成未知。

结果分类必须区分：

- `success`：请求成功且响应验证通过。
- `upstream_failure`：请求已经发出，但上游返回错误、无效响应或超时；影响渠道健康。
- `rate_limited`：上游 429；影响可用性，但不写稳定性样本。
- `local_failure`：请求发出前的本地配置、配额或构造错误；显示部分异常，不判定渠道宕机。
- `skipped`：渠道并发已满、配置已变化等；不判定失败。
- `canceled`：服务停止、租约丢失或管理员暂停后取消；不判定失败。

只有启用的渠道会按配置周期探测；手动禁用和自动禁用渠道都跳过周期探测。管理员显式点击“立即检测”时仍允许检测已暂停、手动禁用或自动禁用渠道，但绝不改变渠道启用状态。

## 6. 排序

首次进入或没有已保存偏好时，默认按**成本倍率从低到高**排序。没有成本倍率的渠道排在有效值之后；倍率相同再按渠道中文名称和 ID 稳定排序。用户主动选择其他排序后继续保存其偏好。

支持以下排序：

- 成本倍率从低到高/从高到低。
- 当前窗口平均首字从低到高/从高到低。
- 当前窗口平均 TPS 从高到低/从低到高。

排序在前端完成，数据缺失项始终排在有效值之后，再以渠道中文名称和 ID 稳定兜底。选择保存在独立的 `localStorage` key 中，不复用现有“渠道”Tab 的排序状态；新 key 没有值、值无效或保存的是已移除排序项时回退到 `ratio_asc`。

## 7. 后端架构

不为每秒扫描创建系统任务记录。新增专用状态探测 Worker，在主节点启动；数据库租约保证误配为多个主节点时同一渠道仍只会被一个实例领取。

```mermaid
flowchart LR
    A["状态探测 Worker<br/>每秒扫描到期配置"] --> B["数据库 CAS 领取<br/>渠道级租约"]
    B --> C["全局渠道 Worker 池<br/>默认并发 5"]
    C --> D["获取渠道并发租约"]
    D --> E["按渠道串行测试模型"]
    E --> F["复用 testChannel<br/>真实上游与计费"]
    F --> G["写执行记录与最新状态"]
    G --> H{"记录调度样本?"}
    H -- "否" --> I["完成并计算下次时间"]
    H -- "是" --> J["写 status_probe 共享样本"]
    J --> I
```

### 7.1 调度策略

- 扫描周期：1 秒。
- 渠道间并行：默认 5，建议由环境变量 `CHANNEL_STATUS_PROBE_CONCURRENCY` 配置，限制 `1..20`。
- 同一渠道的模型串行执行，避免一次轮次同时占满该渠道的多个并发槽。
- 每个模型执行前获取现有渠道并发租约；租约拿不到时记录 `skipped/channel_busy`，不把渠道判为失败。
- 使用固定延迟：一轮完成后再以 `完成时间 + interval_seconds` 计算下次时间，不补跑停机期间错过的轮次，也不允许同一渠道重叠执行。
- 新配置首次执行时间为 `保存时间 + interval_seconds`；“立即检测”走独立手动请求，不偷偷改变计划时间。
- 批量同时配置时给首次时间加入最多 10 秒的确定性抖动，避免整点请求洪峰。
- 全局通用的 `common.RequestInterval` 仍须生效。

### 7.2 租约与故障恢复

- 领取配置时用带 `revision`、`next_run_at` 和过期租约条件的 GORM `UPDATE`，以 `RowsAffected == 1` 作为领取成功依据；不写数据库方言专用 SQL。
- 每个领取生成随机 `lease_token`，运行中定期续租。续租失败时取消上下文，停止发出后续模型请求。
- 完成更新必须携带 `lease_token + captured_revision`。管理员在执行期间修改配置后，旧执行不能覆盖新配置的下次时间。
- 进程崩溃后租约过期，其他实例可以重新领取；不进行密集追赶。
- 每次轮次生成 `run_id`，执行记录以 `(run_id, model_name)` 唯一，重试保存不会产生重复历史。

### 7.3 立即检测

立即检测不是同步 HTTP 长请求：

1. API 在配置行写入 `manual_request_id` 和 `manual_requested_at`。
2. Worker 优先领取手动请求。
3. 一个渠道已有待执行或运行中的手动请求时返回 HTTP `409`。
4. 手动轮次使用已保存的全部模型，`trigger=manual`，不移动原计划的 `next_run_at`。
5. 响应为 HTTP `202`，前端轮询状态并定位到对应 `run_id`。

## 8. 数据模型

为避免 MySQL 5.7 不支持窗口函数导致“每组最新一条”查询复杂化，使用“配置 + 最新状态 + 追加历史”三张表。模型列表使用 TEXT JSON，并通过 `common.Marshal` / `common.UnmarshalJsonStr` 读写。

### 8.1 `ChannelStatusProbeConfig`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int64 | 主键，由 GORM 生成 |
| `channel_id` | int，唯一索引 | 渠道 |
| `enabled` | bool | 是否周期执行，默认由代码设为 false |
| `models_json` | TEXT | 精确模型字符串数组 |
| `interval_seconds` | int | `30..86400` |
| `display_value` | int | 状态条和平均指标的桶数量 |
| `display_unit` | varchar(16) | `minute` / `hour` / `day`，与数值组合后最多 30 天，默认 `60 minute` |
| `record_sample` | bool | 是否尝试写智能调度样本 |
| `next_run_at` | int64，索引 | 下次目标时间，Unix 秒 |
| `manual_request_id` | varchar(64) | 待执行的手动请求 |
| `manual_requested_at` | int64，索引 | 手动请求时间 |
| `revision` | int64 | 乐观锁版本 |
| `lease_token` | varchar(64) | 当前执行租约 |
| `lease_until` | int64，索引 | 租约到期时间 |
| `created_at` / `updated_at` | int64 | 审计时间 |

不使用 GORM `default:true` 一类布尔默认标签，默认值全部在创建和归一化代码中设置。

### 8.2 `ChannelStatusProbeState`

每个 `(channel_id, model_name)` 一行，唯一索引，保存卡片所需最新快照：

- 最近执行 ID、run ID、开始和完成时间。
- 最近结果分类、是否成功、是否真正发出上游请求。
- 最近总响应、首字、TPS，均使用 `*float64` 表达不可用。
- 最近错误码、脱敏错误摘要。
- 连续成功/失败数。
- 最近样本写入状态和原因。
- 最近触发方式、端点和是否流式。
- `minute_buckets_json`：最近 60 个分钟桶。
- `hour_buckets_json`：最近 24 个小时桶。
- `day_buckets_json`：最近 30 个北京时间自然日桶。

三种桶都保存各结果分类计数、涉及模型，以及成功执行的首字/TPS总和与有效样本数。每次执行同时更新三种粒度，因此状态条和平均指标读取同一份滚动汇总，不依赖执行历史保留天数，也不需要总览轮询扫描原始执行表。

更新状态时在 model 层使用 `lockForUpdate(tx)` 读取当前行，仅允许更晚的完成时间或同时间更大 ID 覆盖，防止慢请求晚到后覆盖新结果。SQLite 自动跳过不支持的 `FOR UPDATE`。

状态总览直接批量读取这三层滚动桶并在 Go 中按渠道合并，不在每次前端轮询时扫描原始执行历史。三层桶都使用 TEXT JSON，并通过 `common.Marshal` / `common.UnmarshalJsonStr` 读写。

### 8.3 `ChannelStatusProbeExecution`

每个模型每次执行一行：

- `id`、`run_id`、`channel_id`、`model_name`、`config_revision`。
- `trigger`：`scheduled` 或 `manual`。
- `result`：`success`、`upstream_failure`、`rate_limited`、`local_failure`、`skipped`、`canceled`。
- `started_at`、`finished_at`、`response_time_ms`、`first_token_ms`、`tps`。
- resolved endpoint、stream、request ID。
- Usage：输入、输出、总量、缓存读写、推理 Token，并保存 `usage_available`。
- `error_code`、截断后的 `error_message`。
- `sample_requested`、`sample_status`、`sample_message`。

索引至少包含：

- 唯一索引 `(run_id, model_name)`。
- 分页索引 `(channel_id, finished_at)`。
- 模型筛选索引 `(channel_id, model_name, finished_at)`。
- 清理索引 `finished_at`。

执行记录默认保留 7 天，范围 `1..90` 天，增加 `status_probe_history_retention_days` 渠道监控设置。清理复用现有渠道监控保留任务，但在新 model 文件中实现独立批量删除函数。配置和最新状态不受历史保留期影响。

## 9. API 设计

所有接口继续位于 `/api/channel_monitor` 并使用 `RootAuth`。

### 9.1 状态总览

```http
GET /api/channel_monitor/status?model=
```

返回：

- 服务器当前时间和 Worker 扫描精度。
- 各状态汇总。
- 全部分组、全部已配置探测模型，以及按分组聚合的完整 `models_by_group` 映射；即使请求带具体 `model`，该映射也不能随卡片结果过滤而收缩。
- 全部渠道的基础信息、成本倍率、支持模型、配置、运行态和最近完成状态。
- 每个已配置模型按渠道 `display_value + display_unit` 返回对应数量的分钟、小时或天桶，以及最近结果和完成时间。
- 每渠道和每模型在当前配置窗口内成功探测的平均首字、平均 TPS，以及渠道最近一次首字和 TPS。

`model` 为空时服务端返回渠道全部已配置模型的独立状态条；传入精确模型时只返回该模型的卡片状态和配置窗口结果。状态桶和平均指标直接从每模型的三层滚动汇总中截取并在 Go 内存组装，不产生逐渠道、逐模型查询，也不扫描原始执行历史。该接口不返回原始历史明细。

### 9.2 保存配置

```http
PUT /api/channel_monitor/status/channel/:id/config
Content-Type: application/json

{
  "enabled": true,
  "models": ["gpt-4.1", "gpt-4.1-mini"],
  "interval_seconds": 300,
  "display_value": 60,
  "display_unit": "minute",
  "record_sample": false,
  "revision": 3
}
```

- 新配置 `revision=0`。
- 服务端重新验证渠道、模型、范围和样本间隔。
- 成功后返回归一化配置和新修订号。
- 修订冲突返回 `409`。
- 禁用只停止后续轮次；已经发出的当前请求允许完成，后续模型在修订检查后停止。

### 9.3 立即检测

```http
POST /api/channel_monitor/status/channel/:id/run
```

返回 HTTP `202`、`manual_request_id`。没有已保存模型时返回 `400`；已有待执行或运行手动任务时返回 `409`。

### 9.4 执行记录

```http
GET /api/channel_monitor/status/channel/:id/executions?page=1&page_size=20&model=&result=&trigger=
```

分页返回总数和记录列表。`page_size` 限制 `1..100`，所有筛选使用白名单枚举。

## 10. 智能调度样本集成

新增样本来源：

```go
ChannelSmartScheduleSampleSourceStatusProbe = "status_probe"
```

写入条件与现有手动探测保持一致：

1. 渠道配置开启 `record_sample`。
2. 全局智能调度已开启。
3. 能确定原始和匹配后的模型。
4. 至少存在一条已配置、启用、参与调度且命中策略模型范围的路由。
5. 请求确实已经发往上游。
6. 结果不是 429。

写入粒度仍是 `(渠道, 模型)`，不会因为一个渠道属于多个分组而复制样本。`SampleId` 使用 `execution ID/run ID + model`，保证崩溃重试幂等。

样本开关的真实含义必须在 UI 中写清：

- 成功和失败都会进入共享样本。
- 失败可能参与稳定性降级，成功可能参与试放恢复。
- 429 只进入状态历史并触发现有业务流量冷却，不进入稳定性样本；状态探测自身不受该冷却阻断，后续到期或手动探测仍会请求上游。
- 自动禁用或未参与调度的渠道模型会显示“请求已执行，但不符合样本条件”。

状态执行先持久化，再尝试写样本。执行表记录 `sample_status=pending/recorded/skipped/failed`；短暂数据库错误由 Worker 对近期 `pending` 记录重试，不能为了补样本重新请求上游。

现有智能调度定时探测已经读取共享样本 `LastTime`，因此近期状态样本会自动抑制同一 `(渠道, 模型)` 的重复调度探测。现有全局自动渠道测试具有自动启停语义，不做去重；配置页面应提示两套周期任务可能同时产生请求。

## 11. 流量、计费与安全

- 状态探测调用真实上游，完整走现有测试定价、配额、饱和转换、消费日志和渠道日成本链路。
- 每次探测只保留现有测试请求的小输出上限，不能为了 TPS 生成长文本。
- 不调用 `processChannelError`、`EnableChannel` 或自动禁用逻辑。
- 不更新渠道管理页的 `test_time/response_time`，状态探测使用自己的状态表，避免两个功能互相覆盖“最后测试”。
- 获取不到渠道并发租约时跳过，不抢占超额生产容量。
- 错误消息进入数据库前复用现有脱敏规则，再限制长度；API 不返回堆栈、密钥或上游认证信息。
- API 只允许 Root；前后端都校验配置，不能信任前端模型列表。
- 配置页实时显示预计请求量：`模型数 × 3600 / 间隔秒数`。超过每分钟 60 次时给出明显负载警告，但最终吞吐仍由 Worker 和渠道并发租约限制。

## 12. 前端数据策略

- 新建包含模型筛选的独立 React Query key，例如 `['channel-monitor', 'status-probe', { model }]`；切换模型时重新读取服务端已按配置窗口聚合的结果，并在请求期间保留上一份总览作为占位数据，避免分组和模型选择被加载态重置。分组选择本地过滤渠道，模型候选从服务端完整 `models_by_group` 映射派生。
- 仅当 `view === 'status'` 时启用查询。
- Tab 可见且存在运行中任务时每 3 秒刷新；否则每 15 秒刷新；页面不可见时停止轮询。
- 配置、暂停、恢复或立即检测后只失效状态查询，不刷新成本、成功率和智能调度大查询。
- 卡片排序和筛选使用 `useMemo`，不要在渲染路径反复构造大映射。
- 执行记录按打开的渠道单独查询，关闭抽屉后不轮询。
- 配置表单使用 React Hook Form + Zod；响应数据保留明确 TypeScript 类型，禁止 `any`。
- 所有下游文案使用简体中文常量，不调用 `t()`，不改 locale 文件。

## 13. 建议代码位置

优先新增下游文件，把对现有文件的修改控制在注册点和路由点。

### 13.1 新文件

```text
model/channel_status_probe.go
model/channel_status_probe_execution.go
controller/channel_status_probe.go
controller/channel_status_probe_worker.go
controller/channel_status_probe_sample.go
web/src/features/channel-monitor/components/channel-monitor-status-view.tsx
web/src/features/channel-monitor/components/channel-monitor-status-card.tsx
web/src/features/channel-monitor/components/channel-monitor-status-config-sheet.tsx
web/src/features/channel-monitor/components/channel-monitor-status-history-sheet.tsx
web/src/features/channel-monitor/lib/status-sort.ts
web/src/features/channel-monitor/lib/status-schema.ts
```

测试放到各模块现有的专属 `__tests__/` 目录。

### 13.2 必须小范围修改的现有文件

- `router/channel-monitor-router.go`：注册四个 Root API。
- `model/main.go`：普通和快速迁移列表加入三张表。这是必要的上游拥有文件改动，应只增加对应条目。
- `controller/system_task_handlers.go`：启动专用 Worker，不把秒级扫描注册成系统任务。
- `model/channel_smart_schedule_sample.go`：增加 `status_probe` 来源白名单。
- 渠道删除清理路径：删除配置、状态和历史。
- 渠道监控保留任务：调用状态历史清理函数。
- `web/src/features/channel-monitor/index.tsx`：增加 Tab、按需查询和懒加载视图。
- `web/src/features/channel-monitor/api.ts`、`types.ts`：增加 API 和类型。

当前渠道监控主体属于下游新增路径；除迁移注册等必要入口外，不应改动上游通用渠道页面或全局调度框架。

## 14. 测试计划

### 14.1 后端

- 配置验证：区间边界、重复模型、空模型、通配匹配、模型移除、样本开关最小间隔。
- 可配置窗口结果：分钟/小时/天各自边界、北京时间自然日、同一桶多结果计数、严重度合并、空桶和 30 天滚动清理。
- 乐观锁：两个旧修订提交只有一个成功，另一个返回 `409`。
- 领取并发：同一到期配置只能被一个 Worker 领取；租约过期可恢复；旧 revision 完成不能覆盖新配置。
- 调度语义：固定延迟、不补跑、不重叠、手动请求不移动计划时间。
- 渠道状态：手动禁用和自动禁用都跳过周期任务，管理员立即检测仍可执行，任何结果都不改渠道启用状态。
- 并发限制：租约满记录 skipped，不记录失败样本。
- 结果分类：成功、上游失败、429、本地失败、跳过、取消。
- 指标契约：显式区分 nil、0 和有效首字/TPS/响应；窗口平均值只纳入成功且对应指标有效的执行，最近一次指标继续独立展示；保存 Usage。
- 样本：开关关闭不写；开启且符合条件写 `status_probe`；多分组只写一次；429 不写；SampleId 重试幂等。
- 计费：一次状态探测只产生一份测试消费和一份成本，不进入普通业务分钟性能统计。
- 历史：分页、筛选、保留清理、渠道删除清理。
- 数据库：全部查询使用 GORM 或已有跨库 helper，SQLite/MySQL/PostgreSQL 均可迁移。

新增或大幅改写的 Go 测试使用 `require` 做前置和致命断言，使用 `assert` 做值比较。

### 14.2 前端

- 全部渠道都有卡片，未配置渠道也可见。
- 未选择分组时模型筛选禁用；选择分组后只出现该分组下已配置的探测模型，切换分组会清空模型选择。
- 具体模型筛选后，状态条、平均指标和排序都只使用该模型。
- 首次进入默认按成本倍率从低到高排序；无倍率排最后，用户改选后能恢复偏好。
- 成本倍率、平均首字和平均 TPS 排序稳定，缺失值排最后。
- 卡片为每个配置模型分别展示配置窗口数量的分钟/小时/天桶，能表达空桶、多结果聚合和模型筛选，Tooltip 数量与服务端一致。
- 卡片展示渠道备注、最近一次首字/TPS和当前窗口平均首字/TPS，不展示总响应。
- 长渠道名、长模型名、无指标、多模型和空状态不破坏布局。
- 卡片主体支持鼠标和键盘打开记录，菜单操作不误触卡片。
- 配置表单边界、预计请求量、revision 冲突和服务端错误。
- 历史抽屉分页、筛选、加载、空、错误和运行中状态。
- 手机宽度下五个 Tab 可访问且不重叠；卡片、抽屉和按钮文字不溢出。
- 轮询只在 Tab 可见时发生，状态变化不会导致无关查询刷新。

### 14.3 建议验证命令

```powershell
go test ./controller ./model ./service
Set-Location web
bun test src/features/channel-monitor
bun run typecheck
bun run lint
bun run build
```

若后续实现触及 `relaykit/`，还必须执行：

```powershell
Set-Location relaykit
$env:GOWORK='off'
go build ./...
```

本设计本身不需要修改 `relaykit/`。

## 15. 分阶段实施

### 阶段一：数据与执行闭环

- 三张表、迁移、配置 API、Worker、真实测试、最新状态、历史、清理。
- 先把“不会重复、不会越过并发、不会改渠道状态、会正确计费”做扎实。

### 阶段二：状态 Tab

- 卡片、筛选、排序、配置 Sheet、历史 Sheet、立即检测和响应式布局。
- 与现有刷新按钮和 React Query 缓存联动。

### 阶段三：智能调度样本

- 增加 `status_probe` 来源、幂等写入、样本结果展示和 pending 重试。
- 回归现有定时探测、手动测试、稳定性降级与恢复。

### 阶段四：运维增强

- 批量配置或复制配置。
- 连续失败与恢复通知。
- 24 小时小趋势和成功率。
- CSV 导出和更长周期聚合。

## 16. 额外值得做的能力

以下能力有价值，但不应阻塞首版：

1. **批量配置**：大量渠道逐个配置成本很高。建议支持选择渠道后批量修改间隔和样本开关；模型只能取各渠道支持模型的交集，避免静默写入无效模型。
2. **失败/恢复通知**：只在连续失败达到阈值或从异常恢复时通知，并设置冷却，避免每次探测都发消息。
3. **性能趋势小图**：首版已有可配置窗口的结果状态带；后续可增加首字和 TPS 趋势，比单一平均值更容易识别抖动，但不应挤压卡片主体。
4. **负载预算**：全局显示计划请求/分钟、实际请求/分钟、跳过数和队列延迟，帮助判断 Worker 并发是否不足。
5. **配置漂移提示**：渠道模型列表变化后，把失效模型标为“配置已失效”，不静默删除，管理员确认后再修正。
6. **状态告警静默**：维护窗口内暂停通知但继续采样，适合计划维护。

优先级建议为：批量配置 > 失败/恢复通知 > 负载预算 > 趋势图 > 导出。

## 17. 验收标准

- 每个现有渠道在“状态探测”中恰好出现一张卡片。
- 单渠道能配置多个模型、`30..86400` 秒间隔和样本开关；重启后配置与下次计划不丢失。
- 同一渠道不会并发执行两个轮次，多实例不会重复领取同一轮次。
- 单渠道可配置 `1..60` 分钟、`1..24` 小时或 `1..30` 天展示范围，默认 `60 分钟`；状态条和平均指标始终使用同一窗口。
- 卡片展示渠道备注、最近一次首字/TPS和窗口平均首字/TPS，不展示总响应；无有效指标时显示 `-`。
- 每个配置模型都有独立的配置窗口状态条，结果按所选单位聚合并随服务器时间滚动；同一桶内多次执行可从 Tooltip 还原分类数量。
- 记录抽屉能分页还原每次执行、错误、Usage 和样本决策。
- 未配置、暂停、待检测、正常、部分异常、异常、限流、过期均有明确且可访问的表现。
- 排序、筛选和搜索可组合；分组与模型使用两个级联筛选框，必须先选分组，模型只取该分组下已配置的探测模型，切换分组清空模型；搜索仅命中渠道名称、备注和 ID，样本设置仅以标签展示且不提供筛选；默认成本倍率从低到高，刷新后保留用户修改后的排序偏好。
- 关闭样本开关绝不写智能调度样本；开启后只在符合条件时写一次共享样本。
- 状态探测不会自动启停渠道，也不会绕过渠道并发上限。
- 高频扫描不生成大量系统任务历史；历史保留和渠道删除清理有效。
- 前端在 360px 手机和常见桌面宽度下无重叠、溢出或不可操作控件。

## 18. 最终建议

建议按本文方案实现“状态探测”，而不是在现有“渠道”表格上继续堆列，也不要把它塞进现有全局自动渠道测试。三者职责应保持清楚：

- **渠道 Tab**：配置、成本、窗口性能和调度关系总览。
- **状态监测 Tab**：每渠道多模型、按秒执行、逐模型可配置窗口状态、最近指标、窗口平均指标和历史。
- **全局自动渠道测试**：自动禁用与恢复。
- **智能调度探测**：为参与调度的路由补充共享样本。

这样首版已经具备完整的运维闭环，同时给批量配置、告警和趋势分析留下稳定扩展点。
