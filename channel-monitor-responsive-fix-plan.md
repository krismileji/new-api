# 渠道监控移动端响应式修复计划

## 1. 背景与当前问题

渠道监控页面的部分子组件已经使用 Tailwind 响应式断点，但完整页面在手机宽度下无法正常使用，因此目前不能认定为已支持响应式布局。

在已部署页面上使用 `360 × 800` 视口复现得到：

- 页面标题区域只剩约 `52px` 宽。
- 实时状态内容被压缩后纵向换行，高度约 `1092px`。
- 整个页头高度约 `1114px`。
- 主内容区只剩约 `16px` 高，并被推到视口下方。
- 页面没有全局横向溢出，但这是因为宽表格被内部滚动容器裁住；真正阻塞手机使用的是页头布局坍缩。

主要原因：

1. `ChannelMonitorRealtimeStatus` 被直接放入 `SectionPageLayout.Title`。
2. 页头右侧同时存在 7 个固定尺寸操作按钮，手机端合计占用约 `272px`。
3. 通用页头仍尝试让标题和操作区位于同一行，标题区域因此被压缩到极窄宽度。
4. 实时状态包含多个徽标和一段较长的诊断信息，在极窄标题区域内连续换行，最终占满整个页面高度。
5. `Main` 使用 `overflow-hidden`，超高页头进一步挤压了可滚动内容区域。

## 2. 修复目标

- 渠道监控页面在 `360px`、`390px`、`430px`、`768px` 和桌面宽度下均可正常浏览和操作。
- 手机端标题、实时状态、操作工具栏必须分区展示，不互相挤压。
- 手机端首屏能够看到统计卡片或主要内容，不能被页头占满。
- 保持桌面端现有信息密度和操作入口，不改变渠道监控业务逻辑。
- 不引入页面级横向滚动；宽表格继续在自身容器内横向滚动。
- 不修改后端接口、数据结构和计费逻辑。

## 3. 约束与变更原则

- 开始实现前比较相关文件与 `upstream/main`，确认文件归属和上游差异。
- 优先采用渠道监控功能内的局部修复，避免影响其他使用 `SectionPageLayout` 的页面。
- 如果必须扩展 `SectionPageLayout`，只新增默认关闭的可选布局能力，保持所有现有调用行为不变。
- 保留当前工作区中与本任务无关的未提交修改，不做重排、重命名或顺手重构。
- 本功能属于下游功能时继续使用简体中文，不新增或修改 locale 文件。

## 4. 推荐实现方案

### 4.1 拆分页头内容

- `SectionPageLayout.Title` 中只保留“渠道监控”标题。
- 将 `ChannelMonitorRealtimeStatus` 从 `h2` 标题区域移出，放到独立的状态区域。
- 状态区域应位于标题下方或主内容顶部，并拥有完整可用宽度。
- 保持标题的语义结构，避免大量运行状态文本被包含在页面主标题中。

### 4.2 手机端操作工具栏独占一行

- 手机端标题占满第一行。
- 7 个操作按钮放到第二行，允许换行，但优先保持单行排列。
- 桌面端仍保持标题在左、操作按钮在右的现有布局。
- 若通用布局无法局部控制，可为 `SectionPageLayout` 增加类似 `stackHeaderOnMobile` 的可选能力；默认值必须保持原行为。

### 4.3 压缩实时状态的手机展示

- 手机端默认只展示关键状态，例如：
  - Redis 是否可用。
  - 事件处理是否运行。
  - 是否存在降级、积压或失败告警。
- 队列深度、时间戳、重试次数等完整诊断信息放入可展开区域、Popover、Sheet 或“查看详情”入口。
- 桌面端可以继续展示较完整的状态摘要，但长诊断文本不得进入页面标题。
- 告警状态仍需直接可见，不能因为折叠而隐藏严重故障。

### 4.4 保持各业务视图可用

- 统计卡片在手机端保持单列。
- 页签导航继续使用局部横向滚动，并确保当前页签可见。
- 筛选器和选择器在手机端保持全宽纵向排列。
- 渠道、分组、模型性能宽表格继续使用表格容器内部横向滚动。
- 检查智能调度、状态监测、模型检测三个卡片视图，确保没有固定宽度元素撑破容器。
- 检查设置、历史记录和详情 Sheet/Dialog 在 `360px` 下的宽度、滚动和底部操作区。

## 5. 预计涉及文件

优先修改：

- `web/src/features/channel-monitor/index.tsx`
- `web/src/features/channel-monitor/components/channel-monitor-realtime-status.tsx`

仅在局部方案无法实现时修改：

- `web/src/components/layout/components/section-page-layout.tsx`

计划新增或更新测试：

- `web/src/features/channel-monitor/components/__tests__/realtime-status.test.tsx`
- `web/src/features/channel-monitor/components/__tests__/page-responsive-layout.test.tsx`
- 如扩展通用页头，在对应 layout 模块的 `__tests__/` 中增加独立回归测试。

## 6. 测试计划

### 6.1 组件与结构回归测试

- 断言实时状态不再位于 `h2` 内。
- 断言手机端标题和操作工具栏使用独立布局区域。
- 断言手机端只直接展示关键状态，完整诊断信息仍可访问。
- 断言存在严重告警时，告警在折叠状态下仍然可见。
- 断言桌面模式仍保留全部原有操作按钮和状态信息。

### 6.2 真实视口验证

分别验证以下视口：

- `360 × 800`
- `390 × 844`
- `430 × 932`
- `768 × 1024`
- `1440 × 900`

每个视口检查：

- 页面标题没有被异常压缩或截断。
- 页头没有占满整个视口。
- 主内容首屏可见且可滚动。
- 操作按钮全部可点击，没有被遮挡。
- 页面文档宽度不超过视口宽度。
- 页签和宽表格只在各自容器内部横向滚动。
- 弹窗和侧边 Sheet 不产生页面级横向溢出。

### 6.3 工程检查

从 `web/` 执行：

```bash
bun run test -- <受影响测试文件>
bun run typecheck
bun run lint
bun run build
```

完成前执行：

```bash
git diff --stat
git diff --check
```

## 7. 验收标准

- `360px` 宽度下，标题容器应接近内容区完整宽度，不再缩小到几十像素。
- `360 × 800` 下页头高度应控制在合理范围，主内容必须在首屏可见。
- 页面根节点不存在横向滚动条。
- 实时状态的关键健康信息在手机端无需横向滚动即可读取。
- 7 个页头操作入口在手机端全部可访问。
- 六个渠道监控页签均完成手机、平板和桌面验证。
- 新增回归测试能够在重新把实时状态塞入标题或让操作区挤压标题时失败。
- 受影响测试、类型检查、Lint 和生产构建全部通过。

## 8. 风险与回退

- 修改通用 `SectionPageLayout` 可能影响其他后台页面，因此必须优先采用功能内修复；若确需修改，使用默认关闭的可选行为并检查所有调用点。
- 折叠实时状态可能降低桌面信息密度，需要保证严重告警始终直接可见。
- 表格改成移动卡片会扩大改动范围，本轮先保留表格内部横向滚动，后续再单独评估移动卡片视图。
- 如果新布局引入回归，可回退到仅移动实时状态、保持其他布局不变的最小方案。

## 9. 实施顺序

1. 比较 `upstream/main` 并确认相关文件归属。
2. 新增能够保护页头结构和手机布局契约的失败测试。
3. 将实时状态移出标题区域。
4. 调整手机端标题和操作工具栏排列。
5. 实现实时状态的手机精简展示与完整详情入口。
6. 逐一验证六个页签以及相关 Dialog/Sheet。
7. 运行测试、类型检查、Lint、构建和 Git 差异检查。
