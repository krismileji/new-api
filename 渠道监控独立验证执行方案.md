# 渠道监控独立验证执行方案

## 1. 目的

将《渠道监控并发审查报告》拆成互不依赖、可单独执行和判定的验证项。每个验证项都包含固定输入、执行步骤、观测指标和通过标准，避免必须先完成整个模块审查或依赖其他用例结果。

本方案默认只读验证，不修改生产代码。若某项需要修复后回归，使用独立测试分支或独立数据库/Redis 实例。

## 2. 统一执行规则

### 2.1 环境隔离

- 每个用例使用独立 SQLite 文件或独立 MySQL/PostgreSQL schema。
- Redis 用独立 database、key prefix 或独立实例，避免残留 projection、Stream、consumer group 和 lease。
- 固定 `T0`、事件 ID、request ID、channel ID、API Key ID 和模型名；禁止随机数据。
- 不使用 `sleep`、随机 fuzz、时延比较或日志断言作为通过依据。
- 业务数据、最终账本、事件条数、Redis lag/PEL 和数据库查询计数必须通过结构化结果读取。

### 2.2 规模参数

| 参数 | 含义 | 基线 | 压力档 |
|---|---|---:|---:|
| `C` | 渠道数 | 10 | 100 |
| `M` | 分钟窗口 | 15 | 1440 |
| `K` | 每渠道 API Key/高基数维度 | 10 | 100 |
| `N` | 单批事件数 | 100 | 100 |
| `Q` | 并发调用数 | 1 | 50 |

### 2.3 统一通过标准

- 结果数、金额、quota、计数与确定性预期完全一致。
- 重放不产生重复扣费、重复累加或负余额。
- 旧版本修正不能覆盖新版本。
- Redis 入队失败必须可观测，并且事件可重放或明确进入死信。
- 用例只验证自身合同；发现跨边界问题时单独记录，不把其他用例结论作为前置条件。

### 2.4 本机环境与数据保留规则

- 主验证环境：本机可达的 MySQL 8.4.10（本地 3306，数据库 `new-api`）和 Redis 8.8.0（本地 6379）；两者由 Docker Desktop 提供服务，但当前 PowerShell 无 Docker CLI。
- MySQL 测试 DSN 在执行时从本机既有运行配置（当前为工作区 `.env` 的 `SQL_DSN`；若容器 CLI 可用可再核对）读取，密码不得输出或写入文件。
- Redis 验证使用独立 DB 15 或带 `verify_<run-id>_` 前缀的 key；禁止 `FLUSHALL`、`FLUSHDB` 和删除非本次验证数据。
- 本次验证创建的表、行、Redis key、Stream entry 和 consumer group 默认保留，便于复核。需要清理时必须单独列出精确目标并再次确认。
- 未配置 PostgreSQL 时继续完成 MySQL/Redis 主线，PostgreSQL 和 SQLite 仅在本机可用时补充执行。

### 2.5 调度和动态更新规则

- 所有验证项按 V01→V18 逐项串行执行；主会话负责调度、去重、归档结果，子代理只执行当前项。
- 每项完成后先记录结构化结果，再决定下一项；不能把前一项的临时数据作为后一项的隐含前置条件。
- 若发现方案未覆盖的新问题，主会话必须先更新《渠道监控并发审查报告》和本执行方案，新增验证项或修订通过标准，然后重新执行受影响项。
- 验证阶段不自动修改生产代码；只有用户明确授权后，才将失败项拆成修复补丁并执行回归。

## 3. 独立验证项

### V01：日成本 SQL 分页与聚合

**目标**：确认 `page/page_size` 是否真正下推数据库，以及 Go 是否全量加载后聚合。

**范围**：`model/channel_daily_cost.go`、`controller/channel_monitor_cost.go`。

**固定输入**：`C=100`、`D=90`，每个渠道每天一条成本记录；分别请求 `page_size=10` 和 `page_size=100`。

**执行步骤**：

1. 预置 9000 条日成本数据。
2. 开启 GORM SQL 计数器或测试级 DB callback。
3. 调用日期分页接口两次，只改变 `page_size`。
4. 记录返回行数、数据库扫描/返回行数、Go 处理行数和内存峰值。

**通过标准**：数据库返回量随 `page_size` 降低；端点总路径不能对同一全范围重复全量读取，最终应使用 SQL 聚合/分页或等价的单次读取。页内日期顺序稳定，跨页无重复或遗漏。

**失败证据**：`page_size` 变化不影响全量 `Find` 行数，或响应内存随 9000 条数据增长。

**首次实测（2026-08-28，commit `779ea5bc5`）**：MySQL 8.4.10，本地 `new-api` 数据库；固定 `C=100`、`D=90`，保留验证表 `verify_v01_20260828_01_channel_daily_costs` 共 9000 行。`page_size=10` 返回 10 天但基础查询读取 1000 行，`page_size=100` 返回 90 天并读取 9000 行；两次 SQL 均无 `LIMIT`/`GROUP BY`。控制器端点还会重复读取全范围，page=10 总扫描约 19000 行，page=100 约 27000 行。日期顺序和跨页完整性通过，但总体判定失败。

### V02：API Key 成本高基数与归属

**目标**：确认 API Key 成本是否存在无界加载、N+1、归属覆盖和名称拆分问题。

**范围**：`model/channel_daily_api_key_cost.go`、`controller/channel_monitor_cost.go`。

**固定输入**：`C=100`、`K=100`、`D=90`；设置同一入站 Token 对应两个 fingerprint，并在中途修改 display/name。

**执行步骤**：

1. 预置 `C×K×D` 条 API Key 日成本。
2. 请求 API Key 详情和 owner 信息。
3. 统计 SQL 查询次数、`IN` 参数规模、响应体大小和最终分组键。
4. 校验同一 Token 多 fingerprint、改名跨日和空白名称场景。

**通过标准**：owner 查询为批量查询而非逐行 N+1；金额总和不变；展示归属按稳定身份可解释；空白名称按产品规则回填或明确显示未知。

**首次实测（2026-08-28，MySQL 8.4.10）**：固定 `C=100`、`K=100`、`D=90`，保留 `verify_v02_18cffcf62396397c_` 前缀验证表，共 900,000 条 API Key 日成本和 9,000 条日成本记录。owner 查询为批量 `IN(100)`，未发现 N+1；overview 返回约 1,390,347 字节，模型 totals 原始聚合行 10,100，查询无 `LIMIT`。金额守恒通过，但高基数全量加载失败；同一 Token 改名跨日后展示被旧名称覆盖，归属稳定性失败；真正空白名称的“未命名”回填待补测。

### V03：成本接口端到端查询计数

**目标**：确认摘要和详情模式的 DB/Redis 查询数量及重复读取。

**范围**：`controller/channel_monitor_cost.go`、`controller/channel_monitor_realtime_cost.go`。

**执行步骤**：

1. 在快照命中、快照绕过、Redis status cache 命中三种状态下分别请求摘要和详情。
2. 分别记录 DB 查询数、Redis Pipeline 次数、`HGETALL` 数量和 owner 查询数。
3. 对详情请求检查 rows、chart、page 是否读取重叠范围。

**通过标准**：摘要路径复用基础数据；详情路径查询数量和重复范围与设计文档一致。修复后，重叠范围应只读取一次。

**首次实测（2026-08-28，MySQL 8.4.10 + Redis 8.8.0）**：独立 schema `verify_v03_20260828_07`，Redis DB15，固定 `C=3`、`K=3`、`D=3`。摘要快照绕过路径为 1 次 DB、2 个 Pipeline、1337 个 `HGETALL`；详情为 11 次 DB（含 tokens/users 各一次批量 `IN`）、2 个 Pipeline，rows/chart/page 对相同 3 日范围各读取 9 行，重复 3 次。快照命中时 DB=0、Redis=1 次 GET；status cache 命中比 miss 少 6 条命令。快照复用、owner 批量和 status cache 判定通过；详情重叠读取判定失败。

### V04：历史分钟聚合 SQL 与 latest 查询

**目标**：验证时间、channel、group、model、API Key 过滤和 latest 查询的索引利用。

**范围**：`model/channel_monitor_minute.go` 查询函数。

**固定输入**：`M=15`、`M=1440`，分别使用 channel-only、channel+model、group-only、model-only、API Key 明细过滤。

**执行步骤**：

1. 为每个维度预置固定分钟聚合数据。
2. 通过三种数据库的 EXPLAIN/执行计划或 GORM DryRun 记录实际 SQL。
3. 统计候选行、临时排序、GROUP BY 结果行和 latest 读取行数。

**通过标准**：半开窗口口径一致；channel/group 过滤命中对应索引；latest 查询不会无界拉取整窗数据。仅 model 过滤和 API Key 高基数场景若仍全扫，必须记录为已知限制。

**首次实测（2026-08-28，MySQL 8.4.10）**：独立表前缀 `verify_v04_20260828_01_`，route 5,760 行、API Key 23,040 行。M=1440 时 channel-only 扫描 2,880 行、channel+model 扫描 1,440 行，半开窗口和 end 边界通过；group-only 仍扫描 5,760 行后过滤，model-only 全表扫描 5,760 行，API Key 明细全表扫描 23,040 行。latest 查询 M=1440 全表扫描并排序 5,760 行，Go 侧结果口径正确但无界拉取/排序。判定为部分通过，后四类保留为失败项。由于本机 5432 无监听且未配置 PostgreSQL，V04 暂未执行 PG/SQLite 对照。

### V05：Redis 共享投影读取规模

**目标**：验证长窗口 `HGETALL` 数量、响应体和 Go 内存是否受控。

**范围**：`service/channel_monitor_redis_shared_projection.go`。

**固定输入**：`M=1/15/1440`，维度组合使用 `C=10/100`、`K=10/100`，固定失败类别和固定字符串长度。

**执行步骤**：

1. 写入固定数量分钟和日成本 hash。
2. 调用 projection query，记录 Pipeline 次数、命令数、每个 hash 字段数、RESP 字节数和 Go 堆峰值。
3. 单独增加模型名、错误样本、API Key 长度，验证字段长度放大行为。

**通过标准**：窗口上限、字段上限和响应字节上限明确可观测；超过上限时返回受控降级，而不是继续无限加载。

**当前基线预期**：1440 分钟约 1441 个分钟 `HGETALL`，日成本最多 2 个；若仍无限制，标记 V05 失败但保留量化结果。

**首次实测（2026-08-28，Redis 8.8.0，DB15）**：M=1 时 HGETALL 为 2，M=15 时为 16，Pipeline 固定为 2。M=15、`C=100,K=100` 时返回 225,776 个字段、约 17.1 MB RESP、Go heap 峰值约 36.6 MB；`C=10,K=100` 时约 8.1 MB RESP。M=1440 未动态执行，按固定字段模型估算对齐窗口 `C=100,K=100` 约 20,348,062 字段、1.54 GB RESP；非整分钟边界最多约 1,541,357,766 字节/20,362,173 字段，仍无字段/字节/内存上限。判定失败，且高维组合存在 OOM 风险。

### V06：成功率明细 metadata 重复投影

**目标**：确认明细和 metadata 是否对同一窗口执行两次完整 projection query。

**范围**：`controller/channel_ratio_monitor_performance.go`、`controller/channel_monitor_realtime_response.go`。

**执行步骤**：

1. 使用 `M=1440` 请求成功率明细。
2. 通过 Redis hook 记录每次 Pipeline 和 `HGETALL`。
3. 对照明细窗口与 metadata 窗口的起止时间、watermark 和 cutoff。

**通过标准**：同一请求最多一次完整 projection query；metadata 复用首次查询的时间和水位字段。最大窗口下不应出现两遍约 2886 个 `HGETALL`。

**首次实测（2026-08-28，Redis 8.8.0，DB15）**：使用 `verify_v06_20260828_success_detail_` 前缀，M=15 时明细+metadata 为 4 个 Pipeline、34 个 `HGETALL`；最大非整分钟窗口实际为 1441 分钟，明细+metadata 为 4 个 Pipeline、2886 个 `HGETALL`。两次窗口和 watermark 一致，说明口径当前稳定，但读取重复，V06 判定失败。修复后门槛为每请求最多 2 个 Pipeline、最多 1443 个 `HGETALL`。

### V07：概览渠道列表重复查询

**目标**：确认概览是否重复查询渠道列表和并发配置。

**范围**：`controller/channel_ratio_monitor.go`、`service/channel_concurrency.go`。

**执行步骤**：

1. 清空并发配置缓存后请求概览。
2. 记录 `GetAllChannelsForMonitor` 和 `GetChannelConcurrencyConfigs` 的调用次数。
3. 再次请求验证缓存命中时的查询次数。

**通过标准**：概览已取得的渠道 ID/列表可传给并发快照服务，不再重复查询；配置缓存过期只触发一次刷新。

**首次实测（2026-08-28，MySQL 8.4.10 + Redis 8.8.0）**：使用固定 2 个渠道和 2 条并发配置，概览显式绕过快照。冷缓存请求 `GetAllChannelsForMonitor=2`、`GetChannelConcurrencyConfigs=1`、settings=1；配置缓存命中请求仍为 `GetAllChannelsForMonitor=2`、settings=1。HTTP 200 和结果口径通过，但渠道列表重复查询判定失败；并发配置缓存只刷新一次，符合预期。额外观察：monitor settings 当前每次请求均查询数据库。

### V08：页面快照并发、force 单调性和冷启动

**目标**：验证 singleflight、lease、stale、force 发布和冷启动 503 行为。

**范围**：`controller/channel_monitor_page_snapshot.go`、`service/channel_monitor_page_snapshot.go`。

**固定输入**：同一 identity 并发 `Q=50`；另使用 10 个不同 filters 形成 10 个 identity。

**执行步骤**：

1. 同一 identity 同时请求普通刷新和 force 刷新。
2. 预置新旧 revision/watermark，验证 force 不得发布旧快照。
3. 删除 Redis 快照，验证冷启动行为。
4. 触发一次管理操作，统计 goroutine、handler、DB 和 Redis fan-out。

**通过标准**：同一 identity 构建去重；force 不能回滚；stale 可返回旧数据并异步刷新；管理操作 fan-out 不超过配置上限；Redis 错误不会进入永久 503 状态。

**首次实测（2026-08-28）**：service 快照测试 13 项、controller 快照测试全部通过；固定同一 identity `Q=50` 时 builder=1，10 个不同 filters 各 `Q=50` 时 builder=10、Redis 命令 201，说明 singleflight 按 identity 生效。force 与普通刷新并发时未重复 builder，单独 force 可发布，revision/watermark/cutoff/generation 回滚测试通过。冷启动、Redis 禁用和 local stale fallback 均通过。管理刷新静态确认 6 路/15 秒，但因完整 controller fixture 依赖未动态统计 DB/Redis fan-out，标记为待补测。

### V09：事件 writer 队列满、失败和停止

**目标**：验证队列上限、丢弃状态、重试和停止排空。

**范围**：`service/channel_monitor_event_writer.go` 及调用方。

**固定输入**：队列容量 8192；事件批量 `N=100/10000`；Redis 分别正常、拒绝发布、连接不可用。

**执行步骤**：

1. 用固定事件持续入队，读取 `QueueDepth`、入队结果和 dropped 计数。
2. 模拟 Redis 发布错误，确认最大重试次数和最终去向。
3. 在队列非空时调用 Stop，分别使用足够和不足的 context。
4. 检查 controller/service 调用方是否接收 PublishStatus。

**通过标准**：队满不阻塞业务；每次丢弃都有结构化计数和原因；失败事件进入可重放路径或死信；停止行为不会无记录清空事件。

**首次实测（2026-08-28，Redis 8.8.0，DB15）**：`N=100` 正常入队 100、丢弃 0，Stop drain 后 depth=0；writer 未启动时 `N=10000` 入队 8192、丢弃 1808，队满非阻塞行为复现。fake XADD 连续失败时 attempts=3、retries=2、dropped=1，均有结构化计数；真实拒绝端口返回 unavailable。Stop 超时会取消阻塞 XADD 并计 drop。现有 writer/publisher/emit 测试通过，但失败最终仅 drop、无 replay/dead-letter，且生产调用方普遍忽略 PublishStatus，V09 部分失败。

### V10：Redis projection 写入冲突与错误原子性

**目标**：验证 WATCH 冲突、运行时命令错误、marker 去重和成本修正。

**范围**：`service/channel_monitor_redis_shared_projection.go` 写入函数。

**固定输入**：`N=100`，同一 costID 并发修正；固定一个非法数值或非数值字段。

**执行步骤**：

1. 用两个固定 worker 同时写同一批事件。
2. 强制一个聚合命令失败，检查 dashboard hash、cost state 和 event marker 是否同时成功或同时失败。
3. 记录 WATCH 重试次数、每次命令数和最终 marker 数量。
4. 将迟到修正放在 48 小时边界前后，检查重复累加。

**通过标准**：单事件最终只计一次；命令错误不能留下“已处理但未聚合”的 marker；冲突重试有退避并受上限控制；迟到事件不会重复加总。

**首次实测（2026-08-28，Redis 8.8.0，DB15）**：`N=100` 两 worker 并发修正最终只计一次，WATCH=3、TxFailed=1、marker=100，冲突去重通过。将分钟 global hash 预置为非数值后，`EXEC` 返回 Redis 类型错误，但 channel scope、day cost、cost state 和 marker 仍部分写入，复现“marker 已处理但 dashboard 未完整聚合”，原子性失败。强制冲突达到 128 次后受控退出，但无退避；marker/state 过期而 hash 保留时重放会重复累加。V10 部分失败。

### V11：Stream consumer 吞吐、pending 和 poison 消息

**目标**：验证单消费者吞吐上限、handler 超时、pending 优先和 lag/PEL 增长。

**范围**：`service/channel_monitor_redis_consumer.go`。

**固定输入**：事件批量 `N=100`，写入速率分为低于、等于、高于单批处理能力三档；固定一个 poison 事件。

**执行步骤**：

1. 记录 XADD、XREAD、XAUTOCLAIM、ACK、PEL、lag、最老 pending 时间和 trim watermark。
2. 让 poison handler 返回固定失败，验证重试和隔离阈值。
3. 让 handler 持续阻塞，验证 batch deadline 是否终止处理。
4. 模拟消费者退出后等待接管条件，检查旧 pending 的接管和恢复。

**通过标准**：handler 超时可恢复；poison 不阻塞新消息；lag 在输入停止后最终下降；pending 不永久钉住 trim watermark。

**首次实测（2026-08-28，Redis 8.8.0，DB15）**：consumer 相关 service 测试 15/15 通过，覆盖 pending 保留、失败不 ACK、XAUTOCLAIM、毒消息隔离、去重 TTL、租约并发、trim 恢复和序列边界。本地 Redis 最小 Stream 观测验证了 XREADGROUP、XPENDING、XAUTOCLAIM 和跨消费者接管。真实长时间高并发、handler 卡死和故障重启未执行，因此核心语义通过但吞吐/lag 风险仍待压力验证。

### V12：分钟聚合事务和三数据库锁行为

**目标**：验证日志扫描、范围删除和批量插入的锁持有时间及跨库差异。

**范围**：`model/channel_monitor_minute.go`、`model/locking.go`。

**执行步骤**：

1. 使用固定 `M=15` 和 `M=1440` 日志数据执行普通聚合、WithState、Upgrade、Backfill。
2. 在一个 worker 扫描时，另一个 worker 写日志或聚合相邻分钟。
3. 分别在 SQLite、MySQL、PostgreSQL 记录 lock wait、deadlock、`SQLITE_BUSY`、事务时长和最终行数。

**通过标准**：成功时最终聚合完整；失败时事务不留下半套数据；锁等待和事务时长在预设阈值内；分离 `LOG_DB` 时明确记录非跨库原子行为。

**首次实测（2026-08-28）**：MySQL 8.4.10（3306，独立 schema `verify_v12_1787930973280575900`，凭据由本机运行环境提供且未输出）、SQLite 临时文件；PostgreSQL 5432 无监听，未执行。固定 `T0=1800000000`，输入为 15 条前置日志、1440 分钟窗口日志和 2 条边界日志，`Q=4`。MySQL 普通聚合 M=15/1440 均完整（扫描/route/api=15/1440），WithState、Upgrade、CacheBackfill、RegularBackfill 全部成功；4 路同范围 WithState(false) 无错误，耗时约 218/442/744/969 ms，`Innodb_row_lock_waits=6`、`row_lock_time=1382ms`、deadlocks=0。相邻分钟并发写入 7.5ms 成功，最终 route/api=1440、窗口日志 1441、覆盖和 revision 状态完整。SQLite M=15/1440 及四路径成功；Q=4 出现 3 次 `sqlite_busy`、1 次成功，耗时约 5/13/19/572ms；并发相邻写入 10ms 成功，重建后无半套聚合。结论：MySQL/SQLite 语义通过，但 SQLite 并发写必须串行或可分类重试；MySQL 热点状态行存在可观测等待。`LOG_DB` 分离的跨库非原子场景及 PostgreSQL 仍待补测。临时测试文件已删除，验证 schema/数据按约定保留。

### V13：dirty-minute 标记、重试查询和 lease 续租

**目标**：验证迟到日志和高重试率下的额外查询、重复领取和恢复。

**范围**：`model/log.go`、`model/channel_monitor_dirty_minute.go`、`service/channel_monitor_aggregation.go`。

**固定输入**：同一 request 固定产生 `r=1/5/20` 次跨分钟重试；重建时间用固定可控 handler，不使用 sleep 判定。

**执行步骤**：

1. 写入主日志和跨窗口错误日志，记录 request_id 补查次数、扫描行数和 upsert 次数。
2. 并发两个修复 worker，检查 claim/release/complete 的 CAS 结果。
3. 在 lease 即将到期的确定性测试钩子下继续重建，检查是否续租或重复领取。
4. 强制 dirty marker 写入失败，检查源日志、marker 和后续修复状态。

**通过标准**：同一分钟最终只保留正确聚合；marker 失败不重复写日志；重建不会因 lease 过期造成无界重复；客户端取消可以停止非必要查询或有明确后台语义。

**首次实测（2026-08-28）**：MySQL 8.x（本机 `.env` 中既有 `SQL_DSN`，凭据未输出）和 SQLite，PostgreSQL 未测；Redis 本项无直接依赖。`r=1/5/20` 时 MySQL/SQLite 均只执行 2 次日志查询，扫描行数为 2/6/21（`r+1`），route 与 API-key 各 upsert 1 次，说明同一 request 的重试不会按重试次数线性放大；但 request_id 超过 500 后补查按 `ceil(K/500)` 扩展。claim/release/complete 顺序 CAS 和 MySQL 双 worker barrier 均通过（winners=1）；SQLite barrier winners=1，但另一协程出现 `SQLITE_BUSY`。lease 到期后 B 可重领，A 的 stale complete 不会删除 B 的 claim，但实现没有续租 API/调用，长重建超过 2 分钟会重复执行，lease 判失败/P1。注入 dirty marker 写入失败后，MySQL/SQLite 均出现 source log=1、dirty marker=0、aggregation state=0；日志虽已持久化但无自动补偿，marker failure 判失败/P1。临时测试已删除，验证 schema/数据保留。

### V14：成本账本并发增量和直接重放

**目标**：验证唯一冲突、原子增量、溢出保护和 EventId 幂等边界。

**范围**：`model/channel_daily_cost.go`、`model/channel_daily_api_key_cost.go`。

**固定输入**：同一 channel/day 由 `Q=1/10/50` 并发写入固定 delta；另重复提交同一 EventId。

**执行步骤**：

1. 预置空账本，固定并发写入目标金额。
2. 检查最终 channel 总账、API Key 分摊、计数和错误分类。
3. 注入接近 int64 上限的 delta，验证事务是否完整回滚。
4. 不经过 outbox 直接重放同一 delta，验证当前幂等边界。

**通过标准**：正常路径无丢计数和回绕；溢出不产生部分写入；同一 EventId 若合同要求幂等则只能计一次，否则必须在接口文档中明确“调用方负责去重”。

**首次实测（2026-08-28）**：MySQL 8.4.10（本机 `.env` `SQL_DSN`，凭据未输出）和 SQLite，Redis 无直接依赖，PostgreSQL 未测。固定 `T0=1700000000`、`day_start=1699977600`、delta=100 CNY nanos、settled=1、APIKeyId=14014、固定 fingerprint；同 channel/day/key 以 `Q=1/10/50` 并发写入且每事件 EventId 唯一。MySQL Q=1/10/50 均全成功，channel 与 API-key 成本/计数分别为 100/1、1000/10、5000/50，金额守恒且无回绕；Q=50 首行竞争单条 INSERT 最慢约 206–364ms，但无死锁。直接绕过 outbox 用同 EventId 连续写两次后 channel/key 均变为 246、count=2（SQLite 同样翻倍），证明底层 `AddChannelDailyCostBatch` 不检查 EventId，直接重放非幂等。接近上限的 channel 或 API-key delta 返回 `ErrChannelDailyCostLedgerOverflow`，两侧均保持原值且无半套写入。结论：正常并发、分摊和溢出保护通过；直接重放幂等边界失败/P1（若存在绕过 outbox 的调用路径），MySQL 首行竞争需观测锁等待。验证表和 SQLite 数据按约定保留，临时测试已删除。

### V15：任务成本修正和重复回调

**目标**：验证退款、差额结算、重复回调和逆序 quota 更新的一致性。

**范围**：`service/task_billing.go`、`model/channel_task_cost_event.go`。

**固定输入**：同一 task、同一旧 quota，`Q=1/10/50` 并发执行退款或 `RecalculateTaskQuota`；再提交新值后提交旧值。

**执行步骤**：

1. 记录任务 quota、用户余额、Token 余额、渠道余额、`used_quota` 和成本事件行。
2. 并发执行相同回调，检查净变更是否只发生一次。
3. 先提交新 `updatedAt`，再提交旧值，检查旧值是否被拒绝。
4. 模拟成本事件事务失败和 Redis 修正事件入队失败，检查账务与投影补偿状态。

**通过标准**：重复回调不重复扣费/退款；旧版本不能覆盖新版本；账务、成本事件和实时投影最终一致，失败必须可重试或进入补偿队列。

**首次实测（2026-08-28，含外层 CAS 补测）**：MySQL 8.4.10（独立 schema `verify_v15_1787932535640444700`，凭据未输出），Redis 失败注入使用 127.0.0.1:6399/DB15，SQLite 既有回归通过，PostgreSQL 未测。固定 pre=1000、actual=500。`RecalculateTaskQuota` 在独立旧 task 副本上 Q=1 正常；Q=10/50 时 task quota、成本事件和 daily cost 仍为 500，但 user quota、used_quota、token、channel used、日志按并发次数重复调整（Q=10 used=-4000，Q=50 used=-24000）。`RefundTaskQuota` 同样随 Q 重复退款（Q=10 used=-9000，Q=50 used=-49000）。成本事件模型层有幂等，但任务账务/日志无内部幂等；当前生产 polling 调用通常先做外层 task 状态 CAS。补测 Q=10/50 多 worker 同一 `IN_PROGRESS` snapshot：MySQL refund/settle 均 CASWins=1、账务和 event/daily 只执行一次；SQLite 也 CASWins=1，但重复运行中至少 2/12 场 settle 因 `SQLITE_BUSY` 导致 event/daily correction 未落库而 task/user/channel 已结算。旧 `updatedAt` 写入可覆盖新值（先 2000 后 1000，最终为旧值），说明成本事件 CAS 未比较时间版本。篡改 daily cost 触发成本修正失败时，资金/usage/task quota 已提交而 event/daily 未同步；坏 Redis 入队 3 次失败后无 retry/dead-letter/pending 补偿状态，调用方忽略结果。结论：MySQL 外层 CAS 合同通过；helper 旁路幂等、旧版本覆盖、成本失败和投影入队失败判失败/P1；SQLite 高并发还需对 `SQLITE_BUSY` 做分类重试/补偿。临时测试已删除，验证 schema/数据按约定保留。

### V16：Redis 连接池隔离和拥塞观测

**目标**：验证用户、监控读、监控写、消费池的隔离和等待指标。

**范围**：`common/redis.go`、`service/channel_monitor_redis_observability.go`。

**固定输入**：默认池大小用户 10、写 4、读 8、消费 4；分别制造读池、写池和消费池饱和。

**执行步骤**：

1. 开启 role pool stats，记录 total、idle、misses、timeouts、command count 和延迟。
2. 将 `REDIS_CLIENT_POOL_ISOLATION` 分别设为 true/false，确认实际连接归属。
3. 设置短请求 deadline，区分 context deadline 与 pool timeout。
4. 检查 status JSON 是否标记 isolation 状态和拥塞角色。

**通过标准**：不同角色不会互相耗尽连接；连接等待、超时和 in-use 有可观测指标；隔离关闭时不会伪装成独立池。

**首次实测（2026-08-29）**：本机 Redis 8.8.0（127.0.0.1:6379，DB15），go-redis/v8.11.5；临时池大小 user=10、write=4、read=8、consumer=4，连接字符串设置 `pool_timeout=100ms`、短读写超时。`REDIS_CLIENT_POOL_ISOLATION=true` 时四个 client 指针不同，分别饱和 read/write/consumer 后，跨角色 user Ping 仍约 0.5–0.7ms 成功；20ms deadline 返回 context deadline，500ms 请求在约 100ms 返回 pool timeout，阻塞连接均释放。`false` 时四角色指针完全相同，read 池饱和会使 user 请求同样约 100ms pool timeout，证明关闭隔离时拥塞跨角色传播。PoolStats 的 misses/timeouts/command_count/latency 均可观测；但 status JSON 没有显式 isolation 布尔字段，也没有按拥塞角色输出 degraded_reason/role 标记，只能由累计计数外部差分。现有单测和临时动态验证通过，临时文件已删除，Redis verify key 按约定保留。

### V17：三数据库兼容性回归

**目标**：验证 SQLite、MySQL、PostgreSQL 的 SQL、锁、保存点和迁移语义。

**执行步骤**：

1. 分别执行 V01、V12、V14、V15 的最小档位。
2. 检查唯一冲突、范围删除、500 批插入、`lockForUpdate`、SavePoint 和事务回滚。
3. 若 `LOG_DB` 独立，分别验证日志库成功/主库失败及反向失败。

**通过标准**：三库最终数据合同一致；允许的锁错误必须可分类、可重试，不得静默丢数据。SQLite 不应使用不支持的 `FOR UPDATE`；MySQL/PG 不应出现未处理的死锁/序列化错误。

**首次实测（2026-08-29）**：MySQL 8.4.10（本机 `.env` DSN，表前缀 `v17_c280_`）与 SQLite（glebarez，busy_timeout=5000）通过；PostgreSQL 5432 无监听且未配置 DSN，阻塞。V01/V14 最小档位（500 个 API Key delta）两库均最终 channel day cost=500、API-key 行=500，分页 `page_size=1` 返回 1；EventId 冲突均分类为 `ErrChannelDailyCostOutboxEventIDCollision`（MySQL 1062/SQLSTATE 23000，SQLite constraint error）；非法批次回滚后无 channel 1703 行。V12 replacement 通过 500+1 批插入、范围删除保留边界行；SavePoint/RollbackTo 外层事务两库通过。DryRun 显示 SQLite 不含 `FOR UPDATE`、MySQL 含 `FOR UPDATE`。分离 `LOG_DB` 成功路径通过，主库或日志库缺表时均返回明确错误且无静默提交。结论：MySQL/SQLite 最小兼容合同通过；PostgreSQL 需提供环境后补测。临时测试已删除，MySQL verify 数据按约定保留。

### V18：现有测试和确定性基线

**目标**：区分已有回归、fixture 问题和未覆盖风险。

**执行步骤**：

```text
go test ./service -run 'ChannelMonitor|channelMonitor' -count=1
go test ./model -run 'ChannelMonitor|channelMonitor' -count=1
go test ./controller -run 'ChannelMonitor|channelMonitor' -count=1
```

记录测试名、合同、失败原因和数据库/Redis依赖；不把异步日志作为唯一失败依据。

**通过标准**：model/service 目标测试保持通过；controller fixture 失败与业务失败分开记录；补充 V09、V11、V15 后，队列丢弃、Stream pending、重复计费均有确定性断言。

**首次实测（2026-08-29）**：service 基线首次出现 `TestChannelMonitorPageSnapshotForceRefreshRebuildsFreshSnapshot` 返回 old，但单独运行及完整命令重跑均通过，判为并发/生命周期不稳定噪声而非稳定业务失败；model 基线通过（约 9.1s）。controller 基线稳定失败于 `TestGetChannelMonitorCostOverviewIncludesAPIKeyOwnersForUserGrouping`，单独运行同样失败，原因是 fixture 插入 alice/bob 时默认空 `aff_code` 触发 `users.aff_code` 唯一约束（`controller/channel_monitor_cost_test.go:534`），属于测试 fixture/环境问题。V09 writer 11 项、V11 consumer/Stream 13 项、V15 task billing service 20 项及 model 3 项确定性测试全部通过；注入的 unavailable/timeout/record-not-found 日志均为预期故障模拟。Go 1.26.5 Windows amd64、CGO_ENABLED=0，未执行 `-race`。临时文件已清理，`git diff --check` 通过。

## 4. 执行顺序

每个验证项原则上可独立执行。为了降低资源占用，建议按以下批次运行，但批次之间不共享数据：

1. **查询类**：V01～V07。
2. **实时链路类**：V05、V06、V08～V11、V16。
3. **数据库和一致性类**：V12～V15、V17。
4. **测试基线**：V18。

若发现业务账务问题，立即暂停压力验证，先保留最小复现数据和结构化结果。

## 5. 每项输出格式

每个验证项单独产出一份结果，格式固定为：

```text
验证项：Vxx
环境：SQLite/MySQL/PostgreSQL，Redis 版本，代码 commit
固定输入：C/M/K/N/Q、T0、数据行数
执行结果：通过 / 失败 / 阻塞
观测值：查询数、命令数、最终账本、lag/PEL、错误分类
证据：测试名、SQL/Redis 结构化采样、文件:行号
影响：业务请求 / 账务 / 实时监控 / 数据库
后续：修复项或无需处理的已知限制
```

## 6. 修复后的回归门槛

修复不能只通过单元测试，至少需要满足：

- V01/V02/V03：成本分页和高基数请求不再全量重复读取。
- V05/V06：长窗口投影有明确上限，成功率明细不重复扫描。
- V09/V10/V11：丢事件可观测、投影可重放、Stream 不因 poison 永久积压。
- V12/V13/V17：三库事务失败不留下半套聚合数据。
- V14/V15：成本账本和任务账务不重复、不回绕、可补偿。
- V16：连接池隔离状态和等待指标可直接观测。

只有在 SQLite、MySQL、PostgreSQL 的最小并发矩阵均通过后，才可考虑扩大压力规模。

## 7. 修复轮次回归记录（2026-08-29）

按实施顺序完成了任务计费幂等、成本 SQL 聚合/有界高基数、Redis projection 上限与事务重试、事件 outbox、Stream consumer、dirty-minute lease、Redis pool 观测、页面快照单调性和 controller API 暴露等修复。

验证结果：

- `go test ./model -count=1`、`go test ./service -count=1`、`go test ./controller -count=1`：通过。
- `go test ./... -run '^$' -count=1`：全仓编译通过。
- `cd relaykit && GOWORK=off go build ./...`：通过，relaykit 保持独立可构建。
- web：`bun run typecheck`、相关 Oxlint、`bun run build` 及成本历史布局回归：通过。
- `git diff --check`：通过。

完整 `go test ./... -count=1` 仅剩既有 `relay/channel` 的 `TestUpstreamGetBody_HTTP2RetryAfterGracefulGoAway_PassThrough` 在 Windows 本机网络时序下失败；本轮没有修改 relay/channel，单测重复运行仍复现连接被远端中止，需独立处理，不归因于渠道监控修复。

PostgreSQL 因本机 5432 无监听仍未执行；因此本轮不宣称三库完整动态通过，生产迁移已将 `ChannelMonitorEventOutbox` 纳入 `migrateDB`/迁移列表。
