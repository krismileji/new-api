# 渠道逻辑归组：多渠道共享调度、状态监测与模型检测方案

## 1. 目标与结论

保留“一个 Key 一个渠道”的现有配置方式：每个物理渠道继续拥有独立 Key、余额、倍率、成本、并发和成员级状态。新增逻辑渠道关联后，多个物理渠道可以声明为同一个上游逻辑入口，但只共享智能调度数据、状态监测/状态探测和模型检测；其他数据继续按单渠道处理。

产品界面保持不变：现有渠道列表、渠道详情、渠道监控、模型检测、状态探测、调度和成本页面不改版、不增加父子行，不要求用户切换新的逻辑视图。只新增一个“同渠道配置”入口，用于管理员配置哪些渠道属于同一逻辑渠道。

共享范围：智能调度样本、评分、优先级/动态路由、状态监测/状态探测配置与结果、模型检测配置与结果，以及逻辑组成员 weight。

独立范围：性能、成功率、首字、TPS、延迟、失败分类、成本、余额、倍率、倍率历史、上游账号凭据、成本基线、单 Key 禁用状态和物理渠道并发限制。以上数据和页面继续按物理渠道/API Key 处理，不做逻辑组汇总。并发限制完全沿用现有按渠道（即单 Key）管理的功能，本方案不新增逻辑组并发限制、并发计数或并发租约。

这不是按名称或标签做展示层相加，而是引入稳定、可审计、可迁移的逻辑身份。

## 2. 当前架构与问题

目前 channel_id 同时承担物理 Key 身份和逻辑上游身份，以下本应共享的能力按物理渠道分裂：

- 智能调度样本、路由状态、运行时保护和采样债务；
- 模型检测配置、目标、轮次和执行记录；
- 状态探测配置、状态、执行历史和周期租约；
- 其他渠道监控指标、成本、余额和倍率本来就是物理渠道数据，本方案不改变其单渠道边界。

现有标签模式只改变管理页面的树状展示，不改变统计边界，也没有稳定外键。现有多 Key 模式把凭证放在同一条 Channel 中，不能让余额和倍率独立，因此不适合作为本需求的基础。

## 3. 术语与不变量

| 名称 | 标识 | 含义 |
| --- | --- | --- |
| 物理渠道 | channel_id | 现有 channels 行，含一个上游 Key，是实际发起请求和保存余额/倍率的单位 |
| 逻辑渠道组 | logical_channel_id | 新增稳定身份，代表同一上游/模型能力但 Key 不同的共享调度、状态探测和模型检测单位 |

未归组渠道使用自身 channel_id 作为虚拟逻辑 ID，保证旧行为一致。

不变量：

1. 一个物理渠道最多属于一个逻辑组；逻辑组至少有一个成员才可启用。
2. 事件发生时固化逻辑 ID，不能只在查询时按照当前成员关系回溯，否则换组会改写历史。
3. 所有日志、成本、审计和检测执行保留物理 channel_id，可追溯实际 Key；普通接口只返回遮罩值。
4. 智能调度、状态探测和模型检测的共享状态不能覆盖成员认证失败、Key 禁用、余额不足和并发限制。
5. 成员关系、共享路由状态和检测运行均使用 revision；过期请求不能写回新关系。

## 4. 数据模型

### 4.1 逻辑组表

新增 channel_logical_groups 表，字段建议为：

| 字段 | 说明 |
| --- | --- |
| id | GORM 主键 |
| name | 展示名称，只用于显示，不作为身份判断 |
| remark | 管理备注 |
| status | 管理启停开关；成员是否可用由运行时派生 |
| revision | 成员或共享配置变化时递增 |
| created_at、updated_at | Unix 时间戳 |

channels 增加可空、可索引的 logical_channel_id；新增 channel_logical_group_members 关联表，至少包含 logical_group_id、channel_id、weight、created_at、updated_at。关联表中的 weight 是逻辑组内 Key 分配权重，默认 1，与物理渠道的 Channel.Weight 和智能调度 Ability.Weight 分开。服务层校验目标逻辑组、成员唯一、权重范围和 revision；不强制依赖数据库外键，降低 SQLite、MySQL、PostgreSQL 迁移差异。

逻辑组不复制 Base URL、模型、请求转换等配置，成员仍是配置事实来源。逻辑归组由管理员明确配置，系统只用规范化后的上游请求地址判断是否允许归组；其他差异由管理员负责确认。

### 4.2 逻辑组归属必须由管理员配置

“哪些渠道属于同一个逻辑渠道”不能仅由名称、标签、Base URL 或 Key 前缀自动推断，必须由管理员在新增的“同渠道配置”入口中显式配置。该入口是唯一新增的页面/弹窗，现有渠道相关页面不增加逻辑组概念。

配置流程保持简单：

1. 管理员打开“同渠道配置”，创建一个关联组并选择多个现有物理渠道。
2. 系统比较规范化后的请求地址；地址不一致时禁止保存。
3. 每个关联成员配置一个简单的组内 weight，默认 1；该值只负责逻辑组已选中的多个 Key 之间的分配，不改变物理渠道原有权重。
4. 已属于其他关联组的渠道不能重复加入；移组时先从原组移除。
5. 删除关联组只解除成员关系，不删除物理渠道、Key、余额、倍率、成本和历史数据。

不新增逻辑组专用的监控、模型检测或调度页面。现有页面继续使用原来的 `channel_id`、筛选、分页、详情和操作入口；服务端仅在智能调度、状态探测和模型检测内部根据关联关系转换为逻辑组 ID。标签仍只用于筛选和批量管理，不作为逻辑归属。

接口只新增关联配置所需的管理 API：

- 获取关联组及成员；
- 创建/删除关联组；
- 替换关联组成员及成员 weight；
- 成员地址预检和 revision 冲突校验。

不新增面向用户的逻辑监控查询 API；现有监控、模型检测、状态探测、调度和成本 API 保持原有路径和响应结构。

### 4.3 归组判断：只比较请求地址

管理员决定“谁加入哪个逻辑组”，系统不再比较 Provider 类型、模型列表、模型映射、Header、代理、组织、倍率或余额等字段。唯一的硬判断条件是成员的规范化上游请求地址相同。

请求地址建议按以下规则规范化后比较：补全渠道默认上游地址、去除首尾空白、统一 Scheme 和 Host 大小写、去除默认端口、清理末尾斜杠，并保留有语义的路径；不要把包含凭据的查询参数或完整 Key 写入地址摘要。地址变更后，成员必须重新检查逻辑组归属。

这意味着管理员可以有意把同一请求地址下但模型映射或其他配置不同的渠道放入一个逻辑组；系统只在智能调度、状态探测和模型检测范围内共享，其他配置语义由管理员承担。成员关系中保存的 fingerprint 只是规范化请求地址的不可逆摘要，用于事务校验、缓存一致性和审计，不是额外的“成员兼容指纹”。已归组渠道修改请求地址或影响默认地址的渠道类型时，必须在同一事务内重新校验整组：仍一致则刷新摘要并递增 revision，不一致则拒绝修改；系统不自动替管理员重组成员。

### 4.4 共享功能的运行时身份

只在智能调度、状态探测和模型检测相关运行记录中增加 LogicalChannelId；原始请求日志、性能监控事件、成本记录继续以 ChannelId 为唯一物理归属。生产端从请求上下文或渠道缓存快照写入逻辑 ID，不在 Redis 聚合器中查询当前关系。

需要增加逻辑维度或逻辑组状态的区域仅包括：

- 智能调度样本、路由、执行明细、运行时保护和冷却投影；
- 状态探测配置、状态、执行；
- 模型检测配置、轮次和执行；
- 关联关系审计快照。

路由性能、成本和 API Key 明细不增加逻辑组唯一维度，继续保留物理 channel_id；只有智能调度、状态探测和模型检测使用逻辑组维度。

## 5. 数据归属矩阵

| 领域 | 逻辑组共享 | 成员独立 | 页面表现 |
| --- | --- | --- | --- |
| 性能、成功率、首字、TPS、延迟、失败分类 | 不共享 | 每物理渠道独立记录和查询 | 现有渠道页面完全不变 |
| 稳定性、成员状态和普通监控 | 不共享 | 每物理渠道独立状态 | 现有渠道状态字段保持不变 |
| 智能调度 | 共享样本、评分、采样债务、主备意图 | 成员可用性和逻辑组成员 weight 选择 Key | 现有调度流程内部使用逻辑组，页面不变 |
| 余额 | 不共享、不汇总 | 每成员一份 ChannelRatioMonitor | 现有渠道行继续显示各自余额 |
| 倍率 | 不覆盖成员值 | 倍率、换算、历史、上游配置每成员一份 | 现有渠道行继续显示各自真实值 |
| 成本 | 不共享、不汇总 | 日成本、API Key 成本、检测成本按成员保存 | 现有渠道成本页面保持不变 |
| 模型检测 | 配置、目标、轮次、结论 | 实际成员、Key 脱敏标识、成本快照 | 现有模型检测页面不变，后台结果按关联组共享 |
| 状态探测 | 配置、周期、模型、健康结果 | 实际探测成员和成本 | 现有状态探测页面不变，后台结果按关联组共享 |
| 并发 | 不新增共享并发能力，仅共享候选成员池 | 沿用现有按渠道/Key 的并发限制、活跃租约和成员级排除 | 现有并发字段和页面保持不变 |

## 6. 智能调度

### 6.1 目标与核心原则：逻辑组调度 + 成员 weight 分配 Key

功能目标是让多个 Key 的独立并发额度可以同时使用，解决单 Key 并发已满但同组其他 Key 空闲的问题。每个请求仍只使用一个物理渠道和一个 Key，不做请求拆分。

并发功能不在逻辑组层重新实现：每个物理渠道继续使用现有的并发限制、Redis/本地活跃租约、续租、释放和 429 行为。逻辑组先作为智能调度的候选单位参与评分；逻辑组被选中后，再按关联表中的成员 weight 在多个 Key 之间选择具体物理渠道，最后直接调用现有渠道并发流程。

### 6.2 组内流量分配算法

逻辑组内不复用物理 Channel.Weight 或智能调度 Ability.Weight 作为成员配额，而是使用关联表中的单一成员 weight：

1. 先按共享逻辑组健康状态过滤，再排除成员禁用、Key 认证失败、余额不足和成员级冷却的成员；显式指定 `channel_id` 的请求不跨成员改投。并发是否已满由现有渠道并发流程判断。
2. 对剩余成员按关联表 weight 随机/平滑轮询选择。weight 为 3:1 时，在成员都可用的时间窗口内，流量约为 3:1；weight 相同则平均分配。若组内全部 weight 为 0，按平均分配处理。
3. 成员 weight 只决定逻辑组内部的 Key 倾向，不能改变或绕过现有渠道并发限制。选中成员后进入现有并发获取流程；如果该流程返回并发已满，则沿用现有重选/429 行为，不新增一套逻辑组并发实现。

示例：A、B 两个 Key 的并发上限均为 1，权重均为 1。前两个并发请求分别占用 A、B，第三个请求在两者都满时按现有策略返回 429；任一请求结束后，新的请求即可进入释放出来的 Key。

### 6.3 故障处理

- 成员返回疑似 Key 级并发限制的 429 时，只对该成员做短 TTL 冷却；确认是上游账号级限流时才使用逻辑组级冷却。
- 成员失败、余额不足只排除成员；成员是否并发已满由现有渠道并发流程处理，所有成员都不可用时逻辑组才退出候选。
- 后台模型检测、状态探测和手动测试同样走该成员 weight 分配器，并复用实际成员已有并发限制，但每个逻辑目标每轮只执行一次。

### 6.4 推荐架构：逻辑组评分，成员路由投影

为降低中继改造风险，第一阶段不重写所有请求选路：

1. 仅在智能调度内部按逻辑组、路由分组、模型读取成员样本。
2. 智能调度内部可以按成功率、首字、TPS 等现有评分规则计算逻辑组评分；这些计算结果只用于调度，不写入普通渠道监控的共享汇总。
3. 只生成一个逻辑组级评分、主备意图、优先级和稳定性状态，不为每个 Key 生成独立调度评分。
4. 将逻辑组结果写入逻辑组路由状态；中继先选逻辑组，再由 6.2 按关联表成员 weight 选择具体 Key，随后进入现有渠道并发流程。
5. 成员级并发、冷却和认证状态只影响组内成员是否可选，不覆盖逻辑组共享评分。

### 6.5 共享路由状态

新增 channel_logical_schedule_routes 作为共享状态源，主键为逻辑组、分组和模型，包含参与、排除、评分、优先级、稳定性保护、临时采样、探索、滚动健康、固定逻辑组和 revision。逻辑组成员 weight 来自 channel_logical_group_members，不被智能调度动态权重覆盖。

现有 ChannelSmartScheduleRouteState 在兼容期作为成员投影或只读数据，不能继续对同一逻辑组独立评分。调度执行明细保存逻辑组评分快照、成员样本/倍率/可用性、被排除原因和投影前后 Ability 值。

### 6.6 三种权重的边界

需要明确区分三种权重，避免把它们混为一谈：

1. 物理渠道 `Channel.Weight`：现有普通渠道选择的默认权重。它只影响未被智能调度接管的物理渠道池；智能调度接管后不作为最终成员比例。
2. 智能调度 `Ability.Weight`/路由权重：用于智能调度在不同候选路由之间计算和执行动态流量，属于“逻辑组与其他候选的竞争”以及调度保护状态。
3. 逻辑组成员 weight：新增关联表中的简单配置，只用于逻辑组已经被选中后，在多个 Key 之间分配请求。它不参与智能调度评分，也不与 Ability.Weight 相加。

因此，之前如果直接复用物理渠道默认 weight，在智能调度模式下确实可能没有预期效果。修正后的方案是：智能调度负责选逻辑组，成员 weight 负责选 Key，物理 Channel.Weight 保持原有行为且不被关联配置修改。

### 6.7 倍率与调度的口径

倍率必须独立保存，不参与组内流量权重、并发槽位或 Key 选择。性能样本只在智能调度内部用于逻辑组评分，不改变普通渠道监控；倍率只用于成员成本结算、余额换算和明细展示。若未来需要成本导向调度，应另增明确的逻辑组经济评分开关，不能把倍率隐式混入当前 `Channel.Weight` 或智能调度的有效权重。

### 6.8 故障边界

- 401/403、Key 无效、单 Key 余额不足：只标记成员，不触发逻辑组保护。
- 429：默认成员级冷却；确认是上游账号级限流时才使用逻辑组级冷却。
- 5xx、模型不支持、统一协议错误：进入逻辑组的调度/探测共享保护窗口，但保留成员 ID；普通渠道监控状态仍按物理渠道记录。
- 所有成员不可用：逻辑组退出候选；任一成员恢复后重新进入。
- 共享保护和恢复使用逻辑组 Redis key，成员状态使用成员 key，不能互相覆盖。

Redis 建议使用新版本前缀 channel_monitor:v2:logical:*，不改变 v1 含义。完整调度、实时刷新、429 冷却和保护恢复均带逻辑组 revision。

## 7. 普通渠道监控与接口兼容

### 7.1 保持单渠道处理

本方案不改造普通渠道监控和成本聚合。性能、成功率、首字、TPS、延迟、失败分类、余额、倍率、成本和并发均继续按物理 channel_id 记录、统计和查询。只有智能调度内部为了生成逻辑组评分而读取成员样本，不能把这份内部评分数据变成渠道监控页面的逻辑组汇总。

### 7.2 API 兼容

现有监控、成本、性能、成功率、调度、任务、状态探测和模型检测 API 的路径、请求参数和响应结构保持不变，继续以 `channel_id` 作为页面查询和操作入口。服务端仅在智能调度、状态探测和模型检测内部把 channel_id 解析到逻辑组；普通监控、成本、余额、倍率和并发接口完全走原有单渠道逻辑，不能要求前端理解 `logical_channel_id`。

仅新增“同渠道配置”管理 API，供新增配置入口使用：

    GET    /api/channel/logical-groups
    POST   /api/channel/logical-groups
    PUT    /api/channel/logical-groups/:id/members
    DELETE /api/channel/logical-groups/:id

成员预检可以作为新增入口的独立接口或创建接口的 dry-run 参数；不新增面向监控页面的逻辑聚合查询接口。

### 7.3 前端交互

- 现有渠道列表、渠道详情、渠道监控、模型检测、状态探测、调度和成本页面原样保留，不增加逻辑组父行、子行、筛选项、列、跳转或新的操作流程。
- 在渠道管理或渠道监控设置区域增加一个“同渠道配置”入口；入口内展示关联组、成员渠道、成员 weight 和规范化请求地址校验结果。
- 余额、倍率、Key、并发限制、单渠道启停等操作继续在原有渠道页面完成。
- 关联组配置保存后，仅调度、状态探测和模型检测后台使用共享关系；普通渠道页面继续读取各自物理渠道数据，用户不需要手动切换逻辑视图。
- 标签仍可用于筛选，但不作为逻辑身份来源。

## 8. 模型检测与状态探测

### 8.1 模型检测

ChannelModelDetectionConfig、目标和定时状态迁移为逻辑组粒度；未归组渠道继续使用自身虚拟逻辑配置。

模型检测的配置、目标、执行范围、轮次、结论和监控数据全部以逻辑组为边界，不按成员复制检测任务，也不把 Key 数量当成检测目标数量。一次模型检测轮次只生成一份逻辑组运行记录和每个目标一份逻辑执行记录。

每个目标实际发出的请求仍通过 6.2 的组内流量分配器选择一个健康成员，随后复用该成员现有的渠道并发流程；执行快照保存逻辑组 ID、请求地址摘要、成员选择结果、成员 channel_id、Key 遮罩、倍率快照和请求 ID。这样检测结果共享，但不会绕过任意 Key 的并发限制。

一次执行过程中默认不向其他成员复制请求；只有发生可重试的连接/传输错误时，才按既定重试策略释放旧租约并改投另一个成员。成员 Key 失败只影响该次尝试；同一模型的最终结论由逻辑组执行记录给出。检测成本事件始终保留实际成员归属，不生成逻辑组成本汇总。

现有按 channel_id 的模型检测接口继续兼容，服务端内部解析到逻辑组。活动轮次、取消、清理和检测器短期凭证均以逻辑轮次为边界，前端不感知 logical_channel_id。

### 8.2 状态探测

ChannelStatusProbeConfig、探测模型列表、周期、执行范围和聚合健康状态改为逻辑组粒度。每个周期每个目标只执行一份逻辑组探测，请求通过同一个组内流量分配器选择成员并复用该成员现有渠道并发限制；不为每个 Key 重复执行。成员认证失败只污染该成员，逻辑组健康由共享结果和成员可用性共同计算。无健康成员时探测跳过并说明原因，不伪造上游失败。

状态探测执行历史保留逻辑组、实际成员、模型、请求和成本字段；旧成员接口保留兼容查询。成本记录仍按实际成员保存，不生成逻辑组成本汇总。模型检测、状态探测和手动测试使用同一套组内权重分配，并复用实际成员已有的渠道并发限制。

### 8.3 其他监控任务

状态探测和智能调度探针读取逻辑组共享状态；普通渠道监控不读取逻辑组聚合。模型检测、状态探测和智能探针每个逻辑目标每轮只产生一份共享观测样本，实际成员明细另存；这些请求同样走组内成员 weight 分配和现有渠道并发流程，防止同一请求重复计数或绕过 Key 限制。

## 9. 迁移、历史和关系变更

### 9.1 数据库迁移

1. 新增逻辑组表、成员关联表和 channels.logical_channel_id，只使用 GORM/兼容 SQL，避免数据库专属类型和不可逆 ALTER COLUMN。
2. 仅为智能调度、状态探测、模型检测和关联审计增加逻辑 ID 或新逻辑组表；普通监控事件、分钟聚合、成本和日志表不增加逻辑组汇总维度。
3. 升级后所有旧渠道可保持未归组，虚拟逻辑 ID 等于物理 ID。
4. 普通监控事件、分钟数据、成本和日志不按名称、标签或相似配置自动合并，归组不会改写物理渠道历史。
5. 不做历史数据回填；归组只影响变更后的智能调度、状态探测和模型检测，普通监控和成本历史保持原样。

### 9.2 成员关系变更

- 加入、移出、合并、拆分在一个事务内完成并递增逻辑组 revision。
- 关系变化后刷新智能调度、状态探测和模型检测缓存；活动检测/探测冻结旧成员快照。
- 移出后新的调度、探测和检测任务使用新关系；普通监控、成本和日志仍按物理渠道记录，不随当前关系移动。
- 删除逻辑组前必须处理活动任务并确认成员去归属；删除逻辑组不删除物理渠道、成本、日志和历史明细。

## 10. 安全、缓存和并发

- 新建逻辑组、改成员关系、合并/拆分和共享调度策略使用现有 RootAuth/渠道操作权限。
- 审计记录旧组、新组、成员 ID、请求地址摘要、操作人、revision 和失败原因，绝不记录完整 Key。
- 逻辑详情只返回遮罩 Key；检测报告和调度快照隐藏 Key、短期 Bearer、账号密码和刷新令牌。
- 渠道缓存快照保存逻辑 ID、revision、成员列表和成员可用状态；构建失败继续使用上一份完整快照。
- 智能调度锁、状态探测租约、模型检测轮次和关联关系使用逻辑 ID；普通监控、余额、倍率、并发、成本和日志继续使用物理 ID。
- 多实例下逻辑组关系、调度和模型检测继续使用现有数据库行锁/Redis 租约；渠道并发直接复用现有按 channel_id 的实现，不新增并发锁或 Redis key。
- 逻辑组全部成员不可用时返回无可用成员；单成员自动禁用不能永久禁用共享组。

## 11. 实施阶段与回滚

### 阶段 0：模型和只读验证

增加逻辑组表、成员关系 API、请求地址规范化和缓存解析器，不改变请求路由和现有页面；新增“同渠道配置”入口及地址一致性检查。

### 阶段 1：关联关系和调度只读验证

新增关联组配置、地址校验和成员 weight；智能调度以影子模式读取逻辑组关系，验证组级评分和成员 Key 选择，不改变普通监控、成本和渠道页面。

### 阶段 2：状态探测和模型检测

配置、执行范围和运行迁移到逻辑组，每个逻辑目标每轮只执行一次，执行快照保留实际成员；验证成员成本归属、取消、清理、短期凭证，并确认复用现有渠道并发流程。

### 阶段 3：共享智能调度

增加逻辑组路由状态和 Redis 投影；先影子计算评分不写 Ability，再按逻辑组开启投影，验证成员选择、重试、保护、429 和恢复。

### 阶段 4：逐组启用

逻辑组独立启停共享调度、状态探测和模型检测，现有页面和物理 API 保持不变；不新增普通监控或成本聚合。

每阶段使用功能开关和按逻辑组回滚开关。回滚只停止新逻辑调度/探测/检测写入，不删除物理日志、成本和旧路由状态。

## 12. 测试和验收

### 数据与一致性

- SQLite、MySQL 5.7.8+、PostgreSQL 9.6+ 迁移、唯一索引、分页和锁行为。
- 未归组渠道与升级前接口结果一致；成员换组后新旧事件归属正确。
- 删除成员不删除成本、日志、检测报告和调度明细。

### 单渠道数据不变

- 性能、成功率、首字、TPS、延迟、失败分类、成本、余额、倍率和并发仍按原物理渠道统计，现有 API 结果与归组前一致。
- 不生成逻辑组成本、性能或成功率汇总，不改变现有渠道分页、筛选和详情。
- API Key 明细不泄露完整上游 Key，关联审计和检测/调度执行仍可追溯实际 channel_id。

### 调度和故障

- 逻辑组只产生一次评分/主备决策，不因成员数量重复占用候选层。
- 两个成员各自上限为 1 时，同时进入 2 个请求分别进入两个物理渠道；任一 Key 不超过自身上限，第三个请求沿用现有渠道并发流程返回 429。
- 成员权重为 3:1 时，双方可用窗口内的长期流量约为 3:1；成员是否并发已满、租约获取和释放均由现有渠道并发功能负责。
- 单成员满载不应阻塞其他成员；全部成员满载时沿用现有 429 行为。
- 单成员 401、余额不足只排除成员；并发满按现有渠道流程处理，所有成员不可用才排除逻辑组。
- 逻辑组级 5xx/模型不支持进入共享保护，成员级 429 按策略处理。
- 完整调度、实时事件、冷却、固定主渠道和 revision 冲突在多实例下无旧写覆盖。

### 检测、探测和前端

- 同一逻辑组同一目标每轮只生成一次逻辑执行；实际成员选择和 Key 记录正确。
- 检测/探测请求复用成员现有并发上限；中途成员删除不会错误结算。
- 现有渠道页面的权限、分页、筛选、空数据和逐渠道操作保持不变；新增同渠道配置入口的成员操作正确，旧 channel_id API 仍可用。

## 13. 已确定的产品约束

本方案不再设置待产品确认项，以下规则按已确认的需求执行：

1. 组内流量使用关联表中的单一成员 weight，默认 1；不复用物理渠道 Channel.Weight，也不受智能调度 Ability.Weight 覆盖。成员 weight 为 0 时组内全部为 0 则平均分配，存在正 weight 时按正 weight 分配。
2. 429 默认成员级；只有明确属于上游账号级限流时才进入逻辑组保护。
3. 现有页面和渠道视图不改，只新增“同渠道配置”入口；不增加父子行、逻辑视图切换或新的监控页面。
4. 普通监控、成本、余额、倍率和并发不纳入逻辑组共享，继续按物理渠道处理。
5. 归组资格只校验规范化请求地址相同；不再增加 Provider、模型、Header、代理、组织或成员兼容指纹等产品条件。

智能调度数据、状态探测、模型检测和各自执行范围按逻辑组共享已确定：每个逻辑目标每轮只生成一份共享执行，请求仍通过组内分配器选择一个实际 Key，随后复用该成员现有渠道并发流程；普通监控和成本不共享。

以上约束已覆盖本次实现所需的产品决策，当前没有待产品确认项。后续发现的问题按实现缺陷、兼容性风险或发布阻断项处理，不再回退为产品待确认。

## 14. 代码落点参考

| 模块 | 主要落点 |
| --- | --- |
| 身份、缓存、成员关系 | model/channel.go、model/channel_cache.go、新增逻辑组 model/service/controller |
| 普通监控与成本 | 现有 model/service/controller 代码保持物理渠道逻辑，本方案不新增逻辑组聚合 |
| 性能、成功率、成本 | model/channel_monitor_performance.go、model/channel_monitor_success.go、controller/channel_monitor_cost.go、model/channel_daily_cost.go |
| 智能调度 | controller/channel_ratio_monitor_schedule*.go、model/channel_smart_schedule_*.go、service/channel_monitor_redis_*.go |
| 倍率/余额独立链路 | model/channel_ratio_monitor.go、service/channel_daily_cost.go、倍率更新 controller |
| 状态探测 | model/channel_status_probe.go、状态探测 controller/service/worker |
| 模型检测 | model/channel_model_detection.go、service/channel_model_detection_*.go、controller/channel_model_detector_fixed_executor.go |
| API 和页面 | router/channel-monitor-router.go、web/src/features/channel-monitor/ |

## 15. 完成定义

当两个或多个物理渠道加入同一逻辑组后：

1. 现有渠道列表、渠道详情、渠道监控、模型检测、状态探测、调度和成本页面保持不变，不出现父子行或新的逻辑视图切换。
2. 后台智能调度、模型检测和状态探测按逻辑组共享数据；实际请求先由智能调度选择逻辑组，再按成员 weight 选择具体渠道。普通监控和成本仍按物理渠道。
3. 多 Key 的并发额度可以叠加使用；并发限制、租约、释放和 429 完全复用现有按渠道/Key 的功能。
4. 模型检测和状态探测只维护一份共享配置、执行范围与结果，每个逻辑目标每轮只发一份请求，执行可追溯实际成员和 Key 脱敏标识。
5. 余额、倍率、倍率历史、成本基线和成员级失败状态完全独立。
6. 仅新增一个“同渠道配置”入口，用于配置成员渠道和成员 weight；现有渠道页面不增加逻辑组配置项，物理渠道原有 weight 保持不变。
7. 所有日志、成本、审计、检测执行和请求仍可通过 channel_id 定位真实物理渠道。
8. 未归组渠道、旧 API 和三种数据库行为与改造前兼容。

## 16. 执行计划：可验证小任务与并行调度

本节是实际开发执行清单。主会话负责拆分任务、维护接口契约、协调依赖、合并结果和最终验证；子代理只执行分配到的边界任务，不自行扩大范围或修改其他代理负责的文件。每个任务完成后必须返回：修改文件、验证命令、验证结果、未解决风险和是否影响其他任务。

### 16.1 主会话职责

主会话不把核心决策下放给子代理，固定负责以下事项：

1. 冻结本方案范围：只共享智能调度、状态探测和模型检测；普通监控、成本、余额、倍率、并发和日志保持物理渠道。
2. 先完成现状基线和接口契约，再启动依赖这些契约的并行任务。
3. 为每个子代理指定明确的文件边界；同一文件不得由多个子代理同时修改。
4. 审核所有数据库模型、迁移、API 响应兼容性和 `channel_id`/`logical_channel_id` 映射。
5. 每个并行波次结束后执行集成验证；失败时先暂停下游波次，不允许子代理各自绕过失败继续扩展。
6. 最终运行 `git diff --check`、后端测试、三数据库迁移验证、前端相关测试和回归验收，并确认普通单渠道行为未变化。

### 16.2 子代理通用执行规则

- 一个子代理只接收一个可验收的小任务；任务描述必须包含目标、允许修改的目录、禁止修改的目录、依赖和验收命令。
- 优先新增下游文件，避免无必要修改上游已有文件；必须修改上游文件时在回报中说明原因。
- 不修改受保护的项目标识，不修改无关未提交改动。
- 不直接重写普通监控、成本、余额、倍率和并发链路；这些模块只允许增加兼容映射或回归测试。
- 子代理完成后不得自行合并其他任务的改动；主会话统一处理冲突、合并和回滚。
- 代码任务必须同时提交行为测试；文档或契约任务必须提供示例请求/响应和验证结果。

### 16.3 任务清单

| 编号 | 小任务 | 依赖 | 是否可并行 | 子代理交付物 | 主会话验收点 |
| --- | --- | --- | --- | --- | --- |
| P0 | 建立基线：梳理渠道选择、智能调度、状态探测、模型检测、并发和普通监控的现有入口，记录未归组行为 | 无 | 否，首个任务 | 基线说明、关键函数/表/API 清单 | 主会话确认范围与文件边界；未归组请求、成本和普通监控行为有可复现实例 |
| P1 | 定义逻辑组与成员关系模型：逻辑组表、成员表、revision、成员 weight、地址摘要字段 | P0 | 可与 P2 并行 | GORM 模型、索引建议、跨库迁移草案、模型测试 | SQLite AutoMigrate 通过；唯一成员约束、revision 和 weight 边界测试通过 |
| P2 | 请求地址规范化和成员预检：只比较规范化请求地址，拒绝地址不一致的归组 | P0 | 可与 P1 并行 | 纯函数/服务、预检结果结构、地址边界测试 | scheme/host 大小写、默认端口、末尾斜杠、路径和敏感参数案例通过；Key 不出现在摘要 |
| P3 | 同渠道配置管理 API：创建、查询、替换成员、修改 member weight、删除、revision 冲突 | P1、P2 | P1/P2 完成后 | Controller/service/router、权限校验、API 测试 | 管理员权限、重复成员、跨组成员、地址不一致、并发更新冲突均返回稳定错误；旧 API 不变 |
| P4 | 同渠道配置入口：只新增配置入口，不改现有渠道/监控/成本页面布局和查询协议 | P3 | 可与 P5 并行；P5 通过后还可与 P7 并行 | 新入口组件、调用 API、空态/错误态测试 | 现有页面快照/测试不变；入口可创建、编辑、移除关联和 member weight |
| P5 | 逻辑组运行时解析器和缓存：channel_id → logical_group_id、成员快照、revision | P1、P3 | 可与 P4 并行 | service/model cache、失效刷新、缓存测试 | 未归组返回虚拟逻辑 ID；换组后新任务读取新 revision；缓存构建失败保留上一份完整快照 |
| P6 | 智能调度逻辑组评分：将已归组成员作为一个调度候选，聚合样本只用于调度，不写普通监控汇总 | P5、P7 | 可与 P8/P9 并行 | 逻辑组路由状态、评分/投影适配、影子模式测试 | 同一逻辑组只产生一次评分/主备决策；未归组渠道和非托管池行为不变 |
| P7 | 逻辑组成员选 Key：逻辑组被选中后按关联表 member weight 选择健康成员 | P5 | P5 验收后可与尚未结束的 P4 并行；必须先于 P6/P8/P9 的集成验收完成 | 成员选择器、权重边界、成员排除测试 | weight 3:1 的比例趋势、全 0 平均、成员禁用/余额不足/冷却排除；最终调用现有渠道并发流程 |
| P8 | 状态探测共享：按逻辑组配置和目标每轮只执行一次，记录实际成员 | P3、P5、P7 | 可与 P6/P9 并行 | 状态探测适配、执行快照、取消/清理测试 | 同组多 Key 不重复探测；成员认证失败不污染其他成员；成本仍记录实际 channel_id |
| P9 | 模型检测共享：按逻辑组配置和目标每轮只执行一次，记录实际成员 | P3、P5、P7 | 可与 P6/P8 并行 | 模型检测适配、轮次/目标/执行记录、重试测试 | 同组多 Key 不复制目标；可重试错误按策略改投；检测成本保留实际成员归属 |
| P10 | 普通单渠道回归保护：确认监控、成本、余额、倍率、并发、日志不读取逻辑聚合 | P3、P5 | 可与 P6/P8/P9 并行 | 回归测试、API 对账脚本 | 归组前后普通 API 响应结构和物理数据边界一致；不生成逻辑组成本/性能汇总 |
| P11 | 数据库兼容与迁移验证：SQLite、MySQL 5.7.8+、PostgreSQL 9.6+ | P1、P3、P5 | 可与 P10 并行 | 迁移脚本/测试记录、索引和锁验证 | 三数据库建表、唯一约束、分页、revision 更新和回滚路径通过 |
| P12 | 集成测试和灰度开关：按逻辑组启用/关闭调度、探测、模型检测 | P6、P7、P8、P9、P10、P11 | 否，收敛任务 | 功能开关、灰度用例、回滚说明 | 关闭逻辑组功能后恢复旧物理渠道选择；单组回滚不影响其他组 |
| P13 | 最终验收和发布清单 | P12 | 否，最后任务 | 验收报告、变更文件清单、风险和回滚命令 | 主会话完成全量测试、diff 检查、文档核对和发布阻断项确认 |

#### P6/P8/P9 可独立验收子项

三条业务线可以由不同子代理并行，但每条线内部仍按下表逐点交付。主会话每验收一项就记录测试证据；前一项未通过时，同一业务线不得把后续项标记完成。

| 子项 | 可验证交付 | 依赖 | 主会话最小验收 |
| --- | --- | --- | --- |
| P6.1 | 建立 `(logical_group_id, revision, group, model)` 的独立样本与路由状态，不覆盖物理 Ability/route 基线 | P5、P7 | schema/编码往返测试；关闭功能后物理基线原样可读 |
| P6.2 | 内存缓存与 `MEMORY_CACHE_ENABLED=false` 两条选择路径都把同组成员合并为一个候选 | P6.1 | 两条路径分别验证同组只出现一个逻辑候选、未归组行为不变 |
| P6.3 | affinity、共享评分、采样债务、动态保护和主备意图统一读写逻辑状态 | P6.1 | 同一逻辑身份只生成一份决策；旧 revision 写入被拒绝且不回写物理样本 |
| P6.4 | 逻辑组被选中后按 P7 选物理成员，busy/禁用成员可排除并改投 | P6.2、P6.3 | 仍调用物理并发租约；所有成员 busy 才返回原有 busy/429 行为 |
| P6.5 | 全局/单组关闭时停止逻辑状态读写并即时恢复物理调度路径 | P6.4 | 开关前后对照 route/Ability；关闭期间不污染逻辑或物理基线 |
| P8.1 | 使用独立逻辑探测配置/状态存储，保留旧物理配置作为回退基线 | P5、P7 | 启用共享不删除/覆盖物理配置；关闭后原配置完整恢复 |
| P8.2 | 每轮冻结逻辑 ID、revision、成员和目标，并按逻辑身份去重 claim | P8.1 | 同组每目标一份执行；关系变更后活动任务继续使用旧快照 |
| P8.3 | 按模型支持范围和 P7 weight 选择成员，busy 时尝试其他成员 | P8.2 | 实际成员调用物理并发；全部 busy 才返回 busy |
| P8.4 | 执行历史保留实际 channel_id，调度样本使用冻结逻辑身份 | P8.3 | 成本/执行物理可追溯；旧 revision 样本不按当前成员关系重解析 |
| P8.5 | 全局/单组关闭时恢复物理探测配置、状态、周期和执行 | P8.4 | 关闭前后 API 对照；其他逻辑组不受单组停用影响 |
| P9.1 | Run 创建时冻结逻辑 ID、revision、成员、weight 和目标范围 | P5、P7 | 删除/停用关系后旧 Run 仍按冻结快照执行 |
| P9.2 | 同组配置、目标、轮次和结论只维护一份，旧 channel_id API 做成员投影 | P9.1 | 同一目标不复制 Run/Execution；成员查询结果一致 |
| P9.3 | Relay 过滤物理状态、模型支持和可判定余额，busy 时改投其他成员 | P9.1 | 每次尝试调用物理并发；全部 busy 才返回 busy |
| P9.4 | execution、cost 和审计保存实际物理成员，不生成逻辑组成本汇总 | P9.2、P9.3 | 成本按 channel_id 对账；普通成本 API 无逻辑聚合 |
| P9.5 | 全局/单组关闭时 overview/history/config/run 全部恢复物理路径 | P9.4 | 关闭期间不共享结果；重新启用后只影响新任务 |

### 16.4 并行波次

依赖关系固定为以下波次，主会话按波次调度，不提前启动未满足依赖的任务：

#### Wave 0：基线

- 执行 P0。
- 主会话输出接口契约、数据边界和文件所有权表。
- P0 未完成前不启动实现代理。

#### Wave 1：基础模型与校验

- 并行执行 P1（数据模型）和 P2（地址规范化）。
- 主会话在两项都通过单元/迁移验证后，冻结 P3 的 API 契约。

#### Wave 2：管理入口、缓存和功能适配

- 先执行 P3（管理 API）；P3 通过后并行执行 P4（前端入口）和 P5（运行时缓存）。
- P5 通过后执行 P7（成员选择器）；P7 可与仍在进行的 P4 并行，但必须独立完成选择器契约和定向测试。
- P6、P8、P9 必须使用 P3/P5 的稳定关系接口和同一个 P7 选择器，不允许各自定义第二套成员关系读取或组内选 Key 方式。

#### Wave 3：三条业务线并行

- P7 经主会话验收后，并行执行 P6（智能调度）、P8（状态探测）、P9（模型检测）。
- P6/P8/P9 都必须从冻结的 P5 快照调用同一个 P7 选择器，并在选出成员后进入现有物理渠道并发流程；任何一条业务线不得复制选择算法。
- 同时执行 P10（普通单渠道回归）和 P11（三数据库验证），它们不得修改业务实现来“适配”失败测试。

#### Wave 4：集成与发布

- Wave 3 全部通过后执行 P12。
- P12 通过后由主会话执行 P13；P13 不再引入新功能，只做验证、清单和发布阻断判断。

### 16.5 每个任务的验收模板

子代理回报必须使用以下固定格式，主会话据此决定是否进入下一波次：

```text
任务编号：P?
完成内容：
修改文件：
未修改但检查过的关键文件：
验证命令：
验证结果：
失败/风险：
依赖的接口或数据契约：
是否可以进入下一波次：是/否
```

主会话验收顺序：

1. 先检查任务是否越界、是否修改了其他代理负责的文件。
2. 再运行子代理提供的最小验证命令。
3. 检查未归组和普通单渠道回归。
4. 检查逻辑组共享范围是否仅限智能调度、状态探测和模型检测。
5. 最后才允许合并到集成分支并启动依赖任务。

### 16.6 最小可验证验收矩阵

| 验收项 | 必须证明的行为 |
| --- | --- |
| 未归组兼容 | 未归组渠道的选路、监控、成本、余额、倍率、并发、日志和旧 API 与改造前一致 |
| 地址归组 | 只有规范化请求地址相同才能保存；Key、倍率、模型映射差异不作为自动归组依据 |
| 调度共享 | 同一逻辑组只产生一份智能调度评分/主备决策；成员 weight 只负责组内选 Key |
| Key 并发 | 选出成员后调用现有渠道并发功能；不新增逻辑组并发计数或租约 |
| 状态探测共享 | 每个逻辑组目标每轮只执行一次，记录实际成员，成员失败不污染其他成员 |
| 模型检测共享 | 每个逻辑组目标每轮只执行一次，重试和成本记录保留实际成员 |
| 普通数据隔离 | 性能、成功率、成本、余额、倍率、并发和普通监控不生成逻辑组汇总 |
| 页面兼容 | 只新增“同渠道配置”入口；现有渠道视图、页面结构和 API 不变 |
| 关系变更 | 新任务使用新关系，进行中的探测/检测使用冻结快照；旧物理历史不被改写 |
| 数据库兼容 | SQLite、MySQL、PostgreSQL 迁移和 revision 并发更新均通过 |
