# 渠道监控查询、数据处理与大并发审查报告

## 1. 结论摘要

本次对渠道监控的 HTTP 查询、数据库聚合、Redis 实时投影、Stream 消费、页面快照、成本账本和并发控制进行了只读审查。

结论是：

- 短时间窗口、低并发下，基础查询和账本增量逻辑基本可用。
- 大时间窗口、高渠道数、高 API Key 数或大量管理员同时访问时，存在明显的查询放大、内存放大、Redis 积压和数据库锁等待。
- 任务成本重复回调可能导致余额、Token 或 `used_quota` 重复扣减/返还，属于最高优先级业务一致性风险。
- 事件队列满、Redis 投影写入失败、Stream 消费落后时，业务请求通常不会失败，但实时监控可能缺样本、缺成本或长时间滞后。
- 未发现能够证明整个系统必然立即中断的 P0 问题，但以下 P1 风险应优先处理。

后续修复轮次已按本报告的实施顺序落地业务代码、迁移注册和回归测试；以下修复后结果以本地 SQLite/MySQL 可执行验证为准，PostgreSQL 仍受本机无实例限制。

## 1.1 后续验证执行约定

- 以本机 Docker 中的 MySQL 和 Redis 为主：MySQL 使用容器 `mysql` 暴露的本地 3306 端口和数据库 `new-api`；Redis 使用容器 `redis` 的本地 6379 端口，验证默认使用独立 DB 15。
- MySQL 凭据仅由本机运行环境（既有 `.env`/进程配置）在执行时提供，不写入仓库、文档或日志；本轮未输出密码。
- 验证数据允许保留，但必须使用带 run/verify 前缀的表名、key、Stream 和 consumer 名称；禁止执行 `FLUSHALL`、`FLUSHDB` 或删除非本次验证创建的数据。
- 验证任务按执行方案逐项串行调度：子代理执行单项，主会话汇总结果、更新文档并决定下一项。
- 发现新问题时，先更新本报告和独立执行方案，再重新执行受影响的验证项；未经额外确认不修改生产代码。

## 1.2 动态验证总览

| 验证项 | 本机结果 | 关键结论 |
|---|---|---|
| V01 | 修复后通过 | 成本日聚合和日期分页下推数据库，详情不再重复分页读取 |
| V02 | 修复后通过 | API Key 概览采用有界聚合（最多 1000 条）并以残差保持金额守恒，owner 批量回填 |
| V03 | 修复后通过 | 详情 rows 复用 chart 结果，Redis 页面查询复用请求级 projection memo |
| V04 | 修复后部分通过 | model-leading 索引和 latest SQL 聚合已补齐；group/API-Key-only 仍依赖窗口和维度上限 |
| V05 | 修复后通过 | Redis 查询窗口、hash 字段、响应字节、维度和错误样本均有软/硬上限及降级 |
| V06 | 修复后通过 | 明细与 metadata 共享一次 projection 读取，metadata 不再 HGETALL 完整业务 hash |
| V07 | 修复后通过 | 概览复用已加载 monitor 配置，并发/设置冷加载分别做请求内复用和串行缓存填充 |
| V08 | 部分通过 | singleflight、fence、stale fallback 通过；真实 fan-out 计数待补 |
| V09 | 修复后通过 | 队满、Redis 失败和停止超时写入 outbox，可重放且保留失败/死信观测 |
| V10 | 修复后通过 | 投影事务预校验、WATCH 退避、marker/state TTL 和失败补偿语义已统一 |
| V11 | 语义通过/压力待补 | pending、接管、poison、trim 通过；长时吞吐/handler 卡死未压测 |
| V12 | MySQL/SQLite 通过，PG 阻塞 | SQLite 并发写出现 `SQLITE_BUSY`；MySQL 有行锁等待无死锁 |
| V13 | 修复后通过 | dirty-minute lease 可续租，SQLite_BUSY 和 pending/dead-letter 有界补偿 |
| V14 | 修复后通过 | 带 EventId 的 direct batch 拒绝绕过 outbox，outbox apply 保持幂等 |
| V15 | 修复后条件通过 | 任务账务和成本事件有幂等/CAS/补偿；SQLite 锁错误分类重试仍需真实多进程压力验证 |
| V16 | 修复后通过 | pool isolation/mode/shared/degraded roles 已进入 controller API，PoolStats 保留等待指标 |
| V17 | MySQL/SQLite 通过，PG 阻塞 | 唯一冲突、SavePoint、锁方言、分离 LOG_DB 失败分类均通过 |
| V18 | 修复后通过（除环境噪声） | model/service/controller 全量通过；`-race` 因 `CGO_ENABLED=0` 未执行，relay HTTP/2 单测仍有 Windows 网络时序噪声 |

上述结果均来自本机验证或代码级证据；修复轮次已修改生产代码并补充回归测试。MySQL/Redis 验证数据按约定保留，PostgreSQL 结论仅标记为环境阻塞，不外推为三库通过。

## 2. 审查边界与复杂度符号

审查范围包括：成本概览、性能和成功率、并发快照、页面快照、Redis 事件写入和消费、分钟聚合、脏分钟修复、任务计费修正、三数据库事务差异及现有测试。

复杂度说明：

- `C`：渠道数；`D/M`：天数或分钟数；`K`：API Key/高基数维度数。
- `R`：日成本查询命中行数；`L`：日志主查询命中行数；`E`：重试补查的窗口外错误日志行数。
- `N`：事件批大小；`Q`：并发请求或并发回调数。

## 3. 风险清单

### P1-01：任务计费并发回调可能重复扣费或退款

触发条件：同一任务同时收到重复回调、重试回调或退款回调。

`service/task_billing.go:190`、`:243` 读取旧的 `task.Quota` 后，在独立步骤中调整余额、Token、`used_quota` 并更新任务，没有任务级行锁或 CAS。`model/channel_task_cost_event.go:152` 的成本事件锁不能覆盖这些账务操作。

影响：两个并发回调可能分别基于同一个旧值计算 delta，造成双扣、双退或任务配额错误。此问题直接影响业务账务，而不仅是监控显示。

V15 本机动态验证已复现：MySQL Q=10/50 并发 `RecalculateTaskQuota` 时任务 quota/成本事件/daily cost 只记一次，但 user quota、used_quota、token、channel used 和日志按 Q 次重复调整；`RefundTaskQuota` 同样重复退款。当前生产 polling 调用通常先执行 task 状态 CAS，因此这是 helper 被重试或旁路调用时的条件风险，仍需验证多 worker CAS 是否始终只有一方获胜。旧 `updatedAt` 可覆盖新修正，说明成本事件 CAS 未做时间单调校验。成本修正失败发生在资金/usage/task quota 提交之后，会留下账务已调整而 event/daily 未同步；Redis 入队失败时只重试 3 次后丢弃，调用方未持久化 pending/dead-letter。结论：helper 并发幂等、旧版本覆盖、成本失败和投影入队失败是生产代码路径风险，前两项需结合外层 CAS 合同复核，不能归因于测试 fixture。

V15 外层 CAS 补测：MySQL Q=10/50 refund/settle 多 worker 均 CASWins=1，账务、任务状态、成本事件和日志只执行一次，确认 polling CAS 可阻断重复 helper。SQLite 也只有一个 winner，但重复运行中至少 2/12 场 settle 遇 `SQLITE_BUSY`，出现 task/user/channel 已结算而 event/daily correction 未落库；需对 SQLite 写锁错误做分类重试或补偿，不能仅依赖外层 CAS。

### P1-02：成本详情接口全量加载，分页没有下推到 SQL

`model/channel_daily_cost.go:327` 的日期分页先读取整个时间范围，再在 Go 中聚合、排序和截断；`page_size` 不会减少数据库扫描、网络返回或 Go 内存。

成本详情控制器还会先读取全量日成本，再读取 chart 数据和 page 数据，见 `controller/channel_monitor_cost.go:366`、`:471`、`:764`。API Key 成本同样通过 `Find` 全量加载，见 `model/channel_daily_api_key_cost.go:435`。

影响：90 天窗口下，处理量近似 `O(R + D log D)`，内存至少 `O(R + D)`；高并发管理员访问时容易造成数据库、CPU、GC 和响应体放大。

MySQL 8.4.10 实测：`C=100`、`D=90`、9000 条验证数据时，`page_size=10` 仍读取 1000 行，`page_size=100` 读取 9000 行；详情端点因重复读取全范围，单次总扫描约 19000～27000 行。日期结果正确，但未达到真正分页标准。

端到端实测（MySQL 8.4.10 + Redis 8.8.0）显示：摘要快照绕过路径为 1 次 DB；详情路径为 11 次 DB，rows/chart/page 对同一范围重复读取 3 次。快照命中可将 DB 查询降为 0，但 Redis status cache miss 时单次摘要仍产生 1337 个 `HGETALL`，说明缓存只能避免数据库链路，不能解决投影读取规模。

Redis 8.8.0 实测成功率明细最大窗口：明细和 metadata 各执行一次完整 projection，合计 4 个 Pipeline、2886 个 `HGETALL`，读取量约为单次的两倍；两次 watermark 一致，但重复消耗 Redis 和 Go 解析资源。

页面快照验证中，同一 identity 的 50 并发请求只产生 1 个 builder，10 个不同 filters 各自并行构建；force 与普通刷新没有重复 builder，单调发布和冷启动/stale fallback 测试通过。管理刷新固定 6 路 fan-out 已由静态代码确认，但真实 DB/Redis 扇出尚未动态测量。

### P1-03：Redis 投影长窗口使用大量 `HGETALL`

`service/channel_monitor_redis_shared_projection.go:933` 对每个分钟桶执行 `HGETALL`，1440 分钟窗口约 1441 个分钟命令，再加最多 2 个日成本 hash。代码没有单 hash 字段数、总响应字节或维度基数上限。

低维度示例下，1440 分钟解析内存已经可能达到几十 MB；模型名、错误样本和 API Key 维度变长后会继续放大。

影响：Redis 响应体、网络、Go 反序列化和聚合内存均随窗口线性增长，多个并发请求会同时放大。

Redis 8.8.0 实测（DB15）：M=15、`C=100,K=100` 已产生 225,776 个字段、约 17.1 MB RESP、Go heap 峰值约 36.6 MB；按相同字段模型外推 M=1440、`C=100,K=100` 约 1.54 GB RESP，说明长窗口可能触发 OOM。M=1/15 的 Pipeline 数固定为 2，但没有数据量上限。

### P1-04：成功率明细会重复完整扫描同一 Redis 窗口

`controller/channel_ratio_monitor_performance.go:163` 先读取成功率明细，随后 metadata 又通过 `controller/channel_monitor_realtime_response.go:57` 重新读取投影。

每次投影查询包含分钟和日成本两个 Pipeline。1440 分钟窗口下，明细和 metadata 合计最多约 4 个 Pipeline、2886 个 `HGETALL`，还不包括 status 查询。

影响：Redis RTT、响应体解析、CPU 和内存重复消耗；请求跨分钟时两次查询甚至可能使用不同窗口，造成元数据与明细口径不一致。

### P1-05：Stream 消费者是全局单线程，且 handler 没有独立超时

`service/channel_monitor_redis_consumer.go:303` 使用全局 lease，跨实例只有一个消费者，每批最多 100 条；pending 优先于新消息。`OperationTimeout` 只限制 Redis 命令，handler 本身没有 batch deadline。

Redis 8.8.0 最小 Stream 观测和 15 个 consumer 单元测试均通过，证明 pending、XAUTOCLAIM、毒消息隔离和 trim 语义正确；真实长时间高并发与 handler 卡死尚未压测，单消费者吞吐风险仍然成立。

影响：

- 写入速率超过单批处理能力时，PEL 和 stream lag 持续增长。
- poison 消息最多重试 20 次，可能让同批新消息饥饿。
- handler 卡住时 heartbeat 仍续租，消费者不会进入下一轮。
- pending 会固定 trim 水位，导致 Stream 历史无法及时删除。

### P1-06：事件 writer 队满或 Redis 失败时会丢实时事件

`service/channel_monitor_event_writer.go:132` 使用默认容量 8192 的有界队列，非阻塞入队；单 writer 串行发布，失败最多重试 3 次。队满、Redis 不可用或停止超时都会丢事件。

`controller/channel_monitor_event_emit.go:113`、`service/channel_monitor_model_detection_event.go:54` 等调用方忽略入队状态。

影响：业务请求通常不被阻塞，但实时成功率、成本和水位会缺样本；单事件是否丢失无法由调用方感知。目前只能从聚合健康指标间接观察。

Redis 8.8.0 实测：队列容量 8192 时，10000 个事件有 1808 个立即丢弃；Redis 连续失败最多尝试 3 次后 drop。正常 drain、取消和结构化 dropped/publisher_failed 指标均可观测，但没有 replay/dead-letter，生产调用方也普遍忽略 PublishStatus。

Redis 8.8.0 投影写入实测：并发 WATCH 冲突和同事件去重通过；但预置某个 global hash 为非数值后，`EXEC` 类型错误不会回滚其他 scope/state/marker，形成“已标记但指标不完整”。强制冲突可连续 128 次重试且无退避；过期 marker/state 后保留 hash 的迟到重放会再次累加。

### P1-07：Redis 投影写入存在事务误标记和重试放大

`service/channel_monitor_redis_shared_projection.go:295` 在事务中执行聚合命令后写 event marker。Redis `EXEC` 对单个 `HINCRBY*` 运行时错误不会回滚其他命令，marker 仍可能写入，后续重放会被去重，造成永久漏记。

同文件 `:284` 的 WATCH 冲突最多无退避重试 128 次，每次都重新读取和重排整批事件，热点下会形成 Redis 命令风暴。成本状态和 marker 仅保留约 48 小时，迟到重放还可能重复累加。

### P1-08：分钟聚合事务持锁时间过长，脏分钟重试会放大查询

`model/channel_monitor_minute.go:1055` 的部分路径在主库事务内扫描日志，随后连续删除聚合行并以 500 行批次插入，锁一直持有到最终提交。

- SQLite：无行级 `FOR UPDATE`，单写者下容易 `database is locked`。
- MySQL/InnoDB：范围删除可能产生 next-key/gap lock，增加锁等待和死锁概率。
- PostgreSQL：长事务会延迟旧版本回收并造成 MVCC 膨胀。

`model/channel_monitor_dirty_minute.go:305` 对重试日志同步查询窗口外错误；高重试率下可能出现近似 `O(r²)` 的扫描和 upsert。`service/channel_monitor_aggregation.go:180` 的 2 分钟 lease 没有续租，慢重建可能被另一 worker 重复领取。

V12 本机动态验证补充：MySQL 8.4.10 的普通聚合、WithState、Upgrade、CacheBackfill、RegularBackfill 在 M=15/1440 均完成；Q=4 同范围 WithState 无业务错误，但出现 6 次行锁等待、累计约 1382ms，无死锁。SQLite 四路径均可重建完整结果，但 Q=4 出现 3 次 `sqlite_busy`，说明并发写必须串行或按数据库错误分类重试。相邻分钟并发写入两库均成功，事务失败后未观察到半套聚合。PostgreSQL 因本机 5432 无监听未执行，`LOG_DB` 分离的跨库非原子场景仍需补测。

V13 本机动态验证补充：同一 request 的 r=1/5/20 重试在 MySQL/SQLite 均固定为 2 次日志查询，扫描为 2/6/21 行，route/API-key 各 upsert 一次；request_id 高于 500 后补查按批次线性增加。CAS claim/release/complete 防止重复领取和 stale complete 覆盖，MySQL barrier winners=1；SQLite 并发会出现 `SQLITE_BUSY`。lease 没有续租，超过 2 分钟的慢重建可被再次领取并重复执行；dirty marker 写入失败时 source log 已落库但 marker/state 均为空，且无自动补偿，需补偿队列或可恢复重试。

### P2-01：页面快照存在 fan-out、回滚和冷启动 503 边界

管理操作在 `controller/channel_monitor_page_snapshot.go:119` 最多并发刷新 6 路页面，并等待共享 15 秒上下文。不同 identity 没有全局构建限流。

普通快照有 singleflight、lease、fence 和单调发布保护；但 force 刷新绕过单调校验，旧 revision 可能覆盖较新快照，见 `service/channel_monitor_page_snapshot.go:416`。

冷启动或超过保留时间时，控制器只提交后台刷新并返回 503；Redis 禁用或某些连接错误场景不会触发后台重建。

### P2-02：概览和历史查询存在可避免的重复工作

概览先读取渠道列表，并发快照服务又读取一次，见 `controller/channel_ratio_monitor.go:492` 和 `service/channel_concurrency.go:315`。并发配置缓存过期时还会重新读取 `ChannelRatioMonitor`。

MySQL 8.4.10 实测确认：概览冷缓存和缓存命中请求均调用 `GetAllChannelsForMonitor` 两次；配置查询冷缓存只调用一次、命中时不重复。每次请求还会读取一次 monitor settings。接口结果为 200，但渠道列表重复查询得到复现。

历史分钟查询中，纯 model 过滤缺少 model-leading 索引；latest 值先全量排序再在 Go 中取首行，见 `model/channel_monitor_minute.go:1339`。API Key 明细按高基数列分组，长窗口时临时聚合空间较大。

MySQL 8.4.10 实测：1440 分钟窗口的 channel-only/channel+model 查询可利用索引；group-only、model-only、API Key 明细分别出现 5,760、5,760、23,040 行全量扫描，latest 也全量排序 5,760 行。半开时间口径正确，但长窗口性能限制得到复现。

### P2-03：成本展示归属和直接重放幂等性有边界问题

`model/channel_daily_api_key_cost.go:388` 将可变名称纳入聚合键，改名可能拆成多条；控制器按 APIKeyId 合并时忽略 fingerprint，并可能以最后一行覆盖展示名称，见 `controller/channel_monitor_cost.go:541`。

MySQL 8.4.10 实测在 `C=100`、`K=100`、`D=90` 下产生 900,000 条 API Key 日成本记录，overview 响应约 1.39 MB，totals 原始聚合行 10,100；owner 查询是批量 `IN`，没有 N+1。金额守恒通过，但改名跨日后展示名称被旧名称覆盖，高基数全量加载风险得到复现。

成本账本增量使用原子更新和溢出保护，正常生产路径不会回绕；但 `ChannelDailyCostDelta.EventId` 只是携带字段，直接绕过 outbox 重放仍可能重复累加。

V14 本机动态验证补充：MySQL Q=1/10/50 并发写入均无错误且 channel/API-key 金额守恒；Q=50 首行竞争单条 INSERT 最慢约 206–364ms。接近上限的 channel 或 API-key 增量会完整返回溢出错误，两侧原值保持不变。直接绕过 outbox 用同 EventId 重放两次后两侧成本和计数均翻倍，SQLite 亦复现；因此 EventId 幂等只在 outbox apply+processed 事务内成立，底层 direct API 若可被调用必须增加唯一 marker 或明确禁止绕过。

### P2-04：Redis 连接池隔离状态缺少显式健康标记

V16 本机动态验证：开启 `REDIS_CLIENT_POOL_ISOLATION` 时 user/write/read/consumer 使用独立连接池，任一角色饱和不会拖垮其他角色；关闭时四角色共享同一 client，read 池饱和会使 user 请求出现约 100ms pool timeout，隔离语义符合预期。PoolStats 可提供 misses、timeouts、命令数和延迟，但 status JSON 没有 isolation 布尔字段，也没有按角色输出拥塞/degraded 标记，运维只能通过累计计数差分判断；建议补充显式状态字段和角色标签。

## 4. 已有保护与测试结论

### 4.1 已有的合理设计

- 日成本和 API Key 成本使用条件增量、唯一冲突恢复和溢出保护。
- 页面快照具备 singleflight、Redis lease、fence、stale-while-revalidate。
- dirty-minute 有 claim/release/complete 条件保护。
- Redis 读、写、消费默认使用独立连接池。
- 事件去重、成本修正状态单调比较和分钟聚合 retry/final 去重已有基础保护。

### 4.2 测试现状

- `go test ./model -run 'ChannelMonitor|Verify02' -count=1`：通过。
- `go test ./service -run ChannelMonitor -count=1`：通过。
- controller 定向测试唯一主要失败为 `controller/channel_monitor_cost_test.go:532` 的空 `AffCode` fixture 触发 SQLite 唯一约束；`options` 表相关日志属于测试生命周期/异步 DB 噪声，未形成业务断言失败。
- 当前缺口：PostgreSQL 实例/DSN（本机 5432 无监听）、Redis 6/7 网络故障差异、Stream 集群语义、完整 pre-consume→settle/refund 并发集成测试，以及生产 polling 外层 CAS 的多 worker 动态矩阵。
- `go test -race` 在当前环境因需要 `CGO_ENABLED=1` 未执行成功。

V17 本机兼容性回归：MySQL 8.4.10 与 SQLite 的最小 V01/V12/V14/V15 合同均通过，覆盖 500 批插入、范围删除、唯一冲突分类、SavePoint/RollbackTo、溢出/非法批次回滚和 `lockForUpdate` 方言（SQLite 跳过、MySQL 使用 `FOR UPDATE`）。分离 `LOG_DB` 时成功路径及主库/日志库缺表失败均明确报错，无静默提交。PostgreSQL 因 5432 无监听且无 DSN 未执行，不能据此宣称三库完整通过。

V18 测试基线：model 目标测试通过；service 首次快照测试出现一次不稳定 old 结果，但单测和整套重跑通过；controller 稳定失败是 `users.aff_code` 空值唯一约束的 fixture 问题（`controller/channel_monitor_cost_test.go:534`），不是业务断言。V09 writer 11 项、V11 consumer 13 项、V15 service 20 项和 model 3 项确定性测试全部通过。当前 Go 环境 CGO_ENABLED=0，`-race` 未执行。

## 5. 对应解决方案

### 5.1 第一优先级：先修业务账务和可靠性

1. **任务计费幂等和并发保护**
   - 为退款、差额结算和重复回调引入稳定的业务幂等键。
   - 在同一事务中使用 `lockForUpdate(tx)` 锁任务行，或使用带旧版本条件的 CAS 更新任务配额。
   - 账务调整、任务 quota 更新、成本事件状态更新必须有明确的提交顺序；冲突按数据库类型分类重试。
   - 重复回调应返回“已处理”而不是再次调整余额。

2. **事件可靠投递**
   - 将实时监控事件写入可重放的 outbox/持久队列，Redis Stream 只作为投影传输层。
   - 保留 event id 去重，补充失败重试、死信和人工/定时重放。
   - 调用方至少记录入队结果和原因；队满、publisher unavailable、invalid event 必须进入计数和告警。

3. **修复投影事务语义**
   - 在进入 Redis 事务前校验所有数值字段和 key 长度，避免 `HINCRBY*` 运行时失败。
   - 只有聚合命令全部成功后才写 marker；更稳妥的做法是使用 Lua 脚本将“校验、聚合、写 marker”放在一个脚本内，并返回明确错误。
   - WATCH 冲突使用有限次数、指数退避和随机抖动，超过上限后保留事件待重放，不直接静默丢弃。
   - marker TTL、cost state TTL 和允许的迟到窗口必须统一，避免 marker 先过期导致重复累加。

### 5.2 第二优先级：消除查询和内存放大

1. **成本聚合和分页下推数据库**
   - 日成本按天的 totals 使用 GORM `GROUP BY`、`SUM`、`ORDER`、`LIMIT/OFFSET`，不要先拉全量明细到 Go。
   - API Key 成本按稳定身份键聚合；名称和展示字段在聚合后单独回填。
   - 详情接口复用同一次基础查询结果，避免 rows/chart/page 三次重叠读取。
   - 保留 SQLite、MySQL、PostgreSQL 的 GORM 方言兼容，不引入单库函数。

2. **减少 Redis 投影读取**
   - 为页面查询增加请求级 memo：明细和 metadata 共用同一次 projection 结果及 watermark。
   - 增加按小时/按天的汇总 hash，长窗口优先读取汇总，不逐分钟 `HGETALL`。
   - 对单 hash 字段数、总字节数、错误样本长度和维度基数设置上限；超限时降级为摘要并记录告警。
   - metadata 只读取所需的时间、水位和处理状态字段，不重新聚合完整业务投影。

3. **补充高基数索引和 latest 查询**
   - 根据实际过滤器增加 model-leading 或组合索引，先用执行计划确认三库效果。
   - latest 值优先使用每组最新值表/专用 key；无法改表时至少缩小候选窗口并固定 tie-break。

### 5.3 第三优先级：提高异步链路吞吐

1. **Stream consumer**
   - 为 handler 设置独立 batch deadline，超时后将批次拆分或转入隔离队列。
   - 将 poison 消息从主消费路径快速隔离，避免 pending 优先策略饿死新消息。
   - 按渠道或哈希分片 Stream/consumer group，保证同一维度顺序的同时提高跨渠道并行度。
   - 监控 `lag`、PEL 数量、最老 pending age、重试次数、trim watermark 和隔离数量。

2. **事件 writer**
   - 保留有界队列，但加入持久化溢出路径；队满时写入 outbox，而不是直接丢弃。
   - 将单 writer 改为可配置 worker 数或按 key 分片；需要明确同一维度的顺序约束。
   - 暴露 queue depth、drop count、publish latency、重试次数和 drain 时间。

3. **分钟聚合和 dirty-minute**
   - 将日志扫描移出写事务；扫描完成后在短事务内锁状态、校验 revision，再执行删除和批量插入。
   - 删除和批插尽量缩小范围，按分钟/渠道分批提交，避免长事务覆盖整个回填窗口。
   - dirty-minute rebuild 增加 lease 续租和可恢复状态；重试日志查询应设置边界、缓存 request_id 结果并避免同一请求重复 upsert。

### 5.4 第四优先级：快照、连接池和兼容性

1. force 快照也必须执行 revision/watermark/cutoff 单调校验；管理刷新使用全局 semaphore 限制同时构建数。
2. stale 数据可继续服务时优先返回旧快照并异步刷新；对 Redis 禁用、连接池超时和其他错误统一降级，避免部分错误永久 503。
3. 显式设置 Redis `PoolTimeout`，区分“连接池等待超时”和“命令执行超时”；增加等待时长直方图、in-use、idle、timeouts 和 p95/p99 指标。
4. 对 SQLite、MySQL、PostgreSQL 分别验证长事务、范围删除、唯一冲突和保存点恢复；`LOG_DB` 分离时接受非原子事实，并通过 outbox/重放修复跨库失败。

## 6. 建议实施顺序

1. 任务计费幂等、任务行锁/CAS、失败补偿。
2. 成本 SQL 聚合和真正分页，删除详情接口重复读取。
3. 投影事件可靠投递、marker 原子语义和重试退避。
4. Stream/Writer 吞吐、handler deadline、死信和积压观测。
5. 分钟聚合短事务、dirty-minute lease 续租。
6. Redis 长窗口 rollup、查询级 memo、高基数限制。
7. 快照 force 单调校验、fan-out 限流、连接池指标和三库实测。

## 7. 建议验证矩阵

固定 `N=100`，使用确定性 ID 和固定时间基准，不使用随机 fuzz、sleep 或日志断言：

| 场景 | C | M | K | Q | 目的 |
|---|---:|---:|---:|---:|---|
| 基线 | 10 | 15 | 10 | 1 | 验证正常口径 |
| 同热点高并发 | 10 | 15 | 10 | 50 | 验证任务/成本锁与重复回调 |
| 全日高基数 | 100 | 1440 | 100 | 10 | 验证 SQL、Redis 内存和分页 |
| 最大锁压 | 100 | 15 | 100 | 50 | 验证 SQLite/MySQL/PG 锁等待 |

每个场景分别验证：重复登记、同目标修正、退款重放、差额结算重放、逆序版本更新、Redis 入队失败和 Stream pending 隔离。

通过标准：不重复扣费、不重复累加、不出现负余额；日成本与 Key 分摊一致；旧版本不能覆盖新版本；入队失败可观测且可重放；监控 lag 和 PEL 在处理能力足够时最终归零。
