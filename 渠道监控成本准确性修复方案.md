# 渠道监控成本准确性修复方案

**文档版本**：V1.2
**编制日期**：2026-08-16
**实施日期**：2026-08-17
**文档状态**：核心修复已实施
**适用范围**：渠道监控中的日成本、实时成本、API Key 成本、探测成本、模型检测成本、异步任务成本及成本倍率展示。

## 1. 结论与修复目标

本次审计确认，成本计算公式本身基本正确，但当前监控成本链路不能作为完整、稳定的金额事实源。问题主要集中在：

- 异步任务最终退款或差额结算只修正 Redis，历史数据库仍保留初始金额。
- 成本批处理在高基数或持续数据库压力下可能丢弃事件。
- Redis 实时投影和数据库历史聚合是独立写入，没有可靠对账。
- 渠道详情的实时 API Key 成本存在跨渠道归属和多日覆盖问题。
- 累计金额、覆盖率和历史倍率展示缺少完整的边界保护。

修复后的目标是：

1. 每个成本事件都有稳定 ID、原始渠道、API Key、来源和北京时间归属日。
2. 异步任务的初始成本、最终成本、退款和差额调整全部幂等，且调整原始归属日。
3. 数据库中的成本事实可独立恢复；Redis 故障只影响实时性，不影响历史金额。
4. 任何队列满、写入失败、版本冲突或溢出都不能静默丢失金额。
5. 渠道、API Key、趋势、探测和模型检测明细之间满足可核对的加总关系。
6. 页面明确区分“已结算金额”“未解析尝试”“待处理/降级数据”，不把不完整数据显示成完整账单。

本方案只修复渠道监控统计和展示，不改变用户实际扣费、充值、订阅扣款或分组加价规则。提供商账单仍是最终财务对账依据。

## 2. 已确认问题清单

| 优先级 | 问题 | 影响 | 主要位置 |
| --- | --- | --- | --- |
| P0 | 异步任务退款/重算只发布 Redis 修正，未更新日成本表 | 当天可能正确，跨日历史金额恢复为初始成本 | `service/task_billing.go` |
| P0 | 批处理器达到 4096 个待处理键时丢弃新键，调用方仍标记已记录 | 成本永久缺失，无法从历史表恢复 | `service/channel_daily_cost_batcher.go`、`service/channel_daily_cost.go` |
| P0 | Redis 与数据库双写无事务、无补偿、无对账 | 实时金额和历史金额可能不一致 | `controller/channel_monitor_realtime_cost.go`、成本事件发布链路 |
| P1 | 渠道详情的今日 API Key 成本使用全局 API Key 投影 | 一个渠道会显示其他渠道的成本 | `controller/channel_monitor_realtime_cost.go` |
| P1 | 无日期查询合并今日数据时覆盖历史同一 API Key，而不是累加 | 多日 API Key 总额被低估 | `controller/channel_monitor_realtime_cost.go` |
| P1 | 日累计和控制器汇总缺少全链路 `int64` 溢出保护 | 极端合法配置下可能写入失败、投影停滞或汇总溢出 | `model/channel_daily_cost.go`、Redis 投影、Controller |
| P2 | 实时覆盖逻辑丢失成本配置缺失数量 | 页面可能错误显示“覆盖完整” | `controller/channel_monitor_realtime_cost.go` |
| P2 | `previous_cost_ratio` 使用当前换算配置重算 | 换算配置变化时历史倍率变化展示不真实 | `controller/channel_ratio_monitor.go` |
| P0 | 状态监测使用简化的测试请求公式 | 缓存命中可能按全价重复计费，并遗漏缓存创建、图片/音频、其他倍率和工具费 | `service/channel_daily_cost.go` |
| P0 | 模型检测的未解析事件不写日账，且迟到结算按结算日归属 | 覆盖率少计，跨日金额归属错误 | `service/channel_model_detection_cost.go` |
| P0 | 请求运行中修改 `QuotaPerUnit` 时，部分分支混用新旧单位 | 固定价、工具费、Gemini 音频或阶梯计费可按新旧 QPU 比例永久偏差 | `service/channel_daily_cost.go`、`service/channel_model_detection_quota.go` |

## 3. 统一数据口径和不变量

### 3.1 金额口径

成本继续以 `int64` 纳米人民币保存：

```text
成本纳米人民币 = 去除本地分组倍率后的额度 / QuotaPerUnit
                 × 渠道成本倍率（含充值/订阅换算）
                 × 1,000,000,000
```

用户分组倍率、用户特殊倍率和本地加价不能进入渠道成本。所有浮点结果必须经过现有十进制计算和边界检查，禁止重新引入裸 `int` 转换。

### 3.2 成本事件状态

每个真实上游尝试只能有一个当前事实状态：

| 状态 | 金额 | 计数 | 说明 |
| --- | ---: | ---: | --- |
| `settled` | 已知最终金额 | 已结算数 +1 | 有权威 usage、确定性固定价或最终扣费结果 |
| `unresolved` | 0 | 未解析数 +1 | 已访问上游但无法可靠确定金额 |
| `not_started` | 0 | 不计数 | 本地校验、限流、并发过滤或路由选择阶段未访问上游 |

退款或差额结算不创建第二笔独立金额，而是提升同一成本事件的版本，写入新的绝对最终状态。内部允许使用有符号差额，但任何最终聚合值都必须非负。

### 3.3 加总不变量

对任意查询范围、渠道和日期，必须满足：

```text
总成本 = 所有 API Key 成本 + 未归属 API Key 成本
渠道成本 = 该渠道所有 API Key 成本 + 该渠道未归属成本
总已结算数 + 总未解析数 = 已进入成本口径的尝试数
探测成本 <= 总成本
模型检测成本 <= 总成本
```

金额、计数和分类字段都必须有 checked add/subtract。发现不变量不成立时返回错误并告警，不能用 `max(0, value)` 静默掩盖。

## 4. 目标架构

### 4.1 数据库成本事实账本

新增下游模型 `ChannelCostLedger`，使用 GORM 和 `bigint`/SQLite 兼容字段，至少包含：

- `cost_event_id`：全局唯一成本事件 ID。
- `revision`：从 1 开始单调递增的事实版本。
- `channel_id`、`day_start`、`source`、`request_id`、`attempt_no`。
- `api_key_id`、稳定的 `key_fingerprint` 和脱敏显示值。
- `cost_status`、`settled_cost_nano_cny`、`probe_cost_nano_cny`、`model_detection_cost_nano_cny`。
- `settled_count`、`unresolved_count`、`created_at`、`updated_at`。
- `applied_revision` 和 `applied_cost_nano_cny`，用于聚合器幂等应用。

唯一约束使用 `cost_event_id`，不能依赖 Redis key 或请求时间去重。修正成本时更新同一记录的绝对最终金额，不能对日聚合表直接追加一个未经账本确认的负数。

### 4.2 成本事件 Outbox

新增 `ChannelCostOutbox` 或复用同等能力的数据库表：

- 成本账本和 outbox 在同一个数据库事务中提交。
- outbox 使用 `cost_event_id + revision` 唯一键。
- 发布 Redis 成功后记录发布时间和尝试次数；失败保留待重试。
- Redis 消费端按事件 ID 和版本幂等，旧版本不能覆盖新版本。

数据库是金额事实源，Redis 是实时读模型。Redis 不可用时，历史金额仍可正常读取；页面显示实时滞后和 outbox 队列状态，不把 Redis 缺失当成零成本。

### 4.3 聚合策略

优先采用“账本事实 + 事务性聚合”方案：

1. 写入或修正账本时，在同一事务内锁定对应渠道日聚合和 API Key 日聚合。
2. 使用 `lockForUpdate(tx)` 读取当前值，进行 checked add/subtract。
3. 更新渠道日、API Key 日和分类字段。
4. 写入 outbox，事务全部成功后才返回成功。

如果性能压测证明必须异步聚合，则账本必须先同步持久化；聚合器只能从账本重放，队列丢失不能丢金额。页面需要返回 `data_cutoff_at`、`pending_count` 和 `realtime_degraded`，明确当前金额是否已覆盖账本最新水位。

### 4.4 异步任务结算

异步任务提交成功时创建稳定的 `task:<task_id>` 成本事件，并保存提交时快照。完成、失败、退款和 token/时长重算时：

1. 读取当前账本版本。
2. 根据最终成本生成新绝对状态。
3. 只有新版本大于当前版本时才应用。
4. 按原始提交日更新渠道日和 API Key 日聚合。
5. 在同一事务中写入 outbox。

任务重试、重复轮询、重复回调和进程崩溃恢复都必须得到同一个最终金额。不得只发布 Redis correction，也不得把调整记到结算当天。

## 5. 分阶段实施

### 阶段 0：建立回归基线和观测

- 为当前成本公式、分类成本、探测、模型检测、API Key 加总和异步任务增加确定性 Go 测试。
- 增加 `cost_event_id`、成本写入成功/失败、队列深度、丢弃数、outbox lag、账本与聚合差额等指标。
- 记录当前日成本表和 Redis 投影水位，作为上线前后对比基线。
- 不修改用户扣费链路。

### 阶段 1：先修复无数据库结构变更的查询错误

- 今日 API Key 投影按 `channel_id` 过滤；必要时从带渠道维度的 API Key scope 重新聚合。
- 无日期查询合并时，以 API Key ID/稳定 fingerprint 为键做加法，并合并渠道明细；历史行和今日行不能互相覆盖。
- 实时覆盖保留 `missing_cost_config_channel_count`、`free_group_channel_count` 等覆盖字段。
- 只有在成本换算配置未变化时才计算 `previous_cost_ratio`；配置变化时改为持久化旧成本倍率或明确返回不可比较。

### 阶段 2：引入成本账本和 outbox

- 新增模型、索引、迁移和回滚检查，覆盖 SQLite、MySQL >= 5.7.8、PostgreSQL >= 9.6。
- 增加账本写入服务，统一普通 Relay、固定价、文本/音频、探测、模型检测、异步任务和 Midjourney 入口。
- 账本写入、日聚合、API Key 聚合和 outbox 必须使用同一事务。
- 对现有批处理器改为“账本提交后的唤醒/聚合加速器”；队列满只能增加延迟，不能丢失事实。
- 对每个事件保存原始归属日，禁止用当前时间重新分配历史调整。

### 阶段 3：接入异步最终结算

- 在任务提交成功处创建账本事件并持久化 `task:<task_id>`。
- 在退款、按 token 重算、按时长重算和适配器调整处改为账本版本更新。
- 删除 `emitTaskChannelCostCorrection` 的“仅 Redis 修正”行为，改为调用统一账本服务。
- 为 Midjourney 的提交失败、构图失败、退款和特殊操作确认成本语义后接入同一机制。

### 阶段 4：切换读取和对账

- 成本金额、API Key 明细和历史趋势以数据库账本/聚合作为事实源。
- Redis 只提供低延迟投影、实时水位和健康状态；投影落后时不覆盖数据库金额。
- 增加按日期、渠道、API Key、来源和 revision 的对账任务。
- 对账发现差异时优先从账本重放聚合，再修复 Redis；不能手工直接修改日总额。

### 阶段 5：清理旧路径和完成上线

- 观察至少一个完整成本保留周期，确认异步任务跨日、退款和重复回调均可重放。
- 账本、日聚合、API Key 聚合和 Redis 的金额/计数对账为零差异后，再移除旧的无 ID 批处理路径。
- 保留历史旧数据的“账本覆盖起始时间”标记；无法从旧日聚合反推出事件级事实的历史数据不得伪造补账。

## 6. 溢出和数据库兼容要求

- 所有金额加减在内存、SQL 更新前和 Redis 投影前都检查 `math.MaxInt64`/`math.MinInt64`。
- 禁止直接使用 `gorm.Expr("cost_nano_cny + ?", delta)` 作为唯一保护；先用共享锁和 checked arithmetic 验证，再更新。
- 标准行锁必须通过 `lockForUpdate(tx)`；SQLite 路径不能生成 `FOR UPDATE`。
- 不使用 MySQL 专用函数、PostgreSQL 专用运算符或 SQLite 不支持的列修改语法。
- 事件单笔金额、每日累计金额、分类金额和计数均拒绝负值；修正后的最终总额不能小于零。
- 任何溢出、版本冲突、聚合不变量失败都必须返回错误、保留待处理记录并告警，不能把事件标为成功。

## 7. 测试和验收矩阵

### 7.1 后端测试

新测试使用 `require` 做前置/致命断言，`assert` 做非致命值断言，覆盖真实合同而非实现细节：

| 场景 | 验收结果 |
| --- | --- |
| 同一任务提交、重复轮询、重复退款 | 账本只有一个事件，最终金额只应用一次 |
| 初始 10、最终 14 | 原始提交日最终为 14，不产生第二个任务金额 |
| 初始 10、最终退款 | 原始提交日最终为 0，日聚合不为负 |
| 跨日完成/退款 | 所有调整仍归属提交日 |
| Redis 发布失败 | 数据库金额不变，outbox 可重试，页面显示投影滞后 |
| 数据库写入失败/超时 | 事件不标记成功，不丢失，重试不重复累计 |
| 队列达到上限 | 触发同步回退或保留账本，绝不静默丢金额 |
| API Key 在多个渠道和多日使用 | 渠道明细、API Key 明细和总额相互可加总 |
| 按渠道查看今日 API Key | 不出现其他渠道金额 |
| 最大合法倍率和累计金额 | 不溢出、不产生负数、不静默截断 |
| SQLite、MySQL、PostgreSQL | 迁移、事务、唯一键、行锁降级和 upsert 均通过 |

### 7.2 前端测试

- API Key 成本表验证渠道过滤、多日合并和渠道明细加总。
- 成本历史验证未解析、配置缺失、投影滞后和金额不完整提示。
- 倍率历史验证换算配置变化时不伪造旧成本倍率。
- 模型检测报告验证 settled、unresolved、unknown 和 not_started 的成本语义。

### 7.3 通过标准

1. 账本到日聚合、API Key 聚合和 Redis 投影的对账差异为零，或明确处于可观测的待处理水位内。
2. 任意单个成本事件可以通过 `cost_event_id` 重放，重放次数不限且最终金额不变。
3. 任务跨日完成、退款和重复回调不会改变错误日期，也不会重复累计。
4. 队列、Redis、数据库任一组件短暂故障后恢复，历史金额最终自动收敛。
5. 成本页面不再把跨渠道、覆盖历史或缺少配置的金额展示成可信的完整结果。

## 8. 上线、回滚和数据处理

### 上线顺序

1. 先发布只读指标和查询修复，确认 API Key 过滤及覆盖率字段正确。
2. 执行扩展迁移，先创建新表、索引和必要字段，不删除旧表。
3. 灰度开启账本写入和旧聚合对比，持续记录差异但暂不切换读取。
4. 对账差异为零后切换成本页面到新事实源。
5. 最后开启异步任务最终结算和 Redis outbox 投影。

### 回滚原则

- 任何阶段都不得删除旧日成本表、旧 Redis key 或账本事实。
- 读取切换异常时可回退到旧读路径，但必须在页面标记旧路径可能存在异步任务和实时一致性缺陷。
- 写入路径回滚时保留新账本和 outbox，禁止重复写同一成本事件。
- 旧历史只有日聚合、没有事件级来源时，不自动生成虚假账本事件；通过覆盖起始时间和页面提示说明范围限制。

## 9. 文件边界和实现约束

本仓库是下游 fork。预计新增的成本账本、outbox、聚合、对账和控制器修复均放在下游新增文件或现有下游文件中。`service/task_billing.go` 属于上游文件，如确需修改，只保留调用统一成本账本服务的最小改动，不做无关重构。

实施前必须再次对照 `.agents/downstream.md`、数据库兼容规则和 `pkg/billingexpr/expr.md`。所有 JSON 序列化使用 `common.*` 包；所有新的账单数量转换使用现有 quota math checked helper；不修改受保护的项目和作者身份信息。

## 10. 本次实施结果

本次已完成影响当前金额正确性的核心修复：

- 成本金额与计数统一以数据库日成本表为权威来源；Redis 只补充投影水位、队列和降级状态，不再覆盖金额。
- 新增异步任务成本事件表，以 `task:<task_id>` 幂等登记初始成本，并将退款、重算修正到原始提交日。
- 任务成本只有数据库更正成功后才发布 Redis 修正；持久化失败不会产生 Redis-only 金额。
- 批处理队列满时同步落库，数据库失败批次独立保留重试；失败或溢出的事件不会被误标记为已记录。
- 渠道日成本、API Key 日成本、跨日汇总、控制器汇总和 Redis 投影合并均增加 `int64` 边界检查。
- 渠道过滤下的 API Key 成本不再串入其他渠道，多日 API Key 金额按 nano-CNY 累加，不再覆盖历史值。
- 成本换算配置变化时清除不可比较的历史倍率，避免用新换算系数伪造 `previous_cost_ratio`。
- 异步任务成本事件随成本保留期清理；删除渠道配置时保留历史金额事实，确保迟到结算仍可完成。
- 状态监测不再使用简化公式，而是按权威 `BillingUsage` 重跑生产文本计费；缓存读取/创建、图片、Gemini 音频、`OtherRatios`、工具附加费和阶梯表达式都进入成本。
- 状态监测成本始终去除用户分组倍率，同一笔金额同时进入渠道总成本和 `probe_cost_nano_cny`；无权威 usage、配置缺失或计算饱和时改记 `unresolved`，不猜测金额。
- 模型检测使用事件级快照保存成本倍率和 QPU；结算同样覆盖缓存、图片/音频、工具费、其他倍率、固定价和阶梯表达式，且组前供应商成本不包含本地分组加价。
- 模型检测从 `pending` 进入 `unresolved` 时就写权威日账；迟到结算在同一事务中执行“未解析 -1、已结算 +1、金额入账”，并按事件 `CreatedAt` 的北京日期归属。
- 模型检测的 settle 和 mark-unresolved 使用跨数据库兼容的行锁串行化，重放幂等；日账溢出或计数异常会回滚事件状态。
- 普通请求、状态监测和模型检测都在请求内冻结 QPU；固定价、工具费、Gemini 音频、阶梯额度与人民币换算使用同一份快照，管理员在请求中途修改 QPU 不会改变该笔成本。
- 状态探测事件现在同步写入渠道日账和 API Key 日账；普通请求仍使用约一秒的批处理窗口，强制进程终止时如果尚未刷新，还需通用 outbox 才能完全消除这个持久性窗口。

当前普通请求仍采用约一秒的内存批次写入，优雅退出会主动刷新，队列满和数据库暂时失败已有同步回退/保留重试。若后续要求覆盖进程被强制终止前的最后批次窗口，应继续实施第 4.2 节的通用数据库 outbox；这属于进一步提高故障耐久性，不影响本次已修复的计算、归属、溢出和异步结算正确性。
