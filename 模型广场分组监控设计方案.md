# 模型广场分组监控设计方案

**文档状态**：设计稿，待评审
**版本**：V1.0
**日期**：2026-08-19
**适用范围**：模型广场用户侧分组监控、渠道监控管理员侧配置、分组探测成本统计

## 1. 结论摘要

本功能分成两个明确的视图：

1. 渠道监控继续面向管理员，显示单个渠道、单个模型的详细状态和探测记录。
2. 模型广场分组监控面向模型广场可见用户，通过顶部导航中“模型广场”右侧的“分组监控”菜单进入独立页面，按用户可见的模型分组展示聚合状态，只展示分组名称、首字响应时间、成功率和状态等非敏感信息。

“分组监控”菜单和模型广场使用同一套导航开关、认证方式和分组可见性规则。管理员在渠道监控中配置总开关、监控分组顺序、每组探测模型、探测间隔和状态展示范围；系统按分组发起真实的上游探测请求，探测成本沿用渠道成本换算和成本事实链路，记入渠道监控成本，但不扣除用户额度。

每个分组配置一个具体的文本探测模型。候选模型只来自该分组当前实际可用、且支持自动文本探测的模型；模型不再由后台隐式自动选择。

## 2. 背景和边界

当前状态探测以渠道为主，适合管理员判断某个渠道是否异常。用户选择的是模型分组，用户真正关心的是“这个分组现在是否可用”，不应看到渠道名称、渠道类型、API Key、错误详情或成本。

分组监控不能简单把渠道状态字段复制到前端：

- 一个分组可能包含多个渠道，渠道启停和路由优先级会变化。
- 模型广场的分组可见性受用户角色、用户组和模型 enable_group 共同影响。
- 渠道状态探测的样本是管理员运维数据，不应直接暴露给普通用户。
- 分组探测是额外的真实上游请求，必须独立标记成本来源，不能伪装成用户业务请求。

本设计不改变用户请求路由、分组倍率、模型定价、用户配额或渠道自动禁用策略。分组监控只提供可用性展示和成本监控数据。

## 3. 术语和口径

| 术语 | 定义 |
| --- | --- |
| 分组 | 渠道 group、能力 abilities.group、定价分组中使用的同一个规范化名称。 |
| 渠道状态监控 | 管理员在渠道监控页面配置的单渠道、单模型状态探测。 |
| 分组监控 | 本功能新增的按分组执行的用户可见探测和聚合状态。 |
| 逻辑探测 | 一次分组探测任务。任务可以因路由重试访问多个渠道，但属于同一个分组探测周期。 |
| 已发出探测 | 已获得渠道并发租约且请求实际发送到上游。只有已发出的探测才产生上游成本。 |
| 展示范围 | 用于成功率和其他窗口统计的回溯范围，不参与当前状态判定，也不等于数据保留期限。 |

成本时间统一按渠道监控现有的北京时间日切分规则保存，金额继续使用纳米人民币整数。

## 4. 权限和数据隔离

### 4.1 模型广场权限复用

分组监控接口必须挂在与 /api/pricing 相同的权限中间件上，即复用 HeaderNavModuleAuth("pricing") 的导航开关、访客访问策略和登录要求。不得单独增加“分组监控权限”或管理员权限。

用户可见分组取以下集合的交集：

    管理员配置且启用、探测模型仍有效的分组
    ∩ 当前用户通过 GetRoleUsableGroups 得到的可用分组
    ∩ 至少存在一个当前可展示模型的分组

未配置探测模型、探测模型已失效或不满足上述条件的分组不进入用户响应，不展示“未配置”状态。分组不可见时，接口不能返回其名称、首字响应时间、成功率、状态、时间戳或错误信息。管理员配置了一个普通用户不可见的分组，不构成信息泄漏。

### 4.2 管理员权限

配置、手动触发、执行记录和成本明细继续放在现有 /api/channel_monitor 根权限下，由 RootAuth 保护。管理员可以看到所有配置分组及其成员渠道；普通用户接口永远不返回这些字段。

### 4.3 关闭状态的可见性

全局分组监控关闭时，分组监控页面仍可以显示已配置且对当前用户可见的分组，但所有行状态为“已停用”，并在页面顶部显示“分组监控已停用”。这样用户能区分“没有配置”和“暂时停用”。如果没有任何可见配置分组，显示空状态，不显示隐藏分组数量。

## 5. 用户侧体验

### 5.1 页面布局

在顶部导航中将“分组监控”放在“模型广场”右侧，点击后进入独立页面 /group-monitor：

- 页面使用公共布局，不复制模型广场的筛选和模型列表。
- 桌面端以固定宽度的内容表格或网格展示分组，状态刷新不改变列宽和行高。
- 移动端改为纵向列表，分组名称、首字响应时间和状态保持同一行可读。
- 顶部导航菜单只有在模型广场导航模块启用时显示，并复用模型广场的认证要求。

页面标题使用“分组监控”，显示最近一次数据更新时间、当前展示范围和全局启停状态。数据查询独立于 /api/pricing，刷新不会影响模型广场的价格和筛选状态。

### 5.2 行内容

每个分组按管理员配置的顺序展示一行：

| 字段 | 展示规则 |
| --- | --- |
| 分组名称 | 原样显示，超长名称截断并通过无障碍名称或悬浮提示提供完整值。 |
| 首字响应时间 | 展示最新一次完成探测请求的首字响应时间；该请求没有有效首字时间时显示 --，不回退到历史请求。 |
| 成功率 | 展示范围内完成的有效逻辑探测成功率，按百分比显示，保留 1 位小数；没有可计算样本显示 --。 |
| 状态 | 使用颜色圆点加文字，不仅依赖颜色；限流状态显示为“波动”；状态值见第 6 节。 |
| 更新时间 | 可选的次要文本，只显示最后状态时间，不显示渠道和错误详情。 |

首期不在用户页面显示探测模型、渠道数、成本和错误信息。首字响应时间和成功率属于用户可见的聚合性能指标，不暴露渠道或模型身份；后续若需要扩展其他指标，必须单独评审信息泄漏和权限影响。

### 5.3 加载、空和异常

- 加载显示固定高度骨架，不能造成模型列表跳动。
- 无配置显示“暂无分组监控”。
- 已停用保留配置行，状态统一显示“已停用”。
- 探测模型配置有效、但最新一次逻辑探测结果为无法分配可请求渠道的分组显示“无可用路由”，而不是“异常”；未配置探测模型或探测模型失效的分组直接不返回、不展示。
- 数据过期显示“数据已过期”，同时保留最后状态；客户端网络错误不得把状态改成“正常”。
- 接口返回权限拒绝时沿用模型广场现有处理，不回退到管理员接口。

## 6. 状态模型

渠道监控内部继续保留现有八类健康状态码；分组监控用户侧采用更少的状态粒度，不返回 `unconfigured`，不使用 `partial`。其中 `rate_limited` 的用户展示文案固定为“波动”，用于表示请求受到上游限流但尚不能判定为整体不可用。管理员渠道监控仍按自身现有状态和文案展示。

| 状态码 | 展示 | 进入条件 |
| --- | --- | --- |
| unavailable | 无可用路由 | 分组及探测模型配置仍有效，且最新一次逻辑探测无法分配可请求渠道；不等同于未配置或模型失效。 |
| paused | 已停用 | 分组监控全局开关关闭。分组被移出配置列表、未配置探测模型或探测模型失效时不返回该行。 |
| pending | 待检测 | 配置有效且已启用，但还没有完成过一次可用于健康判断的逻辑探测。 |
| healthy | 正常 | 最新一次有效逻辑探测最终成功，且结果未过期。 |
| rate_limited | 波动 | 最新一次有效逻辑探测最终结果为上游 429。 |
| unhealthy | 异常 | 最新一次有效逻辑探测最终结果为非 429 上游失败或本地探测故障。 |
| stale | 已过期 | 有历史有效结果，但最近一次有效完成时间早于当前时间减去 2 × interval_seconds。 |

状态以分组最新一次有效逻辑探测的最终结果为准，不把管理员渠道状态直接作为用户状态，也不聚合展示范围内的历史结果。一次逻辑探测成功表示该分组和指定模型在该次探测中至少存在一条可用路由；成功率独立反映展示范围内的可用比例。

### 6.1 当前状态计算

对每个分组读取最新一次有效逻辑探测结果。一次逻辑探测可能包含路由重试，但状态只使用该次逻辑探测的最终结果，重试尝试只用于成本和管理员执行明细：

1. 先判断全局启停和分组探测模型是否仍有效；全局启停和模型有效性允许即时改变可见性或状态，路由可用性只在逻辑探测执行时判定。
2. 如果没有有效配置模型，管理员 overview 标记为 `unconfigured`，但用户接口直接过滤该分组，不执行请求，也不产生成本。
3. 如果最新一次有效逻辑探测因没有可用路由而完成，返回 `unavailable`，不把它标记为“异常”；该次无路由结果仍作为失败样本计入成功率分母。路由配置后来恢复时，在下一次探测完成前继续显示该最新结果。
4. 忽略因停用、配置无效或调度条件不满足而未执行的 `skipped`；如果从未完成过有效逻辑探测，返回 `pending`。
5. 如果最近一次有效完成时间超过过期阈值，返回 `stale`，不能用旧的 healthy 冒充实时状态。
6. 最新结果成功返回 `healthy`；最新结果为上游 429 返回 `rate_limited`（展示为“波动”）；最新结果为其他上游失败或本地探测故障返回 `unhealthy`。不读取更早结果修正当前状态，也不返回 `partial`。
7. 没有有效完成结果但存在仍在执行的任务时返回 `pending`，不能提前显示为异常。

### 6.2 成功率计算

成功率用于展示范围内的可用性概览，按逻辑探测而不是按渠道重试次数计算：

- `success_rate = 成功逻辑探测数 / 有效完成逻辑探测数 × 100%`，前端按百分比显示并保留 1 位小数。
- 一次逻辑探测无论发生多少次路由重试都只计 1 次；只有最终结果为成功才计入成功数。
- 分母包含最终成功、429、其他上游失败、本地探测故障和配置有效但无可用路由的有效完成探测。
- 因停用、配置无效、租约冲突或其他未真正执行原因产生的 `skipped` 不计入分母。
- 展示范围内没有有效完成探测时返回 `null`，前端显示 `--`；不使用更早窗口的成功率补齐。
- 成功率与状态独立返回：例如展示范围内曾有失败、但最新一次探测成功时，状态为“正常”，成功率仍按窗口内全部有效结果计算；最新一次为 429 时状态为“波动”，不受更早结果类型影响。

### 6.3 最新一次首字响应时间

首字响应时间沿用渠道监控已有的 `FirstTokenMs` 指标，但用户侧只展示最新一次完成的逻辑探测请求：

- 按 `finished_at` 选择该分组最新完成的逻辑探测；路由重试只保留同一逻辑探测的最终结果。
- 最新逻辑探测成功且包含有效、非负且有限的 `FirstTokenMs` 时，返回该值（单位毫秒）。
- 最新逻辑探测失败、被限流、被跳过、未发出上游请求，或没有有效 `FirstTokenMs` 时，返回 `null`，前端显示 `--`。
- 不回退到更早的成功探测；即使历史请求有有效首字时间，最新请求没有有效值时仍显示 `--`。
- 如果同一 `finished_at` 存在并列记录，以执行记录 ID 或其他稳定的服务端排序字段打破平局，确保结果确定。
- API 返回 `latest_first_token_ms`，不返回具体模型或渠道样本。

## 7. 管理员配置设计

### 7.1 配置入口

在渠道监控现有设置弹窗或设置页新增“分组监控”区域。不要把分组监控混入单渠道状态探测表单，页面文案必须明确两者用途不同：

- 渠道状态监控：管理员查看每个渠道。
- 分组监控：用户查看每个分组。

### 7.2 字段

| 字段 | 类型 | 默认值/范围 | 说明 |
| --- | --- | --- | --- |
| enabled | bool | false | 全局启用或停用。停用保留配置和历史。 |
| groups | 有序对象数组 | 空 | 每项包含 group_name 和 probe_model；数组顺序就是用户页面顺序。 |
| interval_seconds | int | 60 | 30 至 86400 秒；和现有状态探测使用相同边界。 |
| display_value | int | 60 | 展示范围数值。 |
| display_unit | enum | minute | minute、hour、day；范围分别为 1-60 分钟、1-24 小时、1-30 天。 |
| revision | int64 | 递增 | 乐观并发控制，保存时必须带当前修订号。 |

首期不允许管理员为每个组配置独立探测间隔。所有分组使用统一间隔，避免调度器产生不可控的请求峰值；未来若需要独立间隔，应增加明确的总成本上限。

保存时将展示范围换算为秒，并要求 display_range_seconds 不小于 2 × interval_seconds。这样展示窗口至少覆盖两个探测周期，避免仍在有效期内的最近结果因为窗口过短而消失。

每个分组配置项必须包含：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| group_name | string | 分组名称，必须来自当前有效分组集合。 |
| probe_model | string | 具体文本模型，必须属于该分组当前实际可用模型集合。 |
| sort_order | int | 服务端归一化的排序值；保存请求以数组顺序为准。 |

### 7.3 分组选择和排序

- 候选分组来自当前启用渠道和 abilities 的有效分组，并去重、排序后展示。
- 选择分组后，探测模型候选项只显示该分组当前实际可用的具体文本模型。
- 模型候选项排除通配符、图片、音频、视频及其他不支持自动文本探测的模型。
- 保存时重新读取数据库快照，拒绝已不存在、名称为空、探测模型为空或模型已不属于该分组的配置。
- 展示范围小于两个探测周期时拒绝保存，并提示管理员扩大范围或缩短探测间隔。
- 每个分组只能出现一次；拖拽排序和上下移动都产生同一份完整有序数组。
- 分组从配置中移除后不删除历史状态和执行记录；用户页面不再展示。
- 已保存的探测模型后来失效时保留配置，管理员配置页和 overview 标记为“未配置”并提示重新选择；用户接口直接过滤该分组。
- 如果配置列表超过 100 个分组，拒绝保存并要求拆分；这是首期的调度和页面保护上限。

### 7.4 交互和并发

- 保存按钮在请求期间禁用，成功后刷新设置、状态和成本查询。
- revision 不匹配返回 HTTP 409，提示管理员刷新后重试，不覆盖其他管理员的排序。
- 关闭全局开关不清空分组列表，不取消历史保留任务。
- 开启时立即唤醒分组探测 worker；首次执行按下一个整点间隔计算，避免保存瞬间产生请求洪峰。
- 管理审计记录启停状态、分组顺序、间隔、展示范围和变更前后摘要，不记录渠道密钥。

## 8. 探测执行架构

### 8.1 组件

建议新增独立的分组监控模型和服务，复用已有渠道探测的租约、并发控制、请求测试和成本记录能力：

1. ChannelGroupMonitorSettings：单例配置和调度修订。
2. ChannelGroupMonitorGroup：每个配置分组一行，包含名称、排序、启用状态和当前配置快照。
3. ChannelGroupMonitorState：每组当前状态、成功率统计、连续成功或失败、最近完成时间和按分钟、小时、天保存的窗口桶。
4. ChannelGroupMonitorExecution：每次逻辑探测的结果、选用模型、实际渠道、请求 ID、成本事件 ID 和错误摘要。

配置和状态分开存储，避免每次保存排序都重写历史 JSON，也便于按组查询和清理。窗口桶可以像现有状态探测一样使用 TEXT 保存 JSON，但所有序列化必须使用 common.Marshal、common.Unmarshal。

### 8.2 探测模型校验

每个分组使用管理员配置的一个具体文本模型：

1. 候选模型由后端根据 abilities 中该分组的启用能力生成。
2. 只允许具体的文本模型，排除通配符和不支持自动文本探测的渠道类型或端点。
3. 保存配置和每次领取任务时都重新校验分组与模型关系，防止渠道或能力变更后继续探测失效模型。
4. 模型失效时本轮不发请求，管理员状态为 `unconfigured`，不产生成本，并提示重新选择；用户接口不返回该分组。

候选规则必须在后端实现，不能依赖用户当前筛选条件。普通用户页面不返回配置的探测模型。

### 8.3 调度流程

分组 worker 只在主节点运行，结构与已有状态探测 worker 一致：

1. 扫描到期分组，使用数据库租约领取，租约包含配置 revision 和 run ID。
2. 按配置顺序执行分组；单个分组之间沿用现有请求间隔，使用独立并发上限。
3. 使用该分组配置的探测模型调用正常路由选择；路由重试仍遵守现有重试和渠道并发策略。
4. 每个实际发出的上游尝试都写入成本事件；本地校验、渠道忙、无路由等未发出情况只写执行结果，不写成本。
5. 逻辑探测完成后，以所有尝试的最终结果更新分组状态和窗口；同一个 run ID 重复回调必须幂等。
6. 写入状态后使用户侧分组监控缓存失效，并设置下一次执行时间。

管理员手动触发时可以立即领取一次全量分组任务，但仍受租约和并发上限限制。手动任务不改变周期调度时间，避免“手动刷新”造成周期漂移。

### 8.4 与渠道状态监控的隔离

- 渠道状态监控继续写入 source=status_probe，可参与管理员的渠道健康和智能调度样本。
- 分组监控写入 source=group_probe，默认不计入业务成功率、渠道智能调度样本或模型广场性能统计。
- 两类 worker 可以复用请求执行器、成本换算和结果枚举，但不得共享同一条配置记录或用一个布尔字段互相覆盖。

## 9. 数据模型和数据库兼容

### 9.1 推荐字段

ChannelGroupMonitorSettings 至少包含：

    id = 1
    enabled
    interval_seconds
    display_value
    display_unit
    revision
    next_run_at
    lease_token
    lease_until
    running_run_id
    running_started_at
    created_at
    updated_at

ChannelGroupMonitorGroup 至少包含：

    id
    group_name
    probe_model
    sort_order
    enabled
    config_revision
    created_at
    updated_at

建议唯一索引为 group_name，排序索引为 enabled、sort_order、id。名称长度沿用渠道分组长度限制；所有字符串保存前做 trim、UTF-8 长度校验和重复检查。

ChannelGroupMonitorState 至少包含：

    group_name
    health_status
    last_result
    last_finished_at
    latest_first_token_ms
    success_rate
    success_count
    completed_count
    last_success_at
    last_failure_at
    consecutive_successes
    consecutive_failures
    last_execution_id
    minute_buckets_json
    hour_buckets_json
    day_buckets_json
    updated_at

ChannelGroupMonitorExecution 至少包含：

    run_id
    group_name
    config_revision
    probe_model
    channel_id
    request_id
    result
    request_dispatched
    first_token_ms
    started_at
    finished_at
    settled_cost_nano_cny
    cost_event_id
    error_code
    error_message
    created_at

所有金额、计数和时间字段使用现有 GORM 类型标注。迁移必须同时验证 SQLite、MySQL 5.7.8+ 和 PostgreSQL 9.6+，不使用数据库专属 JSON 类型、序列或 ALTER COLUMN。

### 9.2 配置快照和删除

执行记录保存当次 group_name、模型和配置 revision，即使管理员后来调整排序或删除分组，历史仍可解释。删除分组只删除当前配置关联，不物理删除状态和成本事实；清理任务按现有渠道监控保留设置处理。

## 10. 成本计算设计

### 10.1 成本归属

分组探测的上游实际请求成本计入被选渠道的渠道监控成本，并标记：

    source = group_probe
    group_name = 分组名称
    probe_run_id = 逻辑探测 run_id
    channel_id = 实际发出请求的渠道

分组探测不扣除用户额度、不写入用户用量日志的业务消费金额，但管理员渠道监控的总成本、探测成本和按渠道成本必须包含它。

### 10.2 金额规则

沿用现有渠道成本快照和倍率换算：

    分组探测成本 = 上游实际使用量
                 -> 渠道成本换算
                 -> 渠道成本倍率
                 -> 纳米人民币整数

用户分组倍率和用户特殊倍率不得进入渠道成本。无法得到权威使用量或固定价结果时，记录 unresolved，金额为 0，不能猜测或按请求数填充金额。

所有浮点到额度或金额的转换必须使用现有 common.QuotaFromFloat、common.QuotaRound、common.QuotaFromDecimal 及其 Checked 变体；任何饱和都要挂到成本事件的 quota_saturation 审计信息。不得引入裸 int 转换。

### 10.3 分类字段

为保持当前 API 的兼容性，建议在渠道日成本中新增 GroupProbeCostNanoCNY：

    ProbeCostNanoCNY = ChannelStatusProbeCostNanoCNY + GroupProbeCostNanoCNY
    CostNanoCNY      = 业务成本 + ProbeCostNanoCNY + ModelDetectionCostNanoCNY

如果现有成本事实账本已经支持来源维度，则优先在账本增加 source=group_probe，日聚合表再投影 GroupProbeCostNanoCNY，避免在多个入口各自维护分类逻辑。旧接口只读取 ProbeCostNanoCNY 时仍能看到完整探测成本；新增管理员明细接口可以分别显示渠道探测和分组探测。

### 10.4 失败、重试和对账

- 未发出上游请求：不产生探测成本。
- 上游已收到请求但没有权威用量：写一条 unresolved 成本事实，进入未解析计数。
- 路由重试：每个实际发出的渠道尝试单独记成本，使用同一 probe_run_id 关联；不能只记录最后一次请求。
- 重复执行、超时回收、进程重启：使用 cost_event_id 和 revision 幂等，不能重复累计。
- 异步结算或迟到 usage：按原始探测发生日修正，不按结算日新增一笔伪成本。
- 日成本、Redis 实时投影和账本对账失败时，页面返回降级元数据，不把缺失数据显示为零。

## 11. API 设计

### 11.1 管理员接口

全部接口位于现有 RootAuth 的 /api/channel_monitor 路由组：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | /api/channel_monitor/group_monitor/settings | 返回配置、候选分组、各分组可选文本模型、当前顺序和 revision。 |
| PUT | /api/channel_monitor/group_monitor/settings | 更新启停、分组顺序、每组探测模型、间隔和展示范围。 |
| GET | /api/channel_monitor/group_monitor/overview | 管理员查看完整分组状态、执行水位和成本摘要。 |
| POST | /api/channel_monitor/group_monitor/run | 手动触发一次分组探测，返回 run ID。 |
| GET | /api/channel_monitor/group_monitor/executions | 分页查看执行记录，支持分组、结果和日期筛选。 |

更新请求建议使用指针字段或完整配置 DTO，避免 JSON 中显式的 false、0 被当成缺失。返回 409 时必须带当前 revision，不自动覆盖。

### 11.2 用户接口

新增 GET /api/pricing/group-monitor。

该接口使用和 /api/pricing 完全相同的 HeaderNavModuleAuth("pricing") 中间件，并在控制器内复用同一个用户上下文和 GetRoleUsableGroups 逻辑。建议响应：

    {
      "success": true,
      "data": {
        "enabled": true,
        "server_now": 1760000000,
        "data_cutoff_at": 1759999980,
        "display_value": 60,
        "display_unit": "minute",
        "items": [
          {
            "group": "default",
            "latest_first_token_ms": 842,
            "success_rate": 96.7,
            "status": "healthy",
            "status_text": "正常",
            "last_finished_at": 1759999980
          }
        ]
      }
    }

用户响应禁止返回 channel_id、渠道名称、模型名称、探测成本、API Key、错误消息、路由优先级、用户组原始配置和管理员审计字段。

### 11.3 缓存和水位

用户接口可以使用短 TTL 的内存或 Redis 读模型，建议 5 至 10 秒；状态写入后主动失效。响应至少带 server_now 和 data_cutoff_at，让前端能够标记“数据已过期”。用户端不使用高频轮询，沿用模型广场手动刷新或低频刷新策略。

## 12. 前端落地位置

设计对应的代码边界建议如下，实际实现时尽量新增文件，减少对现有上游文件的大范围改动：

    web/src/features/group-monitor/
      index.tsx
      api.ts
      types.ts
      components/group-monitor-table.tsx
      lib/group-monitor-status.ts

    web/src/routes/group-monitor/
      index.tsx

    web/src/features/channel-monitor/
      components/channel-group-monitor-settings.tsx
      lib/group-monitor-schema.ts
      lib/group-monitor.ts

顶部导航生成逻辑在加入“模型广场”链接后立即加入“分组监控”链接，两者使用同一个 pricing 导航模块配置和 requireAuth 结果。分组监控页面使用独立路由，不修改模型广场页面布局。

下游新增 UI 文案遵循 .agents/downstream.md：使用简体中文 literals，不新增前端 locale key，不修改现有 locale 文件。图标使用项目现有图标库，状态同时提供文字和无障碍标签。

渠道监控配置应复用现有设置弹窗的表单、提交错误处理、加载态和 409 revision 处理，不新建第二套表单框架。

## 13. 迁移和发布计划

### 阶段 0：数据模型和只读接口

- 增加四类模型、索引、迁移和跨数据库测试。
- 实现管理员只读 overview，确认分组解析、排序和成本来源字段。
- 用户接口先返回关闭状态，不启动探测。

### 阶段 1：探测和成本事实

- 实现 master worker、租约、并发、配置模型校验和执行记录。
- 接入统一成本事实或日成本投影，增加 source=group_probe 和分组探测分类成本。
- 增加重复执行、超时、未解析和进程重启后的幂等测试。

### 阶段 2：管理员配置

- 在渠道监控设置中加入分组监控表单。
- 支持分组选择、排序、启停、间隔和展示范围校验。
- 保存成功后唤醒 worker，保存冲突返回 409。

### 阶段 3：用户页面和导航

- 在“模型广场”右侧增加“分组监控”导航菜单，并新增独立页面。
- 增加桌面表格、移动端列表、加载、空、错误和过期状态。
- 验证模型广场权限与分组过滤一致，不泄漏不可见分组。
- 默认关闭功能，灰度开启后观察一整个成本日。

### 阶段 4：对账和清理

- 对账 group_probe 事件、渠道日成本、实时投影三者的数量和金额。
- 成本保留任务纳入分组执行表和状态表。
- 观察稳定后再开放默认配置或提高并发上限。

## 14. 测试计划

### 后端

- 配置校验：空分组、重复分组、超长名称、探测模型为空、模型不属于分组、非文本模型、非法间隔、展示范围边界、超过 100 组。
- 权限：匿名、普通用户、不同用户组、管理员对同一配置得到正确可见集合。
- 状态判定：失败后最新成功为正常、成功后最新 429 为波动、成功后最新非 429 失败为异常，并覆盖无样本、过期、无可用路由和停用；确认更早结果不改变当前状态、用户侧不返回未配置分组且不使用“部分异常”。
- 调度：多实例只有一个租约持有者；配置 revision 变化会取消旧 run；手动触发不重复周期任务。
- 成本：只对已发出请求计费；重试按实际渠道尝试计费；unresolved 不产生金额；迟到结算按原始日修正；分类和总额满足加总不变量。
- 数据库：SQLite、MySQL 5.7.8+、PostgreSQL 9.6+ 迁移和并发首行插入。
- 安全：用户响应无渠道、模型、密钥、成本和错误详情。

### 前端

- “分组监控”菜单紧跟“模型广场”，并复用同一个导航开关和认证要求。
- 独立页面在桌面和移动端保持稳定布局，不影响模型广场页面。
- 分组行按管理员排序，最新一次首字响应时间、成功率和无有效值占位正确；最新请求失败时首字响应时间不回退到历史值。
- 颜色状态同时有文字和可访问名称。
- 覆盖加载、空、停用、无路由、过期、请求失败和权限拒绝。
- 配置表单覆盖开关、排序、边界校验、保存中禁用和 409 冲突。

## 15. 监控指标和验收标准

建议新增指标：

    channel_group_monitor_due_total
    channel_group_monitor_executed_total{result}
    channel_group_monitor_request_dispatched_total
    channel_group_monitor_skipped_total{reason}
    channel_group_monitor_cost_total{status}
    channel_group_monitor_lease_conflict_total
    channel_group_monitor_stale_groups
    channel_group_monitor_user_api_latency

验收必须满足：

1. 用户在分组监控页面只看到自己有权使用且管理员配置的分组。
2. 分组监控不会暴露渠道、模型、API Key、错误和成本信息。
3. 管理员可以启停、选择和排序分组，配置探测间隔与状态展示范围。
4. 每个分组必须配置一个当前实际可用的具体文本探测模型。
5. 同一时间只有一个实例执行同一分组周期，重试和进程重启不会重复扣成本。
6. 所有实际发出的分组探测都进入渠道监控成本；未发出请求不产生金额。
7. 渠道状态监控和分组监控在来源、状态和智能调度样本中保持可区分。
8. 三种数据库迁移和成本边界测试通过，且不存在负成本、溢出或静默丢失。
9. 用户接口不返回未配置分组，不产生“部分异常”状态；状态严格取最新一次有效逻辑探测结果，最新 429 显示为“波动”，成功率独立按展示范围内的有效逻辑探测计算。

## 16. 风险和待确认事项

### 已在本设计中确定

- “分组监控”作为“模型广场”右侧的独立导航菜单和独立页面。
- 首期每组每周期只执行一次逻辑探测，每个分组由管理员配置一个具体文本模型。
- 统一探测间隔，不支持每组独立间隔。
- 渠道监控继续保留现有状态码；分组监控用户侧不展示未配置分组，去掉“部分异常”，将最新一次探测的限流结果展示为“波动”，并以最新一次有效逻辑探测的最终结果判定当前状态。
- 用户页面展示分组名称、最新一次首字响应时间、成功率和状态。
- 分组探测成本纳入渠道监控探测成本，并新增可区分的分组探测分类。
- 管理员成本页首期直接分列展示“渠道探测成本”和“分组探测成本”，同时保留兼容的总探测成本。
- 用户侧复用模型广场权限和分组可见性，管理员侧复用渠道监控根权限。

### 实现前需要产品确认

当前产品口径已确认，无阻塞实施的待确认项。实现期间如果现有路由或成本账本约束与本文假设不一致，再针对具体冲突补充评审。
