# 渠道监控查询与并发审查方案

## 1. 目标

确认渠道监控各接口的查询、聚合、缓存和实时数据处理是否满足以下要求：

- 数据口径一致：成本、成功率、性能、并发、失败明细在不同页面和时间范围内不出现明显偏差。
- 查询复杂度可控：请求规模随时间窗口、渠道数、模型数、分组数、API Key 数增长时不会无界放大。
- 高并发可用：大量管理员同时打开监控页面、刷新筛选条件或触发快照重建时，不造成数据库、Redis、CPU、内存或 Goroutine 风暴。
- 异步链路可观测且可恢复：事件队列满、Redis 延迟、消费者落后、聚合失败和脏分钟修复不会静默丢失关键数据。
- 三种数据库兼容：SQLite、MySQL、PostgreSQL 的查询、锁和迁移语义保持一致。

## 2. 审查范围

### 2.1 HTTP 查询入口

- 渠道监控概览
- 成本概览、日期分页、渠道筛选、摘要模式、API Key 明细
- 性能统计
- 成功率概览与成功率明细
- 今日成功率、历史日期和图表
- 并发快照
- 智能调度路由和执行明细

### 2.2 数据来源与处理链路

- 日成本表、API Key 日成本表
- 分钟聚合表、成功率/性能/延迟桶聚合
- 原始日志扫描、重试匹配、脏分钟标记与修复
- Redis Stream 写入、消费、去重、共享投影和实时查询
- 页面快照、本地 singleflight、Redis lease、失效和刷新
- 数据库连接池、Redis 读写/消费连接池

## 3. 审查维度

每个功能都按同一模板输出：

1. **查询路径**：入口函数、调用链、SQL/Redis 命令数量和返回数据量。
2. **数据处理**：聚合位置（数据库或 Go/Redis）、排序、分页、去重、溢出和空数据行为。
3. **并发模型**：锁、事务、singleflight、队列、lease、消费者数量和失败重试。
4. **复杂度估算**：以渠道数 C、时间窗口 D/M、模型数、分组数、API Key 数 K 表示 CPU、内存、网络和数据库/Redis 压力。
5. **一致性风险**：迟到日志、重试日志、成本结算、实时投影与持久化账本之间的偏差窗口。
6. **故障行为**：Redis/数据库不可用、队列满、连接池耗尽、超时、重复请求和快照冷启动。
7. **证据**：文件及行号、现有测试、必要的定向测试或 SQL/Redis 命令统计。
8. **结论**：按 P0/P1/P2/P3 分级，并给出最小可行优化建议。

## 4. 细粒度子任务拆分

后续由主会话逐项调度子代理。每个子任务只允许审查指定文件集合和一个问题边界，避免一次加载整个渠道监控模块。

### A 组：成本读取

#### A1：日成本基础查询

- 文件范围：`model/channel_daily_cost.go`
- 只回答：时间范围、渠道筛选、排序、索引利用是否合理。
- 输出：查询条件、预计扫描行数、明确问题和行号。

#### A2：日成本日期聚合与分页

- 文件范围：`model/channel_daily_cost.go`、`controller/channel_monitor_cost.go`（仅分页调用段）
- 只回答：`page/page_size` 是否落到 SQL，是否存在全量加载后截断。
- 输出：一次请求的查询次数和 O(行数) 处理量。

#### A3：API Key 成本查询与归属

- 文件范围：`model/channel_daily_api_key_cost.go`、`controller/channel_monitor_cost.go`（API Key 汇总段）
- 只回答：API Key 聚合键、名称回填、owner 查询和高基数内存成本。
- 输出：可能重复、漏归属或 N+1 查询的证据。

#### A4：成本接口端到端调用链

- 文件范围：`controller/channel_monitor_cost.go`、`controller/channel_monitor_realtime_cost.go`
- 只回答：一个成本请求实际触发多少次 DB/Redis 查询，摘要与详情是否重复读取。
- 输出：按调用顺序列出查询清单，不重复分析底层实现。

### B 组：性能与成功率读取

#### B1：Redis 共享投影读取器

- 文件范围：`service/channel_monitor_redis_shared_projection.go`（`Query` 函数及附近辅助函数）
- 只回答：分钟/天 HGETALL 数量、Pipeline 响应体、哈希字段规模和 Go 累加内存。
- 输出：按 1 分钟、15 分钟、1440 分钟窗口分别估算命令数。

#### B2：性能接口

- 文件范围：`controller/channel_ratio_monitor_performance.go`（性能接口）、`service/channel_monitor_redis_page_query.go`（页面转换）
- 只回答：一次性能请求是否重复查询、过滤/排序是否在 Redis 侧完成。
- 输出：请求→服务→Redis 的最短调用链和并发放大点。

#### B3：成功率明细接口

- 文件范围：`controller/channel_ratio_monitor_performance.go`（成功率明细）、`controller/channel_monitor_realtime_response.go`
- 只回答：明细数据和 metadata 是否触发第二次完整投影扫描。
- 输出：一次请求的最大 Pipeline 数、重复查询证据和修复方向。

#### B4：历史分钟聚合查询

- 文件范围：`model/channel_monitor_minute.go`（查询函数，不含聚合写入）
- 只回答：GROUP BY、窗口过滤、最新值查询、API Key 维度的 SQL 成本和索引匹配。
- 输出：可能的慢查询条件，不提出大范围重构。

### C 组：概览、并发与快照

#### C1：概览接口查询重复

- 文件范围：`controller/channel_ratio_monitor.go`（`GetChannelMonitorOverview`）、`service/channel_concurrency.go`
- 只回答：渠道列表、并发配置、监控配置是否重复从 DB 加载。
- 输出：重复查询清单和可复用的数据边界。

#### C2：并发状态读写

- 文件范围：`service/channel_concurrency.go`、`controller/channel_concurrency.go`
- 只回答：本地锁、Redis 快照、配置刷新和高并发读是否会阻塞业务。
- 输出：锁粒度、Redis 命令数量、连接池影响。

#### C3：页面快照读取与重建

- 文件范围：`controller/channel_monitor_page_snapshot.go`、`service/channel_monitor_page_snapshot.go`
- 只回答：identity、TTL、singleflight、lease、冷启动 503 和 stale 行为。
- 输出：同一 identity 与不同 identity 的并发行为。

#### C4：管理操作刷新 fan-out

- 文件范围：`controller/channel_monitor_page_snapshot.go`（刷新函数及调用点）
- 只回答：一次管理操作会并发重建哪些页面、是否等待、是否可能形成 DB/Redis 扇出。
- 输出：最大 goroutine 数、超时边界和触发条件。

### D 组：事件写入与 Redis 投影

#### D1：事件生成和调用方处理

- 文件范围：`service/channel_monitor_event_emit.go`、`controller/channel_monitor_event_emit.go`、`service/channel_monitor_model_detection_event.go`
- 只回答：事件是否在每个业务分支生成、返回状态是否被处理、丢弃是否可见。
- 输出：丢事件对业务请求和监控数据的分别影响。

#### D2：事件 writer 队列

- 文件范围：`service/channel_monitor_event_writer.go`
- 只回答：队列容量、非阻塞入队、单 writer、重试、停止/排空和队满行为。
- 输出：吞吐上限的组成因素和明确的数据丢失条件。

#### D3：共享投影写入事务

- 文件范围：`service/channel_monitor_redis_shared_projection.go`（写入函数）
- 只回答：Watch 重试、每事件 Redis 命令数、去重和成本修正的并发冲突。
- 输出：事件批大小固定时的命令量估算。

#### D4：Redis 连接池与可观测性

- 文件范围：`common/redis.go`、`service/channel_monitor_redis_realtime_status_cache.go`、`service/channel_monitor_redis_observability.go`
- 只回答：读/写/消费池隔离、默认大小、连接等待和指标是否能暴露拥塞。
- 输出：默认配置风险与建议观测项。

### E 组：Stream 消费与分钟聚合

#### E1：Stream consumer 吞吐

- 文件范围：`service/channel_monitor_redis_consumer.go`（读取、claim、process）
- 只回答：单消费者、批大小、Block、operation timeout、重试和 pending/lag 行为。
- 输出：积压形成条件，不分析投影内部命令。

#### E2：分钟聚合日志扫描

- 文件范围：`model/channel_monitor_minute.go`（`aggregateChannelMonitorMinuteLogsFromDatabase`）
- 只回答：扫描列、过滤、排序、Go map 聚合、重试边界二次查询。
- 输出：每分钟和回填窗口的扫描成本。

#### E3：分钟聚合事务与锁

- 文件范围：`model/channel_monitor_minute.go`（范围聚合和替换写入）
- 只回答：同库日志扫描是否处在主库事务中，删除/批量插入的锁持有时间。
- 输出：SQLite、MySQL、PostgreSQL 分别给出风险。

#### E4：脏分钟标记与修复

- 文件范围：`model/log.go`、`model/channel_monitor_dirty_minute.go`、`service/channel_monitor_aggregation.go`（修复调用）
- 只回答：日志写路径的同步额外查询/upsert，以及 claim/release/complete 的并发行为。
- 输出：迟到日志、跨分钟重试和高重试率下的额外负载。

### F 组：一致性与数据库兼容

#### F1：成本账本并发更新

- 文件范围：`model/channel_daily_cost.go`、`model/channel_daily_api_key_cost.go`
- 只回答：唯一冲突、原子增量、溢出保护和热点行串行化。
- 输出：是否会丢计数、重复计数或让业务请求等待。

#### F2：任务成本修正

- 文件范围：`model/channel_task_cost_event.go`、`service/task_billing.go`
- 只回答：修正事务、行锁、重试和实时投影修正事件的一致性。
- 输出：重复回调、并发修正和失败后的数据状态。

#### F3：日志写入失败语义

- 文件范围：`model/log.go`、相关测试
- 只回答：日志已持久化但脏标记失败时，调用方是否可能重复写日志或误报请求失败。
- 输出：只描述现状和影响，不修改代码。

#### F4：三数据库兼容矩阵

- 文件范围：上述任务中发现的 SQL/锁代码及 `model/main.go`
- 只回答：SQLite/MySQL/PostgreSQL 的语法、索引、锁、事务行为差异。
- 输出：仅汇总已被其他子任务指出的具体风险，避免重新扫描全部代码。

### G 组：测试与验证

#### G1：现有测试覆盖

- 文件范围：`controller/*channel_monitor*_test.go`、`model/*channel_monitor*_test.go`、`service/*channel_monitor*_test.go`
- 只回答：每个高风险点已有哪个行为测试，哪些失败属于 fixture/环境问题。
- 输出：测试名、覆盖的合同、缺口。

#### G2：确定性并发验证方案

- 不修改生产代码；只设计测试或基准。
- 固定 C/M/K、事件批量、并发数和时间窗口，不使用随机 fuzz、sleep 或日志断言。
- 输出：输入规模、观测指标、通过标准和预期瓶颈。

## 5. 子任务交接格式与上下文限制

每个子代理必须遵守：

- 最多读取 4 个生产文件；需要更多文件时先向主会话申请。
- 输出不超过 1200 字，先给结论，再给最多 5 条证据。
- 每条证据必须包含 `文件:行号`、触发条件和影响；禁止粘贴大段源码或完整测试日志。
- 只分析分配的边界，不重复总结其他子任务。
- 若发现跨边界问题，只写一条“交叉验证请求”，由主会话另行派发。
- 默认只读，不提交代码、不改测试、不改变配置。

## 6. 主会话调度方式

1. 先派发 A、B、D、E 四个独立审查任务。
2. 根据它们的发现，再派发 C、F 做交叉验证，避免重复读取同一链路。
3. 最后派发 G 汇总测试缺口和验证方案。
4. 主会话负责统一风险等级、去重结论、确认文件行号，并形成最终报告。
5. 默认只做只读审查；若发现需要修改，先列出最小改动清单，再单独确认是否实施。

## 7. 验证命令

基础定向测试：

```text
go test ./service -run 'ChannelMonitor|channelMonitor' -count=1
go test ./model -run 'ChannelMonitor|channelMonitor' -count=1
go test ./controller -run 'ChannelMonitor|channelMonitor' -count=1
```

必要时补充：

```text
go test ./service ./model ./controller -count=1
go test ./service ./model ./controller -run 'ChannelMonitor|channelMonitor' -count=1 -v
```

测试失败需区分：

- 代码行为回归；
- fixture/表初始化不完整；
- 环境依赖问题；
- 与本次审查无关的既有失败。

## 8. 最终交付物

- 按 P0/P1/P2/P3 排序的问题清单。
- 每个问题的证据、影响范围、触发条件和是否会影响业务请求。
- 查询/处理链路的复杂度和并发瓶颈总结。
- 不改代码前提下的优先优化顺序。
- 若用户确认实施，再拆分为小补丁并逐项验证。
