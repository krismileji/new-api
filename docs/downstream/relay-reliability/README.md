# 中继失败切换与错误可见性

中继请求在上游渠道失败时，根据请求类型、重试配置和候选渠道状态尝试后续渠道。失败渠道加入当前请求排除集合；渠道并发满载时同样排除并尝试其他候选，显式指定渠道满载则返回 HTTP 429。

重试支持 Chat Completions、Responses 和任务中继。重试上下文保留原始请求和计费边界，最终成功或最终失败写入用户可见日志；中间重试错误不会作为普通用户的独立成功记录。管理员日志保留完整诊断字段，用户侧视图按使用日志功能的脱敏规则处理。
