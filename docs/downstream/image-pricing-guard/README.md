# 图像生成定价保护

`/v1/images/generations` 请求在选择渠道、估算用量、预扣费和调用上游前检查原始模型是否配置有效图像倍率。

未配置倍率时立即返回 HTTP 200 的 `invalid_request` 错误，不调用上游、不预扣费、不进入计费流程，并标记为不可重试。该保护只针对图像生成，不包含图像编辑；检查原始模型名，避免模型映射绕过定价边界。

配置对应模型的图像倍率后，请求恢复原有图像中继路径。
