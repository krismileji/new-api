# 渠道并发限制

每个渠道可配置 0 到 100000 的并发上限，0 表示不限制。Redis 启用时，配置、活动租约和 revision 使用 `channelConcurrency:v1:*` 键，租约 TTL 为 2 分钟并每 30 秒续租；Redis 未启用时使用进程内计数。

自动选路遇到满载渠道会加入当前请求排除集合并尝试其他候选；显式渠道满载或全部候选满载时返回 HTTP 429。上游请求结束后释放租约，重复释放不会重复扣减。

状态探测使用相同的物理渠道租约；逻辑归组只在冻结成员中改选物理渠道，不创建逻辑组级并发计数。
