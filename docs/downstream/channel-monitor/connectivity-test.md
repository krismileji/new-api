# 渠道连通性测试

渠道管理和渠道监控的连通性测试使用同一测试控制器，实际请求会调用上游并校验响应、Usage、首字时间和 TPS。

批量测试按渠道和模型展开；并发循环限制并发数、循环次数和总请求数，停止操作只阻止尚未发出的请求，已发出的请求继续完成。测试结果会记录测试来源、错误分类和成本。

状态探测和分组探测沿用测试请求，但使用独立来源标记和结果类型，不会伪装成普通业务请求。测试流式能力取决于渠道适配器支持，不能假定所有渠道都支持相同的流式选项。
