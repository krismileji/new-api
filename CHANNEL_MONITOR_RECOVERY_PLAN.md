# 渠道监控异常处理现状与修改计划

更新时间：2026-09-08

状态：前三阶段已完成本地实现与针对性验证；第四阶段暂未实施。第 2、3 节保留修改前的现状，实施结果见第 7 节。尚未部署或核验线上实际恢复结果。

## 1. 背景与目标

线上曾反复收到包含 `redis_context_deadline`、`cost_outbox_backlog` 的渠道监控异常邮件。邮件内容已在提交 `5d0cc6ef4` 中简化为中文原因、影响、建议和时间，但本次文案调整没有改变异常判定、补偿机制或通知策略。

后续需要让管理员清楚知道：发生了什么、系统正在做什么、是否已恢复、是否需要人工处理。

监控链路异常不等于上游接口调用失败。运行恢复与历史数据完整性也需要分别判断：后台恢复工作，不代表已经丢失的样本自动补齐。

## 2. 修改前已具备的机制

### 2.1 异常通知与页面展示

- 邮件将已知错误代码转换为简短中文，例如“Redis 操作超时”“成本记录保存延迟”。
- 同一节点内，按收件人和异常原因组合进行 15 分钟冷却；冷却结束后再次触发检查，仍可能发邮件。
- 页面已有 Redis 状态、事件处理状态、待处理数量、最早待处理时间、处理延迟、重试次数、自动接管次数及丢弃或隔离数量。
- 部分异常在后台直接触发通知，部分状态在查询渠道监控接口时触发通知。
- 当前没有完整的恢复通知流程，也没有用于异常事件补偿的页面操作入口。

相关文件：`service/channel_monitor_health_notification.go`、`service/channel_monitor_health_email.go`、`controller/channel_monitor_today_success.go`、`controller/channel_monitor_realtime_cost.go`、`web/src/features/channel-monitor/components/channel-monitor-realtime-status.tsx`。

### 2.2 自动重试与补写

| 异常场景 | 当前处理方式 | 恢复边界 |
| --- | --- | --- |
| 已进入监控写入队列的事件写 Redis 失败 | 最多尝试 3 次；仍失败时尝试保存到数据库待发送表，后台默认每 500 毫秒轮询补发 | 需要事件已成功入队，且数据库兜底保存成功；补发耗时取决于积压与依赖状态 |
| 监控 Redis 消费流程中断 | 自动重新初始化消费流程，重试等待从 1 秒逐步增加到最多 30 秒 | 适用于运行中的消费流程；依赖恢复后才能继续处理 |
| 消费者退出后存在未完成事件 | 继续处理自身待确认事件，并按租约规则接管其他消费者遗留的事件 | 需要原始事件仍存在；接管和处理不是瞬时完成 |
| 成本事件发布到 Redis 失败 | 尝试将成本事件保存到数据库待处理表 | Redis 和数据库兜底都失败时，不能保证事件已被可靠保存 |
| 成本记录保存延迟 | 后台启动时处理一次，随后默认每分钟批量补写；失败记录保留并安排后续重试 | 正常分钟批处理允许短暂积压；持续数据库故障仍需人工排查 |
| 成本补写重复执行 | 按事件标识去重，成本账本更新与完成标记在事务内处理 | 去重保护成本统计，不应把补偿实现为重新调用上游或重复扣费 |
| 无效事件或无法正常处理的事件 | 部分事件会进入隔离队列或成本异常队列，避免持续阻塞正常事件 | 当前没有通用的自动修复、重放与人工确认流程 |

相关文件：`service/channel_monitor_event_writer.go`、`service/channel_monitor_redis_runtime.go`、`service/channel_monitor_redis_consumer.go`、`service/channel_daily_cost_outbox.go`、`model/channel_daily_cost_outbox.go`。

### 2.3 不能保证自动补齐的情况

- 监控写入队列已满时，新样本直接丢弃，不会在请求路径同步写数据库兜底。
- 写入器未启动或已停止时，新监控样本可能直接丢弃。
- Redis 发布和数据库兜底保存都失败时，没有可靠保存成功的事件不能凭空恢复。
- 数据格式错误、事件标识冲突或被隔离的事件，需要根据原始错误和保留的数据复核。
- Redis 中原始事件丢失或超出保留范围时，恢复能力受现有数据库记录与检查点覆盖范围限制。

因此，积压清零只能证明相应队列处理完毕，不能单独证明所有历史监控数据完整。

## 3. 修改前已确认的问题

### 3.1 历史超时会持续被当作当前异常

`common/redis.go` 中的 `redisClientPoolStats` 使用累计计数判断降级原因：

- `PoolTimeoutCount > 0` 时标记连接池超时。
- `ContextDeadlineCount > 0` 时标记操作超时。

这些计数在后续请求成功时不会自动清零。`service/channel_monitor_redis_observability.go` 又将它们转换为监控异常原因。

结果是：一次历史超时，即使后续连接和事件处理已恢复，仍可能在页面持续显示异常，并在邮件冷却结束后再次告警。这是当前应优先修复的问题；它也可能解释部分重复告警，但不能据此断言线上所有告警都是误报。

### 3.2 累计故障、当前故障与数据缺口混在一起

当前还有通过累计丢弃、累计成本发布失败、累计异常事件数量标记降级的逻辑。它们有保留价值，但含义不同：

- 当前故障表示后台处理仍受阻。
- 历史故障表示曾经发生过问题。
- 数据缺口表示某段统计可能永久不完整，不能随运行恢复而清除。

尤其不能简单清零所有计数来消除告警，否则会掩盖真实数据缺口。

### 3.3 缺少统一的恢复状态与通知闭环

- 页面能看到计数，但不能直接区分“正在自动恢复”“运行已恢复”“仍有数据缺口”“需要人工处理”。
- 已有 `ChannelMonitorHealthState` 和健康状态推导函数，但当前检索到的推导函数调用仅在测试中，尚未成为邮件和页面共同使用的生产状态来源。
- 邮件发送函数在异常原因为空时直接返回，因此没有正常恢复邮件。
- 通知冷却状态保存在单节点内存，异常原因顺序也会影响去重键；多节点和进程重启后不能保证同一故障只通知一次。
- 冷却时间在邮件实际发送前写入，发送错误未在该函数中处理，可能出现投递失败后仍进入冷却的情况。

相关文件：`service/channel_monitor_health_contract.go`、`service/channel_monitor_health_notification.go`、`service/channel_monitor_redis_observability.go`。

## 4. 修改计划

前三阶段已按以下顺序实施，具体取舍与验证结果见第 7 节。人工补偿工具后续单独评估。

### 第一阶段：修正当前异常判定

1. 保留现有累计计数供诊断使用，新增或复用能判断近期异常的观测信息。
2. Redis 超时按近期失败、当前可用性和后续成功情况判断，不再以累计计数大于零作为持续故障依据。
3. 按 Redis 客户端角色分别判断读、写、消费链路，避免某一路成功就覆盖其他链路仍在发生的故障。
4. 明确观察窗口、采样间隔和稳定恢复条件；阈值结合现有超时、重试及分钟批处理节奏确定，并通过可控时间测试验证。
5. 无新请求、观测失败或样本过期时，不能仅凭时间经过就认定恢复；需要有效探测或实际处理成功的证据。
6. 成本积压仍按当前待处理记录、重试情况和最早待处理时间判断，保留正常分钟批处理的容忍范围。当前代码通常在待处理记录发生重试或最早记录超过 120 秒时报告成本待写入积压。
7. 将历史丢弃和异常隔离记录作为独立的数据完整性提示保留。

预计涉及：`common/redis.go` 中必要的观测扩展、`service/channel_monitor_redis_observability.go` 及相关测试。实施前先核对 `upstream/main` 文件归属，尽量将监控专属判定放在下游文件，减少共享代码改动。

### 第二阶段：统一运行状态与恢复进度

1. 评估并接入已有健康状态结构，避免新增另一套含义不一致的状态机。
2. 后台定期采样并更新状态，使恢复识别不依赖管理员是否打开页面；页面与邮件读取相同的状态结果。
3. 将运行状态与数据完整性分开记录。运行状态描述正常、受影响或不可用；恢复详情描述补偿是否进行中、最近进展、遗留缺口和人工处理原因。
4. 记录首次异常时间、最近异常时间、最近成功处理时间、待处理量、最早待处理时间及恢复时间，并区分节点本地计数与共享队列计数。
5. 只有在相关后台任务工作且确有重试或补偿证据时展示“正在自动恢复”；积压长期无进展、任务未运行或存在不可自动修复的事件时提示人工处理。
6. 多节点明确采样与通知的责任节点、状态归属及接管策略；Redis 故障时保留本地故障观测，不能将共享状态不可读解释为正常。
7. 沿用已有后台重试、数据库兜底、租约接管和去重机制，避免为了显示恢复进度额外触发一套并发补偿任务。

预计涉及：`service/channel_monitor_health_contract.go`、监控后台观测逻辑、`controller/channel_monitor_realtime_response.go` 及相关接口元数据。

### 第三阶段：页面展示与简短通知

页面建议使用以下状态文案，具体数量和时间必须来自实际观测：

| 场景 | 建议显示 |
| --- | --- |
| 依赖已恢复，后台仍有待处理事件且有处理进展 | 正在自动恢复：还有 128 条待处理 |
| 相关链路稳定正常、待处理事件完成且未发现遗留缺口 | 运行已恢复 |
| 后台已正常，但曾有无法补齐的样本 | 运行已恢复，部分历史统计不完整 |
| 长期没有进展、后台未运行或存在异常隔离事件 | 需要人工处理：显示具体原因和检查项 |

通知计划：

1. 首次确认异常时发送简短邮件，说明原因、影响，以及已确认的自动处理动作或需要人工检查的事项。
2. 相同故障在短暂抖动期间合并通知；仅在持续异常达到提醒间隔或状态明显恶化时再次提醒。
3. 对已成功发送异常通知的故障，在稳定恢复后发送一次恢复邮件。
4. 存在历史数据缺口时，恢复邮件说明“运行已恢复，部分统计仍不完整”，不能写“数据已全部补齐”。
5. 异常原因先规范化、排序、去重，再用于通知判重；为故障建立可关联的状态周期，处理多节点并发和进程重启后的重复发送。
6. 区分发送中、发送成功和发送失败。只有成功投递后确认通知记录；失败要有可观测结果和有界重试，避免漏报或无限重发。
7. 保留现有通知开关、收件人和通知类型配置。恢复通知纳入“监控健康”通知类型，不扩大收件范围。

预计涉及：`service/channel_monitor_health_notification.go`、`service/channel_monitor_health_email.go`、邮件预览及测试、渠道监控接口和 `web/src/features/channel-monitor/`。

### 第四阶段：按需要补充人工处理能力

1. 先展示待复核事件的时间、来源、失败原因和可恢复性，避免只显示数量。
2. 仅对原始数据仍在、格式有效且能保证去重的事件提供重放能力。
3. 操作需要管理员权限、范围预览、并发保护和结果记录；大批量重放应在后台执行。
4. 缺失原始数据或无法证明正确性的事件标记为数据缺口，禁止通过补零、清空异常计数等方式伪装完成。
5. 对确需修正的数据单独设计复核流程，不直接开放修改金额或重新结算的操作。

此阶段需在前三阶段稳定后评估，具体存储和接口方案尚未确定。

## 5. 验收标准

- [x] 一次 Redis 超时后，同一客户端在最后失败至少 30 秒后成功，能够解除超时状态，累计次数保留。
- [x] 持续失败、无成功证据、其他连接池成功、观察数据过期，均不能单独确认恢复。
- [x] 后台定期采样并更新状态，页面读取结果；正常恢复判定通过显式时间输入测试。
- [x] 正常成本分钟批处理保留容忍范围，真实积压和停止工作的后台任务能够识别。
- [x] 已有监控事件重试、数据库兜底、补发及成本处理回归测试通过。
- [x] 丢弃、隔离与历史恢复缺口独立保留，不宣称数据全部补齐。
- [x] 页面展示恢复进度、历史缺口和人工处理状态，邮件使用相同状态结果。
- [x] 异常邮件成功发送后建立恢复通知关系，稳定恢复后发送一次恢复通知。
- [x] 原因变化不绕过冷却；按节点标识协调并发与重启，Redis 故障时的限制见第 7.3 节。
- [x] 未新增上游请求重放、用户扣费、账本修正或人工补偿操作。

## 6. 实施与验证约束

- 按 `.agents/downstream.md` 核对代码归属，优先在下游模块新增或调整逻辑，保持修改范围集中。
- 下游新增文案使用简体中文，不修改语言包；保留项目身份与署名。
- 状态与时间窗口测试使用显式时间输入，通知测试模拟投递结果，避免用长时间等待验证行为。
- 涉及重试与接管时，验证 Redis 中断、恢复、未确认消息接管和重复处理路径；必要时使用真实 Redis 做故障恢复集成测试。
- 页面改动验证桌面和手机视口，状态、数量及时间变化时不能产生遮挡或布局溢出。
- 若实现涉及数据库行为、模型、事务、迁移或数据库依赖变更，必须按项目规则在真实 SQLite、MySQL、PostgreSQL 上验证；涉及迁移时覆盖已有数据升级、首次启动和重复启动。
- 人工事件重放、修正和确认工具不在本次实现范围内。

## 7. 实施记录与验证结果

### 7.1 已实现行为

| 阶段 | 结果 |
| --- | --- |
| 第一阶段 | 超时状态按同一 Redis 客户端的近期失败与后续成功解除，累计统计不清零 |
| 第二阶段 | 后台采样、稳定恢复、进展识别、历史缺口保留和按节点保存状态已接入 |
| 第三阶段 | 页面恢复摘要、简短异常与恢复邮件、投递失败重试、通知去重和 SMTP 超时已接入 |
| 第四阶段 | 暂未实施，仍需要人工检查日志及被隔离的原始记录 |

- 后台启动后等待 10 秒进行首次采样，之后每 10 秒检查一次。健康检查随成本后台运行生命周期启动和退出。
- 页面通过受管理员权限保护的 `GET /api/channel_monitor/health` 在进入页面和手动刷新时读取本节点状态，不启用轮询。每次读取时，接口会检查后台结果是否过期；请求失败或接口返回过期状态时，不会继续显示缓存中的正常结果。
- 超时解除要求同一客户端在最后失败至少 30 秒后完成有效操作；空键响应属于有效 Redis 响应。后台对闲置客户端进行探测，写入角色额外验证写入能力。
- 确认运行恢复需要连续正常至少 60 秒；两次有效采样间隔超过 30 秒会重新开始确认。新的正常分钟批次不要求长期为空，避免持续流量下永远无法恢复。
- 监控积压、成本事件积压、成本投影更新分别观察。成本账本仍保留现有正常分钟批处理范围；成本后台进展也用于识别任务是否停止工作。
- 异常持续 5 分钟、观测失败、相关后台任务停止工作，或异常期间存在隔离记录时，显示需要人工处理。显示自动恢复需要实际处理进展。
- 恢复状态与历史缺口分别记录。进程重启时加载本节点保留的状态，重新进行稳定确认；历史缺口不会随进程重启直接消失。
- 邮件保持简短中文，包含原因、影响、处理与时间，附节点标识。恢复邮件有历史缺口时明确保留说明。
- 同一故障正常重复提醒仍为 15 分钟；由降级恶化为不可用时允许升级提醒。投递失败先等待 30 秒、60 秒，连续失败后按 15 分钟间隔重试。
- 本监控通知的 SMTP 整个交互设置 30 秒超时，失败记录可在页面看到；其他邮件调用保持原接口及默认超时行为。

### 7.2 本地验证

真实依赖版本：SQLite 3.50.4、MySQL 8.4.10、PostgreSQL 18.6、Redis 8.8.0。MySQL、PostgreSQL 和 Redis 使用独立临时测试容器。数据库没有生产模型、迁移或依赖变更，验证覆盖已有待处理表查询和健康采样，未声称验证最低版本或上线迁移。

针对性后端回归命令：

```powershell
go test ./common ./service ./controller ./router -run '^(TestRedisClientPool|TestRedisCommandHealth|TestRedisPoolRecovery|TestSMTPTimeout|TestSendEmail|TestNewSMTP|TestChannelMonitorRecovery|TestChannelMonitorHealth|TestGetChannelMonitorRecoveryBefore|TestBuildChannelMonitorHealth|TestBuildChannelMonitorRecovery|TestPreviewChannelMonitorNotificationEmail|TestChannelMonitorRedisRuntime|TestApplyChannelMonitorRedisPoolHealth|TestChannelMonitorEventWriter|TestCM07ChannelDailyCost)' -count=1
```

真实数据库与 Redis 验证命令，以下地址和凭据仅用于独立临时测试容器：

```powershell
$env:TEST_MYSQL_DSN='root:monitor_test_only@tcp(127.0.0.1:23306)/monitor_recovery?parseTime=true'
$env:TEST_POSTGRES_DSN='host=127.0.0.1 port=25432 user=postgres password=monitor_test_only dbname=monitor_recovery sslmode=disable'
$env:TEST_MONITOR_REDIS_ADDR='127.0.0.1:26379'
go test ./service -run '^(TestChannelMonitorHealthObservationDatabaseMatrix|TestChannelMonitorRecoveryNotice)' -count=1 -v
```

- `TestChannelMonitorHealthObservationDatabaseMatrix` 在三种真实数据库上通过，覆盖待处理记录、后台任务失活，以及无页面请求时的后台状态更新。数据库测试使用独立表前缀并清理测试表。
- 通知测试覆盖投递失败保留重试机会、恢复通知只发送一次、原因组合变化不绕过冷却、历史缺口不重复提醒、进程内状态清空后读取 Redis 通知记录，以及并发投递受 Redis 租约保护。
- 现有普通邮件、显式 STARTTLS 和隐式 TLS 测试通过；新增测试验证 SMTP 服务器不返回欢迎消息时连接会超时。
- 前端 `bun run typecheck`、修改文件的 `oxlint` 和 `bun run build` 通过；相关 API、页面框架、统计覆盖提示和恢复状态测试通过。
- 使用真实恢复组件与生产 CSS，在 Playwright / Edge 的 1280、390、320 像素视口分别检查自动恢复、历史缺口和人工处理，共 9 组布局，无水平溢出，手机截图已目视检查。
- `go test -race` 未能运行：当前 Windows 工具链未启用 cgo，且没有可用 C 编译器。普通回归和显式并发通知测试通过，不能等同于完成竞态检测。
- 2026-09-08 提交前复核：上述针对性后端回归和前端恢复状态的 6 项测试再次通过。本轮数据库检查仅执行 SQLite，MySQL、PostgreSQL 因未配置测试连接而跳过，Redis 使用 miniredis；以上真实三库与 Redis 结果属于此前的实施验证记录，不代表本轮重新执行。

### 7.3 明确的边界

- 本次以节点为健康状态和通知归属范围，邮件带节点标识。相同节点标识的并发进程通过 Redis 去重；不同节点会各自报告自身故障，没有实现全群集告警合并。
- 节点标识沿用 `CHANNEL_MONITOR_CONSUMER_ID`、`INSTANCE_ID`、`POD_NAME`、`HOSTNAME` 或主机名。需要跨重启关联时，应提供稳定且每节点唯一的标识。
- Redis 可用时保存状态和通知记录，停止刷新后保留 30 天；Redis 不可访问时退回本节点内存通知冷却，并暂停依赖共享记录的恢复邮件。此时跨进程重启无法保证去重。
- SMTP 已接受邮件但返回结果不明确，或发送成功后进程在保存通知记录之前退出，仍可能重复投递。现有邮件协议不能提供严格的仅投递一次保证。
- 本次验证使用测试数据和模拟邮件投递，未向线上收件人发送测试通知，未进行生产部署。

### 7.4 上游文件改动

- `common/redis.go`：连接现有 Redis 命令观测与可恢复的超时状态，保留原有累计指标。恢复判定主体放在新增的 `common/redis_command_health.go`。
- `common/email.go`：为监控通知增加可选择的 SMTP 超时入口，复用原有认证、TLS 和内容发送逻辑，避免复制邮件发送器。
- 其余业务和前端改动均位于已确认的下游文件或新增文件中；未修改项目身份、署名或语言包。
