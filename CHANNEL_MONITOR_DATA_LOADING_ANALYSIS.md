# 渠道监控数据加载与改造进度

## 结论

渠道监控的数据不是每次都扫描原始日志，而是先按分钟汇总到数据库，再从分钟汇总表读取。

之前的问题主要有两个：

1. 页面展示的是业务请求数据，实际调度还会合并测试和探测样本，两边口径不完全一致；
2. 调度详情每次打开都会重复查询和计算，渠道、分组、模型多时压力会变大。

改造一共分 **3 批**，目前已经全部完成。

## 数据从哪里来

```text
正常业务请求 -> 原始日志 -> 每分钟汇总表 --+
                                            +-> 调度窗口 -> 内存评分
手动测试/定时探测 -> 共享测试样本 -----------+
```

- 原始请求日志：保存在数据库；
- 分钟汇总：保存在数据库，不会把所有历史请求长期放进内存；
- 测试和探测样本：按“渠道 + 模型”保存在数据库；
- 窗口合并、归一化和评分：查询后在内存中完成；
- 查询结果：短时间放在内存缓存中复用。

## 第 1 批：统一数据口径（已完成）

这一批解决“页面看到的数据和调度使用的数据不一致”。

- 分开返回业务请求、测试/探测样本、调度合并数据；
- 性能窗口和稳定性窗口分别统计测试样本；
- 同一份“渠道 + 模型”样本 JSON 在一次请求中只解析一次；
- 稳定性试放窗口改成批量查询，不再每条路由查一次数据库；
- 只查询当前策略真正需要的性能、稳定性和抖动数据；
- 只有进入智能调度视图时，才加载完整窗口数据。

结果：页面“窗口数据 / 测试样本”现在展示的是实际调度会使用的合并口径，同时仍能看到业务和测试样本各占多少。

## 第 2 批：缓存和当前预计得分（已完成）

这一批解决“重复聚合”和“得分是历史值”的问题。

- 性能、稳定性和试放窗口增加 10 秒内存缓存；
- 相同窗口的并发请求只执行一次数据库聚合；
- 试放窗口先排序、去重，再生成缓存键，路由顺序变化不会重复查询；
- 调度看板和实际调度共用同一套窗口缓存；
- GET 调度接口不再初始化路由状态，也不再直接清理到期的固定主渠道；
- 页面新增“当前窗口预计得分”；
- “当前窗口预计得分”和“最近实际调度得分”分开展示；
- 当前预计得分复用实际调度的候选归一化和评分函数；
- 当前预计只读，不会修改优先级、权重或历史调度得分；
- 禁用、未参与调度的路由不会生成当前预计得分；
- 样本不足时仍返回缺少什么样本的明细，但最终得分为空；
- 保本兜底和管理员固定主渠道继续使用实际调度已有规则，不限制手动固定。

结果：管理员能同时看到“按现在窗口算出来的分数”和“上一次真正执行调度时的分数”，不会再把历史分数误认为实时分数。

## 现在的性能情况

按几十个渠道、10 个以上分组、10 个以上模型考虑：

- 数据库查询次数不会跟路由数量一起增长；
- 不会为几千条路由逐条查询试放窗口；
- 同一时间窗口内，多次刷新和并发请求会复用缓存；
- 前端不开智能调度视图时，只读取轻量摘要；
- 得分仍在请求内存中计算，但不会把全部历史请求加载到内存。

因此当前主要开销是少量分钟汇总查询、样本解析和内存评分，不再是“路由越多，数据库查询越多”。

## 第 3 批：数据完整性提示（已完成）

这一批解决“配置显示 60 分钟，但数据库可能只有 5 分钟数据”的问题。

- 新部署先生成最近数据，再从近到远按 1 小时一段补齐调度窗口；
- 渠道视图和调度详情都会返回分钟汇总实际覆盖的开始、结束时间；
- 当前查看的性能窗口或稳定性窗口不完整时，页面会直接提示；
- 清理旧数据时会保留调度窗口仍然需要的分钟汇总，不会因为保留天数过短把它删掉；
- 覆盖范围单独记录，只查询一行状态，不会重新扫描原始日志来判断是否完整。

结果：管理员能分清“窗口内确实没有请求”和“历史分钟汇总还没补齐”。第 3 批没有改变评分规则。

手动选择的性能范围可能比智能调度窗口更长。页面仍会准确提示是否完整，但后台优先补齐实际配置的性能、稳定性调度窗口。
