# 渠道监控统一事件流与实时读模型设计

## 1. 最终结论

本次直接改成一套请求事件、一套实时缓存、一套读取口径，不保留旧实时链路兼容，也不做双写或灰度切换。

```text
请求、探测、测试完成
        │
        │ 非阻塞入队，不执行数据库和网络 I/O
        ▼
有界异步事件队列
        │
        ▼
统一实时投影缓存
        │
        ├── 页面：首字、TPS、成功率、缓存、成本、窗口数据
        ├── 智能调度：状态、预计得分、优先级、权重、稳定性
        └── 历史压缩：分钟、日、成本历史
```

核心约束：

- 请求线程只构造事件并尝试放入内存队列，不等待消费者，不写新增监控表，不访问 Redis。
- 页面和智能调度只读取统一实时投影，不再各自统计请求数据。
- 分钟聚合不再参与实时页面和实时调度，只负责历史查询、压缩和清理。
- 页面不自动轮询、不在窗口聚焦或网络恢复时自动刷新，只由用户点击刷新。
- 删除旧的请求成功缓冲、分钟刷新等待、实时查询缓存和重复窗口，不保留兼容分支。

## 2. 可独立验证的实施子任务

| 子任务 | 改动 | 独立验收 |
|---|---|---|
| A. 统一事件契约 | 定义请求、探测、测试的统一事件字段和来源 | 构造、校验、JSON、显式零值和非法值测试通过 |
| B. 非阻塞异步队列 | 新增有界队列、批处理、幂等、重试、关闭刷新和统计 | 入队不阻塞且无数据库/网络 I/O；满队列、重复、重试、关闭测试通过 |
| C. 统一实时投影 | 消费事件并生成渠道、模型和全局窗口快照 | 固定事件下 TTFT、TPS、成功率、缓存、成本、水位、跨日结果正确 |
| D. 业务请求接线 | 成功、失败、重试最终结果在公共完成点发事件 | 每个请求最终结果只计一次；重试尝试和最终结果可区分 |
| E. 探测与测试接线 | 状态探测、智能探测、手动测试、模型检测使用同一事件 | 来源计数正确，业务成功率不会混入维护测试 |
| F. 页面切换 | 总览、性能、今日成功率、缓存、今日成本读取统一快照 | 同一水位下页面各指标可对账，不再等待分钟聚合 |
| G. 智能调度切换 | 稳定性、当前窗口评分、自适应刷新读取统一窗口 | 状态、得分、P/W 和页面窗口使用同一事件水位 |
| H. 手动刷新 | 删除渠道监控内全部自动刷新 | 页面静置不发请求；点击刷新更新所有已挂载查询 |
| I. 删除旧实现 | 删除旧请求缓冲、实时分钟触发、旧缓存和死代码 | 全局引用检查无旧实时入口，编译和回归测试通过 |
| J. 全量验证 | 后端、前端、并发、性能和差异检查 | 定向测试、根模块测试、前端检查、`git diff --check` 通过 |

并行关系：A、B、H 可并行；C 依赖 A；D、E、F、G 依赖 B/C；I 必须在 F/G 完成后执行。

### 当前实施状态

| 子任务 | 状态 | 实施结果 |
| --- | --- | --- |
| A-C | 已完成 | 统一事件、非阻塞有界队列、批消费、幂等和渠道/模型/页面实时投影已落地 |
| D-E | 已完成 | 普通请求、重试、流式/WebSocket、任务结算、状态探测、智能探测、手动测试和模型检测已接入 |
| F-G | 已完成 | 页面与智能调度的性能、成功率、缓存、成本、稳定性、样本和当前窗口评分读取同一事件投影 |
| H | 已完成 | 渠道监控查询关闭定时、焦点和重连刷新，顶部按钮统一手动刷新 |
| I | 已完成 | 旧成功缓冲、Redis 成功窗口、分钟读取等待、路由指标缓存和周期完整调度已删除 |
| J | 已完成 | 后端三包全量测试、定向前端测试、类型检查、lint、格式和生产构建完成；最终结果见本文末尾 |

## 3. 统一事件

事件至少包含：

```text
event_id                幂等 ID
event_sequence          消费水位
schema_version
occurred_at             请求或测试完成时间
created_at              入队前事件创建时间
processed_at            投影处理完成时间
channel_id
group
model
request_id
node_id
source                  business/status_probe/smart_probe/manual_test/model_detection
outcome                 success/failure/unresolved/canceled
is_stream
is_retry_attempt
is_final_attempt
request_dispatched
status_code
first_token_ms
tps
prompt_tokens
completion_tokens
cache_read_tokens
cache_write_tokens
input_tokens
attempt_duration_ms
settled_cost_nano_cny
unresolved_cost_nano_cny
```

事件规则：

1. 业务成功在结算和使用量确定后发出，包含最终 TTFT、TPS、缓存 token 和结算信息。
2. 每次上游失败可记录尝试事件，但页面“最终成功率”只计算最终事件。
3. 重试成功后，前面的失败尝试计入稳定性和尝试成功率，不计入最终失败率。
4. 状态探测、智能探测、手动测试和模型检测必须标记来源，不能混入业务成功率。
5. 流式请求必须在流结束后记录最终成功；收到首字不等于请求成功。
6. `event_id` 重复时只处理一次。

## 4. 请求线程与队列

请求线程只执行：

1. 从已经存在的请求上下文、使用量和结算结果构造事件。
2. 校验事件必要字段。
3. 使用非阻塞 `select` 放入有界队列。

禁止在该路径执行：

- 新增同步数据库写入；
- Redis 命令；
- 等待队列空间；
- 页面快照计算；
- 智能调度重算。

队列按批次消费。批次内去重后一次更新实时投影，减少锁竞争。队列暴露容量、深度、已接收、已处理、重复、失败、满队列丢弃和最后处理时间。

队列满时优先保证业务请求：新事件不阻塞请求，增加丢弃计数，并将实时快照标记为降级。这里不回退旧实时链路，否则页面和调度会重新出现两套口径。

## 5. 统一实时投影

实时投影按 `channel_id + normalized_model` 保存有限滚动窗口，同时维护渠道级和全局汇总。

每个窗口保存：

- 事件水位、数据截止时间、处理时间、窗口开始时间；
- 业务请求数、最终请求数、成功数、失败数、重试尝试数；
- TTFT 总量和样本数；
- 输出 token、生成耗时和 TPS；
- 缓存读取 token、缓存写入 token、总输入 token；
- 已结算成本、未解析成本和各来源成本；
- 错误、风险、健康、首字告警等稳定性样本；
- 各事件来源计数。

窗口使用固定最大事件数和时间范围双重限制，防止内存无限增长。写入由单个消费者批量完成；读取返回不可变快照或副本，不在 API 请求期间重算全部历史。

## 6. 指标口径

| 指标 | 统一计算方式 |
|---|---|
| 首字 | 有 `first_token_ms` 的完成事件平均值与样本数 |
| TPS | `completion_tokens / generation_seconds`，无有效生成耗时不计样本 |
| 尝试成功率 | 成功尝试数 / 已发出的完成尝试数 |
| 最终成功率 | 最终成功请求数 / 最终完成请求数 |
| 缓存命中率 | 有缓存读取 token 的请求数 / 有输入 token 的请求数 |
| 缓存利用率 | 缓存读取 token / 总输入 token |
| 缓存写 | 缓存写 token 和缓存写请求数 |
| 今日已结算成本 | 北京时间今日窗口内 `settled_cost_nano_cny` 之和 |
| 未解析成本 | 北京时间今日窗口内 unresolved 事件数和金额 |
| 稳定性 | 同一窗口事件计算错误、风险、健康、首字告警和压力 |
| 测试样本 | 按 `source` 单独统计，不混入业务最终成功率 |

所有分母为 0 时返回“无样本”状态，不能用 0% 伪装成已完成统计。

## 7. 页面读取

实时页面 API 返回统一元数据：

```text
event_watermark         已处理事件水位
data_cutoff_at          已纳入的最后事件时间
processed_at            投影最后处理时间
page_generated_at       本次 API 响应时间
queue_depth             当前队列深度
realtime_degraded       队列丢弃或消费失败后为 true
```

总览、性能、今日成功率、缓存和今日成本直接读取统一快照。历史日期、历史分钟图和历史成本分页继续读取历史表，但不得触发实时聚合等待。

前端只保留一个刷新入口。页面加载时请求一次；之后只有点击刷新、用户执行明确操作后主动刷新对应数据。取消定时器、焦点刷新和网络重连刷新。

## 8. 智能调度读取

智能调度的稳定性、自适应备援和当前窗口评分从统一实时窗口读取，不再读取单独的成功请求缓冲，也不再等待分钟聚合。

调度保留两类执行：

- 请求事件触发的轻量自适应应用：消费批次完成后，根据受影响的渠道/模型窗口去抖执行。
- 配置变更、人工操作和低频校准触发的完整调度：重新生成基础优先级和权重。

取消“每分钟刷新调度”。周期任务只允许做历史压缩、过期清理和低频校准，不能作为实时状态更新入口。

页面展示的当前状态、预计得分、优先级、权重、窗口数据和测试样本必须带同一个 `event_watermark`。调度应用结果另带 `scheduler_applied_at`，用于说明窗口已更新但 P/W 是否已经应用。

完整调度 handler 只作为按需系统任务保留，不再实现周期任务接口。启用中的智能调度配置、参与状态、成本等管理变更会创建一次完整调度任务；管理员仍可手动执行或强制重置。请求完成事件只刷新受影响的调度池。

## 9. 旧实现删除范围

完成切换后直接删除：

- `EnsureChannelMonitorAggregationFresh` 在实时页面和调度读取中的调用；
- 智能调度独立的请求成功缓冲及其 Redis 窗口；
- 页面实时查询的 10 秒独立缓存和基于分钟版本号的刷新判断；
- 分钟聚合完成后触发实时调度的逻辑；
- 前端所有渠道监控自动轮询；
- 只为旧实时链路服务的设置、测试和死代码。

保留：

- 原始消费日志和错误日志；
- 分钟、日、成本历史表；
- 历史压缩、历史重建、保留期清理；
- 配置变更和异常恢复所需的完整调度。

## 10. 性能要求

- 请求线程入队操作目标为微秒级，不等待锁竞争和队列空间。
- 消费者批量更新投影，避免每个事件单独写数据库。
- 页面实时接口只复制已有快照，不扫描日志表。
- 智能调度只重算受影响的渠道/模型，不进行每分钟全量调度。
- 实时窗口必须有事件数上限、时间上限和幂等集合上限。
- 历史写入使用批处理，在消费者之外异步执行。

## 11. 验收标准

1. 同一 `event_watermark` 下，页面和智能调度使用同一批事件。
2. 请求完成后不需要等待分钟边界即可更新实时窗口。
3. 请求线程不会因为监控队列、数据库或 Redis 变慢。
4. 页面静置时不产生自动刷新请求。
5. TTFT、TPS、成功率、缓存、成本、稳定性和测试样本能从事件逐项对账。
6. 当前窗口有样本时，错误率、风险率、健康率和首字告警率不会因为缺字段显示为假 0。
7. 队列满、消费失败或数据延迟会明确标记降级，不会静默显示为实时。
8. 旧实时缓冲、旧分钟触发和旧页面缓存无剩余引用。
9. 后端定向测试、前端测试和类型检查、`git diff --stat`、`git diff --check` 全部通过。

## 12. 当前边界与诚实展示

当前统一实时投影是单进程内存缓存，目的是让请求线程不增加数据库或 Redis I/O，并让同一进程内的页面与调度共享一份数据。当前实现具备以下边界：

- 进程重启后，重启前尚未压缩为历史数据的实时窗口不会自动恢复。
- 多实例部署时，各实例只持有本节点消费到的请求事件，尚未形成跨节点共享实时窗口。
- API 返回 `projection_started_at`；当请求窗口早于投影启动时间，或队列发生丢弃/消费失败时，`realtime_degraded=true`。
- 页面会展示数据截至时间、队列积压和实时降级状态，不再把部分窗口或丢失事件伪装成完整实时数据。

因此，当前版本在单实例内实现页面、智能调度和稳定性处理的统一口径；多实例全局一致需要后续把异步消费者接到共享 Redis Stream 或等价事件流，并让各节点从同一全局水位重放投影。在共享事件流落地前，不能宣称跨节点强一致。

### 12.1 窗口覆盖与截断

实时窗口不是无限历史。每个渠道/模型窗口达到事件上限或路由被淘汰时会丢弃最旧事件，并将 `coverage_start` 更新为当前窗口仍覆盖的最早事件序号。API、页面和智能调度必须同时检查 `coverage_start`、`event_watermark` 和窗口事件数；发生截断、淘汰、队列丢弃或消费者失败时标记 `realtime_degraded=true`，不得把剩余窗口显示成完整样本。

### 12.2 样本资格与任务成本

- `scheduling_eligible` 是事件字段，不再由事件来源推断。业务请求和智能调度探测默认可参与调度；关闭“计入样本”的手动测试、关闭记录的状态探测以及批量自动维护测试只进入监控窗口，不改变智能调度。
- 异步任务提交使用稳定的 `task:<task_id>` 成本事件 ID。提交阶段记录未解析/初始成本，最终差额结算或退款只发送成本修正事件；投影按成本事件 ID替换金额，不重复计入成功请求。
- 模型检测的监控事件只在最外层数据库事务成功提交后发送；事务内部 fallback 不直接发事件，避免提交失败留下脏实时数据。
- Midjourney 的最终成功、失败和真实成本路径统一发出监控事件，使用真实渠道 ID 和动作模型名，不能再旁路旧成本统计。

### 12.3 请求路径性能边界

事件入队不执行 DB/Redis I/O。渠道成本快照仍可能命中一分钟内存缓存；若缓存缺失，现阶段存在少量同步读取，后续可改为配置变更主动刷新内存快照并让请求事件携带 unresolved 状态。该边界必须在性能监控中单独观测，不能将其描述为所有请求路径绝对零 DB 查询。

## 13. 最终验证记录

- `go test ./model ./service ./controller ./relay -count=1`
- `go vet ./model ./service ./controller ./relay`
- `bun run typecheck`
- `bun run build`
- 渠道监控统一元数据、手动刷新、智能调度、样本资格和任务成本替换前端/后端定向测试（前端 `30 pass / 0 fail`）
- 变更文件 `oxlint`、`oxfmt`、生产构建和 `git diff --check`

本次必须修改的 upstream-owned 文件及原因：

| 文件 | 原因 |
| --- | --- |
| `controller/channel-test.go` | 手动/自动渠道测试需把 `record_sample` 传入统一事件的调度资格，并返回统一样本状态 |
| `controller/relay.go` | 普通重试/最终完成边界需接入统一事件投影 |
| `model/task.go` | 异步任务必须持久化稳定成本事件 ID、提交成本快照及解析状态，供轮询结算阶段替换 |
| `service/quota.go`、`service/text_quota.go` | 非任务请求的统一成功/失败结算事件只能在公共扣费完成点接入 |
| `service/task_billing.go` | 异步任务提交、差额结算和退款需要稳定成本事件及修正事件 |
| `service/task_billing_test.go` | 为任务成本比例换算、未解析成本跳过和非法输入补充行为回归测试 |
| `service/channel_model_detection_cost.go`、`service/channel_model_detection_worker.go` | 模型检测事务提交后才发送事件，避免 fallback 脏事件和重复事件 |
| `relay/mjproxy_handler.go` | Midjourney 成功/失败/成本路径没有公共 relay 完成点，必须补统一监控事件 |
