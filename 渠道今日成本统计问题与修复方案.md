# 渠道今日成本统计问题与修复方案

**文档版本**：V1.1

**更新时间**：2026-08-06

**文档状态**：阶段一、阶段二、阶段三核心能力和阶段五部分能力已实施；异步任务最终结算待后续独立实施

**适用范围**：渠道监控中的普通 Relay、重试、渠道测试、稳定性探测、异步任务、Midjourney、渠道日成本聚合和前端成本展示。

## 0. 本次实施结果

本次代码修改已经完成以下修复：

1. `/v1/alpha/search` 等总 Token 为 `0`、但存在确定性工具费用的请求会计入已结算成本。
2. 文本、音频、Realtime 和渠道测试中的固定价格请求不再依赖权威 usage；成功后会按固定价格结算。
3. 普通 Relay 的每次重试尝试都建立独立成本状态；真正进入 HTTP、WebSocket 或 AWS SDK 传输但没有完成成本结算时，增加一次未解析尝试。
4. 手动渠道测试、定时测试、智能调度探测和稳定性探测复用同一测试链路；失败且已访问上游时增加一次未解析尝试，未发出请求的本地错误不记录。
5. 异步任务提交和 Midjourney 计费提交在真实访问上游后失败时会记录未解析；成功提交仍按当前暂估或按次价格记录。
6. 缺少有效渠道成本配置的真实请求不再静默消失，而是记录为未解析；页面同时显示“配置缺失”。
7. 渠道、API Key、每日趋势和总览均展示已结算请求数、未解析请求数和解析率；只有未解析事件的记录不再被过滤。
8. 页面金额统一明确为“已结算成本”，存在未解析请求时显示金额不完整提示。
9. 成本批处理写入失败后会把批次放回内存等待后续重试；服务正常退出时会在关闭数据库前主动刷盘。

本次没有实施以下高风险或需要新增持久化账本的能力：

1. 异步任务完成后的最终费用差额、失败退款和跨日调整。
2. Midjourney 提交后的失败退款、构图失败冲减和特殊操作最终结算。
3. 将所有适配器的本地 usage 标记一次性迁移为统一的 `authoritative / deterministic / estimated / missing` 枚举。
4. 缓冲区新聚合键达到上限时的同步回退写入、完整批处理指标和最近成功落库时间展示。
5. 为成本批次增加事件 ID 或批次 ID，消除数据库“已提交但客户端收到超时”时重试造成重复累计的歧义。
6. 将请求 ID、尝试序号和来源持久化为独立成本尝试账本；当前防重复仅覆盖单次请求进程内的尝试状态。

异步任务和 Midjourney 不能直接对现有日聚合表写负数冲减，否则无法同时保证幂等、跨日归属和非负成本约束。后续应先增加以任务唯一 ID 为键的成本结算账本，再接入最终结算和退款。

---

## 1. 背景

当前渠道监控中的“今日成本”不是上游实际发起请求的完整成本，也不是上游账单的直接对账结果。现有实现只在请求成功完成、获得可结算用量或结算结果、渠道存在有效成本倍率且成本事件成功落库时增加成本金额。

这会造成三类偏差：

1. 已经访问上游，但没有产生任何成本事件。
2. 产生了“未解析”事件，但成本金额仍为 `0`，且页面不展示该事件。
3. 已经记录成本，但异步任务最终结算、退款或写入失败没有反映到今日成本中。

因此，页面金额可能低于上游实际支出，也可能因异步任务退款未冲减而高于最终支出。

## 2. 目标与非目标

### 2.1 目标

1. 每次真正发往上游的请求尝试都能进入成本审计口径。
2. 能确定金额的请求计入已结算成本，不能确定金额的请求明确计入未解析数量。
3. 固定价格、工具调用等不依赖 Token 的已知费用不再因为 Token 为 `0` 而漏记。
4. 重试失败、稳定性探测失败和流式处理中断不再静默消失。
5. 异步任务和 Midjourney 的最终结算或退款能够修正初始成本。
6. 页面同时展示已结算成本、未解析数量和成本解析率，避免把不完整数据误认为完整成本。
7. 成本事件写入失败必须可观测，并在服务正常退出时完成缓冲刷盘。

### 2.2 非目标

- 不把本地参数校验失败、鉴权失败、限流拒绝等未访问上游的请求计入成本。
- 不在缺少提供商计费依据时猜测失败请求金额。
- 不把渠道成本与用户扣费、分组加价混为同一口径。
- 不承诺本地统计可以替代提供商账单；提供商账单仍是最终财务对账依据。
- 不改变智能调度、重试顺序或稳定性保护的选路行为。

## 3. 统一口径

### 3.1 成本状态

每个真实上游请求尝试必须归入以下一种状态：

| 状态 | 定义 | 是否增加成本金额 | 是否增加未解析数量 |
| --- | --- | ---: | ---: |
| 未访问上游 | 请求在本地结束，或者探测被跳过 | 否 | 否 |
| 已结算 | 有权威用量、权威结算结果或已知固定价格 | 是 | 否 |
| 未解析 | 已访问上游，但无法可靠确定费用 | 否 | 是 |
| 已调整 | 异步任务最终费用与初始记录不同 | 按最终结果修正 | 否 |

“今日成本”页面必须明确显示金额为“已结算成本”，不能把它描述成没有误差的上游实际成本。

### 3.2 用量质量

用量来源统一分为：

| 用量质量 | 示例 | Token 计费 | 固定价格或确定性按次计费 |
| --- | --- | --- | --- |
| 权威 | 上游明确返回 usage 或最终扣费结果 | 可结算 | 可结算 |
| 确定性本地计算 | 协议固定费用、明确工具调用次数、固定任务单价 | 按计费规则决定 | 可结算 |
| 估算 | 根据文本、音频大小、音频时长或本地 tokenizer 推算 | 未解析 | 价格不依赖估算字段时可结算 |
| 缺失 | 没有 usage，且无法可靠计算 | 未解析 | 已知固定价格时可结算 |

不能继续仅使用“是否本地计算”判断能否结算。确定性本地计费与启发式估算必须分开。

### 3.3 成本解析率

页面增加以下指标：

```text
成本解析率 = 已结算请求尝试数 / (已结算请求尝试数 + 未解析请求尝试数)
```

该指标按上游请求尝试计算。一次用户请求先失败一次、重试后成功时，应包含一次未解析尝试和一次已结算尝试。

## 4. 当前问题

### 4.1 上游失败和重试失败完全不记录

普通 Relay 只有在上游请求、HTTP 状态检查和响应处理全部成功后才记录渠道成本。以下情况不会产生成本事件：

- 网络连接失败或超时。
- 上游返回非成功 HTTP 状态。
- 响应反序列化失败。
- 流式响应处理返回错误。
- 客户端中断导致适配器返回错误。
- 重试过程中的每次失败尝试。
- 所有候选渠道都失败后的最终失败请求。

如果上游已经开始生成、按请求收费，或者失败响应本身也会产生费用，今日成本会偏低。重试成功只会记录最终成功渠道，之前已经访问过的失败渠道没有成本审计记录。

主要实现位置：

- `relay/compatible_handler.go`
- `controller/relay.go`

### 4.2 稳定性降级探测只记录成功请求

稳定性降级和恢复探测通过渠道测试链路真实访问上游。当前行为是：

- 探测成功、usage 权威且成本配置有效时，计入成本。
- 探测成功但 usage 缺失或为本地估算时，只增加未解析数量。
- 探测失败、超时或响应解析失败时，完全不记录成本事件。
- 因并发限制、路由不支持或条件不满足而跳过时不记录，这是正确行为，因为没有访问上游。

因此，频繁进入稳定性降级的渠道可能产生大量失败探测请求，但页面不会体现这些尝试。

主要实现位置：

- `controller/channel_ratio_monitor_schedule_probe.go`
- `controller/channel-test.go`

### 4.3 工具费用和零 Token 请求漏记金额

非阶梯计费路径仍使用 `TotalTokens > 0` 作为成本结算前提。这会错误排除只有工具调用、按次费用或其他非 Token 费用的请求。

明确示例是 `/v1/alpha/search`：

1. 上游不返回 usage。
2. 本地记录一次 `web_search_preview` 工具费用。
3. 用户扣费逻辑通过 `hasBillableUsage()` 识别该费用。
4. 渠道日成本逻辑仍因总 Token 为 `0` 而只记录未解析，金额不增加。

主要实现位置：

- `relay/alpha_search_handler.go`
- `service/text_quota.go`
- `service/channel_daily_cost.go`

### 4.4 固定价格错误依赖权威 usage

对于已经确定按请求、按任务或固定单价收费的请求，成本金额不依赖 Token 是否权威。当前通用入口仍可能因 usage 为估算值或缺失而把这类请求标记为未解析。

这会导致“明明单价已知，但今日成本仍为零”的低估。固定价格和 Token 价格必须采用不同的结算前置条件。

### 4.5 本地用量估算标记不一致

部分适配器会设置本地估算标记，因此只增加未解析数量，例如：

- Cloudflare Chat、流式 Chat 和 STT。
- PaLM、Tencent 流式。
- Coze、Dify 返回零 usage。
- Cohere 缺少 prompt usage。
- xAI、OpenAI 流式缺少 usage。
- Chat 与 Responses 格式互转缺少 usage。
- Claude、Gemini usage 不完整。
- Gemini Embedding。
- OpenAI 非流式 TTS 的音频估算。

另一些路径同样使用本地构造或估算 usage，却没有设置相同标记，例如：

- OpenAI 非流式 Chat 缺少 prompt usage。
- OpenAI 原生 Responses 流缺少 usage。
- OpenAI STT 缺少 usage。
- OpenAI 流式 TTS 缺少上游 usage。
- Volcengine TTS、MiniMax TTS。
- Gemini 图片生成固定 Token。
- Realtime 本地 Token 统计。

结果是同样性质的用量在不同适配器中可能分别被当成“未解析”或“已结算”，统计口径不一致。

主要实现位置：

- `service/usage_helpr.go`
- `service/channel_daily_cost.go`
- 各渠道响应适配器

### 4.6 异步任务只记录提交时预估值

异步任务当前只在提交成功时记录一次预估或按次成本：

- 提交失败不记录。
- 后续任务失败并退款时，不冲减已记录成本。
- 最终 Token、时长或适配器返回的实际扣费高于预估时，不补记差额。
- 最终实际费用低于预估时，不冲减差额。

这既可能造成低估，也可能造成高估。

Midjourney 还有以下特殊情况：

- 失败响应不记录。
- `InPaint`、`CustomZoom` 明确不记录。
- 成功提交后若构图失败，用户退款但渠道日成本不调整。

主要实现位置：

- `service/task_billing.go`
- `controller/relay.go`
- `relay/mjproxy_handler.go`
- `controller/midjourney.go`

### 4.7 缺少有效渠道成本配置时静默跳过

渠道没有对应的 `ChannelRatioMonitor`、更新时间无效，或者成本倍率换算配置无效时，当前逻辑不会记录金额，也不会增加未解析数量。

这会让配置缺失表现成“该渠道没有产生请求”，管理员无法从成本页面区分零成本和缺少成本配置。

主要实现位置：

- `service/channel_daily_cost.go`

### 4.8 批处理写入存在事件丢失窗口

成本事件先进入内存缓冲，再批量写入数据库。当前存在以下风险：

- 缓冲区满时事件可能被丢弃。
- 数据库批量写入重试耗尽后事件可能丢失。
- 服务正常退出时没有明确保证所有待写事件完成刷盘。
- 页面没有显示最后成功落库时间或丢弃事件数量。

这类问题会同时影响所有请求类型，并造成无法从业务日志直接解释的随机差额。

主要实现位置：

- `service/channel_daily_cost_batcher.go`

### 4.9 页面隐藏未解析数据

后端已经保留 `unresolved_count`，但页面只展示已结算金额，并过滤 `settled_count == 0` 的行。

结果是：

- 只有未解析事件的渠道或 API Key 完全不显示。
- 管理员看不到当前成本统计覆盖率。
- usage 缺失、失败探测和配置异常都表现为“没有请求”。
- 无法判断今日成本与上游账单是否具备可比性。

主要实现位置：

- `web/src/features/channel-monitor/components/channel-monitor-channel-cost-table.tsx`
- `web/src/features/channel-monitor/components/channel-monitor-api-key-cost-table.tsx`

## 5. 修复方案

### 5.1 修复可计费用量判断

渠道日成本不能再使用 `TotalTokens <= 0` 作为统一拒绝条件。

改造规则：

1. Token 计费使用权威 Token usage。
2. 工具、媒体、按次等费用使用完整的 `hasBillableUsage()` 语义。
3. 固定价格请求在请求成功且单价确定后直接结算，不依赖 usage 是否存在。
4. 阶梯计费优先使用实际 `BillingUsage` 结果；结果缺失时才进入未解析。
5. 启发式估算不能作为 Token 计费的已结算依据。

该修复应优先覆盖 `/v1/alpha/search`、固定价格模型和工具调用费用。

### 5.2 为每次真实上游尝试记录成本观察

在请求真正交给上游适配器后建立一次成本观察，并在请求结束时归类：

```text
本地拒绝或跳过
  -> 不记录

已访问上游 + 可确定成本
  -> 已结算

已访问上游 + 无法确定成本
  -> 未解析
```

具体要求：

- 网络失败、超时、非成功状态和响应处理失败增加未解析数量。
- 重试的每次上游尝试分别记录，不能只记录最终成功请求。
- 业务请求、手动测试、定时探测和稳定性探测使用相同规则。
- 流式请求只有在适配器明确返回权威 usage 时结算；已经访问上游但无法取得完整 usage 时记为未解析。
- 因并发限制或候选过滤而未发起的探测不记录。
- 默认不猜测失败请求金额；只有提供商协议明确给出失败计费依据时才结算。

为避免重复统计，每次尝试应携带稳定的请求 ID、尝试序号、渠道 ID 和请求来源。相同尝试只能结算一次。

### 5.3 统一用量质量模型

将当前分散的本地估算布尔标记统一为明确的用量质量：

```text
authoritative   上游权威值
deterministic   根据明确计费规则得到的确定值
estimated       启发式估算值
missing         缺失
```

所有响应适配器必须在构造 usage 时设置正确质量。成本结算入口只根据“计费模式 + 用量质量”作一次统一决策，不允许各适配器自行形成不同口径。

为降低改造风险，可以分两步实施：

1. 先补齐现有本地估算标记，并修复固定价格不依赖 usage 的规则。
2. 再引入明确枚举，替换上下文中的隐式布尔判断。

### 5.4 增加异步任务最终结算

异步任务需要保存“已记录成本”和“最终成本”，以任务唯一 ID 保证幂等：

1. 提交成功时记录暂估成本和归属日期。
2. 任务完成时读取提供商最终用量或扣费结果。
3. 最终成本高于暂估时补记差额。
4. 最终成本低于暂估或任务退款时冲减差额。
5. 重复轮询或重复回调不能重复调整。
6. 调整归属原任务提交日期，避免跨日任务把同一笔成本拆到不同日期。

内部可以使用有符号差额，但必须保证任务最终成本和日聚合成本不会变成负数，并遵守现有 quota 饱和和审计要求。Midjourney 使用同一套最终结算机制，不再单独依赖提交阶段记录。

### 5.5 显示未解析数量和解析率

成本概览、趋势、渠道汇总和 API Key 明细统一增加：

- 已结算成本。
- 已结算请求尝试数。
- 未解析请求尝试数。
- 成本解析率。
- 最近成功落库时间。

不得再过滤只有未解析事件的记录。存在未解析事件时，页面显示明确提示：“当前金额不包含未解析的上游请求尝试”。

渠道缺少成本倍率时，应显示“成本配置缺失”，不能与 usage 缺失混为一类。

### 5.6 提升批处理可靠性

成本批处理增加以下保障：

1. 缓冲区满时记录带请求上下文的告警，并进行有超时的同步回退写入，不能静默丢弃。
2. 数据库重试耗尽时记录丢失事件数、聚合键和失败原因。
3. 暴露队列长度、写入批次数、重试次数、回退次数和丢失事件数。
4. 服务正常退出时停止接收新事件，等待当前批次完成并执行最终刷盘。
5. 批量 upsert 保持幂等，防止超时重试导致重复累计。

同步回退必须设置短超时，避免数据库故障无限阻塞正常 Relay 请求。

## 6. 实施顺序

### 阶段一：修复确定性漏记

- 将零 Token 判断替换为完整可计费用量判断。
- 修复 `/v1/alpha/search` 工具费用。
- 固定价格不再依赖权威 usage。
- 补充对应后端回归测试。

该阶段不需要修改数据库结构，风险和改动范围最小。

### 阶段二：暴露统计完整性

- 页面展示未解析数量和成本解析率。
- 保留并展示只有未解析事件的渠道和 API Key。
- 区分成本配置缺失与 usage 无法解析。
- 增加最后成功写入时间和批处理异常指标。

### 阶段三：覆盖失败尝试和探测

- 普通 Relay 每次真实上游尝试都产生已结算或未解析结果。
- 重试失败、渠道测试失败和稳定性探测失败增加未解析数量。
- 统一流式错误和 usage 质量语义。
- 通过请求 ID 和尝试序号避免重复记录。

### 阶段四：异步任务最终结算

- 增加任务成本结算状态和幂等调整机制。
- 任务完成、失败、退款和差额结算更新原提交日成本。
- Midjourney 接入相同机制。

### 阶段五：可靠性收口

- 增加退出刷盘、同步回退和批处理指标。
- 对账业务日志、错误日志、成本尝试数和上游账单抽样。
- 观察一个完整成本保留周期后再确认最终口径。

## 7. 验收矩阵

| 场景 | 预期已结算数 | 预期未解析数 | 预期金额 |
| --- | ---: | ---: | --- |
| 本地参数校验失败，未访问上游 | 0 | 0 | 0 |
| Token 计费请求成功且上游返回权威 usage | 1 | 0 | 按权威 usage 计算 |
| Token 计费请求成功但 usage 缺失 | 0 | 1 | 0 |
| Token 计费请求仅有启发式估算 usage | 0 | 1 | 0 |
| 固定价格请求成功但 usage 缺失 | 1 | 0 | 固定价格 |
| 工具调用费用存在且总 Token 为 0 | 1 | 0 | 工具费用 |
| 上游网络失败 | 0 | 1 | 0，除非提供商有明确失败计费结果 |
| 第一次重试失败、第二次成功 | 1 | 1 | 第二次成功的已知成本 |
| 稳定性探测成功且 usage 权威 | 1 | 0 | 按 usage 计算 |
| 稳定性探测失败 | 0 | 1 | 0，除非提供商有明确失败计费结果 |
| 稳定性探测被并发限制跳过 | 0 | 0 | 0 |
| 异步任务或 Midjourney 提交已访问上游但失败 | 0 | 1 | 0，除非提供商返回明确计费结果 |
| 异步任务或 Midjourney 提交成功 | 1 | 0 | 当前暂估或按次价格 |
| 异步任务暂估 10，最终结算 14 | 1 | 0 | 最终为 14 |
| 异步任务暂估 10，最终退款 | 0 | 0 | 最终为 0 |
| 只有未解析事件的渠道 | 0 | 大于 0 | 页面仍显示该渠道 |
| 成本倍率配置缺失且真实请求已访问上游 | 0 | 按请求尝试累计 | 0，页面显示配置缺失告警 |
| 成本队列在服务正常退出前仍有数据 | 按事件决定 | 按事件决定 | 退出前完成落库 |

## 8. 测试要求

### 8.1 后端单元测试

- `TotalTokens == 0` 但存在工具或其他确定性费用时能够结算。
- 固定价格在 usage 缺失或估算时仍能结算。
- Token 价格在 usage 缺失或估算时只增加未解析数量。
- 同一请求尝试不会重复结算。
- quota 异常、NaN、无穷值和溢出继续进入未解析或饱和审计，不能产生负数成本。

### 8.2 Relay 和探测测试

- 覆盖网络失败、非成功状态、响应解析失败和流式中断。
- 覆盖失败后重试成功以及全部重试失败。
- 覆盖业务请求、手动测试和稳定性探测。
- 验证探测被跳过时不会产生虚假成本事件。

### 8.3 异步任务测试

- 覆盖暂估等于、低于和高于最终成本。
- 覆盖失败退款、重复轮询和重复回调。
- 验证跨日完成仍调整原提交日期。
- 验证任何调整都不会让任务成本或日聚合成本变成负数。

### 8.4 数据库和前端测试

- SQLite、MySQL 和 PostgreSQL 的聚合、幂等更新与迁移行为一致。
- 页面展示只有未解析事件的渠道和 API Key。
- 成本解析率分母为零时显示 `-`，不能显示 `0%`。
- 页面明确区分已结算成本、未解析尝试和配置缺失。

## 9. 关键实现位置

- `service/channel_daily_cost.go`：成本资格判断、金额计算和未解析记录。
- `service/channel_daily_cost_batcher.go`：内存聚合、批量落库、重试和退出刷盘。
- `service/usage_helpr.go`：本地用量估算标记。
- `controller/relay.go`、`relay/compatible_handler.go`：普通 Relay 和重试尝试。
- `controller/channel-test.go`：渠道测试和探测成本。
- `controller/channel_ratio_monitor_schedule_probe.go`：稳定性探测调度。
- `service/task_billing.go`：异步任务暂估和最终结算。
- `relay/mjproxy_handler.go`、`controller/midjourney.go`：Midjourney 成本和退款。
- `model/channel_daily_cost.go`、`model/channel_daily_api_key_cost.go`：成本聚合存储。
- `controller/channel_monitor_cost.go`：成本查询接口和展示模型。
- `web/src/features/channel-monitor/`：成本概览、趋势、渠道和 API Key 页面。

## 10. 完成标准

本方案完成需要同时满足：

1. 所有真实上游请求尝试都能被归类为已结算或未解析。
2. `/v1/alpha/search` 和其他零 Token 确定性费用能够进入今日成本。
3. 固定价格请求不再因为 usage 缺失或估算而漏记。
4. 失败重试和稳定性探测失败在页面可见，但不猜测金额。
5. 异步任务最终费用、差额和退款与渠道日成本一致。
6. 页面可以直接判断成本数据是否完整。
7. 成本批处理不再静默丢失事件，并能在正常退出前刷盘。
8. SQLite、MySQL 和 PostgreSQL 测试全部通过。

完成以上改造后，“今日成本”应被理解为“截至当前已结算的渠道成本”，并辅以未解析尝试数量和解析率；最终财务金额仍需与提供商账单对账。
