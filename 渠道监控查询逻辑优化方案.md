# 渠道监控查询逻辑优化方案

## 1. 背景与结论

渠道监控页面已经移除“将完整页面查询结果写入内存或 Redis，并通过 TTL、过期回退或后台重建继续复用”的页面快照方案。后续查询优化不再依赖页面结果缓存，而是采用以下原则：

- 配置、渠道关系和历史记录直接读取当前数据库状态。
- 实时性能、成功率、并发和当日增量读取当前运行态或 Redis 共享投影。
- 只合并同一时刻正在执行的相同查询，查询完成后不保留结果。
- 同一请求内复用已经读取的数据，避免重复查询，但不跨请求保存页面结果。
- 所有实时投影响应明确返回数据截止时间、水位和降级状态，不把最终一致数据伪装成数据库强一致数据。
- 所有渠道监控路由返回 `Cache-Control: no-store`，浏览器和代理不得复用旧响应。

目标不是让每次请求执行更多查询，而是把查询约束在必要的数据源、必要的列、必要的时间窗口和必要的维度内，使“直接查询”仍然正确且高效。

## 2. “移除快照”的边界

### 2.1 必须移除的页面查询行为

- 不保存完整页面响应 JSON。
- 不使用页面级 TTL、stale-while-revalidate、冷启动重建或后台预热。
- 不在管理操作后并发重建多个页面结果。
- 不因存在旧页面结果而跳过数据库或 Redis 的当前查询。
- 不在查询失败时返回上一次成功页面结果并标记为最新。

### 2.2 允许保留的查询优化

- **进行中查询合并**：相同参数、相同数据源和相同变更代次的并发请求可以通过 `singleflight` 共用正在执行的查询；查询结束后立即释放，不保留结果。
- **请求内复用**：控制器已经读取的设置、渠道、监控配置或 Redis 投影，应通过参数继续向下传递，避免同一请求重复读取。
- **Redis 共享投影**：分钟、小时或天级聚合属于实时指标的数据源，不是页面响应缓存。页面每次都按当前时间窗口读取投影，并返回其截止时间和处理水位。
- **前端界面状态**：React Query 可以在组件生命周期内保留最后一次数据用于避免界面闪烁，但进入页面、切换视图或显式刷新时必须重新验证。该状态不能替代服务端查询，也不能被描述为服务端页面快照。

### 2.3 不属于页面查询快照的业务事实

计费依据、审计记录、一次任务实际使用的配置、逻辑渠道成员关系以及智能调度的一次执行输入，可能需要以不可变事实持久化，保证历史可解释、可结算和可审计。这类数据不参与页面跨请求复用，不能与页面查询缓存混为一谈。

本文的优化范围仅是渠道监控页面读取链路。业务执行事实是否需要持久化，应按计费和审计不变量单独评审。

## 3. 查询目标与非目标

### 3.1 目标

- 管理员修改设置、渠道、分组或探测配置后，后续请求能够读取新值。
- 页面展示的渠道集合与当前数据库中的有效渠道集合一致。
- 实时指标的时间口径、截止时间和降级状态明确。
- 单次请求的数据库行数、Redis 字段数、响应字节数和内存占用都有上限。
- 相同并发请求不会形成数据库或 Redis 查询风暴。
- SQLite、MySQL 和 PostgreSQL 使用相同业务语义。

### 3.2 非目标

- 不要求数据库配置和 Redis 实时指标在同一个物理时刻原子提交。
- 不通过缓存旧页面结果来掩盖 Redis、数据库或检测器故障。
- 不为了减少一次简单查询而引入新的全局状态、失效协议或后台刷新任务。
- 不把无界全量查询转移到 Go 内存后再做过滤、聚合或分页。

## 4. 统一查询流程

所有渠道监控 GET 接口按以下流程实现：

1. 校验并规范化查询参数，包括页码、页大小、时间范围、渠道 ID、分组、模型和状态枚举。
2. 在请求开始时确定一次 `generated_at`，基于该时间计算统一的半开区间 `[start_at, end_at)`。
3. 使用请求上下文读取数据库和 Redis；需要合并并发查询时，后台实际查询使用独立的短超时上下文，各调用方仍可独立取消等待。
4. 配置类字段从数据库读取，禁止使用可能滞后的 `OptionMap`、节点 TTL 缓存或历史页面结果作为管理页面的数据源。
5. 实时指标从 Redis 共享投影或本地运行态读取；按本次请求的明确渠道 ID 集合过滤。
6. 在同一请求内复用已经加载的设置、监控配置和投影结果，避免二次完整扫描。
7. 返回 `generated_at`；实时数据同时返回 `window_start`、`data_cutoff_at`、`processed_at`、`event_watermark` 和 `realtime_degraded` 等元数据。
8. 通过渠道监控路由中间件设置 `Cache-Control: no-store, no-cache, must-revalidate, private, max-age=0`。

页面应按以下语义解释混合数据源：

> 配置字段表示数据库在查询时读取到的当前值；实时指标表示共享投影已经处理到 `data_cutoff_at` 的结果。两者不宣称具有跨存储原子一致性。

## 5. 20 个 GET 接口的数据源与优化要求

| 接口 | 主要数据源 | 查询要求 |
|---|---|---|
| `GET /api/channel_monitor/` | 当前渠道、监控配置和设置数据库；当日成本 Redis 投影；并发运行态 | 渠道和监控配置只读取一次；并发配置复用本次监控行；运行态严格按本次渠道 ID 集合返回 |
| `GET /api/channel_monitor/concurrency` | 当前渠道 ID、并发/RPM 配置数据库；Redis 或本地运行态 | 配置字段以数据库为准；不存在监控行时显式返回零配置；过滤已删除渠道的残留运行态 |
| `GET /api/channel_monitor/cost` | 日成本与 API Key 成本数据库；当日未完成增量 Redis 投影 | 日期聚合、筛选、排序和分页下推数据库；只用 Redis 修正当前日；金额合并必须守恒 |
| `GET /api/channel_monitor/performance` | 当前监控设置数据库；Redis 共享投影 | 设置决定窗口；一次投影读取同时生成性能、成功率和元数据；禁止为元数据再次扫描完整窗口 |
| `GET /api/channel_monitor/status` | 当前状态探测配置、渠道关系、执行和聚合数据库 | 每次读取当前数据库；相同筛选只合并进行中查询；配置写入后通过变更代次阻止新请求加入旧查询 |
| `GET /api/channel_monitor/status/channel/:id/executions` | 状态探测执行数据库 | 渠道校验、筛选、排序、总数和分页在数据库完成；页大小最多 100 |
| `GET /api/channel_monitor/group_monitor/settings` | 当前分组监控配置和候选模型关系数据库 | 配置和候选模型使用同一请求上下文；保存但暂时不在候选集中的模型要有明确兼容规则 |
| `GET /api/channel_monitor/group_monitor/overview` | 当前分组配置、渠道关系、探测状态和执行数据库 | 复用已读取配置；避免候选模型、渠道关系和状态逐分组重复查询 |
| `GET /api/channel_monitor/group_monitor/executions` | 分组监控执行数据库 | 分组、结果、排序和分页下推数据库；页大小最多 100 |
| `GET /api/channel_monitor/success/today` | 今日 Redis 共享投影；历史日聚合数据库 | 今日只读一次投影；历史日期直接查数据库日聚合；API Key owner 批量回填，不做 N+1 |
| `GET /api/channel_monitor/success/detail` | Redis 共享投影；API Key owner 数据库 | 明细与元数据来自同一次投影读取；只构造指定渠道或分组的结果；owner 使用批量 `IN` 查询 |
| `GET /api/channel_monitor/tasks` | 系统任务数据库 | 按任务类型分页；列表不加载无界执行明细；异常或旧版大结果不得重新塞回分页响应 |
| `GET /api/channel_monitor/tasks/:task_id/details` | 单个任务和智能调度执行明细数据库 | 先校验任务类型；过滤和分页尽量下推数据库；对单任务明细数量和响应大小设置硬上限 |
| `GET /api/channel_monitor/schedule` | 当前设置、路由和经济配置数据库；可选 Redis 指标投影 | `metrics=false` 不查询指标；启用指标时只读一次所需窗口；路由配置和指标通过稳定键合并 |
| `GET /api/channel_monitor/channel/:id/history` | 渠道倍率历史数据库 | 渠道存在性校验后按稳定顺序分页；使用 `(channel_id, created_at, id)` 对应索引 |
| `GET /api/channel_monitor/model_detection` | 当前模型检测设置、渠道、目标、轮次、执行、成本和逻辑关系数据库 | 直接构造当前概览；批量查询关联数据；禁止逐渠道查询最近轮次、执行或成本 |
| `GET /api/channel_monitor/model_detection/settings` | 当前模型检测设置数据库 | 每次读取当前设置和 revision；不从节点缓存返回管理配置 |
| `GET /api/channel_monitor/model_detection/service` | 当前设置数据库；检测器实时 HTTP 探测 | 设置从数据库读取；检测器请求有独立超时和响应大小限制；失败返回明确状态，不回退旧探测结果 |
| `GET /api/channel_monitor/model_detection/channel/:id/runs` | 模型检测轮次、执行筛选和成本事件数据库 | 使用 `EXISTS` 或等价数据库过滤；总数和分页下推；只批量读取当前页成本事件 |
| `GET /api/channel_monitor/model_detection/runs/:run_id` | 单轮次、执行、报告和成本事件数据库 | 按 `run_id` 定点查询；执行按稳定顺序加载；报告字节数必须有硬上限并做敏感信息清理 |

## 6. 正确性设计

### 6.1 配置以数据库为唯一页面权威源

管理页面读取设置、倍率、分组系数、并发限制、探测配置和模型检测设置时，必须直接查询数据库。进程内配置缓存仍可服务业务热路径，但不能决定管理页面展示值。

当某个配置行不存在时，应在响应组装层应用业务默认值，并区分以下状态：

- 行不存在；
- 列值为 `NULL`；
- 列值为空字符串；
- 列值显式为 `0` 或 `false`。

不能使用 `COALESCE` 或 Go 零值把这些状态无条件合并，否则配置删除、清空和显式关闭会被误判。

### 6.2 明确渠道集合

凡是控制器已经得到渠道 ID 集合，后续 Redis、本地运行态和批量数据库查询都必须使用这个集合：

- 不允许 Redis 中已删除渠道的 key 重新出现在响应中。
- 不允许缺少监控配置的渠道继承旧限制。
- 不允许“空集合”被解释为“查询全部渠道”。
- 返回 map 前应为当前渠道预填默认值，再覆盖当前运行态和配置。

### 6.3 统一时间窗口

- 所有窗口使用半开区间 `[start_at, end_at)`。
- 同一请求只计算一次当前时间，不在多个子查询中分别调用当前时间函数。
- 日统计使用同一时区和统一日界线函数。
- 当前日 Redis 增量和数据库日账本合并时，只替换或补充当前日，不能重复累加已经结算的数据。
- 排序使用稳定的第二关键字，例如 `created_at` 后继续按主键排序，避免翻页重复或遗漏。

### 6.4 多数据源一致性

数据库与 Redis 不可能在页面读取时天然原子一致，因此接口必须返回可解释的边界：

- `generated_at`：本次响应生成时间；
- `window_start`：统计窗口起点；
- `data_cutoff_at`：投影中已经完整纳入的时间；
- `processed_at`：投影最后处理时间；
- `event_watermark`：已处理事件水位；
- `realtime_degraded` 和 `degraded_reasons`：数据缺失、连接池拥塞、消费者落后或上限降级原因。

前端不得只根据 `generated_at` 把数据标记为实时。只有水位和截止时间满足窗口要求时，才能显示为完整窗口。

### 6.5 跨表一致性

单表和聚合查询优先直接执行。只有在一个接口必须同时读取多张强关联表且中途写入会产生不可解释组合时，才使用短只读事务或 revision 前后校验：

- 事务中不执行外部 HTTP、Redis 长查询或大结果反序列化。
- 不使用 `FOR UPDATE` 锁住页面读取。
- revision 在查询期间变化时最多重试一次，仍变化则返回明确的冲突或降级状态。
- SQLite、MySQL 和 PostgreSQL 分别验证读取语义，不能依赖单一数据库默认隔离级别。

## 7. 性能设计

### 7.1 数据库查询

- 使用 `Select` 只取响应、过滤、排序和关联所需列。
- `WHERE`、`GROUP BY`、`SUM`、`COUNT`、`ORDER BY`、`LIMIT/OFFSET` 尽量下推数据库。
- API Key owner、渠道名称、成本事件等关联信息使用批量 `IN` 回填，禁止逐行查询。
- 列表接口先查询当前页主记录，再按当前页 ID 批量加载关联数据。
- 不把全量记录加载到 Go 后再分页；不构造无界 map、切片或 JSON。
- 对高频过滤和稳定排序建立组合索引，并使用三种数据库的执行计划验证。重点覆盖：
  - 渠道倍率历史：`channel_id, created_at, id`；
  - 状态探测执行：`channel_id, created_at, id`，以及常用模型/结果/触发条件；
  - 分组监控执行：分组、结果和时间；
  - 系统任务：类型和创建时间；
  - 模型检测轮次：渠道和创建时间；
  - 模型检测执行：`run_id` 及模型/结果筛选列；
  - 分钟指标：时间桶和实际使用的渠道、分组、模型维度。

新增索引前必须确认现有索引是否已覆盖，避免重复索引增加写放大。

### 7.2 Redis 查询

- 同一时间窗口的多个 key 使用 Pipeline 批量读取，避免逐 key RTT。
- 短窗口读取分钟桶；长窗口优先读取小时或天级汇总，避免按 90 天逐分钟扫描。
- 元数据使用独立小 key 或必要字段读取，禁止为了水位再次 `HGETALL` 全部业务指标。
- 对以下资源设置软上限和硬上限：
  - 时间桶数量；
  - 单个 hash 字段数；
  - Pipeline 总响应字节数；
  - 渠道、路由、模型、分组和 API Key 维度数；
  - 失败样本数量和错误文本长度。
- 达到软上限时返回摘要和降级原因；达到硬上限时终止读取并返回明确错误，不能继续分配无界内存。
- Redis 不可用时，实时接口应返回明确不可用或降级结果。除非接口合同明确提供数据库聚合替代路径，否则不得返回旧页面结果。

### 7.3 进行中查询合并

只对成本较高且容易产生相同并发请求的接口使用 `singleflight`。key 至少包含：

- 数据库或 Redis 客户端身份；
- 规范化后的筛选参数；
- 时间窗口；
- 配置或数据变更代次。

并发合并必须满足：

- 查询完成后不保留结果；
- 不设置页面结果 TTL；
- 任一等待方取消不会取消其他仍在等待的调用方；
- 实际查询有独立、有限的超时；
- 写操作推进变更代次后，新请求不能加入写入前已经开始的查询；
- 合并结果返回前再次校验数据源身份和变更代次，不符合时重新查询。

对于廉价单行查询，不要机械增加 `singleflight`，避免全局 key 和锁管理比查询本身更复杂。

### 7.4 请求内复用

请求内复用通过显式参数完成，不增加全局缓存：

- 概览读取到的监控配置直接传给并发状态查询。
- 性能、成功率和实时元数据从同一次 Redis 投影生成。
- 智能调度一次请求只读取一次设置和一次指标窗口。
- 模型检测概览先批量取得轮次 ID，再批量读取执行和成本事件。
- 分组监控先加载一次关系和配置，再在内存中按稳定 ID 合并。

## 8. HTTP 与前端边界

### 8.1 服务端

- `/api/channel_monitor` 路由组统一使用禁用缓存中间件。
- GET、PUT、POST 的渠道监控响应都不得被浏览器、CDN 或反向代理缓存。
- 对实时接口设置合理请求超时；超时返回错误或降级元数据，不返回旧页面结果。
- 写操作成功后，前端通过 query invalidation 或显式刷新重新查询，不触发后台页面预构建。

### 8.2 Axios

统一 HTTP 客户端可以合并相同会话、相同 URL 和相同参数下正在进行的 GET Promise。该机制在 Promise 完成后删除记录，因此属于请求去重，不是快照。

去重 key 必须使用稳定参数序列化；如果参数对象顺序可能不同，应先排序键，避免语义相同的请求无法合并。

### 8.3 React Query

- 页面实时数据使用 `staleTime: 0`，进入需要展示该数据的视图时重新验证。
- 切换视图时，即使共享 query 仍挂载，也应按视图进入逻辑显式刷新需要的数据。
- 轮询只在任务运行、探测进行或检测进行时开启，结束后立即停止。
- 手动刷新可在很短时间内合并重复点击，但不能跳过用户明确发起的新刷新。
- `placeholderData` 或最后一次成功数据仅用于展示连续性，必须同时显示 `isFetching`、截止时间或降级状态，不能让用户误以为旧数据已经重新查询。
- 只对真正静态或由写操作严格 invalidation 的辅助数据使用较长 `staleTime`；监控主数据不得使用无限新鲜时间。

## 9. 禁止模式

- 在进程内 map、Redis string/hash 或文件中保存完整页面响应。
- 页面查询使用 TTL、stale 回退、后台预热或写后 fan-out 重建。
- 管理页面从 `OptionMap` 或节点缓存读取当前配置。
- 显式空渠道集合退化为“查询全部”。
- Redis `HGETALL`、数据库 `Find` 或 Go 聚合没有窗口和基数上限。
- 为生成元数据再次执行完整业务查询。
- 列表先全量加载，再在 Go 中分页。
- 循环内逐渠道、逐 API Key、逐轮次查询关联数据。
- 将数据库 `NULL`、缺失行、空字符串、零和 false 混成同一种状态。
- Redis 投影落后时仍只返回当前生成时间，不返回截止时间和降级原因。
- 前端通过无限 `staleTime` 把监控主数据永久标记为新鲜。

## 10. 可观测性与查询预算

每个主要接口至少记录或暴露以下指标：

- 请求总耗时、数据库耗时、Redis 耗时和外部检测器耗时；
- 数据库查询次数、返回行数和慢查询数量；
- Redis 命令数、Pipeline key 数、字段数和估算响应字节数；
- singleflight 进行中查询数、合并调用数、实际执行数和超时数；
- 返回渠道、路由、模型、分组、API Key、任务和执行记录数量；
- 投影 lag、pending、writer queue depth、水位和降级原因；
- 响应字节数及达到软/硬上限的次数。

查询预算应根据接口合同固定，而不是根据当前生产数据量自然增长。超过预算时应降级、分页或拒绝，不得继续扩大单次查询。

## 11. 测试与验收

### 11.1 正确性测试

- 两次串行相同请求之间发生数据库写入，第二次必须读到新值，证明没有完成结果复用。
- 多个并发相同请求只执行一次实际高成本查询，完成后下一次请求重新执行。
- 写操作推进变更代次时，写后的请求不能返回写前正在执行的查询结果。
- Redis 中保留已删除渠道运行态时，响应不得包含该渠道。
- 当前渠道没有监控配置行时，并发和 RPM 限制返回显式零值。
- 数据库缺失、`NULL`、空字符串、零和 false 分别按合同返回。
- 时间窗口边界验证 `[start_at, end_at)`，边界事件不重复、不遗漏。
- 当前日 Redis 增量与数据库日账本合并后金额和请求数守恒。
- Redis 投影落后、不可用或达到上限时，截止时间和降级原因正确。
- 模型检测服务实时探测失败时不返回上一次成功探测结果。

### 11.2 查询效率测试

- 使用确定性 fixture 断言查询次数，避免只依赖耗时比较。
- 对 `C=10/100`、分钟窗口 `M=15/1440`、API Key 数 `K=10/1000` 的固定规模检查返回行数、Redis 字段数和响应大小。
- 验证分页查询只加载当前页关联数据。
- 验证 owner、成本事件、执行明细和渠道名称回填不存在 N+1。
- 分别检查 SQLite、MySQL、PostgreSQL 执行计划和索引命中；执行计划差异应记录，但业务结果必须一致。
- 性能基准单独使用 benchmark 或压测，不在普通单元测试中使用 sleep、随机输入或脆弱的绝对耗时断言。

### 11.3 HTTP 与前端测试

- 20 个 GET 路由全部断言 `Cache-Control` 为 no-store 策略。
- Axios 相同 GET 并发合并，完成后再次调用会发起新请求。
- 进入页面和切换主视图会重新验证对应监控查询。
- 任务、状态探测和模型检测仅在 active 状态轮询。
- 手动刷新期间的重复点击被合并，完成后的显式刷新不会被旧 Promise 拦截。
- 展示旧界面数据期间必须同时显示刷新中、截止时间或降级状态。

## 12. 落地顺序

1. 固化 20 个 GET 接口的数据源、参数上限、时间口径和 no-store 合同。
2. 将配置查询统一改为当前数据库读取，并清理残留的页面结果缓存、TTL 和后台重建入口。
3. 将分页、聚合、过滤和排序下推数据库，补齐批量关联查询与必要索引。
4. 将实时页面统一到 Redis 共享投影读取，补齐 Pipeline、长窗口汇总、字段/字节上限和降级元数据。
5. 只为高成本相同并发请求增加进行中查询合并，并增加变更代次保护。
6. 前端统一视图进入刷新、active 轮询和手动刷新去重行为。
7. 在三种数据库和 Redis 正常、延迟、不可用、高基数场景下完成验收。

## 13. 新增查询接口审查模板

新增或修改渠道监控 GET 接口时，代码审查必须回答：

1. 当前配置、历史事实和实时指标分别来自哪个权威数据源？
2. 是否保存了完整页面结果，或在完成后继续复用查询结果？如果是，必须移除。
3. 时间窗口是否为统一半开区间，是否返回数据截止时间和水位？
4. 最大数据库行数、Redis 字段数、维度数和响应字节数是多少？
5. 聚合、过滤、排序和分页是否已下推数据源？
6. 是否存在 N+1、重复投影读取或重复配置查询？
7. 显式空集合是否可能被解释为查询全部？
8. 缺失、`NULL`、空值和显式零值是否被正确区分？
9. 并发相同查询是否需要只合并进行中的工作？写入后如何防止加入旧查询？
10. SQLite、MySQL 和 PostgreSQL 的结果、索引和事务语义是否都已验证？
11. Redis 不可用、投影落后或达到上限时，接口如何明确降级？
12. 服务端 no-store、前端重新验证和 active 轮询合同是否有测试保护？

满足以上要求后，即使不使用页面快照，渠道监控仍可以通过有界查询、请求内复用、进行中查询合并和清晰的一致性元数据，同时保证查询结果正确、性能可控且故障行为可解释。
