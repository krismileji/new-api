# 渠道余额监控执行计划

日期：2026-09-15

状态：业务实现及本轮验证已完成。完整测试集仍有已在未修改代码上复现的 8 项失败，详见第 11.4 节。本轮只修改工作区代码和文档，未部署。

代码基线：`39485b5be`。

## 1. 已确认的需求与前提

- 用户确认：涉及的上游都在请求结束后扣款，不在请求开始时预扣。
- 线上已部署当前版本，配置为每分钟同步一次余额和倍率。
- 以最近一次成功同步的上游余额为基准，扣除该基准之后已完成请求的费用，以及仍在进行的请求预估费用。
- 每次成功同步后重新建立基准，不再无限累计“本地成本减上游余额下降额”的差额。
- 请求完成时，以实际用量计算的费用替换该请求的预估费用，不能将两份费用相加。
- 进行中请求优先采用同渠道、同模型近期已完成请求的平均费用预估，让历史费用中的缓存读写影响自然进入平均值；不要求提前知道当前请求的缓存命中量。
- 本地预估的共享运行状态、近期平均费用样本和实时汇总全部使用 Redis；预估本身不在请求路径新增数据库读写，不通过扫描消费日志或日账本计算平均值。
- 不增加旧版本兼容分支、双算法切换或历史估算差额承接逻辑。

本文中的“实际费用”，指本地根据请求结束后的上游 `usage` 和计价配置计算出的渠道费用；没有上游逐笔账单时，不能将其宣称为上游实际扣款的精确值。

## 2. 目标计算方式

所有参与余额比较的金额统一使用上游余额的额度单位。

```text
当前估算余额
= 最近一次成功同步的上游余额
− 尚未纳入这次余额基准的已完成请求费用
− 仍在进行中的请求预估费用
```

“尚未纳入基准”按请求完成事件和余额同步边界确定，不能按成本什么时候写进数据库确定。

例如：

| 事件 | 上游余额基准 | 已完成费用 | 进行中预估 | 估算余额 |
| --- | ---: | ---: | ---: | ---: |
| 成功同步，当前无进行中请求 | 24 | 0 | 0 | 24 |
| 一笔新请求开始，预估消费 3 | 24 | 0 | 3 | 21 |
| 请求完成，根据用量计算消费 2 | 24 | 2 | 0 | 22 |
| 再次同步，上游返回 22 | 22 | 0 | 0 | 22 |

如果再次同步时另一笔请求还在执行，该请求的预估费用必须继续保留。由于上游在结束后扣款，不能仅因为同步成功，就把所有进行中请求的预估清零。

## 3. 修改前实现与修改原因

| 当前实现 | 已确认的问题 | 修改方向 |
| --- | --- | --- |
| `evaluateChannelMonitorBalance` 读取日成本账本增量 | 数据库入账晚于请求完成，无法直接代表同步之后发生的消费 | 按请求生命周期更新余额运行状态 |
| `BalancePendingConsumption` 跨同步累计差额 | 本地计价偏差可能长期积累；第一次建立基准时可能重复扣算迟到成本 | 成功同步重建基准，删除持续对账差额算法 |
| 普通文本请求在结束并获得可信用量后记录成本 | 尚未完成的并发请求没有渠道余额占用 | 增加按上游请求尝试记录的进行中预估 |
| 余额评估调用 `FlushChannelDailyCostEvents` | 该函数只刷新进程内批处理，不能保证可靠成本 Stream / Outbox 全部进入日账本 | 余额判断不依赖强制刷新日账本 |
| 页面展示保存的 `upstream_balance` | 不能直接看到进行中占用和估算余额 | 保留原始余额，另行展示估算组成 |

本轮分析已用临时 Go overlay 用例验证一个具体边界：60 笔已完成请求，每笔上游扣 2 额度；首次读取余额时，本地 120 额度对应的成本尚未入账。之后补入账，当前算法将这 120 再次扣除，导致余额 `24.374499` 被算成 `-95.625501` 并禁用。有完整前序基准的对照用例能够抵消该批费用。此结果只证明该边界可复现，不能据此认定线上 `116.74470275000006` 的来源已经确定。

## 4. 金额单位与预估规则

### 4.1 成本倍率与上游额度

使用用户提供的配置作为固定验收样例：

```text
订阅价格：408 元
周期：30 天
每日额度：500
上游倍率：4

换算系数 = 408 / (500 × 30) = 0.0272 元/额度
成本倍率 = 4 × 0.0272 = 0.1088

本地人民币成本 = 分组售价倍率作用前的基础费用 × 成本倍率
用于余额判断的消费额度 = 本地人民币成本 / 换算系数
```

- 人民币成本乘成本倍率；从上游余额扣减前，换回上游额度。
- 不把人民币成本直接当作上游额度，不重复乘上游倍率或成本倍率。
- 冻结每次请求尝试使用的模型计价、上游倍率和换算参数；请求执行期间倍率更新，不重新计算该请求已冻结的费用。
- 不能直接复用用户钱包的 `FinalPreConsumedQuota` 作为渠道预估。该值包含用户计费规则，且信任额度机制可能使其为零。

### 4.2 已完成请求

- 复用现有可信 `usage` 和成本计算链，包括普通输入、输出、缓存读取、缓存写入及适用的其他费用。
- 表达式计费复用同一表达式和用量归一规则，实施前完整阅读 `pkg/billingexpr/expr.md`。
- 缓存统计来自上游用量。缺失可信用量时标记未确认，不能把未知消费直接当作零。
- 实际费用到达时，原子移除预估占用并记录一次完成费用；迟到、重放和重复回调不能再次增加金额。

### 4.3 进行中请求：优先使用近期平均费用

已按以下规则实现：

```text
近期平均单次费用 = 有效样本的完成费用总额 / 有效样本数
单个进行中请求的预估费用 = 请求发起时取得的近期平均单次费用
进行中预估总额 = 所有未完成请求各自冻结的预估费用之和
```

例如，同渠道、同模型近期平均每次消费 `1.5` 额度，10 笔请求均按这个平均值开始，则进行中预估共 `15` 额度。其中一笔结束后计算出 `2` 额度，应移除这笔的 `1.5` 预估并增加 `2` 完成费用，合计变为 `15.5`，不能再额外保留其预估。

样本规则：

- 按渠道和模型分别统计，不把不同渠道或不同模型混为一个平均值。对于可识别的不同计价档位或计费模式，分别统计。
- 只使用已经完成、有可信用量和确定费用的同类业务请求。未知费用、失败且无法确认消费的请求、探测及模型检测不能混进业务平均值；可信的零费用请求可以作为零值样本。
- 样本费用已包含实际输入、输出、缓存读取、缓存写入及适用费用，使用第 4.1 节换算后的上游额度。取平均后不再重复乘上游倍率、成本倍率或换算系数。
- 窗口采用最近 30 分钟内最多 100 笔有效样本，至少 5 笔后使用均值。这是本轮采用的默认值；避免直接使用全日平均或只有最近一笔的费用。
- 样本按请求尝试及完成事件去重，保留有界窗口；每分钟余额同步不清空仍有效的平均费用样本。
- 渠道账号、模型计价或上游倍率发生变化时，不能直接套用不再匹配的样本。新请求使用匹配的费用配置样本，不足时按下面的规则处理。原有进行中请求的预估及结算参数保持冻结。

样本不足时：

- 新渠道、新模型、样本过期或计价配置变更后样本不足，临时复用现有输入 token 估算及有界输出预算计算费用，并标注预估来源和样本数。
- 该兜底路径仍去掉用户分组售价倍率，使用渠道成本计价；不另写一套计费公式，不将未知缓存命中当作已命中。
- 如果连兜底费用也无法可靠计算，标记预估数据不完整，不能把缺失当作零费用或使用它自动恢复渠道。

生命周期规则：

- 每次上游尝试开始时读取一次平均值并登记占用；新完成请求更新样本，只影响之后开始的请求，不反复重算已有进行中请求的占用。
- 请求结束后用可信 `usage` 算出的费用替换预估，同时将符合条件的完成费用加入样本。这个过程必须去重，不能平均值统计一次、余额占用又增加两次。
- 平均值反映近期流量中的缓存效果，但不能保证某一笔请求的费用；长输入、长输出或突然缓存未命中可能比平均值贵。显示为“预估”，仍须在完成后校正。
- 预估只用于渠道余额保护，不修改用户钱包的真实扣费、退款或账单。

## 5. 请求状态与共享存储

本地预估以 Redis 为共享运行状态来源，不依赖单进程内存。计价可在服务端利用当前请求已有的价格快照完成，结果及其原子汇总存入 Redis；不为这些临时状态新增数据库表或逐请求数据库读写。现有真实成本的可靠事件、Outbox 和日账持久化继续独立运行，不新增永久增长的独立请求账本。

### 5.1 Redis 与数据库职责

| 数据或操作 | 存储与执行位置 | 数据库约束 |
| --- | --- | --- |
| 本轮余额基准、同步序号、估算可用余额 | Redis | 成功同步时可沿用现有余额快照持久化，不逐请求保存估算余额 |
| 请求尝试的预估、完成替换、释放和去重 | Redis 原子更新 | 不新增预估请求表，不逐请求读写数据库 |
| 同渠道、同模型的近期完成费用样本、总额、数量和平均值 | Redis 有界窗口 | 不查消费日志求平均，不查日账本回填样本 |
| 本轮已完成费用与进行中预估总额 | Redis 增量维护 | 不依赖分钟入账，不查询数据库做实时聚合 |
| 阈值、倍率及预估所需配置 | 复用请求已有快照与共享缓存，配置修改或后台加载时更新 | 不因每次预估再查数据库；缓存缺失不能引发逐请求回源 |
| 是否越过阈值的高频判断 | Redis 状态及原子判定 | 状态未变化时不查询或更新渠道数据库记录 |
| 渠道实际禁用或恢复 | 按渠道合并、去重后，沿用现有状态更新与审计 | 仅真正的状态转换执行必要数据库操作，保留修订及人工禁用保护 |
| 真实账务、可靠成本 Outbox、日成本与已有任务记录 | 沿用现有数据库持久化链 | 原有账务写入继续执行，不额外存一份预估账本 |

页面的预估明细直接读取 Redis 汇总，不通过逐请求扫描或数据库聚合生成。原始余额仍可保留现有数据库快照，但运行中的预估判断只使用与当前同步序号一致的 Redis 基准。

### 5.2 原子更新、容量与异常处理

- 请求开始、完成替换、释放及去重采用短小的原子操作，避免先读总额、在进程内相加、再写回导致多节点丢失更新。
- 平均窗口的总费用和样本数随新增及淘汰维护；每个分组最多保留 100 个有效样本，并按 30 分钟清理。平均值读取不扫描所有渠道或整天请求。
- 请求按尝试身份去重。成功同步折叠已完成费用；完成尝试只保留 30 分钟去重标记，进行中和未确认尝试保留上限为 48 小时，过期后先标记数据不完整，再等待确认空闲并成功同步。清理每次最多处理 128 笔，不遍历全部请求。
- 共享样本池和单笔冻结预估分开维护；一个新样本不能触发重写整个渠道所有进行中记录。
- Redis 不可用、运行状态缺失或费用配置尚未准备好时，明确标记预估不可用或不完整，不降级为逐请求查数据库求平均，也不将缺失金额当作零自动恢复渠道。
- Redis 状态恢复使用新取得的上游余额、可靠请求事件及可确认的进行中状态重新建立基准；平均样本重新积累，不回扫数据库历史日志。无法重建完整状态时继续标记不完整。

### 5.3 请求状态

每个渠道的运行状态至少需要：

- 最新上游余额、成功同步时间、同步序号和配置修订号。
- 本轮基准之后的已完成费用总额、进行中预估总额。
- 请求尝试的唯一标识、所属渠道、发起时间、完成事件时间、预估金额、完成金额和处理状态。
- 同渠道、同模型且匹配费用配置的有界完成费用样本；每笔预估采用的来源、样本数和冻结值。
- 状态完整性及过期信息，区分“没有消费”和“消费数据缺失”。

请求尝试优先复用现有 `CostEventId` 等可靠身份。一个用户请求如果重试了两个上游渠道，应记录两个独立尝试，按各自真实结果处理费用。

| 状态变化 | 对余额运行状态的影响 |
| --- | --- |
| 新增上游请求尝试 | 原子增加一次预估占用 |
| 请求完成且获得可信用量 | 原子将该请求的预估替换为完成费用 |
| 明确没有发往上游 | 释放预估，不新增完成费用 |
| 请求取消、断线或上游结果未知 | 标记未确认，不能只因客户端断开就认定上游没有扣费 |
| 完成事件重复投递 | 按尝试标识及事件版本去重，总金额不变 |
| 请求跨过一次或多次余额同步 | 保留同一份进行中占用，完成时按当前基准处理一次 |

金额、计数和去重状态在同一原子边界更新。Redis Lua 或其他实现不得在大额聚合时越过整数安全精度；金额转换、上下界、NaN/Inf 和审计沿用项目规则。

## 6. 成功同步时怎样重建基准

1. 为该渠道取得同步顺序标识，记录余额请求的发起时间和当时的请求事件位置。
2. 请求上游余额；请求期间业务请求继续运行，不持有全局数据库锁等待 HTTP。
3. 成功返回后，校验配置修订号和同步顺序，丢弃已经过期的响应。
4. 原子安装新余额基准，移除能够确认已包含在新余额里的完成费用；保留未被包含的完成费用和仍在进行的请求预估。
5. 根据新运行状态重新计算估算余额和渠道保护状态。

同步失败不得清零完成费用或进行中占用，不得把失败当成零余额，不得以失败响应自动恢复渠道。

### 6.1 同步边界不能使用数据库落账时间

同步前已经结束的请求，即使日成本事件在同步之后才落库，也不能作为“同步之后的新消费”再次扣减。请求完成状态应先进入可靠的请求事件链，日账本继续按原来的分钟批量方式持久化。

事件发生时间与事件处理时间必须分别保存。只有累计金额、没有请求归属和完成边界的数据，不能用来重建本轮余额占用。

### 6.2 查询余额期间完成的请求

“上游结束后扣款”已明确，但 HTTP 查询仍有时间窗口，不能假设上游采样余额的瞬间与本地收到响应的瞬间完全一致。

- 上游若提供可信的余额采样时间或已计入请求位置，优先使用该边界。
- 没有该信息时，记录查询开始和返回时间；对于查询窗口内完成、无法确认是否已计入余额的请求，单独标记边界不确定。
- 该批不确定费用不能并入无限累计差额。下一次有效同步后应根据其完成边界归入基准。
- 若是否低于禁用阈值只取决于这一批不确定费用，应先复核余额或等待确定的下一次同步；不得仅凭它写入“已确定余额不足”的禁用原因。
- 实施时必须用可控制查询时序的上游测试服务覆盖这个窗口，不能仅使用理想的瞬时余额接口验证。

## 7. 阈值判断与页面展示

### 7.1 判断时机

- 成功同步余额后重新判断。
- 进行中占用增加、请求完成替换费用、明确释放占用后，按渠道触发有界的状态评估，不必等下一分钟。
- 自动禁用、余额恢复自动启用、健康检查自动启用，应读取同一份余额运行状态和有效余额。
- 请求继续完成和结算不能被渠道自动禁用中断，避免占用无法释放。
- 保留人工禁用保护、配置修订校验和现有成本倍率恢复限制。

本计划默认保留现有阈值配置含义：预警阈值和自动禁用阈值同时存在、且上游余额低于预警阈值时，进入本地估算保护；仅设置自动禁用阈值时按上游余额判断。实施时将该条件集中表达并写入测试，不顺带更改阈值开关语义。禁用继续采用严格小于，等于阈值不禁用。

### 7.2 展示与解释

- 原有“上游余额”继续显示最后一次成功同步值及时间。
- 在余额详情增加：本轮已完成费用、进行中预估费用、有效估算余额及数据是否完整，使用同一额度单位。
- 详情展示按均值和按预算预估的进行中笔数，以及最近一次预估的模型、参考样本数和冻结单笔费用；样本不足使用预算时明确标注。多个模型的汇总不展示成一个混合平均值。
- 正在使用的估算、同步失败和边界不确定应清楚区分。
- 禁用原因应能够复算，例如：`上游余额 24，本轮已完成 2，进行中预估 18，估算余额 4，低于自动禁用阈值 5`。
- 对外格式化金额，避免直接输出长串浮点尾数；格式化不改变内部判断精度。
- 下游页面文案使用简体中文，不新增 i18n key，不修改 locale 文件。

## 8. 实施步骤与预计代码位置

### 第一步：固定行为契约

- [x] 将第 2 节公式、请求状态变化、单位与同步边界写成确定性回归用例。
- [x] 明确请求完成事件在成本写入 Stream / Outbox 之前的可靠记录点，验证迟到入账不会改变归属。
- [x] 固定同渠道、同模型的平均费用样本规则、默认窗口、最低样本数和费用配置变更处理。
- [x] 落实样本不足时输入及输出预算的现有复用入口，验证表达式和普通计价都去掉用户分组售价倍率。

### 第二步：实现请求级余额运行状态

- [x] 优先在 `service/` 新增余额运行状态文件，封装原子登记、完成替换、释放、去重、查询与同步换基准。
- [x] 复用 `service/channel_daily_cost.go` 中的尝试身份、成本快照、发出请求及结算入口，接入进行中预估。
- [x] 从去重后的完成费用维护共享平均样本，为新请求冻结平均值；原子替换预估时更新一次有效样本。
- [x] 预估状态、样本、配置读取和高频阈值判断接入 Redis，避免预估路径新增数据库读写；状态变更合并后才执行必要的渠道状态持久化。
- [x] 复用可靠成本事件与 Outbox 的投递和重试能力；不让普通监控事件与成本事件分别记一遍钱。
- [x] 为状态保留设置有界清理和失联处理。未知结果不能通过简单 TTL 直接按零费用清理。

### 第三步：替换余额对账算法

- [x] 改造 `controller/channel_ratio_monitor.go` 的手动与自动余额同步，安装请求级基准。
- [x] 删除持续累计 `BalancePendingConsumption` 的生产读写和估算算法。
- [x] 清理只服务于该算法的成本基线类型、方法和测试；删除前搜索全部调用，保留其他业务实际使用的日账能力。
- [x] 不迁移、承接或继续利用历史估算差额。初始化以新取得的上游余额和当前请求状态为准。
- [x] 不为清理估算算法顺带删除生产数据库历史账单或日成本记录。废弃列是否需要物理删除单独评估，不新增运行时兼容分支。

### 第四步：接入统一保护和展示

- [x] 接通请求状态变化时的渠道余额评估，保证多个节点并发只产生一次有效状态变更。
- [x] 更新定时任务和健康检查，通过既有 `BalanceUnavailable` 参数接入恢复规则，无须修改 `channel_ratio_monitor_recovery.go` 的算法。
- [x] 扩展渠道监控查询和前端余额详情，展示第 7 节的计算组成。
- [x] 更新禁用通知、任务结果和说明文档，去除持续对账差额的描述。

### 第五步：检查与交付

- [x] 完成第 9 节的行为与并发验证。
- [x] 如影响数据库，完成真实三数据库验证，记录版本、命令、结果；未完成不得宣称实现完成。
- [x] 记录修改了哪些上游拥有的文件，以及每处修改无法通过现有扩展点完成的原因。
- [x] 更新本计划的状态和任务勾选，提供最终行为、验证结果与尚存边界。

## 9. 验证清单

### 9.1 计算与生命周期

- [x] 使用 `408 / (500 × 30)`、上游倍率 `4`，验证人民币成本和上游额度往返单位正确。
- [x] 空闲渠道同步后已完成费用为零、进行中费用为零。
- [x] 单笔请求：预估 3、完成 2，最终只计入 2，不计成 5。
- [x] 多笔并发：60 笔各 2 额度，完成费用总计 120，每笔只记一次。
- [x] 历史平均每次 `1.5` 额度，10 笔进行中占用 `15`；其中一笔完成费用 `2`，合计变为 `15.5`，剩余 9 笔的预估不因平均值更新而跳变。
- [x] 同渠道不同模型、不同渠道同模型、不同计价档位不混用样本；取平均后的额度不再次乘倍率。
- [x] 缓存读写费用通过已完成样本参与平均，仍在进行的请求无需假设当前缓存命中量；突发长请求结束后按用量校正。
- [x] 首次使用、样本不足、样本过期、费用配置变化、可信零费用及失败未知费用分别验证平均值与兜底来源。
- [x] 重放完成事件不重复更新平均样本，成功同步余额不清空有效样本窗口。
- [x] 输入、输出、缓存读、缓存写和表达式计费分别验证，不能将有缓存用量的请求全按普通输入计价。
- [x] 明确未发出的请求释放占用；取消、未知结果、缺失用量按未确认状态处理。
- [x] 上游重试、渠道切换、重复回调、事件重放不重复记账。
- [x] 请求期间更新倍率或换算配置，费用继续使用该请求冻结的参数。

### 9.2 同步与并发

- [x] 首次同步前已扣款、同步后才入日账的 120 额度不再次扣减；余额 `24.374499` 保持有效。
- [x] 有前序基准时的相同用例也通过。
- [x] 请求在同步前开始、同步后结束，以及跨过多次同步，预估不丢失、不重复。
- [x] 请求恰好在余额查询窗口内完成，覆盖上游已体现与尚未体现两种响应顺序。
- [x] 两次手动刷新、手动刷新与自动同步重叠、响应乱序时，较旧结果不能覆盖较新基准。
- [x] 两个服务节点共同处理同一渠道，登记、完成替换与同步切换仍保持相同结果。
- [x] 日账 Outbox 积压、重复消费、迟到成本不会使余额差额重新增长。
- [x] 通过统计隔离预估入口的 SQL 调用验证：请求开始、完成替换、平均值查询、状态未变化的阈值判断都不产生新增数据库读写；原有真实账务写入单独统计。
- [x] 样本不足、缓存缺失、Redis 不可用时不回扫数据库日志或日账；并发越过阈值只产生一次有效渠道状态转换。
- [x] Redis 样本窗口不超过配置上限，过期样本正确退出总额与数量；清理无需扫描所有渠道或所有历史请求。
- [x] 同步失败、Redis 暂时不可用、进程重启时，状态不会被误认作零消费，也不会据此错误自动恢复。
- [x] 上游每日额度重置、充值或余额上涨，成功同步后建立新基准；仍在进行的请求保留一次占用。
- [x] 恢复自动启用不覆盖人工禁用，不绕过其他渠道恢复条件。

并发用例使用屏障、可控事件顺序和明确输入输出，不用随机请求、大循环或 sleep 来代替边界验证。Redis 原子操作及多节点行为至少在真实 Redis 上验证一次。

### 9.3 项目检查

- 新增或重写 Go 测试使用 `require` 和 `assert`；运行相关包的定向回归，再根据修改范围构建根模块。
- 改动模型、数据库读写或迁移时，必须使用真实 SQLite、MySQL、PostgreSQL 验证；涉及版本特性时覆盖最低支持版本。
- 如有表结构或迁移变化，覆盖新建数据库和最新发布版本的代表性数据库升级，启动迁移至少执行两次；影响共享路径时覆盖独立日志库。
- 若触及 `relaykit/` 或其公开 API，单独执行 `cd relaykit` 后 `GOWORK=off go build ./...`。
- 前端实施前读取 `web/AGENTS.md` 及适用技能，使用 Bun 执行必要的类型检查、相关验证和构建。
- 完成前执行 `git diff --stat` 和 `git diff --check`，记录真实执行结果，不把计划中的检查标成已通过。

## 10. 下游修改边界

本次已读取 `.agents/downstream.md`，并比对本地 `upstream/main` 的路径归属。

- 余额监控相关 `controller/channel_ratio_monitor*.go`、`controller/channel_health_auto_enable.go`、`service/channel_daily_cost*.go`、`service/channel_concurrency.go`、`model/channel_ratio_monitor.go` 和渠道监控前端目录为下游路径，优先在这些位置或同包新增文件实现。
- `controller/relay.go`、`middleware/distributor.go`、`relay/common/relay_info.go`、`relay/helper/price.go`、`service/text_quota.go` 和 `service/billing_session.go` 已存在于 `upstream/main`。默认不修改；确需接入时先检查现有下游钩子，只做必要的最小变更。
- 不升级依赖，不做无关重构，不改变项目标识和归属信息。
- 本轮已实现后端运行状态、同步与策略，以及前端详情；未部署或修改生产数据。`controller/system_task_handlers.go` 是本轮唯一修改的上游拥有文件，仅在现有启动注册函数增加一行实时余额策略处理器注册，具体实现均放在下游文件。工作区同期其他任务的修改保持原状。

## 11. 实施结果与验证记录

实施日期：2026-09-16。余额业务基线为 `39485b5be`；工作期间另外两个任务提交了 Redis 告警和成本投影索引修复，当前对照基线为 `9be88a7d7`，不包含本轮余额预估改动。

### 11.1 最终行为

- 原始余额只随成功的上游同步改变；估算可用余额随请求开始和完成更新 Redis。页面仍按现有刷新机制取值，没有新增高频数据库轮询。
- 费用统一换算成上游额度。采用 408 元 / 30 天 / 每日 500、上游倍率 4 的配置验证：0.0544 元成本对应 2 上游额度，不会再乘一次倍率。
- 本轮已完成、进行中、未确认和查询窗口费用分开记录；完成替换预估，同步保留仍在进行的请求。原来根据日账增量持续累计差额的算法及生产读写已删除。
- 普通计价和表达式计价复用现有用量结算，缓存读写进入实际完成费用。样本按渠道、账号、模型及计价配置隔离；跨计价档位的完成费用不训练原档位的均值。
- 高并发更新采用 Redis Lua 原子操作。实时保护按渠道共享租约合并，状态不变时不查询或更新数据库；人工禁用和其他恢复条件继续生效。
- 金额先转为整数微额度，再计算保护余额和消费总额；`0.3 − 0.2 = 0.1` 不会因浮点尾数被判为低于 `0.1`。对外最多显示六位小数，未知或不一致的汇总不产生负消费。
- 原始余额功能在显式关闭 Redis 的安装中仍可使用，但没有本地估算，也不查询日账或日志代替估算。已启用 Redis 却发生故障时，不接纳无法校验同步顺序的余额响应。
- 新数据库不创建两个废弃估算列。既有数据库升级时不删除历史列或账单；业务代码不再读取、写入或承接这些列的值。旧字段名仅出现在代表已发布版本的升级测试结构中。

### 11.2 明确的估算边界

- 上游余额接口没有提供与逐笔扣款对应的事务位置。查询期间完成的费用会显示为待确认，不独立触发禁用，也不据此自动恢复；下一次成功同步吸收这一部分。
- Redis 状态丢失后，必须确认请求覆盖并取得新余额才恢复完整状态。启动时存在旧的进行中请求，会先显示不完整；不会将缺失数据当成零。
- 完成事件直接在已有成本事件写入 Stream / Outbox 前更新预估。若直接 Redis 更新失败，复用现有 Outbox 投递读取预估恢复标记，释放已确认完成的占用并要求重新同步；不会用落账时间把旧费用再扣一遍。这条恢复路径没有新增 SQL 读取或写入。
- 异步任务提交、WebSocket 的增量用量、客户端断开或缺失可信用量，不能证明上游已经结束扣款，按未确认处理，不作为业务均值样本。没有可信最终完成信息时，不能展示成精确已完成费用。
- 平均费用只反映近期分布；突发长输入、长输出或缓存未命中仍可能超出均值。未知请求占用过期也不等于免费，必须重新确认基准。
- 真实 Redis 验证使用两个独立客户端同时更新同一渠道，验证多连接原子性；没有部署两个完整 API 服务实例或连接生产上游进行端到端压测。

### 11.3 已执行检查

后端使用 `D:/Go/sdk/go1.26.5/bin/go.exe`。相关定向回归命令：

```powershell
$env:TEST_CHANNEL_BALANCE_REDIS_ADDR='127.0.0.1:16391'
go test ./service ./model ./controller -run 'TestChannelBalance|TestChannelMonitorBalance|TestChannelRatioMonitor|TestChannelDailyCost|TestTextChannelDailyCost|TestAudioChannelDailyCost|TestChannelTestDailyCost|TestChannelMonitor.*Recovery|TestChannelMonitor.*AutoEnable|TestChannelMonitorCustomActionRefreshDatabaseMatrix' -count=1 -timeout=120s
go build ./...
```

相关回归及根模块构建通过。覆盖预估替换、30 分钟 / 100 笔 / 最少 5 笔均值、缓存读写、表达式、配置冻结、已完成事件重复、取消未知、迟到 Outbox、同步乱序、失败响应、Redis 缺失、过期状态、金额溢出、并发状态切换。请求预估路径在 `model.DB=nil` 下执行，并用查询计数验证余额评估不读取日账或渠道表。

真实数据库矩阵：

| 数据库 | 实际版本 | 验证内容 | 结果 |
| --- | --- | --- | --- |
| SQLite | 3.50.4 | 新建与代表线上版本升级、重复迁移、数据与唯一约束、余额持久化与状态转换 | 通过 |
| MySQL | 5.7.44 | 同上；专用测试库使用 utf8mb4 | 通过 |
| PostgreSQL | 9.6.24 | 同上 | 通过 |
| Redis | 8.8.0 | 两个独立客户端并发登记与完成 60 笔，每笔预估 3、完成 2，最终只计 120 | 通过 |

```powershell
$env:MONITOR_BALANCE_MYSQL_DSN='root:balance_test_only@tcp(127.0.0.1:13391)/new_api_monitor_balance_test?parseTime=true&charset=utf8mb4'
$env:MONITOR_BALANCE_POSTGRES_DSN='postgres://postgres:balance_test_only@127.0.0.1:15491/new_api_monitor_balance_test?sslmode=disable'
go test ./controller -run 'TestChannelMonitorBalanceSafetyDatabaseMatrix' -count=1 -timeout=180s
```

以上凭据仅属于本机新建的临时测试容器。每种数据库都验证新建结构与含有废弃字段的升级结构，各重复迁移两次；保留余额、倍率、备注及渠道唯一约束。变化只涉及主库渠道监控模型，没有修改日志库结构或共享迁移入口。最初 MySQL 测试库为 latin1，中文写入失败；改为 utf8mb4 后矩阵通过。

前端执行并通过：

```powershell
cd web
bun run test src/features/channel-monitor
bun run typecheck
bun run build
bunx oxlint -c .oxlintrc.json src/features/channel-monitor/types.ts src/features/channel-monitor/components/channel-monitor-balance-cell.tsx src/features/channel-monitor/components/channel-monitor-channel-view.tsx src/features/channel-monitor/components/__tests__/balance-estimate.test.tsx
bunx oxfmt --check src/features/channel-monitor/types.ts src/features/channel-monitor/components/channel-monitor-balance-cell.tsx src/features/channel-monitor/components/channel-monitor-channel-view.tsx src/features/channel-monitor/components/__tests__/balance-estimate.test.tsx
```

渠道监控测试集为 103 个文件、601 个测试通过；后续增加单笔均值展示后，重新通过受影响的 5 个展示/键盘交互测试、类型检查、lint 和生产构建。

### 11.4 完整测试集的已有失败

额外执行 `go test ./service ./model ./controller -count=1 -timeout=240s`，`model` 包完整通过；完整测试集不是全绿。以下 8 个失败也在不包含本轮改动的 `9be88a7d7` 独立工作区复现：

- `TestUpdateGroupedChannelAddressValidatesMembersAndAdvancesRevision`：测试数据库保留字段引用未初始化，出现 `SELECT ,model`。
- `TestGetChannelMonitorRecoveryBeforeBackgroundCheck`：完整测试顺序下返回 disabled，预期 checking。
- `TestProtectChannelSmartScheduleRuntimeFailureIgnoresMinimumSamples`
- `TestProtectChannelSmartScheduleRuntimeFailureDoesNotRecountPersistedErrors`
- `TestRunChannelSmartSchedulePersistsExecutionTimeScoreDetails`
- `TestPlanChannelSmartScheduleUsesHysteresisAndForceReset`
- `TestRunChannelSmartScheduleManualPrimaryAllowsStabilityDegrade`
- `TestUpdateChannelMonitorSettingsValidatesAndPersists`：既有调度设置断言与当前默认值不一致。

这些是既有分组、调度及测试初始化问题，本轮没有扩大范围修改。余额相关定向检查、三数据库矩阵及构建结果单独记录，不能将其表述为整个后端测试集全部通过。

最终清理：本轮三个专用 Redis / MySQL / PostgreSQL 测试容器及基线对照工作区已删除。现有开发数据库未改动。`git diff --check` 与新增余额文件的空白检查通过；工作区仍有其他任务并行修改的触发接口功能，不属于本轮余额预估改动。
