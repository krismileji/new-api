# 渠道监控数据更新时间与一致性审计

> 本文记录统一事件流改造前的历史时序和问题，用于说明改造原因；当前实现与验收状态以《渠道监控统一事件流与实时读模型设计》为准。

审计对象：`web/src/features/channel-monitor` 与对应 Go Controller/Service/Model。审计基线：2026-08-15 工作区代码。本文是代码级时序审计，不是当前生产实例的实时读数；当前实例的具体时间应以各 API 返回的 `generated_at`、`last_used_time`、`last_sample_at`、`last_schedule_time` 和探测记录时间为准。

## 当前实现时间口径

以下是本次统一事件流改造后的实际规则。本文后面的“结论摘要”“各数据的更新时间链路”和“当前代码中的全部时间处理”保留改造前基线，只用于解释为什么要改，不能作为当前页面状态说明。

| 环节 | 当前规则 | 页面/调度含义 |
| --- | --- | --- |
| 请求完成 | 成功、失败、探测、手动测试、任务结算和模型检测在最终边界构造事件；请求线程只做非阻塞内存入队 | 不等待监控 DB/Redis；事件可能因队列积压尚未处理 |
| 事件消费 | 有界队列批量消费，默认最多约 1 秒等待或达到批大小；同一事件 ID 幂等 | `processed_at` 与 `data_cutoff_at` 表示投影处理进度，不是页面请求时间 |
| 实时窗口 | 渠道/模型窗口按事件数和时间范围淘汰旧数据；截断、淘汰、丢弃或消费失败会标记 `realtime_degraded` | 首字、TPS、成功率、缓存、成本、稳定性和样本从同一窗口读取 |
| 智能调度 | 受影响池的请求事件约 1 秒合并；池级自适应刷新按最短间隔节流；完整评分只在配置/参与/成本变更、手动执行或强制重置时运行 | 自适应百分比不再等待分钟调度；页面显示当前事件水位与调度应用时间 |
| 历史数据 | 分钟聚合、日成本和历史明细仍异步批处理，分钟边界约 1 秒安全延迟 | 历史查询与实时窗口是两个明确数据范围，不混用 |
| 页面刷新 | 概览、性能、今日成功率、成本、智能调度、状态探测和模型检测查询默认不定时、不因焦点/重连自动刷新；首次加载请求一次，切换视图不自动重新请求已有数据 | 后续统一由顶部刷新按钮或各弹窗的明确刷新按钮触发请求 |
| 时间字段 | API 同时返回事件水位、数据截止、投影处理、投影启动、队列深度和页面生成时间 | 页面不能把 `generated_at` 当成数据更新时间；降级时必须显示不完整 |

成本配置快照仍有一分钟内存缓存；冷启动或缓存失效时可能发生一次同步配置读取，这是既有计费快照路径，不是统一事件入队路径。该边界在当前 API 的实时降级和成本未解析计数中保持可见，后续如需完全消除请求侧配置读取，应增加配置变更主动预热/跨节点分发。

## 结论摘要

1. **性能、成功率、缓存率、缓存写不是请求级实时数据。** 它们来自已经落库的分钟聚合，当前未完成分钟被明确排除。正常情况下，源请求到可被 API 查询约为“等分钟结束 + 约 1 秒聚合”，页面再以 60 秒轮询，因此最坏接近 2 分钟；聚合缓存本身约 10 秒。
2. **自适应备援不是首字一高就立即提高百分比。** 成功请求在完整请求结束后才进入运行时窗口，事件合并约 1 秒，并按调度池默认最短 10 秒刷新一次。百分比是“超过首字告警阈值的成功流式请求数 / 所有有效请求数”驱动的预算结果，不是平均首字的线性映射；没有首字数据的成功请求仍在分母中。
3. **页面轮询已将智能调度摘要和明细改为 3 秒，但这只缩短页面显示延迟，不改变后端运行时刷新、分钟聚合或成本批处理的延迟。** 页面隐藏时停止后台轮询。
4. 发现一处确认的页面数据不完整问题：当前窗口评分明细的错误率、风险率、健康率、错误压力、首字压力等字段由持久化路由状态补齐，但状态只持久化了压力、首字告警百分比、样本数和最后样本时间。因此页面可能显示“有证据/有样本”，同时这些分项显示为 0 或旧值。
5. 智能调度页的“更新于”使用本次 API 生成时间，不是最后一条样本时间，也不是最后一次实际调度时间；旁边的“每 N 分钟调度”容易让人误以为运行时自适应状态也只在 N 分钟刷新。
6. 页面“预计流量”是基于当前页面快照的最高优先级层和权重推算，不是某个真实请求的保证比例。真实路由还会受到模型/路径匹配、暂停、冷却、请求大小限制、重试排除、临时采样和多节点缓存状态影响。

## 各数据的更新时间链路

| 页面数据 | 后端真实来源与更新时间 | 后端缓存/排除项 | 前端刷新 | 页面可见的正常滞后 |
|---|---|---|---|---|
| 首字（TTFT）、TPS | `Log` 中成功流式请求的 `frt`、完成 token/耗时，经 `ChannelMonitorMinuteMetric` 按分钟聚合 | 当前分钟不计入；分钟边界延迟约 1 秒；性能查询缓存 10 秒 | 概览/性能视图 60 秒；状态探测页不拉性能查询 | 通常 1 秒至约 61 秒，最坏接近 2 分钟 |
| 成功率（实际/最终） | 分钟聚合中的 consume/error；实际成功率包含重试尝试，最终成功率只看最终结果 | 需要 `LogConsumeEnabled && ErrorLogEnabled`；当前分钟不计入；成功率明细打开时按需查询 | 总览 60 秒；成功率明细打开时请求一次，无持续轮询 | 总览同上；明细为打开时快照 |
| 缓存命中率/缓存利用率 | consume 日志 `cache_tokens`、输入 token；命中率按有缓存字段的请求，利用率按缓存读 token/输入 token | 需要消费日志；当前分钟不计入；缓存利用率历史有独立补齐水位 | 今日成功卡 60 秒；日历/明细打开时按需；后端今日成功缓存 10 秒 | 约 10 秒缓存 + 最多 60 秒页面轮询，另加分钟聚合 |
| 缓存写 | consume 日志中的 `cache_write_tokens`/Anthropic cache creation 字段，按请求计数并按渠道汇总 | 需要消费日志；统计的是“发生过写入的请求数”，不是写入 token 数；当前分钟不计入 | 今日成功卡 60 秒；明细按需 | 与成功率相同 |
| 今日已结算成本 | `ChannelDailyCost`；请求结束/结算时先进入批处理器 | 批处理器默认每 1 秒 flush，批大小 256；DB 超时 2 秒、最多 3 次重试；队列满时高基数事件可能丢弃并记录日志 | 概览成本摘要 60 秒；成本历史弹窗打开时查询，`staleTime=30s`，无持续轮询 | 正常约 1 秒批处理 + 最多 60 秒页面；异常重试可再增加约 4.2 秒或更久 |
| 智能调度摘要（状态、P/W、临时流量、样本欠账） | `/api/channel_monitor/schedule?metrics=false` 读取路由状态表；运行时刷新事务性写入状态和 Ability 并刷新路由缓存 | 自适应事件在完整请求结束后入队，合并约 1 秒；同一调度池默认最短刷新间隔 10 秒；完整调度默认按配置（默认 10 分钟） | 3 秒；隐藏页面停止 | 后端状态变化后通常约 0–3 秒；状态本身可能因 1 秒合并/10 秒节流延后 |
| 智能调度明细（当前窗口评分、窗口数据） | `/schedule?metrics=true`：分钟聚合性能/稳定性 + 持久化共享样本 + 运行时路由状态，读取前会确保最新已完成分钟 | 当前分钟业务聚合仍排除；共享样本按窗口实时过滤；共享样本滚动缓冲最多 1500 条 | 3 秒，仅智能调度视图启用 | 运行时样本变化通常 0–3 秒；业务日志仍受分钟聚合延迟 |
| 最近一次实际调度评分 | 路由状态 `last_schedule_score`/`last_schedule_score_details`，只在完整调度执行写入 | 自适应覆盖不会更新 `last_schedule_time` 或最近一次完整调度快照 | 随智能调度 3 秒刷新 | 反映的是上一次完整调度，不是当前窗口即时估算 |
| 当前窗口预计得分 | 每次 `metrics=true` 请求重新计算，不写回“最近一次实际调度”字段 | 是当前窗口估算；健康分项依赖持久化状态，见下方一致性问题 | 3 秒 | 约 3 秒页面延迟，但数据窗口和最近调度快照不同 |
| 优先级、权重 | 实际选择使用内存路由缓存中的 Ability P/W；智能调度写库后刷新缓存 | 真实选择还按最高优先级层、权重随机选择及请求级过滤 | 3 秒摘要/明细；概览渠道 P/W 60 秒 | 调度缓存刷新后页面通常 3 秒内看到，但概览渠道表可能慢至 60 秒 |
| 状态探测概览 | 探测执行结束写 `ChannelStatusProbeExecution`，事务更新最新状态/分钟/小时/天桶 | Controller 进程内概览缓存默认 3 秒；Worker 默认每 1 秒扫描，探测完成主动失效缓存 | 无运行任务 15 秒；运行中或手动任务 3 秒；隐藏页面暂停 | 约 3–18 秒，取决于是否正在运行 |
| 状态探测历史 | 执行表分页查询；第一页且探测运行中 3 秒轮询，其他页不轮询 | 只在打开历史面板时启用 | 按上述条件 | 活动第一页约 3 秒，否则打开/筛选时快照 |
| 模型检测概览 | 运行/任务状态由模型检测服务持久化；前端概览轮询 | 无发现与分钟聚合相同的后端统计缓存 | 有活动 run 3 秒，无活动 20 秒；只在模型检测视图启用 | 约 3–20 秒 |
| 模型检测历史 | 打开渠道历史时按条件分页查询 | 无持续轮询；只有手动刷新或状态变更后的失效 | 按需 | 打开/刷新时快照 |
| 模型检测运行详情 | 打开详情且 run 未结束时轮询 | 终态停止轮询 | 活跃 run 3 秒 | 约 3 秒 |
| 智能调度执行任务/测试样本 | 任务表和执行明细；任务列表有活动任务时刷新 | 活动任务列表 5 秒；已完成明细 `staleTime=Infinity` | 仅智能调度执行面板打开时 | 活动任务约 5 秒；明细完成后不会自动跟随后续修改 |

## 当前代码中的全部时间处理

渠道监控里的“时间”不是一种用途，主要分为数据归属、数据可用、缓存、后端刷新、页面刷新和统计窗口六类。它们共同决定“某条数据属于哪一段”和“什么时候可以看到”，不是重复判断。

| 时间处理 | 当前规则 | 目的/影响 |
|---|---|---|
| 时间单位 | 后端持久化和 API 时间字段统一使用 Unix 秒；耗时字段使用毫秒；前端轮询间隔使用毫秒 | 避免秒、毫秒混用；展示时再格式化为北京时间 |
| 日归属 | 成本和今日成功率按北京时间日界线（UTC+8）计算，`ChannelDailyCostDayStart` 将时间归到当天 00:00 | “今日成本/今日成功率”不会按服务器本地时区漂移 |
| 分钟归属 | 日志按 `timestamp - timestamp%60` 归入分钟桶 | TTFT、TPS、成功率、缓存数据按完整分钟聚合 |
| 当前分钟排除 | 查询范围的结束点通常是当前分钟起点；正在进行的分钟不进入性能/成功率/缓存统计 | 防止未结束请求、延迟写入日志造成半分钟数据跳动；代价是实时性下降 |
| 聚合安全边界 | 分钟结束后再等待约 1 秒，聚合 worker 只发布“安全完成分钟” | 给日志落库留出边界时间，减少漏记；异常慢日志由后续修复/补齐处理 |
| 聚合补齐等待 | 非主节点读取时，若共享聚合水位落后，最多等待约 10 秒并每 250ms 检查 | 页面读到的不是明显落后的分钟聚合；超时会返回错误而不是静默伪造新数据 |
| 聚合修复 | 启动时修复最近约 5 分钟；运行中保留最近约 2 分钟尾部重建；整点可修复最近约 1 小时 | 处理延迟日志、进程重启和跨分钟写入；修复期间数值可能重新计算 |
| 性能/成功率缓存 | 性能、成功率、稳定性聚合查询缓存约 10 秒；今日/日历史成功率缓存约 10 秒；聚合发布会主动失效 | 降低重复查询；页面轮询不等于每次都直接查库 |
| 成本写入批处理 | 成本事件先入内存聚合队列，默认每 1 秒 flush，最大批量 256；单次 DB 超时 2 秒，最多 3 次尝试 | 把结算写入从请求链路中解耦；正常约 1 秒可查，失败时会重试/保留，队列满时可能丢弃事件并告警 |
| 成本快照 TTL | 请求开始前读取的渠道成本配置快照缓存 1 分钟；结算使用请求开始时的快照 | 管理员中途修改倍率不会改写已开始请求的历史成本 |
| 状态探测缓存 | 探测概览进程内缓存默认 3 秒；探测完成、配置修改、手动触发会主动失效 | 运行中的探测状态不会长期停留在旧值 |
| 智能调度运行时合并 | 成功/失败请求在请求结束后记录；自适应刷新队列延迟约 1 秒合并事件 | 避免一个请求同时触发多次池刷新 |
| 智能调度池级节流 | 自适应刷新默认同池最短间隔 10 秒，可由 `CHANNEL_MONITOR_ADAPTIVE_REFRESH_MIN_INTERVAL_SECONDS` 调整为 1～300 秒 | 避免高并发时频繁重排 P/W；这是自适应百分比变化慢的主要原因之一 |
| 完整调度周期 | 智能调度任务按配置的分钟间隔执行，默认 10 分钟 | 更新基础排名、基础 P/W 和最近一次完整调度评分；不代表运行时状态只在此时变化 |
| 自适应窗口 | 默认取最近约 10 分钟，并限制最多约 100 个请求；具体由分组策略配置 | 防止无限历史影响当前备援判断；新请求会逐步替换旧请求 |
| 性能/稳定性窗口 | 智能调度性能窗口、稳定性窗口分别由设置决定，最大 1440 分钟 | 当前窗口预计得分可能与最近一次完整调度使用的窗口不同 |
| 共享样本保留 | 每个渠道/模型共享样本滚动缓冲最多 1500 条，并按策略窗口过滤 | 测试、手动探测、周期探测可即时进入窗口，但不会无限增长 |
| 前端轮询 | 概览/性能/成本/今日成功率 60 秒；智能调度 3 秒；探测活跃 3 秒、空闲 15 秒；模型检测活跃 3 秒、空闲 20 秒；活动任务 5 秒 | 这是“页面多久重新请求一次”，不是后端数据产生周期；页面隐藏时多数后台轮询停止 |
| 弹窗查询 | 成功率明细、成本历史、模型检测历史、倍率历史通常打开或筛选时查询；部分查询 `staleTime` 为 30 秒，智能调度已完成明细为无限新鲜 | 用户打开旧弹窗时可能看到打开时快照，除非手动刷新或任务仍处于活动状态 |
| `generated_at` | 多数 API 用请求处理时刻填充 `generated_at` | 它只表示响应生成时间，不表示最后纳入样本、最后聚合分钟或最后调度时间 |

### 如何理解这些时间

应把页面上的时间拆成四个独立概念：

1. **事件时间**：请求完成、探测完成或成本结算发生的时间。
2. **数据截止时间**：统计实际纳入的最后一分钟/最后样本时间；分钟指标通常早于当前时间一个未完成分钟。
3. **后端处理时间**：聚合、批处理、自适应刷新和缓存失效完成的时间。
4. **页面刷新时间**：前端最近一次成功请求的时间。

当前页面主要展示第 4 类（`generated_at`），部分路由状态还展示第 1 类或最近完整调度时间；第 2、3 类没有统一展示，这就是用户看到多个时间后难以判断当前状态的根本原因。后续应为每张卡片同时返回并展示“数据截至”和“页面刷新于”。

## 为什么首字很高而自适应备援百分比仍低

自适应窗口的核心不是平均 TTFT。代码按每个有效请求计算：

```text
首字告警百分比 = 首字超过 warning 阈值的成功请求数 / 有效请求数
压力 = max(错误压力, 首字延迟压力)
备援预算 = 样本欠账比例 × [基础比例 + 压力 × (最大比例 - 基础比例)]
```

因此以下情况都会出现“平均首字高、页面自适应百分比低”：平均值被少量极慢请求拉高但超阈值请求占比未达门槛；大量成功请求没有可用首字字段但仍在分母；有效样本数尚未达到 `min_samples`；最近一次请求尚未完成或仍在 1 秒合并/10 秒池级节流内；选中的备用渠道本身不健康，预算会被限制到基础比例。页面显示的百分比是目标预算/状态，不是已经发生的实际请求比例。

## 已确认的页面/真实数据不一致

### P1：当前窗口健康分项可能显示为 0 或旧值

`channelSmartScheduleApplyCurrentWindowScores` 使用 `route.State` 构造健康详情。`ChannelSmartScheduleRouteState` 持久化了 `adaptive_health_state`、`adaptive_health_pressure`、首字告警百分比、样本数、最后样本时间，但没有持久化错误率、风险率、健康率、错误压力、首字压力。于是 `current_window_score_details.health.evidence=true` 时，前端仍会渲染“错误 / 首字告警 / 风险 / 健康”，其中未持久化的分项可能为 0；页面看起来像完整当前窗口数据，实际只是一部分运行时快照。应改为持久化完整健康更新，或在 API 请求时用同一窗口样本重新计算这些分项。

### P1：智能调度“更新于”不是数据更新时间

摘要和明细 API 的 `generated_at` 是收到请求时的 Unix 时间。页面把它显示为“更新于”，但它不代表：最后纳入的分钟、最后一个共享样本、最后一次自适应刷新、或 `last_schedule_time`。因此页面时间会随 3 秒轮询不断变新，即使路由状态和样本没有变化。应分别展示“页面响应时间”“最后样本时间”“最后自适应刷新时间”“最近完整调度时间”。

### P1：不同卡片天然跨快照

概览、性能、成本、今日成功率分别以 60 秒轮询；性能/成功率又受分钟聚合，成本受 1 秒批处理，今日成功率受 10 秒模型缓存。一次渲染中这些卡片可能来自不同秒甚至不同分钟。页面没有统一的“数据截止时间”，用户不能把同一屏数字当成同一时刻的快照。

### P2：预计流量不是实际请求比例

前端 `placeChannelMonitorSmartScheduleRoutes` 只按当前最高优先级层的 `weight / totalWeight` 估算。真实路由还会执行路径/模型匹配、暂停与冷却过滤、请求大小限制、智能调度临时采样、重试排除和不同节点缓存选择。因此预计值可作为当前配置下的静态推算，不能用于断言真实过去 N 分钟流量比例。建议同时显示“配置预计”与后端按请求计数的实际分布。

### P2：最近实际调度与当前窗口预计混在同一行容易误判

页面虽已分别标注“当前窗口只读预估”和“最近一次实际调度快照”，但表头、排序和更新时间仍容易将二者当成同一评分。预计得分会随每次 3 秒请求变化；最近实际调度评分只在完整调度任务运行时变化。

## 设计差异但不是代码错误的地方

- 业务性能/成功率故意使用完成分钟，避免未完成请求和延迟日志污染；智能调度运行时窗口故意使用请求级事件和共享样本。两者有效时间范围不同，首字卡与备援状态不应期待同值同步。
- 状态探测的“首字/TPS”是探测执行记录的最近值或探测窗口平均值，不等于业务日志分钟聚合的平均值。
- 缓存命中率是“有缓存字段的请求命中比例”，缓存利用率是 token 比例，缓存写是写入请求数；三者分母和单位不同。
- 成本按结算时固化的上游成本快照记录，未解析尝试单独计数；它不一定和同一秒的业务 token 统计一一对应。

## 建议优先级

1. 修复当前窗口健康分项：API 直接基于当前窗口样本计算完整错误/风险/健康/压力字段，或扩充路由状态持久化字段，并增加前后端回归测试。
2. 给每个 API 返回明确的 `data_cutoff_at`、`last_sample_at`、`last_aggregate_minute`、`last_adaptive_refresh_at`、`last_full_schedule_at`；页面按卡片显示“数据截至”，不要使用请求 `generated_at` 代替。
3. 在性能、成功率、成本、缓存卡片顶部显示统一快照时间和“当前分钟未计入”提示；成本显示批处理/未解析计数。
4. 为智能调度增加后端实际路由计数（按请求、重试、临时采样分别统计），与前端“预计流量”并列，避免将权重推算当成实际比例。
5. 将成功率明细、今日成功率、成本历史和模型/探测历史的刷新状态显式展示；活动面板可继续按现有轮询，非活动历史提供手动刷新时间。

## 本次代码验证

- 已检查前端所有渠道监控查询的 `refetchInterval`、启用条件、后台轮询策略和弹窗查询行为。
- 已检查分钟聚合边界、10 秒聚合缓存、今日成功率 10 秒缓存、状态探测 3 秒概览缓存、成本 1 秒批处理和智能调度 1 秒合并/10 秒节流。
- 工作区已有智能调度摘要/明细改为 3 秒轮询的变更，并已通过对应 9 个查询策略测试、`bun run typecheck`、定向 oxlint 和 `git diff --check`。
