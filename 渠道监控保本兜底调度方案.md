# 渠道监控保本兜底调度方案

## 1. 背景

渠道监控已经能够读取上游倍率、换算成本倍率，并按 `(分组, 模型)` 对参与智能调度的路由进行评分、选主、优先级分层和权重分配。

当前成本倍率只作为评分指标参与比较。当某条路由的成本倍率与本地分组倍率相等时，该路由虽然没有发生成本倒挂，但其毛利为零。如果继续按照首字时间、TPS 或稳定性参与正常选主，它仍可能成为主渠道并承接长期正常流量，不符合“只用于可用性兜底”的经营目标。

本方案新增“保本兜底”调度身份：自动调度不会把零毛利路由作为正常流量候选，但正常渠道不可用时仍允许其接管请求。

## 2. 目标

- 使用准确的成本口径识别零毛利路由。
- 将零毛利路由放到正常候选之后、稳定性降级路由之前。
- 正常渠道存在且可用时，保本兜底路由不获得自动分配的正常业务流量。
- 正常渠道不可用时，保本兜底路由仍能接管请求，避免直接无渠道可用。
- 保留管理员手动固定主渠道的现有能力，不新增限制、拒绝或额外确认。
- 保持现有智能调度架构：仍通过 `Ability` 的优先级和权重控制选路，不增加一套并行的请求时路由系统。
- 保持同一渠道在不同分组、不同模型下可以拥有不同调度结果。

## 3. 非目标

- 不改变成本倍率高于分组倍率时已有的禁用、移除分组、更新分组倍率或仅记录策略。
- 不改变普通重试次数、错误分类、`skip_retry`、指定渠道和跨分组重试规则。
- 不自动修改分组倍率或成本换算配置。
- 不把未参与智能调度的路由强制纳入经济兜底管理。
- 首版不按请求用户的 `GroupGroupRatio` 动态改变路由身份。

## 4. 倍率口径

### 4.1 必须比较成本倍率

保本判断必须使用成本倍率，而不是原始上游倍率：

```text
成本倍率 = 上游倍率 × 换算系数
```

示例：

```text
上游倍率 = 1.0
换算系数 = 0.8
成本倍率 = 0.8
分组倍率 = 1.0
```

虽然原始上游倍率与分组倍率相等，但实际仍有 `0.2` 的倍率差，不属于保本兜底。

### 4.2 不使用分组同步系数

分组同步系数用于计算建议或目标售价，不是当前实际收入倍率，因此不参与保本判断。

### 4.3 精度规则

复用渠道监控现有的倍率误差：

```text
epsilon = 1e-9
```

分类规则：

| 条件 | 经济状态 | 本方案行为 |
| --- | --- | --- |
| `成本倍率 < 分组倍率 - epsilon` | 有毛利 | 正常参与自动调度 |
| `abs(成本倍率 - 分组倍率) <= epsilon` | 零毛利 | 进入保本兜底层 |
| `成本倍率 > 分组倍率 + epsilon` | 成本倒挂 | 继续执行现有倍率策略 |
| 成本倍率或分组倍率无效 | 无法判断 | 沿用现有缺失倍率处理，不自动标记为保本兜底 |

页面显示精度不参与判断。即使页面格式化后看起来相等，也必须以后端原始浮点值和统一误差为准。

## 5. 生效范围

保本身份按智能调度的实际路由维度计算：

```text
(渠道, 分组, 模型)
```

成本倍率是渠道级数据，分组倍率是分组级数据，因此同一渠道可能出现：

- 在分组 A 中成本倍率低于分组倍率，正常参与调度。
- 在分组 B 中成本倍率等于分组倍率，只作为保本兜底。
- 不同模型继续保留各自的评分、稳定性和基础排名。

只有满足以下条件的路由才由本方案管理：

- 全局智能调度已开启。
- 当前分组命中有效的智能调度策略。
- 当前模型命中该分组策略的模型范围。
- 路由已经明确参与智能调度。
- 渠道和对应 `Ability` 可用于调度。

未参与或明确排除智能调度的路由继续使用原有路由值，不由本方案自动降为保本兜底。

## 6. 调度层级

### 6.1 自动调度层级

智能调度接管的路由使用以下逻辑层级：

```text
正常评分层：P2 及以上
        ↓
保本兜底层：P1
        ↓
稳定性降级层：P0/W0
```

含义如下：

- 正常评分层继续按现有策略评分、选主和分配流量。
- 保本兜底层只在更高优先级的正常路由不可用时接管。
- 稳定性降级层仍是最后的安全兜底层。
- 健康的保本渠道优先于已经触发稳定性降级的渠道。

### 6.2 只有保本路由时

如果一个 `(分组, 模型)` 池内不存在正常候选，但存在健康的保本路由：

- P1 自动成为该池最高可用优先级。
- 请求可以直接选择保本路由。
- 页面显示“兜底接管中”，而不是显示无渠道可用。

“纯兜底”表示相对正常盈利渠道后置，不表示禁止该渠道承接任何请求。

### 6.3 多个保本路由

多个保本路由放在同一个 P1 层内：

- 有足够评分数据时，使用现有性能和稳定性评分计算层内权重。
- 成本倍率在该层内通常相同，成本分项不会产生有效区分。
- 样本不足时按上一轮稳定顺序和渠道 ID 保持确定性顺序。
- 同一请求重试时继续使用现有无放回排除逻辑，优先尝试尚未使用的保本渠道。

## 7. 两种应用模式

### 7.1 优先级分层加权重

当策略为 `priority_weight` 时：

- 正常路由按照现有评分生成唯一主备顺序。
- 如果池内存在保本路由，正常路由的基础优先级从 P2 开始向上排列。
- 所有保本路由进入 P1，并在 P1 内按权重选择。
- 稳定性降级路由保持 P0/W0。

示例：

```text
渠道 A：正常候选，评分第一，P4
渠道 B：正常候选，评分第二，P3
渠道 C：正常候选，样本不足，P2
渠道 D：保本兜底，P1
渠道 E：稳定性降级，P0/W0
```

### 7.2 只调整权重

“只调整权重”无法仅通过把保本渠道设置为 W0 来实现严格兜底。现有选路在某个优先级全部权重为零时仍会给这些渠道分配等效权重，因此保本规则必须保留一个优先级例外。

处理方式：

- 正常候选仍保留原有优先级关系，只在同一优先级内调整权重。
- 保本候选统一移动到 P1。
- 如果参与调度的正常候选最低优先级小于 P2，则对这些正常候选应用统一偏移，使最低正常优先级至少为 P2。
- 统一偏移只改变绝对值，不改变正常候选之间的相对优先级关系。
- P0 继续保留给稳定性降级。

因此“只调整权重”的准确含义调整为：正常候选只调整权重；经济兜底和稳定性保护可以为了安全分层调整优先级。

## 8. 评分与选主

### 8.1 分开评分队列

调度计划应先把候选拆成两个队列：

```text
normalCandidates
breakEvenFallbackCandidates
```

正常候选和保本候选分别建立归一化范围，避免保本渠道的成本和性能数据影响正常候选的分数区间。

### 8.2 正常选主

- 自动主渠道只能从正常候选中产生。
- 保本候选不能挑战正常主渠道。
- 当前主渠道从正常状态变为保本状态时，如果没有手动固定，应立即退出自动主渠道候选，不受主渠道切换分差限制。
- 如果没有正常候选，才从保本候选中选择当前自动兜底主渠道。

### 8.3 样本补充

保本路由不得通过以下机制获得正常业务流量：

- 样本不足探索流量。
- 低优先级轮转提权。

保本路由仍可使用定时探测和管理员手动测试积累样本，因为这些方式不会把它长期提升到正常流量层。

## 9. 手动固定主渠道

手动固定保持现有能力，不增加任何限制。

具体规则：

- 保本路由可以被管理员直接固定为主渠道。
- 不新增 API 拒绝、额外确认字段或强制二次确认。
- 保本身份不阻止固定操作立即提升其优先级和权重。
- 已固定的正常路由在固定期间变为保本状态时，继续保持固定，不自动撤回固定效果。
- 固定到期或管理员主动解除后，下一轮调度重新应用保本规则，将其放回 P1。
- 页面可以显示“保本兜底 · 已手动固定”提示，但提示不得阻断操作。

稳定性保护继续沿用现有固定规则：

- 开启“允许稳定性降级”时，固定的保本路由仍可因失败进入稳定性保护。
- 关闭“允许稳定性降级”时，继续保持现有严格固定行为。
- 本方案不改变稳定性确认、恢复和固定到期逻辑。

调度约束优先顺序为：

```text
现有稳定性保护规则
        ↓
有效的管理员固定意图
        ↓
正常自动调度层
        ↓
保本兜底层
```

管理员固定属于明确的人工覆盖，因此可以让保本路由获得正常流量。

## 10. 重试和实际选路

本方案不新增请求时利润判断，而是继续写入 `Ability` 优先级和权重，由现有选路统一执行：

- 新请求先选择最高可用优先级。
- 正常候选存在时，P1 保本层不会获得预计流量。
- 正常候选因禁用、限流冷却、请求级排除或其他已有过滤条件全部不可用时，P1 成为最高可用层。
- 上游请求失败后，只有错误允许重试且仍有普通重试额度时，才会继续尝试后续候选。
- 保本路由不会绕过 `RetryTimes`、`skip_retry`、指定渠道和错误分类。
- 当前重试额度不足以走完所有正常候选时，保本层可能不会被尝试，这是现有重试预算的正常结果。

这种方式能够让内存缓存选路和无缓存数据库选路保持一致。

## 11. 稳定性保护交互

### 11.1 保本路由触发降级

保本路由触发稳定性保护时：

- 从 P1 降为 P0/W0。
- 保存的恢复目标应记录其保本基础路由，而不是历史正常主渠道路由。
- 恢复后如果仍是保本状态，应回到 P1。

### 11.2 降级期间倍率变化

- 正常路由在降级期间变为保本：恢复时直接回到 P1，不得短暂恢复到正常层。
- 保本路由在降级期间恢复盈利：恢复后可以重新进入正常评分队列。
- 倍率分类必须作为最终路由约束应用，不能只在最初建立候选时判断一次。

### 11.3 试放流量

稳定性试放属于现有安全恢复流程，优先级仍低于健康保本层。保本身份不取消稳定性试放，但试放完成后必须回到当前经济身份对应的层级。

## 12. 数据设计

### 12.1 不新增长期角色字段

不建议在 `ChannelSmartScheduleRouteState` 中保存一个长期的 `break_even` 布尔值。

原因：

- 该身份完全由成本倍率和分组倍率派生。
- 两种倍率都可以独立变化。
- 持久化第二份真值容易出现角色与实际倍率不一致。

路由状态继续只保存最终 P/W、基础排名、稳定性状态和临时流量状态。

### 12.2 候选运行时字段

调度候选增加仅在任务执行期间使用的字段：

```go
CostRatio       *float64
GroupRatio      *float64
EconomicRole    string
GrossMargin     *float64
```

推荐角色值：

```text
normal
break_even_fallback
unknown
```

成本倒挂是否执行禁用或移除仍由现有策略决定，不在本方案中引入新的自动角色。

### 12.3 执行快照

智能调度评分详情增加经济快照，并升级详情版本：

```json
{
  "economics": {
    "cost_ratio": 1,
    "group_ratio": 1,
    "gross_margin": 0,
    "role": "break_even_fallback"
  }
}
```

建议把 `ChannelSmartScheduleScoreDetailsVersion` 从 `3` 升级到 `4`。新增字段保持可选，旧执行记录仍可正常解析和展示。

### 12.4 API 派生字段

智能调度路由响应增加当前派生信息：

```json
{
  "cost_ratio": 1,
  "group_ratio": 1,
  "gross_margin": 0,
  "economic_role": "break_even_fallback"
}
```

这些字段由接口使用当前倍率实时计算，不作为数据库真值。

## 13. 数据一致性与任务触发

### 13.1 立即重新调度

以下操作成功后，如果智能调度已开启，应立即请求一次智能调度任务：

- 手动更新渠道倍率。
- 定时或手动获取到新的上游倍率。
- 修改成本换算配置。
- 手动修改分组倍率。
- 按最高成本倍率同步分组倍率。
- 自动策略更新分组倍率。
- 修改渠道分组关联。
- 修改智能调度参与状态。

这样可以避免等待下一个调度周期后才移动经济层级。

### 13.2 经济数据修订号

建议新增数据库 Option：

```text
ChannelMonitorEconomicRevision
```

任何会改变经济身份的操作都递增该修订号。调度任务开始时读取修订号，写入路由结果时同时校验：

- 智能调度控制版本未变化。
- 路由状态修订号未变化。
- 经济数据修订号未变化。

如果经济修订号变化，本轮旧计划不写入路由，并立即请求新一轮调度。这样可以防止旧任务在倍率更新后重新写回过期的正常优先级。

## 14. 调度算法

推荐的池级处理流程：

```text
1. 读取同一版本的分组倍率和渠道成本倍率
2. 为每条参与路由计算经济身份
3. 先处理渠道状态、Ability 状态和稳定性状态
4. 拆分正常候选与保本候选
5. 正常候选按现有策略评分、选主和排序
6. 保本候选独立评分，只计算 P1 层内权重
7. 合并正常层、P1 保本层和 P0 稳定性层
8. 应用管理员固定结果；固定可以覆盖保本层级
9. 再次校验经济数据修订号和调度控制版本
10. 原子写入 Ability 和路由状态
11. 刷新渠道路由缓存
```

伪代码：

```go
for _, pool := range pools {
    normalCandidates := make([]candidate, 0)
    fallbackCandidates := make([]candidate, 0)

    for _, route := range pool.Routes {
        role := classifyEconomicRole(route.CostRatio, pool.GroupRatio)
        route.EconomicRole = role

        if role == breakEvenFallback {
            fallbackCandidates = append(fallbackCandidates, route)
            continue
        }
        normalCandidates = append(normalCandidates, route)
    }

    normalPlan := planNormalCandidates(normalCandidates)
    fallbackPlan := planFallbackCandidates(fallbackCandidates, priority: 1)
    plan := mergePlans(normalPlan, fallbackPlan)

    // 保留现有管理员固定能力，固定结果可以覆盖经济兜底层。
    plan = applyManualPrimary(plan, pool.ManualPrimary)
    plan = applyStabilityRules(plan)
    applyPlanIfRevisionsMatch(plan)
}
```

实际实现时稳定性直接动作和管理员固定需要沿用现有事务及优先级计算，不应复制一套写入逻辑。

## 15. 前端展示

### 15.1 路由状态

智能调度看板为保本路由增加中文标签：

- `保本兜底`
- `保本兜底 · 已手动固定`
- `保本兜底 · 接管中`

标签只用于解释状态，不阻止任何现有管理员操作。

### 15.2 预计流量

- 正常层存在时，未手动固定的保本路由预计流量显示为 `0%`。
- 只有保本层可用时，按 P1 层权重显示预计流量。
- 保本路由被手动固定时，按实际固定后的最高优先级和权重显示预计流量。

### 15.3 池级汇总

调度池增加：

- 保本兜底数量。
- 当前是否由保本层接管。
- 是否存在手动固定的保本渠道。

### 15.4 详情和执行记录

展示以下信息：

```text
成本倍率：1.000000
分组倍率：1.000000
倍率差：0.000000
经济身份：保本兜底
调度说明：成本倍率与分组倍率相等，自动调度已放入 P1 兜底层
```

如果被手动固定：

```text
管理员已手动固定该保本渠道，当前固定结果覆盖自动经济兜底层级
```

渠道监控属于下游专用功能，新增文案使用简体中文常量，不新增前端 i18n 词条。

## 16. 特殊场景

### 16.1 倍率相等后又恢复盈利

- 下一轮立即调度重新加入正常评分队列。
- 不沿用 P1 兜底层权重作为正常层权重。
- 重新经过主渠道切换保护；如果当前没有正常主渠道，可以立即选出新的正常主渠道。

### 16.2 正常主渠道变为保本

- 未手动固定：立即退出自动主渠道候选并进入 P1，不受切换分差限制。
- 已手动固定：继续保持固定，直至解除、到期或按现有规则进入稳定性保护。

### 16.3 只有一个保本渠道

- 渠道保持可用。
- P1 成为最高可用层。
- 不因为零毛利直接返回无渠道错误。

### 16.4 成本倍率获取失败

- 保留最近一次有效成本倍率的现有行为。
- 最近有效倍率为保本时继续作为保本兜底。
- 从未取得有效倍率时沿用现有“成本倍率缺失”调度处理。

### 16.5 同一渠道属于多个分组

每个 `(分组, 模型)` 路由独立判断，不能把渠道级保本结果直接应用到所有分组。

### 16.6 `GroupGroupRatio`

首版按照渠道监控现有策略口径使用基础 `GroupRatio`。`GroupGroupRatio` 取决于请求用户原始分组，同一个调度池可能服务多个实际计费倍率，因此不能用一个静态路由身份准确表达。

如果后续要求按 `GroupGroupRatio` 保证每个请求都不走零毛利正常渠道，需要新增请求级经济路由判断，作为独立需求设计。

## 17. 后端测试

新增测试应覆盖可观察行为和路由契约：

1. 原始上游倍率等于分组倍率，但换算后成本倍率更低，不进入保本层。
2. 成本倍率与分组倍率完全相等时进入保本层。
3. 差值在 `1e-9` 内时进入保本层。
4. 差值超过误差且成本更低时正常参与调度。
5. 成本更高时继续执行现有倍率过高策略，不被本方案覆盖。
6. `priority_weight` 模式下正常层最低为 P2，保本层为 P1，降级层为 P0。
7. `weight` 模式下保本路由不会因 W0 平滑逻辑获得正常流量。
8. 有正常路由时保本路由不是自动主渠道且预计流量为零。
9. 只有保本路由时能够正常选路。
10. 多个保本路由能够在 P1 层内按权重选择，并在重试中排除已失败渠道。
11. 保本路由不获得探索流量或低优先级轮转提权。
12. 正常主渠道变为保本后立即退出自动主渠道，不受切换阈值影响。
13. 保本路由可以手动固定为主渠道，无新增确认和拒绝。
14. 已固定路由变为保本后继续保持固定。
15. 固定解除或到期后重新回到 P1。
16. 允许稳定性降级的固定保本路由仍能进入 P0/W0。
17. 保本路由稳定性恢复后回到 P1，而不是恢复到历史正常优先级。
18. 倍率变化后会请求立即调度。
19. 经济修订号变化时旧任务结果不会写入。
20. 内存缓存和数据库选路得到相同的经济层级结果。

Go 测试使用 `testify/require` 完成前置和致命断言，使用 `testify/assert` 完成非致命值检查。

## 18. 前端测试

1. 保本路由显示“保本兜底”标签。
2. 正常层存在时保本路由预计流量为 `0%`。
3. 只有保本层时显示“接管中”及正确流量。
4. 手动固定按钮对保本路由保持可用。
5. 固定保本路由显示“保本兜底 · 已手动固定”。
6. 执行详情正确显示成本倍率、分组倍率、倍率差和经济角色。
7. 旧版本评分详情没有 `economics` 字段时仍可正常展示。

## 19. 主要实现位置

预计涉及以下下游功能文件：

- `controller/channel_ratio_monitor_schedule_route.go`
  - 读取分组倍率快照。
  - 计算路由经济身份。
  - 合并正常计划和保本兜底计划。
  - 处理稳定性恢复后的经济层级约束。
- `controller/channel_ratio_monitor_schedule.go`
  - 扩展候选和计划结构。
  - 分离正常候选与保本候选的归一化、选主和权重分配。
- `controller/channel_ratio_monitor_schedule_score_details.go`
  - 写入经济快照和调度原因。
- `model/channel_smart_schedule_score_details.go`
  - 增加经济详情结构并升级版本。
- `model/channel_smart_schedule_route.go`
  - 增加经济修订号写入校验所需接口，不保存长期保本角色。
- `controller/channel_ratio_monitor_schedule_route_api.go`
  - 返回当前派生经济状态。
  - 保持手动固定请求和行为不变。
- `web/src/features/channel-monitor/`
  - 增加类型、标签、池汇总、流量展示和执行详情。
- `docs/downstream/channel-monitor/smart-scheduling.md`
  - 实现完成后补充正式功能文档。

这些渠道监控文件均为下游专用实现。实施时仍应检查 `upstream/main`，避免改动无关的上游文件。

## 20. 验收标准

功能完成后应满足：

- 成本倍率与基础分组倍率相等的参与路由会被自动标记为保本兜底。
- 存在健康正常路由时，未手动固定的保本路由不会获得自动正常流量。
- 正常路由不可用时，保本路由可以按照现有重试和选路规则接管。
- 保本路由始终排在稳定性降级路由之前。
- 管理员可以正常固定保本路由，操作流程与普通路由一致。
- 已固定路由变为保本后不会被自动解除固定。
- 固定解除或到期后，仍为保本的路由自动回到 P1。
- 倍率变更不会被仍在运行的旧调度任务覆盖。
- 内存缓存和数据库选路行为一致。
- 调度看板、执行记录和后端日志能解释保本身份及最终路由结果。

## 21. 推荐实施顺序

1. 增加经济身份纯函数及单元测试。
2. 扩展候选结构和评分详情快照。
3. 实现 `priority_weight` 的 P2/P1/P0 分层。
4. 实现 `weight` 模式的保本优先级例外。
5. 处理稳定性恢复、强制重置和倍率变化场景。
6. 验证并保留手动固定覆盖行为。
7. 增加经济修订号和立即调度触发。
8. 更新路由 API 和前端状态展示。
9. 补齐缓存与无缓存选路回归测试。
10. 更新渠道监控正式文档并完成全量构建验证。
